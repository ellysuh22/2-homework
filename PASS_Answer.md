# 🎯 미션2 평가 답변 노트

> 문항은 [`미션2_평가기준.txt`](미션2_평가기준.txt) 원문을 **한 글자도 바꾸지 않고** 옮겼습니다.
> 증빙은 실제로 실행해서 확인한 것만 담았습니다.

**20문항이 모두 같은 순서입니다.**

| 순서 | 무엇 | 목표 시간 |
| --- | --- | --- |
| 📋 **질문** | 평가기준 원문 | — |
| 🎯 **왜 이걸 묻는가** | 평가자의 의도 — 여기를 알아야 답이 나옵니다 | — |
| ✅ **답변** | 무엇을 했고 **왜 그렇게 했는지** | 10초 |
| 🖥️ **증빙** | 터미널 명령 또는 코드 위치 | 30초 |
| 📖 **왜 이렇게 만들었나** | 코드 + 설계 판단의 이유 | 물어보면 |
| 📚 **학습 포인트** | 용어 + 꼬리질문 대비 | 물어보면 |

> ⭐ **이 노트의 원칙** — 평가자는 **"했는가"가 아니라 "왜 그렇게 했는가"** 를 봅니다.
> `Quiz`는 문제 카드, `QuizGame`은 사회자 — 이건 **결과**입니다.
> **"바뀌는 이유가 다르니까 따로 만들었다"** — 이게 **답**입니다.
> 모든 문항에서 **결과가 아니라 이유**를 말하세요.

**답변 3단 뼈대** — 막히면 이 틀에 넣으면 됩니다.
> **① 무엇을** 이렇게 만들었고 → **② 왜냐하면** ~하기 때문이고 → **③ 안 그러면** ~한 문제가 생깁니다

모르는 질문이 나오면: *"현재 구현은 ~까지입니다. 그 부분은 ~한 방향으로 개선할 수 있다고 생각합니다."*

> 💡 문항 찾기 — `Cmd`+`Shift`+`O` 누르고 `1-3` 입력하면 바로 이동합니다.
> 용어가 막히면 [📚 STUDY_Notes.md](STUDY_Notes.md)의 용어 사전을 보세요.

---

# [항목 1] 기능 동작 검증

<a id="q1-1"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 1-1. 프로그램 실행 시 메뉴가 정상적으로 표시되고, 퀴즈 풀기/추가/목록/점수 확인 기능이 모두 동작하는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 기능을 만든 뒤에도 **프로그램이 살아 있는가**
>
> **✅ 답** — 메뉴 6개 다 동작. 기능이 끝나면 메뉴 재등장, **`6`에서만** 종료
>
> **💡 이유** — [`run()`](main.py#L308-L326)이 **`while True` 반복문**. 없으면 한 번 쓰고 프로그램이 죽음
>
> **🖥️ 증빙** — `python3.11 main.py <<< $'3\n5\n6'`
> **📄 코드** — [show_menu 105-117](main.py#L105-L117) · [run 308-326](main.py#L308-L326)

### 📖 왜 이렇게 만들었나

```python
def run(self):
    while True:                 # 계속 반복
        self.show_menu()        # 메뉴 출력
        choice = self.input_int("선택: ", 1, 6)
        if choice == 1: self.play_quiz()
        ...
        else: break             # 6번이면 종료
```

| 코드 | 뜻 |
| --- | --- |
| `while True` | 계속 반복 — **없으면 한 번 쓰고 프로그램이 끝남** |
| `break` | 반복 탈출 = 종료 |

📄 [run 308-326](main.py#L308-L326) · [show_menu 105-117](main.py#L105-L117)

### 🖥️ 증빙

**방법 1. 한 줄로 자동 시연** (빠르고 실수 없음)

```bash
python3.11 main.py <<< $'3\n5\n6'
```

`3`(목록) → `5`(점수) → `6`(종료)을 **자동으로 눌러주는 명령**입니다. 실제 출력:

```
📂 저장된 데이터를 불러왔습니다. (퀴즈 7개, 최고점수 100점)
========================================
        🎯 나만의 퀴즈 게임 🎯
========================================
1. 퀴즈 풀기 ... 6. 종료
========================================
선택:
📋 등록된 퀴즈 목록 (총 7개)
========================================
        🎯 나만의 퀴즈 게임 🎯          ← ★ 메뉴가 다시 나옴
========================================
선택:
🏆 최고 점수: 100점
========================================
        🎯 나만의 퀴즈 게임 🎯          ← ★ 또 나옴
========================================
선택:
👋 게임을 종료합니다. 안녕히 가세요!      ← 6에서만 끝남
```

> 🗣️ **★ 표시 두 곳만 짚으면 됩니다.**
> *"기능이 끝날 때마다 메뉴가 다시 나오고, `6`을 눌렀을 때만 종료됩니다. **기능을 만든 것보다 기능이 끝난 뒤 돌아오게 만든 것**이 이 문항의 핵심이라고 생각했습니다."*

**방법 2. 라이브 시연** (평가자가 직접 눌러보게 할 때)

```bash
cd ~/Documents/2-homework && clear && python3.11 main.py
```

| 누르는 것 | 나오는 것 | 증명되는 것 |
| --- | --- | --- |
| (실행) | 메뉴 6줄 + `선택:` | 실행 즉시 메뉴 표시 |
| `3` | 퀴즈 목록 → **메뉴 재등장** | ← **핵심**: 반복문이 돈다 |
| `1` | 출제 → 채점 → 메뉴 재등장 | 퀴즈 풀기 |
| `2` | `✅ 퀴즈가 추가되었습니다!` | 퀴즈 추가 |
| `5` | `🏆 최고 점수` | 점수 확인 |
| `6` | `👋 게임을 종료합니다` | 종료 |

**📸 스크린샷** — [menu.png](docs/screenshots/menu.png) · [add_quiz.png](docs/screenshots/add_quiz.png) · [list.png](docs/screenshots/list.png) · [score.png](docs/screenshots/score.png)

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| `while True` | **"조건이 항상 참 = 영원히 반복"**. 파이썬은 코드를 위에서 아래로 **한 번만** 읽고 지나가기 때문에, 되돌아가려면 반복문이 필요합니다. 없으면 메뉴가 한 번 뜨고 프로그램이 죽습니다 |
| `break` | 반복문을 **즉시 탈출**. 무한 반복에 **나가는 문**을 하나 만들어 두는 것 |
| `if / elif / else` | 여러 갈림길 중 **맞는 것 하나만** 실행. `elif`는 "앞이 아니라면 이건?" |
| `self.` | "이 객체 자신의" — `self.show_menu()`는 "내가 가진 show_menu 기능을 부른다" |
| 메서드 vs 함수 | 클래스 **안**에 있는 함수를 메서드라 부릅니다 |

> 🔍 **꼬리질문 대비** — *"`while True` 대신 다른 방법은?"*
> → `while self.running:` 처럼 **상태 변수**로 만들 수도 있습니다. 종료 조건이 여러 곳에서 생길 때 유리하지만, **지금은 종료 지점이 한 곳뿐이라** `break`가 더 읽기 쉽다고 판단했습니다.

---

<a id="q1-2"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 1-2. 퀴즈 풀기 시 정답/오답 판정이 정확하고, 최소 입력 오류 케이스(공백/문자/범위 밖/빈 입력)에 대한 처리가 되어 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — **사용자는 반드시 실수한다**는 걸 아는가
>
> **✅ 답** — 문자·범위밖·빈입력·공백 **4종 다 막고**, 틀려도 안 죽음
>
> **💡 이유** — 숫자 받는 **5곳 전부**가 [`input_int()`](main.py#L153-L172) 하나를 거침. 복붙했다면 규칙 바뀔 때 5군데를 다 고쳐야 함
>
> **🖥️ 증빙** — 메뉴에서 `abc` → `0` → `9` → 빈 Enter 연달아
> **📄 코드** — [input_int 153-172](main.py#L153-L172) · [check 37-39](main.py#L37-L39)

### 📖 왜 이렇게 만들었나

```python
def input_int(self, prompt, low, high):
    while True:
        value = input(prompt).strip()          # ① 공백 제거
        if value == "": ...; continue          # ② 빈 입력
        try: number = int(value)
        except ValueError: ...; continue       # ③ 문자
        if number < low or number > high:      # ④ 범위 밖
            ...; continue
        return number                          # 통과한 값만 내보냄
```

숫자를 받는 **5곳**([191](main.py#L191)·[216](main.py#L216)·[229](main.py#L229)·[256](main.py#L256)·[312](main.py#L312))이 전부 이 함수를 부릅니다.

📄 [input_int 153-172](main.py#L153-L172)

### 🖥️ 증빙

**【1부】 오류 입력 4종** — `선택:` 에서 **끊지 말고 연달아** 칩니다

```bash
python3.11 main.py
```

실제 화면입니다.

```
선택: abc
⚠️ 숫자가 아닙니다. 1-6 사이의 숫자를 입력하세요.
선택: 0
⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.
선택: 9
⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.
선택:
⚠️ 입력이 비어 있습니다. 1-6 사이의 숫자를 입력하세요.
선택:   3
📋 등록된 퀴즈 목록 (총 7개)          ← 앞뒤 공백을 지우고 정상 처리
```

| 누르는 것 | 어떤 오류 | 무엇이 증명되나 |
| --- | --- | --- |
| `abc` | **문자** | `try / except ValueError` 동작 |
| `0` | **범위 밖(아래)** | 범위 검사 동작 |
| `9` | **범위 밖(위)** | 범위 검사 동작 |
| 그냥 Enter | **빈 입력** | 빈 문자열 검사 동작 |
| ` 3 ` (앞뒤 공백) | **공백** → 정상 처리 | `.strip()` 동작 |
| **4번 틀려도 안 죽음** | | ← **이게 핵심** |

> 🗣️ *"네 번을 연속으로 잘못 넣었는데 한 번도 죽지 않았습니다. **에러가 안 나게 만드는 게 아니라, 에러가 나도 다시 물어보게** 만든 것입니다."*

**【2부】 정답/오답 판정** — 메뉴에서 `1`

```
[문제 1]
정해진 횟수만큼 반복할 때 주로 사용하는 반복문은?

1. while
2. for
3. if
4. try

정답 입력 (1-4, 힌트 보기는 5): 1
❌ 오답입니다! 정답은 2번 (for)      ← 번호와 내용까지
```

> 🗣️ *"오답일 때 '틀렸습니다'로 끝내지 않고 정답 번호와 내용까지 알려줍니다. **퀴즈 게임의 목적이 맞히는 게 아니라 배우는 것**이라고 봤기 때문입니다."*
> ⚠️ **일부러 틀린 답도 보여주세요.** 정답만 맞히면 오답 판정이 되는지 확인이 안 됩니다.

**한 줄 증거** — 검증 코드가 한 곳뿐이라는 것

```bash
grep -n "input_int(" main.py
```
```
153:    def input_int(self, prompt, low, high):    ← 정의 1개
191:        answer = self.input_int(...)           ← 호출 5개
216:        number = self.input_int(...)
229:            selected = self.input_int(...)
256:        total = self.input_int(...)
312:            choice = self.input_int(...)
```

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| `try / except` | `try`는 **"일단 해봐"**, `except`는 **"터지면 이렇게 해"** |
| `ValueError` | "값의 형태가 잘못됐다"는 이름의 예외. 예외를 **이름으로 지정**해야 다른 사고를 실수로 삼키지 않습니다 |
| `continue` | 반복문의 **이번 회차만 건너뛰고 처음으로**. `break`(완전 탈출)와 반대 |
| `.strip()` | 문자열 **앞뒤 공백 제거**. ` 3 `과 `3`을 같게 취급하려고 |
| `return` | 값을 돌려주며 **함수를 끝냄**. 4관문을 다 통과했을 때만 여기 도달 |
| 0-based 인덱스 | 리스트는 0번부터 — 사람 번호로 쓰려면 항상 **-1** |

> 🔍 **꼬리질문 대비** — *"메뉴 말고 다른 데서도 되나요?"*
> → `2`(퀴즈 추가)로 들어가 `정답 번호 (1-4):` 에서 `abc`를 넣어 보이세요. **똑같은 경고**가 나옵니다. 5곳이 전부 같은 함수를 쓰기 때문입니다.

---

<a id="q1-3"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 1-3. 프로그램 종료 후 재실행해도 추가한 퀴즈와 최고 점수가 유지되는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — **메모리와 파일의 차이**를 아는가
>
> **✅ 답** — 유지됨. `state.json` 파일에 저장
>
> **💡 이유** — 메모리는 끄면 사라짐 → **켤 때 읽고, 바뀔 때마다 씀**
>
> **🖥️ 증빙** — 퀴즈 추가 → 종료 → 재실행하면 개수가 늘어 있음
> **📄 코드** — [load_state 119-138](main.py#L119-L138) · [save_state 140-151](main.py#L140-L151)

### 📖 왜 이렇게 만들었나

```
┌─────────────────────────────────────────────────────┐
│  main.py  (코드)                                     │
│  · 기본 퀴즈 5개  ← 저장 파일이 없을 때만 씀           │
└──────────────────────┬──────────────────────────────┘
                       │ 켤 때
                       ▼
┌─────────────────────────────────────────────────────┐
│  메모리 (실행 중)                        끄면 사라짐 ✕ │
│  · 퀴즈 목록                                         │
│  · 최고 점수                                         │
│  · 지금 몇 번 문제인지, 맞은 개수  ← 저장 안 함        │
└──────────────────────┬──────────────────────────────┘
                       │ 바뀔 때마다  ↕  켤 때 읽기
                       ▼
┌─────────────────────────────────────────────────────┐
│  state.json  (파일)                       끄면 남음 ✓ │
│  · quizzes    퀴즈 목록                              │
│  · best_score 최고 점수                              │
│  · history    게임 기록                              │
└─────────────────────────────────────────────────────┘
```

```python
save_state()   # 메모리 → 파일   (194·220·291·338행에서 호출)
load_state()   # 파일 → 메모리   (332행, 켤 때 1번)
```

📄 [load_state 119-138](main.py#L119-L138) · [save_state 140-151](main.py#L140-L151)

### 🖥️ 증빙

증명할 것은 **하나** — *껐다 켠 뒤에도 퀴즈와 최고 점수가 그대로인가.*

**① 지금 상태를 확인한다**

```bash
python3.11 main.py
```
```
📂 저장된 데이터를 불러왔습니다. (퀴즈 7개, 최고점수 100점)
```
> 🗣️ *"이 숫자를 기억해 주세요 — 퀴즈 7개, 100점."*

**② 퀴즈를 하나 추가하고 종료한다**

메뉴 `2` → 문제·보기 4개·정답 입력 → 메뉴 `6`

```
✅ 퀴즈가 추가되었습니다!
👋 게임을 종료합니다. 안녕히 가세요!
```
> 🗣️ *"프로그램을 완전히 껐습니다."*

**③ 파일에 진짜 적혔는지 확인한다**

```bash
grep '"question"' state.json
```
```
    "question": "파이썬에서 리스트를 만들 때 사용하는 괄호는?",
    ...
    "question": "방금 추가한 문제",          ← 맨 아래에 있음
```
> 🗣️ *"프로그램 말이 아니라 파일이 증거입니다. 방금 만든 문제가 `state.json` 안에 **글자로** 남아 있습니다."*

**④ 다시 실행해서 유지되는지 본다** ← **이게 정답**

```bash
python3.11 main.py
```
```
📂 저장된 데이터를 불러왔습니다. (퀴즈 8개, 최고점수 100점)
```

| | 퀴즈 | 최고 점수 |
| --- | --- | --- |
| ① 처음 | 7개 | 100점 |
| ④ 재실행 후 | **8개** | **100점** |

> 🗣️ *"추가한 퀴즈는 늘었고 최고 점수는 그대로입니다. 껐다 켜도 유지된다 — 이게 데이터 영속성입니다."*
> 💡 긴장되면 **①의 숫자와 ④의 숫자만** 짚어도 이 문항은 통과합니다.

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 데이터 영속성 | 프로그램이 꺼져도 데이터가 **남는 성질**. 이 문항이 묻는 것 자체 |
| `with open(...)` | 파일을 열고 **자동으로 닫아주는** 문법. 안 닫으면 내용이 안 써질 수 있습니다 |
| `"w"` / `"r"` | write(쓰기, **기존 내용 삭제**) / read(읽기) |
| `encoding="utf-8"` | 한글을 저장하는 규칙. 안 쓰면 컴퓨터마다 글자가 깨질 수 있습니다 |
| `json.dump` / `json.load` | 파이썬 값 ↔ 파일 글자 변환 ([3-2](#q3-2)에서 자세히) |
| 리스트 컴프리헨션 | `[quiz.to_dict() for quiz in self.quizzes]` = "목록 전부를 하나씩 변환한 새 목록" |

> 🔍 **꼬리질문 대비** — *"저장을 매번 하면 느리지 않나요?"*
> → 지금은 파일이 2.4KB라 즉시 끝납니다. 다만 퀴즈가 1000개가 되면 문제가 되고, 그건 [4-1](#q4-1)에서 설명드릴 수 있습니다.

---

<a id="q1-4"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 1-4. 기본 퀴즈가 5개 이상 포함되어 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 처음 받아 실행한 사람이 **바로 쓸 수 있는가**
>
> **✅ 답** — 파이썬 기초 문제 **5개** 내장
>
> **💡 이유** — 변수 아닌 **함수**로 만듦 — 변수면 퀴즈 삭제 때 **복구용 원본까지** 줄어듦
>
> **🖥️ 증빙** — `mv state.json state.json.bak` 후 실행 → `기본 퀴즈로 시작합니다. (퀴즈 5개)`
> **📄 코드** — [default_quizzes 61-94](main.py#L61-L94)

### 📖 왜 이렇게 만들었나

```python
def default_quizzes():
    return [Quiz(...), Quiz(...), ...]   # 부를 때마다 새로 만들어 줌
```

**변수로 뒀다면** 목록이 하나뿐이라, 사용자가 퀴즈를 지울 때 **복구용 원본까지** 줄어듭니다.

📄 [default_quizzes 61-94](main.py#L61-L94)

### 🖥️ 증빙

⚠️ **파일을 지우지 말고 옮기세요.** 실습 후 되돌릴 수 있습니다.

```bash
mv state.json state.json.bak    # ① 저장 파일을 잠시 치움 (삭제 아님)
python3.11 main.py              # ② 실행 → 메뉴에서 3 입력 → 6으로 종료
mv state.json.bak state.json    # ③ 원래대로 되돌리기 ← 잊지 마세요
```

| 단계 | 나와야 하는 결과 |
| --- | --- |
| ② 실행 직후 | `📂 저장된 데이터가 없어 기본 퀴즈로 시작합니다. (퀴즈 5개)` |
| ② `3` 입력 | `📋 등록된 퀴즈 목록 (총 5개)` + 문제 5줄 |

**파일을 안 건드리고 확인하는 법** (더 안전)

```bash
python3.11 -c "from main import default_quizzes; print(len(default_quizzes()), '개')"
```
```
5 개
```

> 🗣️ *"저장 파일이 없는 상태 — 즉 **처음 받아서 실행한 사람**도 바로 5문제를 풀 수 있습니다."*

**📸 스크린샷** — [list.png](docs/screenshots/list.png)

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 함수 vs 변수 (기본값) | 여러 곳에서 쓰는 **목록·딕셔너리**는 함수로 만들어 매번 새로 주는 게 안전. 파이썬에서 리스트는 **같은 것을 공유**하기 때문 |
| `return [ ... ]` | 대괄호 = 리스트. 그 안에 `Quiz(...)` 5개 = **객체 5개를 담은 목록** |
| 객체 생성 | `Quiz("문제", [...], 2, "힌트")` 라고 부르는 순간 `__init__`이 실행되어 카드 한 장이 만들어짐 |
| 기본값 인자 | `def __init__(self, ..., hint="")` — 힌트를 안 줘도 빈 문자열로 채워짐 |
| 가변 객체(mutable) | 리스트·딕셔너리는 **내용이 바뀔 수 있는** 자료형. 그래서 공유하면 위험합니다 |

> 🔍 **꼬리질문 대비** — *"5개는 어디에 저장되나요?"*
> → 코드 안([main.py:61-94](main.py#L61-L94))에 있고, 첫 실행 때 `state.json`으로 **복사되어 나갑니다.** 그 뒤로는 파일 쪽이 진짜 데이터이고 코드 쪽은 **비상용 원본**입니다.

---

<a id="q1-5"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 1-5. GitHub 저장소에 코드가 업로드되어 있고, 10개 이상의 의미 있는 커밋이 존재하는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 결과물만 냈나, **과정을 남겼나**
>
> **✅ 답** — 24개. 전부 `Feat:` `Fix:` `Docs:` 접두어
>
> **💡 이유** — 커밋 = **되돌리기의 최소 단위**. 크게 묶으면 멀쩡한 기능까지 같이 사라짐
>
> **🖥️ 증빙** — `git rev-list --count HEAD` · `git log --oneline | cat`
> **📄 코드** — 📸 [commit_list.png](docs/screenshots/commit_list.png)

### 📖 왜 이렇게 만들었나

```bash
git add main.py
git commit -m "Feat: 퀴즈 풀기 기능 구현"
git push origin main
```

| 접두어 | 뜻 |
| --- | --- |
| `Feat:` | 새 기능 |
| `Fix:` | 버그 수정 |
| `Docs:` | 문서 |
| `Refactor:` | 구조만 정리 |
| `Chore:` | 설정 |

### 🖥️ 증빙

**① 개수를 숫자로 보여준다**

```bash
git rev-list --count HEAD
```
```
24
```

**② 내용이 의미 있는지 보여준다** ← 이게 진짜 평가 대상

```bash
git log --oneline | cat
```
```
52b64f6 Docs: 과제 원문과 학습 노트를 저장소에 포함
2287156 Docs: 제출용이 아닌 캡처 제거 및 커밋 수 갱신
af206ef Docs: 실행 화면 및 Git 이력 스크린샷 추가
334e62f Docs: README 링크 정리 및 스크린샷 폴더 추가
3bf2517 Docs: clone 실습으로 README에 동기화 안내 문구 추가
6934ef3 Docs: README 작성 (개요/주제 선정 이유/실행 방법/기능/구조/데이터 파일)
035cbde Refactor: QuizGame 입력 처리 정리 및 불필요한 분기 제거
c961ad8 Fix: Ctrl+C와 입력 스트림 종료 시 안전하게 종료하도록 처리
52fcfec Feat: 점수 확인과 게임 기록 히스토리 구현 (보너스 포함)
66a7da7 Feat: 퀴즈 삭제 기능 구현 (보너스)
49a4f0f Feat: 퀴즈 목록 조회 기능 구현
6b1ba17 Feat: 퀴즈 추가 기능 구현
2d45928 Merge: 퀴즈 풀기 기능을 main에 병합
```

> 🗣️ *"커밋 하나가 기능 하나입니다. **`update`, `수정` 같은 메시지는 세도 의미가 없다**고 봤고, 무엇을 했는지 드러나게 적었습니다."*
> 💡 `| cat` 을 붙이면 넘김 화면(`less`)이 안 떠서 `q`를 누를 일이 없습니다.

**③ GitHub에 실제로 올라가 있는지 보여준다**

브라우저 → **[github.com/ellysuh22/2-homework](https://github.com/ellysuh22/2-homework)**
첫 화면 오른쪽 위에 **`NN Commits`** 가 표시됩니다. 눌러 목록으로 들어가면 터미널과 같은 내용입니다.

```bash
git remote -v      # 연결된 GitHub 주소 확인
git status         # "nothing to commit, working tree clean" 이 나와야 함
```

> ⚠️ **시연 전 확인** — `git status`가 깨끗하지 않으면 **아직 안 올린 게 있다는 뜻**입니다. `git push` 먼저 하세요.

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 커밋 | 그 순간의 코드 상태를 **이름 붙여 저장**한 것. 게임의 세이브 포인트 |
| `git add` / `commit` | `add`는 **저장할 것 고르기**, `commit`은 **실제로 저장**. 두 단계인 이유는 여러 파일 중 일부만 골라 커밋하기 위해서 |
| `git push` | 내 컴퓨터의 커밋을 **GitHub로 올리기**. 커밋만 하면 내 컴퓨터에만 있습니다 |
| `HEAD` | **"지금 내가 보고 있는 커밋"**. `rev-list --count HEAD`는 "여기까지 몇 개냐" |
| "의미 있는" 커밋 | 개수보다 **메시지가 무엇을 했는지 말해 주는가** |

> 🔍 **꼬리질문 대비** — *"커밋을 몇 개로 나눌지 기준이 있었나요?"*
> → **"기능 하나 완성 = 커밋 1개"** 였습니다. 자세한 규칙은 [2-5](#q2-5)에서 설명드립니다.

---

<a id="q1-6"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 1-6. 브랜치 생성 및 병합 기록이 git log --oneline --graph 에서 확인되는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — **원본을 지키며 실험**할 줄 아는가
>
> **✅ 답** — `feature/play-quiz` 만들어 작업 후 `main`에 병합
>
> **💡 이유** — 퀴즈 풀기가 **가장 복잡·위험**해서. 망가져도 `main`은 무사하게
>
> **🖥️ 증빙** — `git show --stat 2d45928 | head -3` → **부모 2개**
> **📄 코드** — 📸 [git_graph.png](docs/screenshots/git_graph.png)

### 📖 왜 이렇게 만들었나

```bash
git switch -c feature/play-quiz   # ① 브랜치 만들고 이동
git switch main                   # ② 원본으로 복귀
git merge feature/play-quiz       # ③ 합치기
```

```
main:  ●───●───●─────────●
                \       /
feature/play-quiz ●───●─/     ← 여기서 실패해도 main은 무사
```

**병합 커밋만 부모가 2개**입니다. 깃이 자동으로 적는 값이라 조작이 안 됩니다.

### 🖥️ 증빙

**① 전체 이력을 한 번 보여준다**

```bash
git log --oneline --graph --all --decorate | cat
```

> 맨 뒤 **`| cat`** 을 붙이면 넘김 화면(`less`)이 안 떠서 `q`를 누를 일이 없습니다.

**② 브랜치 구간만 확대해서 보여준다** ← 이게 핵심

```bash
git log --oneline --graph --decorate 8adca1c..2d45928 | cat
```

**6줄로 끝납니다.**

```
*   2d45928 Merge: 퀴즈 풀기 기능을 main에 병합
|\
| * ec2775c (feature/play-quiz) Feat: 랜덤 출제, 문제 수 선택, 힌트 기능 추가 (보너스)
| * b432e80 Feat: 퀴즈 풀기 기능 구현
|/
* ff3840e Feat: state.json 저장/불러오기 구현
```

| 짚을 곳 | 증명하는 것 |
| --- | --- |
| `\|\` | 브랜치를 **만들었다** |
| `(feature/play-quiz)` | 브랜치 **이름** |
| `Merge:` 줄 | **병합했다** |
| `\|/` | 다시 **합쳐졌다** |

> 🗣️ *"여기서 갈라져서 `feature/play-quiz` 브랜치로 작업했고, 여기서 `main`에 합쳤습니다."*
> 💡 `8adca1c..2d45928` 은 **"이 커밋부터 저 커밋까지만"** 이라는 뜻입니다. 전체를 다 보면 브랜치 부분이 화면 위로 넘어가 버려서 필요한 구간만 잘라 보는 것입니다.

**③ 반박할 수 없는 증거 — 부모가 2개**

```bash
git show --stat 2d45928 | head -3
```
```
commit 2d4592878ce2c2f531bef919add33f4626194bc2
Merge: ff3840e ec2775c
        ↑ 부모가 2개 = 병합 커밋이라는 증거
```

**왜 이게 결정적인가** — 커밋 메시지에 "Merge"라고 **쓰는 건 누구나** 할 수 있는 자기 주장입니다. 하지만 `Merge:` **줄은 진짜 병합해야만 깃이 자동으로 새깁니다.** 사람이 손댈 수 없는 자리라 조작이 안 됩니다.

```bash
git log --merges --oneline | cat    # 병합 커밋만 골라내기 → 1줄
```

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 브랜치 | 원본을 그대로 두고 **따로 작업하는 갈래**. 시험지 원본 대신 연습장 |
| 병합(merge) | 갈래에서 한 작업을 **원본에 합치는 것** |
| 커밋의 **부모** | 커밋은 자기 **앞 커밋이 누구였는지**를 자기 안에 기록합니다. 족보와 같습니다 |
| 왜 부모가 2개인가 | 병합은 두 줄기를 합치는 것이라 **양쪽 모두를** 앞 커밋으로 가리켜야 합니다. 한쪽만 가리키면 다른 쪽 기록이 끊깁니다 |
| `\|\` `\|/` | 그래프가 그린 **갈라짐 / 합쳐짐**. 부모가 2개라서 선이 두 갈래인 것 |
| `A..B` | 커밋 **구간**. "A 다음부터 B까지만" |

> 🔍 **꼬리질문 대비** — *"왜 브랜치를 쓰나요?"*
> → 자세한 이유는 [3-4](#q3-4)에서 설명드립니다. 1-6은 **"했는가"**, 3-4는 **"왜 하는가"** 를 묻는 문항입니다.

---

<a id="q1-7"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 1-7. clone과 pull 실습 수행 흔적을 README 또는 스크린샷으로 확인할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — GitHub와 **양방향**으로 주고받을 줄 아는가
>
> **⚠️ 답** — 실습은 했고 커밋에 남음. **스크린샷은 아직 없음**
>
> **💡 이유** — 커밋만으론 **clone한 폴더에서 만든 건지 구분이 안 됨**
>
> **🖥️ 증빙** — `git log --oneline -- README.md`
> **📄 코드** — 🔴 [clone_pull.png 촬영](#clone-pull-촬영)

### 📖 왜 이렇게 만들었나

| 명령 | 하는 일 | 언제 |
| --- | --- | --- |
| `git clone` | 통째로 복사 | 처음 1번 |
| `git pull` | 바뀐 것만 받기 | 그 뒤 계속 |
| `git push` | 내 것을 올리기 | 커밋한 뒤 |

혼자면 `push`만 쓰지만, **남이 올린 걸 받으려면 `pull`** 이 필요합니다.

### 🖥️ 증빙

```bash
git log --oneline -- README.md | cat
```
```
3bf2517 Docs: clone 실습으로 README에 동기화 안내 문구 추가
```

| 증거 | 위치 |
| --- | --- |
| clone 실습 후 남긴 커밋 | [`3bf2517`](https://github.com/ellysuh22/2-homework/commit/3bf2517) |
| README 동기화 문구 | [README 맨 아래](README.md#동기화-기록) |

> 🗣️ *"저장소를 별도 폴더에 clone하고, 그쪽에서 README를 고쳐 push한 뒤, 원래 폴더에서 pull로 받아왔습니다. 그 결과가 이 커밋입니다."*
> ⚠️ **솔직하게 덧붙이세요** — *"다만 증거로는 부족하다고 봅니다. 터미널 화면을 캡처해 두는 게 맞습니다."*

<a id="clone-pull-촬영"></a>

### 🔴 평가 전 촬영 절차

⚠️ **먼저 로컬 변경사항을 커밋·푸시해 두세요.** 안 그러면 마지막 `pull`에서 충돌납니다.

```bash
clear
# (1) 다른 폴더에 통째로 복사 — clone
cd ~/Desktop
git clone https://github.com/ellysuh22/2-homework.git clone-test
cd clone-test

# (2) 복사본에서 한 줄 고치고 올리기 — add / commit / push
echo "<!-- clone 실습 -->" >> README.md
git add README.md
git commit -m "Docs: clone 실습 기록 추가"
git push

# (3) 원래 폴더로 돌아와 받아오기 — pull
cd ~/Documents/2-homework
git pull
```

> 📸 (1)~(3)이 한 화면에 보이게 캡처 → `./shot.sh clone_pull`
> 끝나면 연습용 폴더 삭제: `rm -rf ~/Desktop/clone-test`

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 원격 저장소(remote) | GitHub에 있는 **공용 사본**. 내 컴퓨터의 저장소와 **별개**입니다 |
| `origin` | 원격 저장소의 **기본 별명**. `git push origin main` = "origin의 main으로 올려라" |
| clone vs pull | clone은 **폴더 자체를 새로 만듦**, pull은 **이미 있는 폴더를 갱신** |
| 충돌(conflict) | 같은 줄을 양쪽에서 고치면 깃이 **누가 맞는지 모름** → 사람이 골라야 함 |

> 🔍 **꼬리질문 대비** — *"pull과 fetch의 차이는?"*
> → `fetch`는 **받아만 오고 합치지 않고**, `pull`은 **받아와서 바로 합칩니다**. `pull` = `fetch` + `merge`입니다.

---

# [항목 2] 코드 구조 및 설계

<a id="q2-1"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 2-1. Quiz와 QuizGame 등 클래스들의 책임을 어떻게 나눴는지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 클래스를 **무슨 기준으로** 나눴나
>
> **✅ 답** — `Quiz` = 문제 카드 / `QuizGame` = 사회자
>
> **💡 이유** — **고쳐야 할 일이 생기는 원인이 다르면 코드도 따로 둡니다.** 문제 모양이 바뀐다고 점수 규칙이 바뀌진 않으니까요
>
> **🖥️ 증빙** — `grep -n "^class" main.py` → **2개뿐**
> **📄 코드** — [Quiz 18-58](main.py#L18-L58) · [QuizGame 97-326](main.py#L97-L326)

### 📖 왜 이렇게 만들었나

```python
class Quiz:                          # 문제 카드
    self.answer = answer             # 정답은 카드 것
    def check(self, s):
        return s == self.answer      # 채점도 카드가

class QuizGame:                      # 사회자
    self.quizzes = default_quizzes() # 카드 전부는 사회자 것
    self.best_score = 0              # 점수도 사회자 것
```

**`self.○○` 가 어느 클래스 안에 있느냐 = 누가 갖고 있느냐**입니다.

📄 [Quiz 18-58](main.py#L18-L58) · [QuizGame 97-326](main.py#L97-L326)

### 🖥️ 증빙

```bash
grep -n "^class" main.py
```
```
18:class Quiz:
97:class QuizGame:
```

**짚어야 할 3곳** — 이 세 줄이 "책임을 나눴다"의 증거입니다

| 위치 | 코드 | 말할 것 |
| --- | --- | --- |
| [`Quiz.check()` 37-39행](main.py#L37-L39) | `return selected == self.answer` | "**정답을 아는 건 카드 자신뿐**입니다" |
| [`ask_one()` 240행](main.py#L240) | `if quiz.check(selected):` | "**채점할 때는** 사회자가 정답을 몰라도 됩니다 — 물어보기만" |
| [`play_quiz()` 270행](main.py#L270) | `score = max(0, round(...))` | "점수 계산은 **사회자 쪽에만** 있습니다" |

> 🗣️ *"`Quiz`는 자기 문제·보기 4개·정답·힌트만 알고, 자기를 보여주는 `display()`와 자기를 채점하는 `check()`만 합니다. `QuizGame`은 메뉴를 띄우고, 카드를 꺼내 출제하고, 점수를 계산하고, 파일에 기록하는 진행 전체를 맡습니다."*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 클래스 | **데이터 + 기능을 한 덩어리로 묶은 설계도** |
| 객체(인스턴스) | 설계도로 찍어낸 **실물**. `Quiz`는 1개지만 퀴즈 객체는 7개 |
| 단일 책임 원칙 | 하나의 클래스는 **한 가지 이유로만** 바뀌어야 한다 |
| 캡슐화 | 정답 같은 내부 정보를 **자기 안에 숨기고** 물어보기만 하게 하는 것 |
| `self` | "이 객체 자신". `self.answer`는 **이 카드의** 정답 |

> 🔍 **꼬리질문 대비** — *"클래스를 3개로 나눌 수도 있지 않나요?"*
> → 네, **저장 담당(`Storage`)** 을 따로 뺄 수 있습니다. 지금은 저장 메서드가 2개뿐이라 `QuizGame` 안에 두었지만, **저장 방식이 늘어나면(DB, 클라우드) 분리하는 게 맞습니다.** 나누는 것 자체가 목적이 아니라 **바뀌는 이유가 갈릴 때** 나누는 것이라고 생각합니다.

---

<a id="q2-2"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 2-2. "입력 처리(검증)", "게임 진행", "데이터 저장/불러오기" 로직을 어떤 기준으로 분리했는지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 함수를 나눈 **기준이 있었나** — *"길어서"는 기준이 아님*
>
> **✅ 답** — **하는 일이 다르면 따로** 뒀습니다
> · **입력 검사** = 사용자가 친 값이 맞는지 보는 일
> · **게임 진행** = 문제 내고 채점하는 일
> · **저장** = 파일에 쓰고 읽는 일
>
> **💡 이유** — 섞어두면 **하나 고치다 다른 게 망가짐**
>
> **🖥️ 증빙** — `grep -n "input_int(" main.py` → **정의 1 + 호출 5**
> **📄 코드** — [입력 153-181](main.py#L153-L181) · [진행 223-292](main.py#L223-L292) · [저장 119-151](main.py#L119-L151)

### 📖 왜 이렇게 만들었나

| 묶음 | 하는 일 | 코드 |
| --- | --- | --- |
| **입력 검사** | 사용자가 친 값이 맞는지 보는 일 | [`input_int()`](main.py#L153-L172) |
| **게임 진행** | 문제 내고 채점하는 일 | [`play_quiz()`](main.py#L248-L277) · [`ask_one()`](main.py#L223-L246) |
| **저장** | 파일에 쓰고 읽는 일 | [`load_state()`](main.py#L119-L138) · [`save_state()`](main.py#L140-L151) |

```python
def add_quiz(self):
    self.save_state()      # ← "저장해"라고만 시킴. 어떻게 저장하는지는 모름
```

**🧒 식당으로 보면** — 주문 확인 / 요리사 / 장부 적는 사람.
장부를 종이에서 컴퓨터로 바꿔도 **요리사는 하던 대로** 요리만 합니다.

### 🖥️ 증빙

```bash
grep -n "input_int(" main.py
```
```
153:    def input_int(...)   ← 정의는 딱 1개
191, 216, 229, 256, 312     ← 부르는 곳은 5개
```

```bash
grep -n "save_state()\|load_state()" main.py
```
```
194, 220, 291, 332, 338     ← 파일을 만지는 곳은 이 2개 메서드로만 모임
```

| 명령 | 증명하는 것 |
| --- | --- |
| ① | **검증 코드는 한 곳뿐** — 5곳이 불러다 씀 |
| ② | 파일 입출력이 **딱 2개 메서드**에 갇혀 있음 |

> 🗣️ *"①에서 **정의가 1개**인 게 핵심입니다. 검증 규칙이 바뀌면 **153행 하나만** 고치면 5곳이 전부 따라옵니다. 복사해 붙였다면 5군데를 찾아 고쳐야 하고, 하나 빠뜨리면 거기서만 터집니다."*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 함수로 뺀다 = 재사용 | 같은 코드를 **복사해 붙이면** 나중에 고칠 때 전부 찾아야 함. 함수는 한 곳만 고치면 됨 |
| 매개변수 | `input_int(prompt, low, high)` — **범위를 밖에서 정해줄 수 있어야** 5곳에서 재사용 가능 |
| 응집도 / 결합도 | 관련된 것끼리 **뭉치고**(응집), 서로 다른 것끼리 **덜 얽히게**(결합) |
| 계층 분리 | 입력 → 처리 → 저장 이 세 층을 섞지 않는 것이 설계의 기본형 |

> 🔍 **꼬리질문 대비** — *"함수를 나누면 항상 좋은가요?"*
> → 아닙니다. **한 번밖에 안 쓰는 코드**를 억지로 빼면 오가며 읽어야 해서 오히려 어렵습니다. 제 기준은 **"두 곳 이상에서 쓰이거나, 바뀌는 이유가 다를 때"** 였습니다.

---

<a id="q2-3"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 2-3. state.json 읽기/쓰기 흐름이 프로그램의 어디에서, 어떤 순서로 발생하는지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 데이터가 **언제 어디서** 읽히고 쓰이는지 아는가
>
> **✅ 답** — **읽기는 켤 때 1곳, 쓰기는 4곳**입니다
> · ① 프로그램 켤 때 → **읽기** ([332](main.py#L332))
> · ② 게임 하는 동안 → 아무것도 안 함
> · ③ 퀴즈 추가 → **쓰기** ([194](main.py#L194))
> · ④ 퀴즈 삭제 → **쓰기** ([220](main.py#L220))
> · ⑤ 한 판 끝 → **쓰기** ([291](main.py#L291))
> · ⑥ `Ctrl+C` → **쓰기** ([338](main.py#L338))
>
> **💡 이유** — 쓰기 4곳은 전부 **데이터가 실제로 바뀌는 순간**
>
> **🖥️ 증빙** — `grep -n "load_state()"` **1곳** / `save_state()` **4곳**
> **📄 코드** — [읽기 332](main.py#L332) · [쓰기 194](main.py#L194)·[220](main.py#L220)·[291](main.py#L291)·[338](main.py#L338)

### 📖 왜 이렇게 만들었나

| 순서 | 언제 | 읽기/쓰기 | 어디서 |
| --- | --- | --- | --- |
| ① | 프로그램 켤 때 | **읽기** | [main.py:332](main.py#L332) |
| ② | 게임 하는 동안 | 아무것도 안 함 | — |
| ③ | 퀴즈 추가하면 | **쓰기** | [main.py:194](main.py#L194) |
| ④ | 퀴즈 삭제하면 | **쓰기** | [main.py:220](main.py#L220) |
| ⑤ | 한 판 끝나면 | **쓰기** | [main.py:291](main.py#L291) |
| ⑥ | `Ctrl+C` 누르면 | **쓰기** | [main.py:338](main.py#L338) |

**읽기는 ①번 딱 한 곳, 쓰기는 ③~⑥ 네 곳입니다.**

**왜 이 지점?** 계속 읽으면 느리고, 끝날 때 한 번만 쓰면 갑자기 꺼졌을 때 다 잃습니다.
파일이 **2.4KB**라 자주 써도 안 느려서 **안전한 쪽**을 골랐습니다.

### 🖥️ 증빙

```bash
grep -n "load_state()" main.py     # 읽기
```
```
332:    game.load_state()          ← 딱 1곳 (시작할 때)
```

```bash
grep -n "save_state()" main.py     # 쓰기
```
```
194:        self.save_state()      ← 퀴즈 추가 직후
220:        self.save_state()      ← 퀴즈 삭제 직후
291:        self.save_state()      ← 한 판 끝나 점수 기록
338:        game.save_state()      ← Ctrl+C로 중단될 때
```

> 🗣️ *"읽기 1곳, 쓰기 4곳입니다. 쓰기 4곳은 전부 **데이터가 실제로 바뀌는 순간**입니다. 아무 데서나 저장하지 않았습니다."*

**🧒 게임 세이브로 생각하면 똑같습니다**

| 게임 | 제 프로그램 |
| --- | --- |
| 켤 때 세이브 파일을 **한 번** 불러옴 | `load_state()` — 1번 |
| 게임 하는 동안은 안 불러옴 | 메모리(변수)에서만 작업 |
| 중요한 일이 생길 때마다 **자동 저장** | `save_state()` — 4곳 |

**눈으로 확인시키기**

```bash
python3.11 main.py <<< $'3\n6'
```
→ `📂 저장된 데이터를 불러왔습니다` 가 **딱 한 번만** 출력됩니다. 메뉴를 여러 번 오가도 다시 읽지 않는다는 뜻입니다.

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 메모리 vs 디스크 | 실행 중 데이터는 **메모리(변수)** 에, 파일은 **디스크**. 파일에 쓰는 순간만 영구 기록 |
| eager loading | 시작할 때 **미리 다 올려두기**. 데이터가 작을 때 가장 단순하고 빠름 |
| write-through | 바뀔 때마다 즉시 쓰기. 안전하지만 쓰기 횟수가 늘어남 |
| 트레이드오프 | 정답이 없는 선택. **왜 그 지점을 골랐는지 말할 수 있는가**가 평가 대상 |

> 🔍 **꼬리질문 대비** — *"프로그램을 두 개 동시에 켜면요?"*
> → 나중에 저장한 쪽이 앞선 쪽을 **통째로 덮어씁니다.** 지금 방식의 명확한 한계이고, 파일 잠금이나 DB가 필요한 지점입니다. ([4-1](#q4-1))

---

<a id="q2-4"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 2-4. Ctrl+C 또는 EOF 상황에서 "안전 종료"를 위해 어떤 처리를 했는지(또는 어떤 처리가 필요했는지) 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 정상적으로 안 꺼도 **데이터를 지키는가**
>
> **✅ 답** — `try`로 감싸 **안내 → 저장 → 정상 종료**
>
> **💡 이유** — 에러는 **위로 올라오니** 맨 바깥 한 곳에서 잡으면 전부 커버
>
> **🖥️ 증빙** — `python3.11 main.py < /dev/null` → Traceback 없음
> **📄 코드** — [main 333-338](main.py#L333-L338)

### 📖 왜 이렇게 만들었나

**1. 어떤 상황인가**

| 상황 | 뜻 | 언제 |
| --- | --- | --- |
| **Ctrl+C** | 키보드로 **강제 종료** | 답답해서 그냥 꺼버릴 때 |
| **EOF** | 입력이 **끊김** | 자동 실행할 때 넣어줄 값이 다 떨어짐 |

둘 다 **`6`번을 눌러 정상적으로 나가지 않은 경우**입니다.

**2. 안전 종료란**

데이터를 안 잃고, 에러 화면 없이 조용히 끝나는 것입니다.

| | 처리 안 하면 | 안전 종료 |
| --- | --- | --- |
| 화면 | 빨간 에러 글씨 | `⚠️ 입력이 중단되었습니다...` |
| 데이터 | 저장 안 된 건 **사라짐** | **저장하고 나감** |

**3. 어떻게 처리했나**

```python
try:
    game.run()                          # 게임을 돌리다가
except (KeyboardInterrupt, EOFError):   # Ctrl+C 나 입력 끊김이 오면
    print("⚠️ 입력이 중단되었습니다...")   # 안내하고
    game.save_state()                   # 저장하고 끝냄
```

📄 [main.py:333-338](main.py#L333-L338)

### 🖥️ 증빙

**① EOF(입력 끊김)** — 안전하고 재현이 확실해서 이걸 먼저 보여주세요

```bash
python3.11 main.py < /dev/null
```
```
📂 저장된 데이터를 불러왔습니다. (퀴즈 7개, 최고점수 100점)
========================================
        🎯 나만의 퀴즈 게임 🎯
========================================
...
선택:

⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.
```

**② Ctrl+C** — 실행한 뒤 `Control` 키와 `C`를 함께 누름

```bash
python3.11 main.py
```
```
선택: ^C

⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.
```

| 나와야 하는 것 | 나오면 **안 되는** 것 |
| --- | --- |
| `⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.` | `Traceback (most recent call last):` 빨간 글씨 |
| | `KeyboardInterrupt` / `EOFError` |

```bash
grep -n "KeyboardInterrupt\|EOFError" main.py
```
```
335:    except (KeyboardInterrupt, EOFError):    ← 한 줄에 둘 다
```

> 🗣️ *"빨간 오류 화면 대신 안내 문구가 나오고, **나가기 전에 저장까지** 하고 나갑니다. 문을 쾅 닫고 나가더라도 공책은 챙겨 나가도록 만든 것입니다."*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 예외(exception) | 프로그램이 계속할 수 없는 상황에서 **위로 던져지는 신호** |
| Traceback | 예외를 아무도 안 잡았을 때 나오는 **빨간 오류 화면** = 비정상 종료 |
| 예외 전파 | 잡지 않으면 **부른 쪽으로 계속 올라감** |
| `except (A, B)` | 괄호로 묶어 **여러 예외를 한 번에** 잡기 |
| `Ctrl+C` | 운영체제가 프로그램에 보내는 **중단 신호**. 파이썬이 `KeyboardInterrupt`로 바꿔줌 |
| EOF | End Of File — **입력이 더 이상 없음**. 터미널에서는 `Ctrl+D` |

> 🔍 **꼬리질문 대비** — *"`finally`를 쓸 수도 있지 않나요?"*
> → 네, `finally: game.save_state()` 로 하면 **정상 종료 때도** 저장됩니다. 다만 정상 종료 경로는 이미 [`add_quiz()`](main.py#L194) 등에서 저장이 끝난 상태라 **중복 저장**이 되어서, 예외 경로에서만 저장하도록 했습니다.

---

<a id="q2-5"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 2-5. 커밋을 어떤 단위로 나누었고, 커밋 메시지 규칙을 어떻게 정했는지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 커밋에 **나만의 규칙**이 있는가
>
> **✅ 답** — **"기능 하나 = 커밋 1개"**, `종류: 요약` 형식
>
> **💡 이유** — 커밋 = 되돌리기 최소 단위. **기능 추가와 구조 정리는 안 섞음**
>
> **🖥️ 증빙** — `git log --oneline | cat`
> **📄 코드** — 📸 [commit_list.png](docs/screenshots/commit_list.png)

### 📖 왜 이렇게 만들었나

**증빙 방법 3가지 — 셋 다 같은 목록이 나옵니다**

| 방법 | 어떻게 |
| --- | --- |
| **GitHub** | [github.com/ellysuh22/2-homework](https://github.com/ellysuh22/2-homework) → **Commits** 클릭 |
| **터미널** | `git log --oneline \| cat` |
| **스크린샷** | [commit_list.png](docs/screenshots/commit_list.png) |

전부 `Feat:` `Fix:` `Docs:` 로 시작하는 것을 보여주면 됩니다.

| 접두어 | 뜻 | 예 |
| --- | --- | --- |
| `Feat:` | 새 기능 | [`243592b`](https://github.com/ellysuh22/2-homework/commit/243592b) |
| `Fix:` | 버그 수정 | [`c961ad8`](https://github.com/ellysuh22/2-homework/commit/c961ad8) |
| `Docs:` | 문서 | [`6934ef3`](https://github.com/ellysuh22/2-homework/commit/6934ef3) |
| `Refactor:` | 구조만 정리 | [`035cbde`](https://github.com/ellysuh22/2-homework/commit/035cbde) |
| `Chore:` | 설정 | [`7234ac8`](https://github.com/ellysuh22/2-homework/commit/7234ac8) |

**기능 추가와 구조 정리를 안 섞은 이유** — 섞으면 문제가 생겼을 때 **어느 쪽 탓인지** 못 가립니다.

### 🖥️ 증빙

**① 규칙을 처음부터 끝까지 지켰다**

```bash
git log --oneline | cat
```
전부 `Feat:` `Fix:` `Docs:` `Refactor:` `Chore:` 로 시작합니다.

**② 기능 추가와 구조 정리를 섞지 않았다** ← 이게 "단위"의 증거

```bash
git log --oneline --grep="^Refactor" | cat
```
```
035cbde Refactor: QuizGame 입력 처리 정리 및 불필요한 분기 제거
```

```bash
git show --stat 035cbde | head -8
```
```
 main.py | ...
 1 file changed
```

> 🗣️ *"이 커밋은 `main.py` 한 파일만 바꿨고 **동작은 그대로, 구조만** 바꾼 커밋입니다. 새 기능을 넣으면서 동시에 구조를 바꾸면, **나중에 문제가 생겼을 때 둘 중 어느 쪽 탓인지 알 수 없습니다.**"*

**③ 개발 순서가 그대로 보인다**

```bash
git log --oneline --reverse | cat
```
→ 메뉴 → 입력 검증 → `Quiz` 클래스 → 기본 퀴즈 → 저장 → 퀴즈 풀기(브랜치) → 추가 → 목록 → 삭제 → 점수 → 안전 종료 → 리팩터링 → README

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 원자적 커밋 | 커밋 하나가 **더 쪼갤 수 없는 하나의 변경**. 되돌리기의 최소 단위 |
| Conventional Commits | `종류: 요약` 형식은 실무에서 널리 쓰는 관례 |
| `Refactor` | **동작을 바꾸지 않고** 구조만 개선. 기능 추가와 절대 섞지 않는 게 원칙 |
| `git show --stat` | 그 커밋이 **어떤 파일을 몇 줄** 바꿨는지 요약 |
| `--grep` | 커밋 메시지에서 **글자를 찾아** 걸러내기 |

> 🔍 **꼬리질문 대비** — *"커밋 메시지를 한국어로 쓴 이유는?"*
> → 접두어는 관례대로 영어로 두고, 요약은 **제가 나중에 읽었을 때 가장 빨리 이해되는 언어**로 썼습니다. 팀 규칙이 있다면 그쪽을 따르는 게 맞습니다.

---

# [항목 3] 핵심 기술 원리 적용

<a id="q3-1"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 3-1. 클래스를 사용한 이유는 무엇이며, 함수만으로 구현할 때와 어떤 차이가 있는지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 클래스를 **안 썼다면 뭐가 불편할지** 비교 가능한가
>
> **✅ 답 — 함수는 "하는 일"만, 클래스는 "하는 일 + 자기 정보"를 가집니다**
>
> **💡 쓴 이유** — 퀴즈마다 문제·정답이 다른데, **각 퀴즈가 자기 정보를 갖고 있게** 하려고
>
> **🔀 함수만 쓸 때 차이** — 퀴즈를 **매번 넘겨야** 함 (`check_answer(퀴즈1, 2)`) / 클래스는 `퀴즈1.check(2)`
>
> **🖥️ 증빙** — `python3.11 -c "from main import Quiz; q=Quiz('테스트',['가','나','다','라'],2); print(q.check(2))"`
> **📄 코드** — [__init__ 21-25](main.py#L21-L25) · [check 37-39](main.py#L37-L39)

### 📖 왜 이렇게 만들었나

**1. 클래스와 함수의 차이**

| | 함수 | 클래스 |
| --- | --- | --- |
| 가진 것 | **하는 일**만 | **하는 일 + 자기 정보** |
| 예 | `check_answer(...)` | `Quiz` — 문제·정답을 갖고 채점도 함 |

**2. 클래스를 쓴 이유**

퀴즈가 7개인데 **문제도 정답도 다 다릅니다.**
클래스로 만들면 **퀴즈 하나하나가 자기 문제와 정답을 갖고** 다닙니다.

**3. 함수만으로 만들었다면**

```python
check_answer(퀴즈1, 2)    # 함수는 정답을 몰라서 퀴즈1을 매번 넘겨야 함
퀴즈1.check(2)            # 클래스는 퀴즈1이 자기 정답을 앎
```

**기호 뜻**

| 기호 | 뜻 |
| --- | --- |
| `( )` | **"실행해라"** 는 표시 |
| `,` | 값과 값 사이 **구분** |
| `.` | **"~의"** — `퀴즈1.check` = 퀴즈1**의** check |

**함수인지 클래스인지 구분하는 법 — 점(`.`)이 앞에 있으면 클래스**

```python
def check_answer(...):    # def   → 함수
class Quiz:               # class → 클래스
```

**객체란?** 클래스로 만들어낸 **실제 물건 하나**입니다.

```python
퀴즈1 = Quiz("리스트 괄호는?", [...], 2)   # ← 이렇게 만든 게 객체
```

- `Quiz` = **틀** (붕어빵 틀)
- `퀴즈1` = 그 틀로 찍어낸 **실물** (붕어빵 하나)

| | 함수만 | 클래스 |
| --- | --- | --- |
| 데이터 | 매번 넘겨야 함 | 자기가 가짐 |
| 힌트 같은 항목 추가 | 관련 함수 **전부** 수정 | **한 줄** 추가 |
| 오타 냈을 때 | `KeyError` — 어디가 문제인지 모름 | `Quiz에 그런 게 없다`고 알려줌 |

📄 [__init__ 21-25](main.py#L21-L25) · [check 37-39](main.py#L37-L39)

### 🖥️ 증빙

```bash
python3.11 -c "
from main import Quiz
q = Quiz('테스트', ['가','나','다','라'], 2)
print(q.question, '| 정답:', q.answer)              # 데이터를 자기가 갖고 있음
print('2번 →', q.check(2), '| 3번 →', q.check(3))   # 기능도 자기가 가짐
"
```
```
테스트 | 정답: 2
2번 → True | 3번 → False
```

**코드 두 곳을 나란히 짚기**

| 위치 | 코드 | 말할 것 |
| --- | --- | --- |
| [37-39행](main.py#L37-L39) | `return selected == self.answer` | "정답을 아는 건 **카드 자신**입니다" |
| [240행](main.py#L240) | `if quiz.check(selected):` | "**채점할 때** 부르는 쪽은 정답이 몇 번인지 **모릅니다**" |

> 🗣️ *"`q` 하나만 있으면 문제도 정답도 채점도 다 됩니다. **데이터를 따로 들고 다닐 필요가 없습니다.**"*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| **함수 vs 클래스 한 문장** | **함수는 "동작"만 갖고, 클래스는 "동작 + 상태(데이터)"를 함께 갖는다** |
| 클래스 / 객체 | 붕어빵 **틀**(1개) / 붕어빵(**여러 개**) |
| `__init__` | 객체를 만들 때 **자동으로 실행**되는 메서드. 속성의 초기값을 채움 |
| `self` | "이 객체 자신". `self.answer = answer` = "**내** 정답 칸에 넣어라" |
| `.` (점) | "~의". `q.check(2)` = "q**의** check 기능을 부른다" |
| 기본값 인자 | `hint=""` — 안 주면 빈 문자열이 들어감 |

> 🔍 **꼬리질문 대비** — *"함수만 써도 되는 경우는?"*
> → **데이터를 들고 있을 필요가 없을 때**입니다. 실제로 `default_quizzes()`는 클래스로 만들 이유가 없어서 **그냥 함수로 뒀습니다.** 클래스를 쓰는 것 자체가 목적이 아니라고 생각합니다.

---

<a id="q3-2"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 3-2. JSON 파일로 데이터를 저장하는 이유와 JSON 형식의 특징을 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 저장 방법이 여럿인데 **왜 하필 JSON**
>
> **✅ 답** — ① **사람이 읽음** ② 파이썬 dict와 **모양 같음** ③ **세계 표준**
>
> **💡 이유** — 문제가 생겼을 때 **파일만 열어보면 원인이 보임**
>
> **🖥️ 증빙** — `head -20 state.json` → 한글 그대로
> **📄 코드** — [쓰기 148-149](main.py#L148-L149) · [읽기 126-127](main.py#L126-L127)

### 📖 왜 이렇게 만들었나

```python
json.dump(data, file, ensure_ascii=False, indent=2)
```

| 옵션 | 없으면 |
| --- | --- |
| `encoding="utf-8"` | 컴퓨터마다 한글이 깨짐 |
| `ensure_ascii=False` | 한글이 암호처럼 저장됨 |
| `indent=2` | 전부 한 줄로 붙어서 저장됨 |

세 옵션의 목적은 하나 — **사람이 파일을 열어 확인할 수 있게.**

📄 [148-149](main.py#L148-L149)

### 🖥️ 증빙

```bash
head -20 state.json
```
```json
{
  "quizzes": [
    {
      "question": "파이썬에서 리스트를 만들 때 사용하는 괄호는?",
      "choices": [ "( )", "[ ]", "{ }", "< >" ],
      "answer": 2,
      "hint": "순서가 있고 나중에 값을 바꿀 수 있는 자료형에 쓰는 기호입니다."
    },
```

| 짚을 곳 | 증명하는 것 |
| --- | --- |
| 한글이 그대로 보임 | `ensure_ascii=False` + UTF-8 |
| 2칸씩 계단 모양 | `indent=2` |
| `{` 안에 `[`, 그 안에 또 `{` | JSON의 **중첩** 특징 |

**옵션을 뺐다면 어떻게 되는지 직접 보여주기**

```bash
python3.11 -c "
import json
d = {'question': '파이썬에서 리스트를 만들 때?'}
print('옵션 O:', json.dumps(d, ensure_ascii=False))
print('옵션 X:', json.dumps(d))
"
```
→ `옵션 X`는 한글이 `\u`로 시작하는 암호처럼 나와 **사람이 못 읽습니다.**

**파이썬과 모양이 같다는 것도 확인**

```bash
python3.11 -c "import json; print(type(json.load(open('state.json'))))"
```
```
<class 'dict'>     ← 읽자마자 파이썬 딕셔너리
```

> 🗣️ *"읽어들이면 **곧바로 파이썬 딕셔너리**가 됩니다. 변환 코드를 따로 짤 필요가 없어서 `json.load()` 한 줄로 끝납니다."*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| JSON | **JavaScript Object Notation**. 원래 웹에서 나왔지만 지금은 언어 무관 표준 |
| 직렬화(serialize) | 메모리의 값을 **파일에 쓸 수 있는 글자로** 바꾸는 것 = `json.dump` |
| 역직렬화(deserialize) | 그 반대 = `json.load` |
| `dump` vs `dumps` | `dump`는 **파일에**, `dumps`는 **문자열로**(s = string) |
| UTF-8 | 전 세계 글자를 담는 표준 인코딩. 한글은 여기서만 안전 |
| 인코딩 | 글자를 **숫자로 바꾸는 규칙**. 규칙이 다르면 글자가 깨집니다 |

> 🔍 **꼬리질문 대비** — *"JSON에 없는 파이썬 타입은?"*
> → 튜플은 리스트로, `None`은 `null`로 바뀝니다. `datetime`은 아예 못 담아서, 저는 [`record_result()`](main.py#L282)에서 **문자열로 바꿔** 저장했습니다.

---

<a id="q3-3"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 3-3. 파일 입출력에서 try/except가 필요한 이유(발생 가능한 실패 케이스 포함)를 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 내 코드가 완벽해도 **실패할 곳**을 아는가
>
> **✅ 답** — 파일은 내 코드 **바깥 세상**이라 없어지거나 깨질 수 있음
>
> **💡 이유** — 목표가 *"에러가 안 나게"* 가 아니라 **"에러가 나도 게임은 돌게"**
>
> **🖥️ 증빙** — 파일을 깨뜨려도 기본 퀴즈로 실행됨
> **📄 코드** — [읽기 125-138](main.py#L125-L138) · [쓰기 147-151](main.py#L147-L151)

### 📖 왜 이렇게 만들었나

```python
try:
    data = json.load(file)                  # 깨졌으면 여기서 터짐
except (OSError, json.JSONDecodeError, KeyError, TypeError) as error:
    print(f"⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. ({error})")
    self.quizzes = default_quizzes()        # 어떤 사고든 기본 퀴즈로
```

| 실패 | 예외 |
| --- | --- |
| 파일 없음 | (미리 `if`로 확인) |
| 내용 깨짐 | `JSONDecodeError` |
| 칸이 없음 | `KeyError` `TypeError` |
| 권한·디스크 | `OSError` |

📄 [125-138](main.py#L125-L138)

### 🖥️ 증빙

⚠️ **반드시 백업부터.** 마지막 줄(되돌리기)까지 꼭 실행하세요.

```bash
cp state.json state.json.bak        # ① 백업 (필수!)
echo '{ 깨진 내용' > state.json      # ② 일부러 손상
python3.11 main.py                  # ③ 실행 → 3으로 목록 확인 → 6으로 종료
mv state.json.bak state.json        # ④ 원상복구 ← 잊지 마세요
```

③에서 나오는 화면:

```
⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. (Expecting property name enclosed in double quotes: line 1 column 3)
========================================
        🎯 나만의 퀴즈 게임 🎯
========================================
```

| 나와야 하는 것 | 나오면 **안 되는** 것 |
| --- | --- |
| `⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다.` | `Traceback` 빨간 글씨 |
| 그 뒤 **메뉴가 정상 출력** | 프로그램 종료 |

> ⚠️ ③에서 퀴즈를 추가하거나 풀면 **복구된 상태가 저장되어 백업이 무의미해집니다.** 확인만 하고 `6`으로 나오세요.

**파일이 아예 없는 경우도**

```bash
mv state.json state.json.bak && python3.11 main.py
# → 📂 저장된 데이터가 없어 기본 퀴즈로 시작합니다. (퀴즈 5개)
mv state.json.bak state.json
```

```bash
grep -n "except" main.py
```
```
131:  except (OSError, json.JSONDecodeError, KeyError, TypeError)   ← 파일 읽기 4종
150:  except OSError                                                ← 파일 쓰기
164:  except ValueError                                             ← 숫자 변환
335:  except (KeyboardInterrupt, EOFError)                          ← 중단
```

> 🗣️ *"파일을 통째로 망가뜨렸는데도 게임은 실행됩니다. **무엇이 잘못됐는지도 괄호 안에** 그대로 알려줍니다."*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| `try / except / else` | `try` 해보고, 터지면 `except`, **안 터졌을 때만** `else` |
| 예외를 **이름으로** 잡기 | `except:` 만 쓰면 진짜 버그까지 삼킴 |
| `as error` | 예외 객체를 변수에 담아 **무엇이 잘못됐는지 출력** |
| `OSError` | 파일·디스크·권한 관련 오류의 **부모 격** 이름 |
| 방어적 프로그래밍 | 바깥에서 오는 것(파일, 입력, 네트워크)은 **항상 의심**하고 짜는 태도 |

> 🔍 **꼬리질문 대비** — *"복구했는데 사용자 데이터는요?"*
> → **사라집니다.** 그게 지금의 명확한 한계이고, [4-2](#q4-2)에서 개선 방향을 말씀드릴 수 있습니다.

---

<a id="q3-4"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 3-4. 브랜치를 분리해 작업한 이유와 병합(merge)의 의미를 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 브랜치를 **왜 쓰는지** 아는가 — [1-6](#q1-6)은 *"했는가"*
>
> **✅ 답** — `main` = 항상 돌아가는 원본 / 브랜치 = **실험용 옆 책상**
>
> **💡 이유** — **위험이 큰 기능만** 분리. 망가져도 원본이 무사
>
> **🖥️ 증빙** — `git show --stat 2d45928 | head -3` → **부모 2개**
> **📄 코드** — 📸 [git_graph.png](docs/screenshots/git_graph.png)

### 📖 왜 이렇게 만들었나

```
main:  ●───●───●─────────●   ← 항상 돌아가는 원본
                \       /
feature/play-quiz ●───●─/    ← 여기서 실패해도 main은 무사
```

**왜 퀴즈 풀기만 뺐나** — 랜덤·힌트·채점·점수가 얽혀 **가장 복잡하고 실패 위험이 커서**입니다.

**병합** = 옆 책상 결과를 원본에 합치는 것. 그래서 **부모가 2개**가 됩니다.

### 🖥️ 증빙

```bash
git branch -a | cat                                            # ① 브랜치 목록
git log --oneline --graph --decorate 8adca1c..2d45928 | cat    # ② 갈라짐/합쳐짐
git show --stat 2d45928 | head -3                              # ③ 부모 2개
```

| 명령 | 나와야 하는 것 | 말할 것 |
| --- | --- | --- |
| ① | `main`, `feature/play-quiz` | "**기능 이름으로** 브랜치를 팠습니다" |
| ② | `\|\` → `\|/` 그림 | "여기서 갈라져 여기서 합쳐졌습니다" |
| ③ | `Merge: ff3840e ec2775c` | "**부모가 2개** — 이게 병합의 정의입니다" |

```bash
git log --oneline main..ec2775c | cat    # 브랜치에서만 한 작업
```
```
ec2775c Feat: 랜덤 출제, 문제 수 선택, 힌트 기능 추가 (보너스)
b432e80 Feat: 퀴즈 풀기 기능 구현
```

> 🗣️ *"이 두 커밋이 옆 책상에서 한 작업입니다. **이걸 만드는 동안 `main`은 계속 정상 동작하는 상태**였습니다."*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 브랜치 | 커밋을 가리키는 **이름표**일 뿐. 폴더를 복사하는 게 아니라서 가볍습니다 |
| `main` | 기본 브랜치. **항상 동작하는 상태**로 유지하는 게 관례 |
| `feature/이름` | 기능 브랜치 명명 관례. 슬래시로 분류를 표현 |
| `git switch -c` | 브랜치를 **만들고 동시에 이동**. `-c` = create |
| merge vs rebase | merge는 **합친 기록을 남김**(부모 2개), rebase는 **일자로 펴서** 기록을 다시 씀 |
| 충돌(conflict) | 양쪽이 **같은 줄**을 고쳤을 때. 깃이 못 정하니 사람이 골라야 함 |

> 🔍 **꼬리질문 대비** — *"충돌이 났었나요?"*
> → 이번엔 브랜치에서 **새 메서드만 추가**해서 충돌이 없었습니다. 충돌은 **양쪽이 같은 줄을 고쳤을 때** 나고, 그때는 깃이 `<<<<<<<` 표시로 양쪽을 보여주면 제가 골라서 커밋합니다.

---

<a id="q3-5"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 3-5. state.json 데이터 구조(필드/중첩 구조)를 현재 형태로 설계한 이유를 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 저장 구조는 **가장 바꾸기 어려운 결정** — 왜 그렇게 정했나
>
> **✅ 답** — 파일 칸 이름 = **`Quiz` 속성 이름** (글자까지 동일)
>
> **💡 이유** — 이름이 다르면 **하나 틀려도 에러 없이 조용히 데이터가 빠짐**
>
> **🖥️ 증빙** — 칸 이름과 속성 이름을 나란히 출력
> **📄 코드** — [to_dict 41-48](main.py#L41-L48) · [from_dict 50-58](main.py#L50-L58)

### 📖 왜 이렇게 만들었나

```json
{ "quizzes": [ {"question":"...", "choices":[...], "answer":2, "hint":"..."} ],
  "best_score": 100,
  "history":   [ {"date":"...", "score":100} ] }
```

칸 이름 `question` `choices` `answer` `hint` 가 [`Quiz.__init__()`](main.py#L21-L25)의 속성 이름과 **글자까지 같습니다.**
그래서 [`to_dict()`](main.py#L41-L48) / [`from_dict()`](main.py#L50-L58) 두 개로 변환이 끝납니다.

**`answer`를 번호(int)로 둔 이유** — 글자로 두면 **띄어쓰기 하나에도** 오답 처리됩니다.

### 🖥️ 증빙

```bash
python3.11 -c "
import json; d = json.load(open('state.json'))
print('최상위 3칸:', list(d.keys()))
print('퀴즈 1개의 칸:', list(d['quizzes'][0].keys()))
"
```
```
최상위 3칸: ['quizzes', 'best_score', 'history']
퀴즈 1개의 칸: ['question', 'choices', 'answer', 'hint']
```

→ 이 4개가 [`Quiz.__init__()` 21-25행](main.py#L21-L25)의 속성 이름과 **글자까지 똑같습니다.**

```python
self.question = question    # ← 파일의 "question"
self.choices = choices      # ← 파일의 "choices"
self.answer = answer        # ← 파일의 "answer"
self.hint = hint            # ← 파일의 "hint"
```

**하위 호환도 직접 증명** — 힌트 칸이 없는 옛날 형식을 넣어봅니다

```bash
python3.11 -c "
from main import Quiz
old = {'question':'옛날 문제','choices':['가','나','다','라'],'answer':1}  # hint 없음
q = Quiz.from_dict(old)
print('에러 없이 생성:', q.question, '| hint =', repr(q.hint))
"
```
```
에러 없이 생성: 옛날 문제 | hint = ''     ← .get() 덕분에 안 죽음
```

> 🗣️ *"칸 이름이 같으니 `to_dict()` / `from_dict()` **두 개만으로** 객체 ↔ 파일 변환이 끝납니다."*

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 리스트 vs 딕셔너리 | 리스트는 **번호**로, 딕셔너리는 **이름(키)** 으로 값을 꺼냄 |
| 스키마(schema) | 데이터의 **칸 이름과 모양**을 정한 약속 |
| `data.get("hint", "")` | 키가 **없어도 죽지 않고** 기본값을 줌. `data["hint"]`는 없으면 `KeyError` |
| `@classmethod` | 객체가 아니라 **클래스 자체가** 부르는 메서드. `Quiz.from_dict(...)` 처럼 "만들어 주는" 용도 |
| `cls` | `self`의 클래스 버전 — "이 클래스 자신" |
| 하위 호환 | 옛날 파일도 **새 코드가 읽을 수 있게** 만드는 것 |

> 🔍 **꼬리질문 대비** — *"퀴즈에 고유 ID를 넣지 않은 이유는?"*
> → 지금은 목록의 **순서 자체가 번호** 역할을 합니다. 다만 삭제하면 뒤 번호가 밀리므로, 퀴즈를 외부에서 참조해야 한다면 **고유 ID를 넣는 게 맞습니다.** (미구현)

---

# [항목 4] 심층 인터뷰

<a id="q4-1"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 4-1. 퀴즈 데이터가 1000개 이상으로 늘어난다면 현재 JSON 저장 방식에 어떤 한계가 생길 수 있는지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 틀렸다는 지적이 아님 — 지금 선택이 **언제까지 유효한지** 아는가
>
> **✅ 답** — 퀴즈 1개만 추가해도 **파일 전체를 다시 씀**
>
> **💡 이유** — 뿌리는 **"부분 수정이 안 된다"** 하나. 여기서 5가지 문제가 파생
>
> **🖥️ 증빙** — `grep -n "json.dump" main.py` → **149행 1곳**
> **📄 코드** — [save_state 140-151](main.py#L140-L151)

### 📖 왜 이렇게 만들었나

```python
with open(STATE_FILE, "w", ...) as file:   # "w" = 기존 내용 삭제
    json.dump(data, file, ...)              # 1000개 전부 다시 씀
```

| 한계 | 왜 |
| --- | --- |
| 저장이 느려짐 | 1개 추가해도 전체 재작성 |
| 메모리 부담 | 시작할 때 전부 올림 |
| 검색 어려움 | 전부 훑어야 함 |
| 손상 위험 | 쓰는 도중 끊기면 통째로 깨짐 |
| 동시 실행 충돌 | 나중 쓰기가 앞 쓰기를 덮음 |

**개선** — `sqlite3`(표준 라이브러리)로 옮기면 됩니다. [`load/save_state()`](main.py#L119-L151) **2개만** 고치면 끝. (미구현)

### 🖥️ 증빙

```bash
grep -n "json.dump" main.py
```
```
149:  json.dump(data, file, ensure_ascii=False, indent=2)   ← 여기서 전체를 다시 씀
```

```bash
python3.11 -c "
import os; size = os.path.getsize('state.json')
print(f'현재 {size:,}바이트 (퀴즈 7개)')
print(f'1000개 환산 약 {size*140:,}바이트')
"
```
```
현재 2,484바이트 (퀴즈 7개)
1000개 환산 약 347,760바이트
```

> 🗣️ *"퀴즈 1개를 추가할 때마다 이 **약 340KB 전체**를 다시 씁니다. 이게 모든 한계의 뿌리입니다."*

**개선 경로가 실현 가능함도 보여주기**

```bash
python3.11 -c "import sqlite3; print('sqlite3 사용 가능 — 설치 불필요, 파이썬 표준 라이브러리')"
```

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 전체 재작성 vs 부분 수정 | 텍스트 파일은 **중간만 고치기가 사실상 불가능** |
| 시간 복잡도 | 데이터가 N배 늘면 저장 시간도 **N배**. 이걸 O(N)이라고 함 |
| 색인(index) | 책의 **찾아보기**. 없으면 처음부터 끝까지 훑어야 함 |
| 원자성(atomicity) | 저장이 **다 되거나 아예 안 되거나** 둘 중 하나여야 안전 |
| SQLite | 서버 없이 **파일 하나로 동작하는 DB**. 파이썬에 기본 포함 |

> 🔍 **꼬리질문 대비** — *"지금 방식이 틀린 건가요?"*
> → 아닙니다. **수십 개 규모에서는 가장 단순하고 안전한 선택**입니다. 처음부터 DB를 쓰면 코드가 훨씬 복잡해지는데, 그건 **지금 없는 문제를 위해 지금 비용을 내는 것**이라고 생각합니다.

---

<a id="q4-2"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 4-2. 만약 state.json이 손상되어 JSON 파싱에 실패한다면, 사용자가 데이터를 잃지 않도록 어떤 대응(복구/백업/초기화)이 가능한지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — **"안 죽는 것"과 "안 잃는 것"** 의 차이를 아는가
>
> **⚠️ 답** — "안 죽게"만 됨. **추가한 퀴즈는 사라짐**
>
> **💡 이유** — 개선하려면 **임시 파일에 쓰고 성공 시 바꿔치기**가 1순위
>
> **🖥️ 증빙** — 파일을 깨뜨리면 살아나지만 퀴즈가 **5개로 줄어듦**
> **📄 코드** — [손상 감지 131-135](main.py#L131-L135)

### 📖 왜 이렇게 만들었나

```python
# 지금 — 깨지면 초기화 (프로그램은 살지만 데이터는 잃음)
except json.JSONDecodeError:
    self.quizzes = default_quizzes()
```
```python
# 개선안 (미구현) — 임시 파일에 쓰고 성공하면 바꿔치기
os.replace(tmp, STATE_FILE)
```

| 순위 | 방법 | 효과 |
| --- | --- | --- |
| 🥇 | 임시 파일 교체 | **손상 자체를 막음** |
| 🥈 | 저장 전 `.bak` 백업 | 직전 상태로 복구 |
| 🥉 | 깨진 파일 보존 | 나중에 수동 복구 |
| ✅ | 초기화 (현재) | 게임은 실행됨 |

📄 [131-135](main.py#L131-L135)

### 🖥️ 증빙

```bash
cp state.json state.json.bak        # ① 백업 (필수!)
echo '{ 깨진 내용' > state.json      # ② 손상
python3.11 main.py                  # ③ 실행 → 3으로 목록 확인 → 6 종료
mv state.json.bak state.json        # ④ 원상복구
```

| 단계 | 결과 | 이게 보여주는 것 |
| --- | --- | --- |
| ③ 첫 줄 | `⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다.` | ✅ **죽지 않음** (1단계 완료) |
| ③ `3` 입력 | 퀴즈가 **기본 5개뿐** — 추가했던 건 사라짐 | ❌ **잃지 않기는 미구현** (2단계) |

```bash
ls state.json*      # → 백업 파일이 없음 = 개선 여지 그 자체
```

> 🗣️ *"여기까지가 지금 구현입니다. 프로그램은 죽지 않지만 **사용자 데이터는 잃습니다.** 이 둘은 다른 문제이고, 두 번째는 아직 해결하지 못했습니다."*
> 💡 **스스로 한계를 드러내는 게 이 문항의 답입니다.** 숨기면 꼬리질문에서 드러납니다.

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| "죽지 않는 것" vs "잃지 않는 것" | 프로그램이 살아도 **데이터가 사라졌으면 해결이 아님**. 이 문항의 핵심 |
| 원자적 쓰기 | 임시 파일에 다 쓰고 **한 번에 교체**하는 기법. 실무의 표준 방식 |
| `os.replace()` | 같은 디스크 안에서 **원자적으로** 파일 이름 교체 |
| 백업 세대 관리 | `.bak` 하나만 두면 **두 번 연속 손상 시** 소용없음 |
| 실패 격리 | 깨진 파일을 **지우지 말고 남겨** 원인 분석과 수동 복구를 가능하게 |

> 🔍 **꼬리질문 대비** — *"왜 아직 구현 안 했나요?"*
> → 미션 요구사항이 "최소 1개 이상 예외 처리"였고, 우선순위를 **필수 기능 완성과 커밋·브랜치 실습**에 뒀습니다. 다만 **어떤 순서로 넣을지는 정해뒀습니다.**

---

<a id="q4-3"></a>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 4-3. "정답 채점 방식(점수 계산)"이나 "퀴즈 구조(선택지 개수 등)" 요구사항이 바뀐다면, 어떤 파일/클래스/메서드를 먼저 수정해야 하는지 설명할 수 있는가?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> [!IMPORTANT]
> **🎯 출제 의도** — 앞의 설계 답변이 **진짜였는지** 시험하는 최종 문제
>
> **✅ 답** — 점수는 `QuizGame`, 선택지는 `Quiz`, 저장은 `load/save_state()`
>
> **💡 이유** — 역할을 나눠둬서. **단 선택지 개수만 흩어져 있어 아쉬움**
>
> **🖥️ 증빙** — `grep -n "HINT_PENALTY"`(1곳 👍) vs `range(1, 5)`(흩어짐 👎)
> **📄 코드** — [HINT_PENALTY 12](main.py#L12) · [play_quiz 270](main.py#L270)

### 📖 왜 이렇게 만들었나

| 무엇이 바뀌면 | 고칠 곳 |
| --- | --- |
| 점수 규칙 | [`play_quiz()` 270](main.py#L270) · [`record_result()`](main.py#L279-L292) |
| 힌트 감점 | [`HINT_PENALTY` 12행](main.py#L12) **한 줄** |
| 선택지 개수 | [`Quiz`](main.py#L18-L58) · [`add_quiz()`](main.py#L183-L195) · [`ask_one()`](main.py#L223-L246) ⚠️ 흩어져 있음 |
| 저장 방식 | [`load_state()`](main.py#L119-L138) · [`save_state()`](main.py#L140-L151) |

**⚠️ 아쉬운 점** — 선택지 개수 `4`가 여러 곳에 박혀 있습니다. `HINT_PENALTY`처럼 **상수로 뽑았어야** 합니다. (미구현)

### 🖥️ 증빙

**👍 좋은 예 — 상수로 모아둔 것**

```bash
grep -n "HINT_PENALTY" main.py
```
```
12:HINT_PENALTY = 5                                      ← 정의 (여기 한 줄)
237:  print(f"💡 힌트: ... {HINT_PENALTY}점 차감)")
270:  score = max(0, round(...) - hint_count * HINT_PENALTY)
274:  print(f"   └ 힌트 {hint_count}회 사용 → ...")
```
> 🗣️ *"감점을 10점으로 바꾸려면 **12행 한 줄**만 고치면 됩니다."*

**👎 아쉬운 예 — 숫자가 흩어진 것**

```bash
grep -n "1-4\|range(1, 5)" main.py
```
```
189:  for number in range(1, 5):                        ← 보기 4개 입력
191:  answer = self.input_int("정답 번호 (1-4): ", 1, 4)  ← 숫자 4 + 안내문구
230:  f"정답 입력 (1-4, 힌트 보기는 {HINT_CHOICE}): "     ← 여기도 "1-4"
```
> 🗣️ *"선택지를 5개로 늘리려면 **여기 전부를 동시에** 고쳐야 하고, 하나만 빠뜨리면 **안내 문구는 '1-4'인데 실제로는 5까지 받는** 어긋남이 생깁니다. 이건 제 코드의 아쉬운 점입니다."*

**직접 실험해 보이기** — ①이 진짜 한 줄인지

```bash
cp main.py main.py.bak
sed -i '' 's/^HINT_PENALTY = 5/HINT_PENALTY = 10/' main.py
grep -n "HINT_PENALTY = " main.py     # → 10으로 바뀜
python3.11 main.py                     # 퀴즈 풀며 힌트 사용 → 10점 차감 확인
mv main.py.bak main.py                 # 원상복구 ← 잊지 마세요
```

### 📚 학습 포인트

| 개념 | 알아야 할 것 |
| --- | --- |
| 매직 넘버 | 코드에 **그냥 박혀 있는 숫자**. `4`가 무슨 뜻인지 코드만 봐선 모름 |
| 상수로 뽑기 | 이름을 붙여 **맨 위에 모아두면** 뜻도 드러나고 한 줄로 고칠 수 있음 |
| 변경 영향 범위 | 한 곳을 고칠 때 **함께 고쳐야 하는 곳**의 넓이. 좁을수록 좋은 설계 |
| 단일 책임 | 하나의 클래스·함수가 **한 가지 이유로만** 바뀌게 하는 것 |
| 대문자 이름 규칙 | 파이썬에서 `HINT_PENALTY`처럼 **전부 대문자**면 "바꾸지 않는 값(상수)"이라는 관례 |

> 🔍 **꼬리질문 대비** — *"그럼 모든 숫자를 상수로 빼야 하나요?"*
> → 아닙니다. **바뀔 가능성이 있는 값**만입니다. `range(1, 5)`의 `1`처럼 절대 안 바뀌는 건 그대로 두는 게 낫습니다. **판단 기준은 "이 값이 바뀔 일이 있는가"** 입니다.

---

<a id="부록a"></a>

# 부록 A. 30초 요약 — 20문항 한 줄 답

> **결과가 아니라 이유를 말하세요.** 아래 표의 굵은 글씨가 이유입니다.

### [항목 1] 기능 동작 검증

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [1-1](#q1-1) | 메뉴로 돌아온다 — **파이썬은 한 번만 읽고 지나가서 반복문이 필요** | `python3.11 main.py <<< $'3\n5\n6'` |
| [1-2](#q1-2) | 5곳이 `input_int()` 하나를 거친다 — **복사했으면 5군데를 다 고쳐야** | `abc` `0` `9` 빈Enter 연속 |
| [1-3](#q1-3) | 바뀔 때마다 파일에 쓴다 — **끝에 한 번만 쓰면 강제 종료 때 다 잃어서** | 퀴즈 개수 **7 → 8** |
| [1-4](#q1-4) | 기본 퀴즈를 **함수로** 만들었다 — **변수면 삭제할 때 원본까지 줄어서** | 파일 치우고 실행 → 5개 |
| [1-5](#q1-5) | 기능 하나 = 커밋 1개 — **되돌릴 수 있는 최소 단위라서** | `git rev-list --count HEAD` |
| [1-6](#q1-6) | 위험한 기능만 브랜치로 — **실패해도 `main`을 지키려고** | `git show --stat 2d45928 \| head -3` |
| [1-7](#q1-7) | 커밋은 있으나 **스크린샷 미첨부** ⚠️ | `git log --oneline -- README.md` |

### [항목 2] 코드 구조 및 설계

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [2-1](#q2-1) | `Quiz`=카드, `QuizGame`=사회자 — **바뀌는 이유가 다르니까** | `grep -n "^class" main.py` |
| [2-2](#q2-2) | 5곳에 같은 검증이 필요해서 — **저장 방식이 바뀌어도 게임 코드는 무사** | 정의 1 + 호출 5 |
| [2-3](#q2-3) | 읽기 1번, 쓰기는 바뀔 때마다 — **매번 읽으면 느리고 끝에 쓰면 다 잃어서** | 읽기 1곳 / 쓰기 4곳 |
| [2-4](#q2-4) | 가장 바깥에서 잡는다 — **예외는 위로 올라오니 한 곳이면 전부 커버** | `main.py < /dev/null` |
| [2-5](#q2-5) | 기능 추가와 구조 정리를 안 섞는다 — **문제 생겼을 때 원인을 가리려고** | `git log --oneline` |

### [항목 3] 핵심 기술 원리

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [3-1](#q3-1) | 함수는 **동작만**, 클래스는 **동작+데이터** — 데이터를 끌고 다닐 필요가 없어서 | `q.check(2)` 직접 실행 |
| [3-2](#q3-2) | 사람이 읽히는 텍스트 + dict와 모양이 같음 — **파일만 열면 원인이 보여서** | `head -20 state.json` |
| [3-3](#q3-3) | 파일은 **내 코드 밖 세상** — 없거나 깨져도 게임은 돌아야 하니까 | 파일 깨뜨려도 실행됨 |
| [3-4](#q3-4) | **위험이 큰 곳에만** 브랜치 — 실패해도 원본이 무사하려고 | 부모 2개 |
| [3-5](#q3-5) | 파일 칸 이름 = `Quiz` 속성 — **이름이 다르면 조용히 데이터가 빠져서** | 칸 이름 나란히 출력 |

### [항목 4] 심층 인터뷰 — 정답 없음, **한계를 아는지**가 답

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [4-1](#q4-1) | 뿌리는 **"부분 수정이 안 되고 전체를 갈아끼운다"** → SQLite (미구현) | `json.dump` 1곳 |
| [4-2](#q4-2) | **"안 죽는 것"까지만.** "안 잃는 것"은 미구현 — 둘은 다른 문제 | 복구되지만 추가분 사라짐 |
| [4-3](#q4-3) | 상수 1곳 👍 vs 매직넘버 3곳 👎 — **바뀔 값이라 생각했는지의 차이** | `HINT_PENALTY` vs `range(1, 5)` |

### 🎤 막혔을 때 쓰는 문장

| 상황 | 이렇게 |
| --- | --- |
| 기능 질문 | "메뉴 루프가 돌면서 번호마다 메서드를 부르고, 6번에서만 빠져나옵니다." |
| 오류 처리 | "숫자를 받는 5곳이 전부 `input_int()` 하나를 거쳐 똑같이 걸러집니다." |
| 저장 질문 | "시작할 때 1번 읽고, 데이터가 바뀔 때마다 즉시 씁니다." |
| 설계 질문 | "고쳐야 할 원인이 다르면 코드도 따로 뒀습니다." |
| **모르는 질문** | "현재 구현은 ~까지입니다. 개선한다면 ~하겠습니다. (미구현)" |

---

<a id="부록b"></a>

# 부록 B. 평가 전 체크리스트

### B-1. 5분 만에 전부 확인하기

```bash
cd ~/Documents/2-homework
python3.11 --version                         # Python 3.11.15 ← 3.10 이상
git rev-list --count HEAD                    # 10보다 큰 숫자
git status                                   # nothing to commit (전부 push 됨)
git log --merges --oneline | cat             # 병합 커밋 1줄
python3.11 main.py <<< $'3\n5\n6'            # 메뉴·목록·점수·종료 한 번에
python3.11 main.py < /dev/null               # 안전 종료 (Traceback 없음)
```

⚠️ **`python3` 말고 반드시 `python3.11`** — 이 컴퓨터의 기본 `python3`는 3.9.6이라 미션 요구(3.10 이상) 미달입니다.

### B-2. 스크린샷 — 2장 남음

| 파일명 | 무엇 | 문항 | 상태 |
| --- | --- | --- | --- |
| `dev_env.png` | VSCode + Python 버전 + Git 설정 | 제출물 | ✅ |
| `menu.png` | 메뉴 화면 | 1-1 | ✅ |
| `add_quiz.png` | 퀴즈 추가 | 1-1 | ✅ |
| `list.png` | 퀴즈 목록 | 1-1, 1-4 | ✅ |
| `play.png` | 퀴즈 풀기 (정답·오답) | 1-2 | ✅ |
| `score.png` | 점수 확인 | 1-1 | ✅ |
| `git_graph.png` | 브랜치 그래프 | 1-6, 3-4 | ✅ |
| `commit_list.png` | 커밋 목록 | 1-5, 2-5 | ✅ |
| **`clone_pull.png`** | clone → push → pull | **1-7** | 🔴 **필요** |
| **`invalid_input.png`** | 잘못된 입력 4종 | **1-2** | 🟡 권장 |

**촬영 방법**

```bash
./shot.sh                    # 진행 상황 확인
./shot.sh clone_pull         # 방금 찍은 캡처를 이 이름으로 저장
```

| 단축키 | 하는 일 |
| --- | --- |
| `Cmd` + `Shift` + `4` | 드래그한 **영역**만 캡처 |
| `Cmd` + `Shift` + `4` → `Space` → 클릭 | **창 하나**를 통째로 |

- **`clone_pull.png`** — [1-7의 촬영 절차](#clone-pull-촬영) 그대로
- **`invalid_input.png`** — 실행 후 `선택:` 에서 연달아 `abc` → `0` → `9` → 그냥 Enter, ⚠️ 경고 4줄이 한 화면에 보이게

```bash
git add docs/screenshots/ && git commit -m "Docs: clone/pull 실습 스크린샷 추가" && git push
```

### B-3. 실습 후 뒷정리 ⚠️

파일을 건드리는 시연([1-4](#q1-4) · [3-3](#q3-3) · [4-2](#q4-2) · [4-3](#q4-3))을 했다면 되돌렸는지 확인하세요.

```bash
ls state.json.bak main.py.bak 2>/dev/null   # 나오면 되돌리기를 안 한 것
git status                                   # 파일이 바뀌지 않았는지
```

→ `.bak` 파일이 남아 있으면 `mv state.json.bak state.json` 으로 복구하세요.
