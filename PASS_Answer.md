# 🎯 미션2 평가 답변 노트

> 문항은 [`미션2_평가기준.txt`](미션2_평가기준.txt) 원문을 **한 글자도 바꾸지 않고** 옮겼습니다.
> 증빙은 실제로 실행해서 확인한 것만 담았습니다.

**각 문항은 네 부분으로 되어 있습니다.**

| 순서 | 무엇 | 언제 |
| --- | --- | --- |
| **✅ 답** | 1~2줄 | 평가자에게 **이것만** 말합니다 |
| **🖥️ 증빙** | 코드 · 실행 결과 · `main.py` 링크 | 답한 뒤 **바로 보여줍니다** |
| **📖 세부 내용** | 어떻게 만들었나 | *"어떻게요?"* 라고 물으면 |
| **📚 학습 내용** | 개념 정리 · 꼬리질문 대비 | *"왜요?"* 라고 물으면 |

> 💡 문항 찾기 — `Cmd`+`Shift`+`O` 누르고 `1-3` 입력.
> 📖 용어가 헷갈리면 → [**용어 한 장 정리**](#용어정리) (평가 직전 2분)

---

<a id="용어정리"></a>

# 📖 용어 한 장 정리

## 질문 2개면 4개가 갈립니다

| | 클래스 **밖** | 클래스 **안** |
| --- | --- | --- |
| **값을 담고 있다** | **변수** | **속성** |
| **일을 한다** (괄호 `()` 있음) | **함수** | **메서드** |

> **① 값을 담나, 일을 하나?  ② 클래스 안인가, 밖인가?**

## 내 코드로 확인

```python
LINE = "=" * 40              # 값 담음 + 클래스 밖 → 변수
def default_quizzes():       # 일을 함 + 클래스 밖 → 함수

class Quiz:
    self.answer = answer     # 값 담음 + 클래스 안 → 속성
    def check(self, ...):    # 일을 함 + 클래스 안 → 메서드
```

📄 [LINE 19행](main.py#L19) · [default_quizzes() 74행](main.py#L74) · [self.answer 36행](main.py#L36) · [check() 50행](main.py#L50)

## 클래스와 객체는 다른 축

```
class Quiz:              ← 틀 (1개)
     │ 찍어냄
     ▼
퀴즈1  퀴즈2  퀴즈3 ...   ← 객체 = 인스턴스 (7개)
 │
 ├─ 가진 것 → self.question, self.answer   = 속성
 └─ 하는 것 → check(), display()           = 메서드
```

## 헷갈리는 짝 4개 — 무엇이 다른가

| 짝 | 차이 | 한 줄 |
| --- | --- | --- |
| **변수** vs **속성** | **위치** | 둘 다 값을 담는 상자. 클래스 안에 있으면 속성 |
| **함수** vs **메서드** | **위치** | 둘 다 일을 하는 기계. 클래스 안에 있으면 메서드 |
| **클래스** vs **객체** | **틀 vs 실물** | 붕어빵 틀 1개 ↔ 붕어빵 여러 개 |
| **객체** vs **인스턴스** | **없음** | 완전히 같은 말. 용어만 2개 |

## ✏️ 스스로 확인

아래가 각각 무엇인지 말해보세요. (답은 접혀 있습니다)

```python
① HINT_PENALTY = 5
② def show_menu(self):        # class QuizGame 안
③ self.quizzes = [...]        # class QuizGame 안
④ def main():                 # 클래스 밖
⑤ Quiz("문제", [...], 2)      # 이걸 실행하면 생기는 것
```

<details>
<summary>답 보기</summary>

| | 답 | 이유 |
| --- | --- | --- |
| ① | **변수** | 값을 담음 + 클래스 밖 |
| ② | **메서드** | 괄호가 있어 일을 함 + 클래스 안 |
| ③ | **속성** | 값을 담음 + 클래스 안 |
| ④ | **함수** | 일을 함 + 클래스 밖 |
| ⑤ | **객체(인스턴스)** | 틀(`Quiz`)로 찍어낸 실물 |

</details>

## 🧠 한 문장

> **"틀은 하나, 붕어빵은 여럿.
> 안에 있으면 속성·메서드, 밖에 있으면 변수·함수."**

## 🎤 평가자가 물으면

> "**클래스는 틀, 객체는 그 틀로 만든 실물**입니다. 객체가 **가진 값이 속성**, **할 수 있는 일이 메서드**입니다.
> `Quiz` 틀은 하나인데 퀴즈 객체는 7개 있습니다."

---

# [항목 1] 기능 동작 검증

<a id="q1-1"></a>

## 📋 1-1. 프로그램 실행 시 메뉴가 정상적으로 표시되고, 퀴즈 풀기/추가/목록/점수 확인 기능이 모두 동작하는가?

### ✅ 답

> **메뉴 6개가 모두 동작하고, 기능이 끝나면 메뉴로 다시 돌아옵니다.**
> **6번을 눌렀을 때만 종료됩니다.** 한 번 쓰고 꺼지는 프로그램이 아닙니다.

### 🖥️ 증빙

```bash
cd ~/Documents/2-homework
python3.11 main.py
```

| 칠 것 | 뜻 | 이어서 칠 것 |
| --- | --- | --- |
| `1` | 퀴즈 풀기 | 문제 수 `2` → 답 `1~4` (힌트는 `5`) |
| `2` | 퀴즈 추가 | 문제 → 선택지 4개 → 정답번호 |
| `3` | 목록 | — |
| `4` | 삭제 | 지울 번호 |
| `5` | 점수 | — |
| `6` | 종료 | — |

> **기능이 끝날 때마다 메뉴가 다시 나오고, `6`에서만 끝납니다.**


### 📖 세부 내용

```python
def run(self):
    while True:                 # 계속 반복
        self.show_menu()        # 메뉴 출력
        choice = self.input_int("선택: ", 1, 6)
        if choice == 1: self.play_quiz()
        ...
        else: break             # 6번이면 종료
```

| 코드 | 뜻 |
| --- | --- |
| `while True` | 계속 반복 — **없으면 한 번 쓰고 프로그램이 끝남** |
| `break` | 반복 탈출 = 종료 |

📄 [run 321-339](main.py#L321-L339) · [show_menu 118-130](main.py#L118-L130)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| `while True` | **"조건이 항상 참 = 영원히 반복"**. 파이썬은 코드를 위에서 아래로 **한 번만** 읽고 지나가기 때문에, 되돌아가려면 반복문이 필요합니다. 없으면 메뉴가 한 번 뜨고 프로그램이 죽습니다 |
| `break` | 반복문을 **즉시 탈출**. 무한 반복에 **나가는 문**을 하나 만들어 두는 것 |
| `if / elif / else` | 여러 갈림길 중 **맞는 것 하나만** 실행. `elif`는 "앞이 아니라면 이건?" |
| `self.` | "이 객체 자신의" — `self.show_menu()`는 "내가 가진 show_menu 기능을 부른다" |
| 메서드 vs 함수 | 클래스 **안**에 있는 함수를 메서드라 부릅니다 |

> 🔍 **꼬리질문 대비** — *"`while True` 대신 다른 방법은?"*
> → `while self.running:` 처럼 **상태 변수**로 만들 수도 있습니다. 종료 조건이 여러 곳에서 생길 때 유리하지만, **지금은 종료 지점이 한 곳뿐이라** `break`가 더 읽기 쉽다고 판단했습니다.

---

<a id="q1-2"></a>

## 📋 1-2. 퀴즈 풀기 시 정답/오답 판정이 정확하고, 최소 입력 오류 케이스(공백/문자/범위 밖/빈 입력)에 대한 처리가 되어 있는가?

### ✅ 답

> **글자·범위 밖·빈 입력·앞뒤 공백 4가지를 모두 걸러내고, 잘못 넣어도 꺼지지 않고 다시 물어봅니다.**
> **오답일 때는 정답 번호와 내용까지 알려줍니다.**

### 🖥️ 증빙

```bash
cd ~/Documents/2-homework
python3.11 main.py
```

`선택:` 이 뜰 때마다 **하나씩 치고 엔터** — 프로그램은 끄지 않습니다.

| 순서 | 칠 것 | 나오는 것 |
| --- | --- | --- |
| 1 | `abc` ↵ | ⚠️ 숫자가 아닙니다 |
| 2 | `0` ↵ | ⚠️ 잘못된 입력입니다 |
| 3 | `9` ↵ | ⚠️ 잘못된 입력입니다 |
| 4 | **엔터만** ↵ | ⚠️ 입력이 비어 있습니다 |
| 5 | `스페이스 스페이스 3` ↵ | 📋 목록 나옴 ← 공백 지우고 처리 |

> "4번 틀렸는데 **안 꺼지고 계속 물어봅니다.**"

이어서 **정답/오답**

| 순서 | 칠 것 |
| --- | --- |
| 6 | `1` ↵ (퀴즈 풀기) |
| 7 | `1` ↵ (1문제만) |
| 8 | **일부러 틀린 번호** ↵ → `❌ 정답은 N번 (내용)` |

```bash
grep -n "input_int(" main.py     # 정의 1개(165행) + 부르는 곳 5개
```

📄 [input_int() 166-185행](main.py#L166-L185) · [Quiz.check() 50-52행](main.py#L50-L52)
📸 [play.png](docs/screenshots/play.png)

👉 **짚을 곳** — [main.py:166-185](main.py#L166-L185) `input_int()`
→ 빈값 → 문자 → 범위 **3단 검사**를 통과해야만 `return` 합니다.

### 📖 세부 내용

```python
def input_int(self, prompt, low, high):
    while True:
        value = input(prompt).strip()          # ① 공백 제거
        if value == "": ...; continue          # ② 빈 입력
        try: number = int(value)
        except ValueError: ...; continue       # ③ 문자
        if number < low or number > high:      # ④ 범위 밖
            ...; continue
        return number                          # 통과한 값만 내보냄
```

숫자를 받는 **5곳**([204](main.py#L204)·[229](main.py#L229)·[242](main.py#L242)·[269](main.py#L269)·[325](main.py#L325))이 전부 이 함수를 부릅니다.

📄 [input_int 166-185](main.py#L166-L185)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| `try / except` | `try`는 **"일단 해봐"**, `except`는 **"터지면 이렇게 해"** |
| `ValueError` | "값의 형태가 잘못됐다"는 이름의 예외. 예외를 **이름으로 지정**해야 다른 사고를 실수로 삼키지 않습니다 |
| `continue` | 반복문의 **이번 회차만 건너뛰고 처음으로**. `break`(완전 탈출)와 반대 |
| `.strip()` | 문자열 **앞뒤 공백 제거**. ` 3 `과 `3`을 같게 취급하려고 |
| `return` | 값을 돌려주며 **함수를 끝냄**. 4관문을 다 통과했을 때만 여기 도달 |
| 0-based 인덱스 | 리스트는 0번부터 — 사람 번호로 쓰려면 항상 **-1** |

> 🔍 **꼬리질문 대비** — *"메뉴 말고 다른 데서도 되나요?"*
> → `2`(퀴즈 추가)로 들어가 `정답 번호 (1-4):` 에서 `abc`를 넣어 보이세요. **똑같은 경고**가 나옵니다. 5곳이 전부 같은 함수를 쓰기 때문입니다.

---

<a id="q1-3"></a>

## 📋 1-3. 프로그램 종료 후 재실행해도 추가한 퀴즈와 최고 점수가 유지되는가?

### ✅ 답

> **유지됩니다. 추가한 퀴즈와 최고 점수를 `state.json` 파일에 적어두기 때문입니다.**
> 변수는 프로그램을 끄면 사라지지만, 파일은 남습니다.

### 🖥️ 증빙

| | 칠 것 | 나오는 것 |
| --- | --- | --- |
| 1 | `grep -c '"question"' state.json` | `7` |
| 2 | `python3.11 main.py` | 메뉴 |
| 3 | `2` | 퀴즈 추가 |
| 4 | `파이썬에서 화면에 출력하는 함수는?` | |
| 5 | `print` | |
| 6 | `input` | |
| 7 | `len` | |
| 8 | `int` | |
| 9 | `1` | ✅ 퀴즈가 추가되었습니다! |
| 10 | `6` | 👋 종료 |
| 11 | `grep -c '"question"' state.json` | `8` ★ |
| 12 | `python3.11 main.py` | 📂 퀴즈 8개, 최고점수 100점 |

📄 [load_state() 132-151행](main.py#L132-L151) · [save_state() 153-164행](main.py#L153-L164)

👉 **짚을 곳** — [main.py:161](main.py#L161) `open(..., "w")` 적는 곳 ↔ [main.py:139](main.py#L139) `open(..., "r")` 읽는 곳
→ 값을 변수가 아니라 **파일에 글자로** 적어두기 때문에 남습니다.

### 📖 세부 내용

```
┌─────────────────────────────────────────────────────┐
│  main.py  (코드)                                     │
│  · 기본 퀴즈 5개  ← 저장 파일이 없을 때만 씀           │
└──────────────────────┬──────────────────────────────┘
                       │ 켤 때
                       ▼
┌─────────────────────────────────────────────────────┐
│  메모리 (실행 중)                        끄면 사라짐 ✕ │
│  · 퀴즈 목록                                         │
│  · 최고 점수                                         │
│  · 지금 몇 번 문제인지, 맞은 개수  ← 저장 안 함        │
└──────────────────────┬──────────────────────────────┘
                       │ 바뀔 때마다  ↕  켤 때 읽기
                       ▼
┌─────────────────────────────────────────────────────┐
│  state.json  (파일)                       끄면 남음 ✓ │
│  · quizzes    퀴즈 목록                              │
│  · best_score 최고 점수                              │
│  · history    게임 기록                              │
└─────────────────────────────────────────────────────┘
```

```python
save_state()   # 메모리 → 파일   (194·220·291·338행에서 호출)
load_state()   # 파일 → 메모리   (332행, 켤 때 1번)
```

📄 [load_state 132-151](main.py#L132-L151) · [save_state 153-164](main.py#L153-L164)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 데이터 영속성 | 프로그램이 꺼져도 데이터가 **남는 성질**. 이 문항이 묻는 것 자체 |
| `with open(...)` | 파일을 열고 **자동으로 닫아주는** 문법. 안 닫으면 내용이 안 써질 수 있습니다 |
| `"w"` / `"r"` | write(쓰기, **기존 내용 삭제**) / read(읽기) |
| `encoding="utf-8"` | 한글을 저장하는 규칙. 안 쓰면 컴퓨터마다 글자가 깨질 수 있습니다 |
| `json.dump` / `json.load` | 파이썬 값 ↔ 파일 글자 변환 ([3-2](#q3-2)에서 자세히) |
| 리스트 컴프리헨션 | `[quiz.to_dict() for quiz in self.quizzes]` = "목록 전부를 하나씩 변환한 새 목록" |

> 🔍 **꼬리질문 대비** — *"저장을 매번 하면 느리지 않나요?"*
> → 지금은 파일이 2.4KB라 즉시 끝납니다. 다만 퀴즈가 1000개가 되면 문제가 되고, 그건 [4-1](#q4-1)에서 설명드릴 수 있습니다.

---

<a id="q1-4"></a>

## 📋 1-4. 기본 퀴즈가 5개 이상 포함되어 있는가?

### ✅ 답

> **파이썬 기초 문법 문제 5개가 코드 안에 들어 있습니다.**
> 저장 파일이 없는 상태 — 즉 **처음 내려받아 실행한 사람도** 바로 5문제를 풀 수 있습니다.

### 🖥️ 증빙

```bash
python3.11 -c "from main import default_quizzes; print(len(default_quizzes()), '개')"
```
```
5 개
```

**저장 파일이 없을 때도 확인** (⚠️ 지우지 말고 옮기세요)

```bash
mv state.json state.json.bak    # ① 저장 파일을 잠시 치움
python3.11 main.py              # ② → "저장된 데이터가 없어 기본 퀴즈로 시작합니다. (퀴즈 5개)"
mv state.json.bak state.json    # ③ 원래대로 ← 잊지 마세요
```

📄 [default_quizzes() 74-107행](main.py#L74-L107)
📸 [list.png](docs/screenshots/list.png)

👉 **짚을 곳** — [main.py:74-107](main.py#L74-L107) `default_quizzes()`
→ 기본 5문제가 **코드 안에** 들어 있습니다.

### 📖 세부 내용

```python
def default_quizzes():
    return [Quiz(...), Quiz(...), ...]   # 부를 때마다 새로 만들어 줌
```

**변수로 뒀다면** 목록이 하나뿐이라, 사용자가 퀴즈를 지울 때 **복구용 원본까지** 줄어듭니다.

📄 [default_quizzes 74-107](main.py#L74-L107)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 함수 vs 변수 (기본값) | 여러 곳에서 쓰는 **목록·딕셔너리**는 함수로 만들어 매번 새로 주는 게 안전. 파이썬에서 리스트는 **같은 것을 공유**하기 때문 |
| `return [ ... ]` | 대괄호 = 리스트. 그 안에 `Quiz(...)` 5개 = **객체 5개를 담은 목록** |
| 객체 생성 | `Quiz("문제", [...], 2, "힌트")` 라고 부르는 순간 `__init__`이 실행되어 카드 한 장이 만들어짐 |
| 기본값 인자 | `def __init__(self, ..., hint="")` — 힌트를 안 줘도 빈 문자열로 채워짐 |
| 가변 객체(mutable) | 리스트·딕셔너리는 **내용이 바뀔 수 있는** 자료형. 그래서 공유하면 위험합니다 |

> 🔍 **꼬리질문 대비** — *"5개는 어디에 저장되나요?"*
> → 코드 안([main.py:74-107](main.py#L74-L107))에 있고, 첫 실행 때 `state.json`으로 **복사되어 나갑니다.** 그 뒤로는 파일 쪽이 진짜 데이터이고 코드 쪽은 **비상용 원본**입니다.

---

<a id="q1-5"></a>

## 📋 1-5. GitHub 저장소에 코드가 업로드되어 있고, 10개 이상의 의미 있는 커밋이 존재하는가?

### ✅ 답

> **커밋 30개 — 게임 개발 15개 + 문서·자료 15개입니다.** 요구 기준(10개)을 넘습니다.
> 메시지는 전부 `Feat:` `Fix:` `Docs:` 처럼 **무엇을 했는지 드러나게** 적었습니다.

### 🖥️ 증빙

```bash
git rev-list --count HEAD     # → 30
git log --oneline | cat       # 전부 Feat:/Fix:/Docs:/Refactor:/Chore: 로 시작
```
```
52b64f6 Docs: 과제 원문과 학습 노트를 저장소에 포함
035cbde Refactor: QuizGame 입력 처리 정리 및 불필요한 분기 제거
c961ad8 Fix: Ctrl+C와 입력 스트림 종료 시 안전하게 종료하도록 처리
52fcfec Feat: 점수 확인과 게임 기록 히스토리 구현 (보너스 포함)
2d45928 Merge: 퀴즈 풀기 기능을 main에 병합
```

🔗 [github.com/ellysuh22/2-homework](https://github.com/ellysuh22/2-homework) — 첫 화면에 커밋 수 표시
📸 [commit_list.png](docs/screenshots/commit_list.png)

> ⚠️ 시연 전 `git status`가 `nothing to commit` 인지 확인하세요. 아니면 `git push` 먼저.

🔗 **바로가기** — [커밋 목록 전체](https://github.com/ellysuh22/2-homework/commits/main)
→ 깃허브 첫 화면의 커밋 수로도 바로 확인됩니다.

### 📖 세부 내용

```bash
git add main.py
git commit -m "Feat: 퀴즈 풀기 기능 구현"
git push origin main
```

| 접두어 | 뜻 |
| --- | --- |
| `Feat:` | 새 기능 |
| `Fix:` | 버그 수정 |
| `Docs:` | 문서 |
| `Refactor:` | 구조만 정리 |
| `Chore:` | 설정 |

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 커밋 | 그 순간의 코드 상태를 **이름 붙여 저장**한 것. 게임의 세이브 포인트 |
| `git add` / `commit` | `add`는 **저장할 것 고르기**, `commit`은 **실제로 저장**. 두 단계인 이유는 여러 파일 중 일부만 골라 커밋하기 위해서 |
| `git push` | 내 컴퓨터의 커밋을 **GitHub로 올리기**. 커밋만 하면 내 컴퓨터에만 있습니다 |
| `HEAD` | **"지금 내가 보고 있는 커밋"**. `rev-list --count HEAD`는 "여기까지 몇 개냐" |
| "의미 있는" 커밋 | 개수보다 **메시지가 무엇을 했는지 말해 주는가** |

> 🔍 **꼬리질문 대비** — *"커밋을 몇 개로 나눌지 기준이 있었나요?"*
> → **"기능 하나 완성 = 커밋 1개"** 였습니다. 자세한 규칙은 [2-5](#q2-5)에서 설명드립니다.

---

<a id="q1-6"></a>

## 📋 1-6. 브랜치 생성 및 병합 기록이 git log --oneline --graph 에서 확인되는가?

### ✅ 답

> **`feature/play-quiz` 브랜치를 만들어 퀴즈 풀기를 개발한 뒤 `main`에 합쳤습니다.**
> 그래프에서 **갈라졌다가 다시 합쳐지는 모양**으로 보입니다.

### 🖥️ 증빙

```bash
git log --oneline --graph --decorate 8adca1c..2d45928 | cat
```
```
*   2d45928 Merge: 퀴즈 풀기 기능을 main에 병합
|\                                              ← 갈라짐
| * ec2775c (feature/play-quiz) Feat: 랜덤 출제, 문제 수 선택, 힌트 기능 추가
| * b432e80 Feat: 퀴즈 풀기 기능 구현
|/                                              ← 합쳐짐
* ff3840e Feat: state.json 저장/불러오기 구현
```

**반박할 수 없는 증거 — 부모가 2개**

```bash
git show --stat 2d45928 | head -3
```
```
Merge: ff3840e ec2775c      ← 진짜 병합해야만 깃이 자동으로 새기는 줄
```

📸 [git_graph.png](docs/screenshots/git_graph.png)

🔗 **바로가기** — [병합 커밋 `2d45928`](https://github.com/ellysuh22/2-homework/commit/2d45928) · [네트워크 그래프](https://github.com/ellysuh22/2-homework/network)
→ 커밋 화면 맨 위 `Merge: ff3840e ec2775c` — **부모가 2개**인 것이 병합의 증거입니다.

### 📖 세부 내용

```bash
git switch -c feature/play-quiz   # ① 브랜치 만들고 이동
git switch main                   # ② 원본으로 복귀
git merge feature/play-quiz       # ③ 합치기
```

```
main:  ●───●───●─────────●
                \       /
feature/play-quiz ●───●─/     ← 여기서 실패해도 main은 무사
```

**병합 커밋만 부모가 2개**입니다. 깃이 자동으로 적는 값이라 조작이 안 됩니다.

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 브랜치 | 원본을 그대로 두고 **따로 작업하는 갈래**. 시험지 원본 대신 연습장 |
| 병합(merge) | 갈래에서 한 작업을 **원본에 합치는 것** |
| 커밋의 **부모** | 커밋은 자기 **앞 커밋이 누구였는지**를 자기 안에 기록합니다. 족보와 같습니다 |
| 왜 부모가 2개인가 | 병합은 두 줄기를 합치는 것이라 **양쪽 모두를** 앞 커밋으로 가리켜야 합니다. 한쪽만 가리키면 다른 쪽 기록이 끊깁니다 |
| `\|\` `\|/` | 그래프가 그린 **갈라짐 / 합쳐짐**. 부모가 2개라서 선이 두 갈래인 것 |
| `A..B` | 커밋 **구간**. "A 다음부터 B까지만" |

> 🔍 **꼬리질문 대비** — *"왜 브랜치를 쓰나요?"*
> → 자세한 이유는 [3-4](#q3-4)에서 설명드립니다. 1-6은 **"했는가"**, 3-4는 **"왜 하는가"** 를 묻는 문항입니다.

---

<a id="q1-7"></a>

## 📋 1-7. clone과 pull 실습 수행 흔적을 README 또는 스크린샷으로 확인할 수 있는가?

### ✅ 답

> **`clone` → `pull` 을 실행한 터미널 화면을 캡처해 첨부했습니다.**
> clone한 폴더에서 고쳐 push한 기록은 커밋 `3bf2517` 로 남아 있습니다.

### 🖥️ 증빙

```bash
git reflog --date=iso | head -2
```
```
4b72aae HEAD@{2026-08-16 20:35:21 +0900}: pull: Fast-forward
5e9cebd HEAD@{2026-08-16 18:45:44 +0900}: clone: from https://github.com/ellysuh22/2-homework.git
```

👉 clone과 pull 두 줄이 `.git/logs/HEAD` 에 남아 있습니다.
→ `pull: Fast-forward` = 원격에서 **실제로 받아왔다**는 뜻입니다.

📸 **[clone_pull.png](docs/screenshots/clone_pull.png)** ← 이것 하나로 1-7이 끝납니다

```
% cd ~/Documents
% git clone https://github.com/ellysuh22/2-homework.git test-clone   ← clone
% cd test-clone
% git pull                                                            ← pull
Cloning into 'test-clone'...
Receiving objects: 100% (108/108), 789.63 KiB | 4.59 MiB/s, done.
Already up to date.
ellysuh@... test-clone %                    ← ★ clone한 폴더 안에서 실행했다는 증거
```

👉 **짚을 곳** — 마지막 줄의 프롬프트가 `test-clone` 입니다.
→ 커밋 기록만으로는 어느 폴더에서 했는지 구분이 안 되는데, **이 한 줄이 그걸 증명합니다.**

```bash
git log --oneline -- README.md | cat
```
```
3bf2517 Docs: clone 실습으로 README에 동기화 안내 문구 추가
```

🔗 커밋 [`3bf2517`](https://github.com/ellysuh22/2-homework/commit/3bf2517) · [README 맨 아래 동기화 기록](README.md#동기화-기록)

🔗 **바로가기** — [커밋 `3bf2517`](https://github.com/ellysuh22/2-homework/commit/3bf2517) · [README 동기화 기록](README.md)
→ clone한 폴더에서 고쳐 push하고, 원래 폴더에서 pull로 받은 흔적입니다.

### 📖 세부 내용

| 명령 | 하는 일 | 언제 |
| --- | --- | --- |
| `git clone` | 통째로 복사 | 처음 1번 |
| `git pull` | 바뀐 것만 받기 | 그 뒤 계속 |
| `git push` | 내 것을 올리기 | 커밋한 뒤 |

혼자면 `push`만 쓰지만, **남이 올린 걸 받으려면 `pull`** 이 필요합니다.

<a id="clone-pull-촬영"></a>

### ✅ 촬영 완료 — 재촬영이 필요할 때의 절차

⚠️ **먼저 로컬 변경사항을 커밋·푸시해 두세요.** 안 그러면 마지막 `pull`에서 충돌납니다.

```bash
clear
# (1) 다른 폴더에 통째로 복사 — clone
cd ~/Desktop
git clone https://github.com/ellysuh22/2-homework.git clone-test
cd clone-test

# (2) 복사본에서 한 줄 고치고 올리기 — add / commit / push
echo "<!-- clone 실습 -->" >> README.md
git add README.md
git commit -m "Docs: clone 실습 기록 추가"
git push

# (3) 원래 폴더로 돌아와 받아오기 — pull
cd ~/Documents/2-homework
git pull
```

> 📸 (1)~(3)이 한 화면에 보이게 캡처 → `./shot.sh clone_pull`  *(이미 촬영 완료)*
> 끝나면 연습용 폴더 삭제: `rm -rf ~/Desktop/clone-test`

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 원격 저장소(remote) | GitHub에 있는 **공용 사본**. 내 컴퓨터의 저장소와 **별개**입니다 |
| `origin` | 원격 저장소의 **기본 별명**. `git push origin main` = "origin의 main으로 올려라" |
| clone vs pull | clone은 **폴더 자체를 새로 만듦**, pull은 **이미 있는 폴더를 갱신** |
| 충돌(conflict) | 같은 줄을 양쪽에서 고치면 깃이 **누가 맞는지 모름** → 사람이 골라야 함 |

> 🔍 **꼬리질문 대비** — *"pull과 fetch의 차이는?"*
> → `fetch`는 **받아만 오고 합치지 않고**, `pull`은 **받아와서 바로 합칩니다**. `pull` = `fetch` + `merge`입니다.

---

# [항목 2] 코드 구조 및 설계

<a id="q2-1"></a>

## 📋 2-1. Quiz와 QuizGame 등 클래스들의 책임을 어떻게 나눴는지 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — 문제 한 장은 `Quiz`, 게임 전체는 `QuizGame`이 맡게 나눴습니다.
>
> **② 용어** — 여기서 **책임**은 "이 클래스가 **무엇을 알고, 무엇을 할 수 있는지**"입니다.
>
> **③ 무엇** — `Quiz`는 문제·선택지·정답·힌트를 알고 **채점만** 합니다.
> `QuizGame`은 퀴즈 목록·점수·메뉴 흐름·저장을 맡습니다.
> 나눈 기준은 **"고쳐야 할 원인이 다르면 따로 둔다"** 입니다.
>
> **④ ⭐ 왜** — 안 나눴으면 **문제 모양을 바꾸는 일**과 **점수 규칙을 바꾸는 일**이 한 덩어리에 섞여서, 하나 고치다 다른 게 망가집니다.
> 지금은 `QuizGame`이 **정답을 몰라도 채점이 됩니다** — 정답을 아는 건 `Quiz` 자신뿐이니까요.

### 🖥️ 증빙

```bash
grep -n "^class" main.py
```
```
18:class Quiz:
97:class QuizGame:
```

**이 세 줄이 "책임을 나눴다"의 증거입니다**

| 위치 | 코드 | 무엇을 보여주나 |
| --- | --- | --- |
| [50-52행](main.py#L50-L52) | `return selected == self.answer` | 정답을 아는 건 **카드 자신뿐** |
| [253행](main.py#L253) | `if quiz.check(selected):` | 사회자는 **정답을 몰라도** 채점됨 |
| [283행](main.py#L283) | `score = max(0, round(...))` | 점수 계산은 **사회자 쪽에만** |

📄 [Quiz 30-71행](main.py#L30-L71) · [QuizGame 110-339행](main.py#L110-L339)

👉 **짚을 곳** — [main.py:30](main.py#L30) `class Quiz` ↔ [main.py:110](main.py#L110) `class QuizGame`
→ **문제 한 장**과 **게임 전체**로 책임이 갈립니다.

### 📖 세부 내용

```python
class Quiz:                          # 문제 카드
    self.answer = answer             # 정답은 카드 것
    def check(self, s):
        return s == self.answer      # 채점도 카드가

class QuizGame:                      # 사회자
    self.quizzes = default_quizzes() # 카드 전부는 사회자 것
    self.best_score = 0              # 점수도 사회자 것
```

**`self.○○` 가 어느 클래스 안에 있느냐 = 누가 갖고 있느냐**입니다.

📄 [Quiz 30-71](main.py#L30-L71) · [QuizGame 110-339](main.py#L110-L339)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 클래스 | **데이터 + 기능을 한 덩어리로 묶은 설계도** |
| 객체(인스턴스) | 설계도로 찍어낸 **실물**. `Quiz`는 1개지만 퀴즈 객체는 7개 |
| 단일 책임 원칙 | 하나의 클래스는 **한 가지 이유로만** 바뀌어야 한다 |
| 캡슐화 | 정답 같은 내부 정보를 **자기 안에 숨기고** 물어보기만 하게 하는 것 |
| `self` | "이 객체 자신". `self.answer`는 **이 카드의** 정답 |

> 🔍 **꼬리질문 대비** — *"클래스를 3개로 나눌 수도 있지 않나요?"*
> → 네, **저장 담당(`Storage`)** 을 따로 뺄 수 있습니다. 지금은 저장 메서드가 2개뿐이라 `QuizGame` 안에 두었지만, **저장 방식이 늘어나면(DB, 클라우드) 분리하는 게 맞습니다.** 나누는 것 자체가 목적이 아니라 **바뀌는 이유가 갈릴 때** 나누는 것이라고 생각합니다.

---

<a id="q2-2"></a>

## 📋 2-2. "입력 처리(검증)", "게임 진행", "데이터 저장/불러오기" 로직을 어떤 기준으로 분리했는지 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — 하는 일이 다르면 따로 뒀습니다. **입력 검사 / 게임 진행 / 파일 저장** 3덩어리입니다.
>
> **② 용어** — **분리**는 "한 메서드가 **한 가지 일만** 하게 쪼개는 것"입니다.
>
> **③ 무엇** — 입력 검사는 `input_int()`·`input_text()`, 게임 진행은 `play_quiz()`·`ask_one()`, 저장은 `load_state()`·`save_state()`가 맡습니다.
>
> **④ ⭐ 왜** — 안 나눴으면 **똑같은 입력 검사 코드가 5곳에 복사**됩니다 (메뉴·정답·문제 수·삭제 번호·추가).
> 안내 문구 하나 바꾸려면 **5곳을 다 고쳐야 하고**, 하나 빠뜨리면 화면마다 다른 안내가 나옵니다.
> 지금은 `input_int()` **한 곳만** 고치면 전부 바뀝니다.

### 🖥️ 증빙

```bash
grep -n "input_int(" main.py             # 정의 1개(153행), 부르는 곳 5개
grep -n "save_state()\|load_state()" main.py   # 파일을 만지는 곳은 이 2개뿐
```

| 묶음 | 담당 | 왜 따로 |
| --- | --- | --- |
| 입력 검사 | [`input_int()`](main.py#L166-L185) | 검증 규칙이 바뀌면 **153행 한 줄만** 고치면 5곳이 따라옴 |
| 게임 진행 | [`play_quiz()`](main.py#L261-L290) · [`ask_one()`](main.py#L236-L259) | 출제·점수 규칙이 바뀔 때만 수정 |
| 파일 저장 | [`load_state()`](main.py#L132-L151) · [`save_state()`](main.py#L153-L164) | 저장 방식이 바뀌어도 **여기 2개만** |

👉 **짚을 곳** — [main.py:166](main.py#L166) 입력 · [main.py:261](main.py#L261) 진행 · [main.py:153](main.py#L153) 저장
→ 세 가지 일이 **각각 다른 메서드**에 들어 있습니다.

### 📖 세부 내용

| 묶음 | 하는 일 | 코드 |
| --- | --- | --- |
| **입력 검사** | 사용자가 친 값이 맞는지 보는 일 | [`input_int()`](main.py#L166-L185) |
| **게임 진행** | 문제 내고 채점하는 일 | [`play_quiz()`](main.py#L261-L290) · [`ask_one()`](main.py#L236-L259) |
| **저장** | 파일에 쓰고 읽는 일 | [`load_state()`](main.py#L132-L151) · [`save_state()`](main.py#L153-L164) |

```python
def add_quiz(self):
    self.save_state()      # ← "저장해"라고만 시킴. 어떻게 저장하는지는 모름
```

**🧒 식당으로 보면** — 주문 확인 / 요리사 / 장부 적는 사람.
장부를 종이에서 컴퓨터로 바꿔도 **요리사는 하던 대로** 요리만 합니다.

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 함수로 뺀다 = 재사용 | 같은 코드를 **복사해 붙이면** 나중에 고칠 때 전부 찾아야 함. 함수는 한 곳만 고치면 됨 |
| 매개변수 | `input_int(prompt, low, high)` — **범위를 밖에서 정해줄 수 있어야** 5곳에서 재사용 가능 |
| 응집도 / 결합도 | 관련된 것끼리 **뭉치고**(응집), 서로 다른 것끼리 **덜 얽히게**(결합) |
| 계층 분리 | 입력 → 처리 → 저장 이 세 층을 섞지 않는 것이 설계의 기본형 |

> 🔍 **꼬리질문 대비** — *"함수를 나누면 항상 좋은가요?"*
> → 아닙니다. **한 번밖에 안 쓰는 코드**를 억지로 빼면 오가며 읽어야 해서 오히려 어렵습니다. 제 기준은 **"두 곳 이상에서 쓰이거나, 바뀌는 이유가 다를 때"** 였습니다.

---

<a id="q2-3"></a>

## 📋 2-3. state.json 읽기/쓰기 흐름이 프로그램의 어디에서, 어떤 순서로 발생하는지 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — 읽기는 **켤 때 딱 1번**, 쓰기는 **데이터가 실제로 바뀌는 4곳**입니다.
>
> **② 용어** — **읽기**는 파일 → 메모리로 가져오는 것, **쓰기**는 메모리 → 파일로 적는 것입니다.
>
> **③ 무엇** — 읽기는 `main()`에서 게임을 시작하기 **전에** `load_state()` 한 번.
> 쓰기는 **퀴즈 추가 / 퀴즈 삭제 / 한 판 끝 / `Ctrl+C` 중단** 4곳에서 `save_state()`.
>
> **④ ⭐ 왜** — 끝날 때 한 번만 저장했다면, **강제 종료되는 순간 그때까지 추가한 퀴즈가 전부 날아갑니다.**
> 그래서 **바뀐 직후 바로** 적도록 했습니다.

### 📌 먼저 — JSON이 뭔가

**① 정의**

> **JSON**은 데이터를 저장하고 주고받기 위한 **글자(텍스트) 형식**입니다.
> **`.json` 파일**은 그 형식으로 적힌 파일입니다.

**② 특징 4가지**

| | |
| --- | --- |
| **`"항목 이름": 값` 형태로 적는다** | `"best_score": 100` |
| **글자 파일이다** | 메모장으로 열면 사람이 읽을 수 있음 |
| **상자 안에 상자를 넣을 수 있다** | `{ }` 안에 `{ }`, `[ ]` 를 겹쳐 넣음 |
| **언어·운영체제를 안 가린다** | 파이썬·자바·웹 어디서나 읽힘 (그래서 표준으로 씀) |

**③ 왜 JSON인가**

| 방식 | 문제 |
| --- | --- |
| 그냥 텍스트 | 자르는 규칙을 **내가 만들어야** 함, 줄바꿈에 깨짐 |
| CSV(엑셀) | 값에 **쉼표가 있으면** 깨짐 |
| 데이터베이스 | **메모장으로 못 보고** 이 규모엔 과함 |
| **JSON** | 규칙이 이미 있고, **파이썬 딕셔너리와 모양이 같아 두 줄로** 저장·복원 |

**④ 이 프로젝트에서는**

> `state.json` — 퀴즈 목록·최고 점수·게임 기록을 담는 **저장 파일**. (저장소가 아니라 **파일 하나**)

### 🖥️ 증빙

```bash
grep -n "load_state()" main.py     # → 332행 딱 1곳 (시작할 때)
grep -n "save_state()" main.py     # → 194 · 220 · 291 · 338행
```

| 순서 | 언제 | 읽기/쓰기 | 위치 |
| --- | --- | --- | --- |
| ① | 프로그램 켤 때 | **읽기** | [345행](main.py#L345) |
| ② | 게임 하는 동안 | 아무것도 안 함 | — |
| ③ | 퀴즈 추가 | **쓰기** | [207행](main.py#L207) |
| ④ | 퀴즈 삭제 | **쓰기** | [233행](main.py#L233) |
| ⑤ | 한 판 끝 | **쓰기** | [304행](main.py#L304) |
| ⑥ | `Ctrl+C` | **쓰기** | [351행](main.py#L351) |

```bash
python3.11 main.py <<< $'3\n6'   # "불러왔습니다"가 딱 한 번만 출력됨
```

👉 **짚을 곳** — 읽기 [main.py:345](main.py#L345) / 쓰기 [207](main.py#L207) · [233](main.py#L233) · [304](main.py#L304) · [339](main.py#L339)
→ **읽기 1번, 쓰기 4번** — 데이터가 바뀌는 곳마다 씁니다.

### 📖 세부 내용

| 순서 | 언제 | 읽기/쓰기 | 어디서 |
| --- | --- | --- | --- |
| ① | 프로그램 켤 때 | **읽기** | [main.py:345](main.py#L345) |
| ② | 게임 하는 동안 | 아무것도 안 함 | — |
| ③ | 퀴즈 추가하면 | **쓰기** | [main.py:207](main.py#L207) |
| ④ | 퀴즈 삭제하면 | **쓰기** | [main.py:233](main.py#L233) |
| ⑤ | 한 판 끝나면 | **쓰기** | [main.py:304](main.py#L304) |
| ⑥ | `Ctrl+C` 누르면 | **쓰기** | [main.py:339](main.py#L339) |

**읽기는 ①번 딱 한 곳, 쓰기는 ③~⑥ 네 곳입니다.**

**왜 이 지점?** 계속 읽으면 느리고, 끝날 때 한 번만 쓰면 갑자기 꺼졌을 때 다 잃습니다.
파일이 **2.4KB**라 자주 써도 안 느려서 **안전한 쪽**을 골랐습니다.

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 메모리 vs 디스크 | 실행 중 데이터는 **메모리(변수)** 에, 파일은 **디스크**. 파일에 쓰는 순간만 영구 기록 |
| eager loading | 시작할 때 **미리 다 올려두기**. 데이터가 작을 때 가장 단순하고 빠름 |
| write-through | 바뀔 때마다 즉시 쓰기. 안전하지만 쓰기 횟수가 늘어남 |
| 트레이드오프 | 정답이 없는 선택. **왜 그 지점을 골랐는지 말할 수 있는가**가 평가 대상 |

> 🔍 **꼬리질문 대비** — *"프로그램을 두 개 동시에 켜면요?"*
> → 나중에 저장한 쪽이 앞선 쪽을 **통째로 덮어씁니다.** 지금 방식의 명확한 한계이고, 파일 잠금이나 DB가 필요한 지점입니다. ([4-1](#q4-1))

---

<a id="q2-4"></a>

## 📋 2-4. Ctrl+C 또는 EOF 상황에서 "안전 종료"를 위해 어떤 처리를 했는지(또는 어떤 처리가 필요했는지) 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — `Ctrl+C`를 누르거나 입력이 끊겨도 **빨간 오류 없이 안내를 띄우고, 저장한 뒤** 종료됩니다.
>
> **② 용어** — `Ctrl+C`는 사용자가 **강제로 중단**하는 것, **EOF**는 입력이 **더 이상 안 들어오는** 상태입니다.
>
> **③ 무엇** — `main()`에서 게임 실행을 `try`로 감싸고 `KeyboardInterrupt`·`EOFError` 두 가지를 잡아, 안내를 띄운 뒤 `save_state()`를 하고 끝냅니다.
>
> **④ ⭐ 왜** — 안 잡았으면 화면에 **빨간 오류 글자가 쏟아지고**, 사용자는 프로그램이 고장 난 줄 압니다.
> 게다가 **그때까지 한 게 저장되지 않습니다.**
> 문을 쾅 닫고 나가더라도 **공책은 챙겨 나가게** 만든 부분입니다.

### 📌 용어 — 이게 무슨 오류인가

| | 정식 이름 | 뜻 |
| --- | --- | --- |
| **`Ctrl+C`** | `KeyboardInterrupt` | 사용자가 **키보드로 강제 중단**한 것 |
| **`EOF`** | `EOFError` | **입력이 더 이상 안 들어오는** 상태 (End Of File = 입력 끝) |

> 둘 다 **"코드가 잘못돼서 나는 오류"가 아닙니다.**
> 프로그램은 멀쩡한데 **밖에서 중단시킨 것**입니다.

### 🖥️ 증빙

```bash
python3.11 main.py < /dev/null     # 입력이 끊긴 상황을 그대로 재현
```
```
선택:

⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.
```

| 나와야 하는 것 | 나오면 **안 되는** 것 |
| --- | --- |
| `⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.` | `Traceback` 빨간 글씨 |

```bash
grep -n "KeyboardInterrupt\|EOFError" main.py    # → 335행 한 줄에 둘 다
```

📄 [main() 342-351행](main.py#L342-L351)

👉 **짚을 곳** — [main.py:348](main.py#L348) `except (KeyboardInterrupt, EOFError)`
→ Ctrl+C를 **잡아서** 안내 문구를 띄우고 저장한 뒤 종료합니다.

### 📖 세부 내용

**1. 어떤 상황인가**

| 상황 | 뜻 | 언제 |
| --- | --- | --- |
| **Ctrl+C** | 키보드로 **강제 종료** | 답답해서 그냥 꺼버릴 때 |
| **EOF** | 입력이 **끊김** | 자동 실행할 때 넣어줄 값이 다 떨어짐 |

둘 다 **`6`번을 눌러 정상적으로 나가지 않은 경우**입니다.

**2. 안전 종료란**

데이터를 안 잃고, 에러 화면 없이 조용히 끝나는 것입니다.

| | 처리 안 하면 | 안전 종료 |
| --- | --- | --- |
| 화면 | 빨간 에러 글씨 | `⚠️ 입력이 중단되었습니다...` |
| 데이터 | 저장 안 된 건 **사라짐** | **저장하고 나감** |

**3. 어떻게 처리했나**

```python
try:
    game.run()                          # 게임을 돌리다가
except (KeyboardInterrupt, EOFError):   # Ctrl+C 나 입력 끊김이 오면
    print("⚠️ 입력이 중단되었습니다...")   # 안내하고
    game.save_state()                   # 저장하고 끝냄
```

📄 [main.py:346-351](main.py#L346-L351)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 예외(exception) | 프로그램이 계속할 수 없는 상황에서 **위로 던져지는 신호** |
| Traceback | 예외를 아무도 안 잡았을 때 나오는 **빨간 오류 화면** = 비정상 종료 |
| 예외 전파 | 잡지 않으면 **부른 쪽으로 계속 올라감** |
| `except (A, B)` | 괄호로 묶어 **여러 예외를 한 번에** 잡기 |
| `Ctrl+C` | 운영체제가 프로그램에 보내는 **중단 신호**. 파이썬이 `KeyboardInterrupt`로 바꿔줌 |
| EOF | End Of File — **입력이 더 이상 없음**. 터미널에서는 `Ctrl+D` |

> 🔍 **꼬리질문 대비** — *"`finally`를 쓸 수도 있지 않나요?"*
> → 네, `finally: game.save_state()` 로 하면 **정상 종료 때도** 저장됩니다. 다만 정상 종료 경로는 이미 [`add_quiz()`](main.py#L207) 등에서 저장이 끝난 상태라 **중복 저장**이 되어서, 예외 경로에서만 저장하도록 했습니다.

---

<a id="q2-5"></a>

## 📋 2-5. 커밋을 어떤 단위로 나누었고, 커밋 메시지 규칙을 어떻게 정했는지 설명할 수 있는가?

### ✅ 답

### ① 커밋을 어떤 단위로 나눴나

> **"기능 하나 완성 = 커밋 1개"** 로 나눴습니다.

메뉴 만들고 1개, 퀴즈 풀기 만들고 1개 — 이런 식입니다.
**기능 추가와 코드 정리는 한 커밋에 섞지 않았습니다.**

```
Feat: 메뉴 화면 출력과 종료 기능 구현
Feat: Quiz 클래스 구현
Feat: 퀴즈 풀기 기능 구현
Fix: Ctrl+C와 입력 스트림 종료 시 안전하게 종료하도록 처리
```

### ② 커밋 메시지 규칙은 어떻게 정했나

> **"앞에 작업 종류를 쓰고, 뒤에 무엇을 했는지 한 줄로 쓴다."**

```
Feat: 퀴즈 풀기 기능 구현
└종류┘  └──무엇을 했는지──┘
```

종류는 5개만 씁니다.

| 앞에 붙이는 말 | 이럴 때 |
| --- | --- |
| `Feat:` | 새 기능을 만들었을 때 |
| `Fix:` | 잘못된 걸 고쳤을 때 |
| `Docs:` | 문서를 썼을 때 |
| `Refactor:` | 코드를 정리했을 때 |
| `Chore:` | 설정을 만졌을 때 |

### ③ 용어 정리

| 말 | 뜻 |
| --- | --- |
| **커밋** | 여기까지의 상태를 저장하는 것 |
| **커밋 단위** | 그 저장을 **어디서 끊느냐** |
| **커밋 메시지** | 그 저장에 붙이는 **제목** |

### ④ 왜 그렇게 했나

**섞으면 문제가 생겼을 때 어느 쪽 탓인지 못 가립니다.** 되돌리려 해도 **멀쩡한 것까지 같이 되돌아갑니다.**

메시지에 종류를 붙인 건, 게시판 글에 `[공지]` `[질문]` 을 붙이는 것처럼 **목록만 훑어도 무슨 커밋인지 보이게** 하려는 것입니다.

### 🖥️ 증빙

```bash
git log --oneline --grep="^Refactor" | cat
git show --stat 035cbde | head -8
```
```
035cbde Refactor: QuizGame 입력 처리 정리 및 불필요한 분기 제거
 main.py | ...
 1 file changed          ← 동작은 그대로, 구조만 바꾼 커밋
```

| 접두어 | 뜻 | 예 |
| --- | --- | --- |
| `Feat:` | 새 기능 | [`243592b`](https://github.com/ellysuh22/2-homework/commit/243592b) |
| `Fix:` | 버그 수정 | [`c961ad8`](https://github.com/ellysuh22/2-homework/commit/c961ad8) |
| `Docs:` | 문서 | [`6934ef3`](https://github.com/ellysuh22/2-homework/commit/6934ef3) |
| `Refactor:` | 구조만 정리 | [`035cbde`](https://github.com/ellysuh22/2-homework/commit/035cbde) |
| `Chore:` | 설정 | [`7234ac8`](https://github.com/ellysuh22/2-homework/commit/7234ac8) |

🔗 **바로가기** — [커밋 목록 전체](https://github.com/ellysuh22/2-homework/commits/main)
→ 메시지가 전부 `Feat:` `Fix:` `Docs:` `Refactor:` `Chore:` 로 시작합니다.

### 📖 세부 내용

**증빙 방법 3가지 — 셋 다 같은 목록이 나옵니다**

| 방법 | 어떻게 |
| --- | --- |
| **GitHub** | [github.com/ellysuh22/2-homework](https://github.com/ellysuh22/2-homework) → **Commits** 클릭 |
| **터미널** | `git log --oneline \| cat` |
| **스크린샷** | [commit_list.png](docs/screenshots/commit_list.png) |

전부 `Feat:` `Fix:` `Docs:` 로 시작하는 것을 보여주면 됩니다.

| 접두어 | 뜻 | 예 |
| --- | --- | --- |
| `Feat:` | 새 기능 | [`243592b`](https://github.com/ellysuh22/2-homework/commit/243592b) |
| `Fix:` | 버그 수정 | [`c961ad8`](https://github.com/ellysuh22/2-homework/commit/c961ad8) |
| `Docs:` | 문서 | [`6934ef3`](https://github.com/ellysuh22/2-homework/commit/6934ef3) |
| `Refactor:` | 구조만 정리 | [`035cbde`](https://github.com/ellysuh22/2-homework/commit/035cbde) |
| `Chore:` | 설정 | [`7234ac8`](https://github.com/ellysuh22/2-homework/commit/7234ac8) |

**기능 추가와 구조 정리를 안 섞은 이유** — 섞으면 문제가 생겼을 때 **어느 쪽 탓인지** 못 가립니다.

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 원자적 커밋 | 커밋 하나가 **더 쪼갤 수 없는 하나의 변경**. 되돌리기의 최소 단위 |
| Conventional Commits | `종류: 요약` 형식은 실무에서 널리 쓰는 관례 |
| `Refactor` | **동작을 바꾸지 않고** 구조만 개선. 기능 추가와 절대 섞지 않는 게 원칙 |
| `git show --stat` | 그 커밋이 **어떤 파일을 몇 줄** 바꿨는지 요약 |
| `--grep` | 커밋 메시지에서 **글자를 찾아** 걸러내기 |

> 🔍 **꼬리질문 대비** — *"커밋 메시지를 한국어로 쓴 이유는?"*
> → 접두어는 관례대로 영어로 두고, 요약은 **제가 나중에 읽었을 때 가장 빨리 이해되는 언어**로 썼습니다. 팀 규칙이 있다면 그쪽을 따르는 게 맞습니다.

---

# [항목 3] 핵심 기술 원리 적용

<a id="q3-1"></a>

## 📋 3-1. 클래스를 사용한 이유는 무엇이며, 함수만으로 구현할 때와 어떤 차이가 있는지 설명할 수 있는가?

### ✅ 답

### ① 클래스를 사용한 이유

> **퀴즈가 7개인데 문제마다 정답이 달라서, 각 퀴즈가 자기 정답을 갖고 다니게 하려고 썼습니다.**

### ② 함수만으로 구현할 때와의 차이

> **함수는 '동작'만 갖고, 클래스는 '동작 + 데이터'를 함께 갖습니다.**

**함수는 동작만 하니까 →**

- 데이터(정답)를 **밖에 따로** 둬야 합니다
- 문제 목록과 정답 목록이 **따로 놉니다**
- 그래서 문제를 **지우거나 섞으면 짝이 어긋납니다** 💥
- 힌트를 추가하려면 **관련 함수를 전부** 고쳐야 합니다

**클래스는 동작과 데이터를 같이 가지니까 →**

- 문제와 정답이 **한 덩어리**입니다
- 지우든 섞든 **같이 움직여서** 어긋날 수 없습니다
- 힌트 추가도 **`self.hint` 한 줄**이면 끝입니다

> **그래서 클래스를 썼습니다.**

```python
채점(정답목록[2], 3)      # 함수  — 정답을 찾아서 넘겨줘야 함
quiz.check(3)            # 클래스 — 퀴즈가 자기 정답을 앎
```

### ③ 차이점 비교

| 비교 항목 | 함수만 쓸 때 | 클래스를 쓸 때 |
| --- | --- | --- |
| **갖는 것** | **동작**만 | **동작 + 데이터** |
| **데이터** | 쓸 때마다 **넘겨줘야** 함 | **자기가 갖고 다님** |
| **문제와 정답** | 목록 2개를 **짝 맞춰야** 함 | **한 덩어리**라 어긋날 수 없음 |
| **문제 1개 삭제** | 정답 목록도 고쳐야 함<br>→ 깜빡하면 **전부 오답** 💥 | **통째로 버리면 끝** |
| **힌트 항목 추가** | 관련 함수를 **전부** 수정 | `self.hint` **한 줄** |
| **채점 코드** | `채점(정답목록[2], 3)` | `quiz.check(3)` |

### ④ 그래서 왜 클래스를 골랐나

| | |
| --- | --- |
| **클래스를 쓴 이유** | 퀴즈가 **여러 개**이고 **각각 정답이 달라서** |
| **함수만으로 안 한 이유** | 문제와 정답이 따로 놀아 **짝이 어긋나서** |

> ⚠️ 다만 **함수도 씁니다.** [`default_quizzes()`](main.py#L74)는 퀴즈 5개를 만들어 주고 끝나는 일이라 **들고 있을 데이터가 없어서** 함수로 뒀습니다.
> **클래스를 쓰는 것 자체가 목적은 아니라고 생각합니다.**

### 🖥️ 증빙

```bash
python3.11 -c "
from main import Quiz
q = Quiz('테스트', ['가','나','다','라'], 2)
print(q.question, '| 정답:', q.answer)
print('2번 →', q.check(2), '| 3번 →', q.check(3))
"
```
```
테스트 | 정답: 2
2번 → True | 3번 → False      ← 문제도 정답도 채점도 q 하나로 끝남
```

| 위치 | 코드 | 무엇을 보여주나 |
| --- | --- | --- |
| [50-52행](main.py#L50-L52) | `return selected == self.answer` | 정답을 아는 건 **카드 자신** |
| [253행](main.py#L253) | `if quiz.check(selected):` | 부르는 쪽은 정답을 **모름** |

👉 **짚을 곳** — [main.py:52](main.py#L52) `self.answer` ↔ [main.py:253](main.py#L253) `quiz.check(selected)`
→ 정답을 아는 건 **`Quiz` 자신**이고, 채점을 시키는 쪽은 정답을 **모릅니다**.

### 📖 세부 내용

**① 한 줄로 보는 차이**

```python
check_answer(퀴즈1, 2)    # 함수  — 정답을 몰라서 퀴즈1을 매번 넘김
퀴즈1.check(2)            # 클래스 — 퀴즈1이 자기 정답을 앎
```

**② 쉽게 말하면**

함수만 쓰기는 문제지와 정답지가 **따로** 있는 것, 클래스는 문제 뒤에 정답이 **붙어 있는 카드**입니다.
퀴즈가 7개면 앞쪽은 짝이 헷갈리지만, 카드는 **섞일 일이 없습니다.**

**③ 그래서 뭐가 달라지나**

| | 함수만 | 클래스 |
| --- | --- | --- |
| 데이터 | 매번 넘겨야 함 | **자기가 가짐** |
| 힌트 같은 항목 추가 | 관련 함수 **전부** 수정 | **한 줄** 추가 |
| 이름 오타 | `KeyError` — 어디가 문제인지 모름 | `Quiz에 그런 게 없다`고 알려줌 |

**④ `__init__` 한눈에 보기**

```python
def __init__(self, question, choices, answer, hint=""):   # ← 문제 하나 만들 때 자동 실행
    self.question = question    # 문제      (self. = 이 문제 자신의)
    self.choices = choices      # 선택지 4개
    self.answer = answer        # 정답 번호
    self.hint = hint            # 힌트
# 정보 4개를 따로 두면 문제 수만큼 변수가 늘어나서 한 덩어리로 묶음
```

> **맨 윗줄 = 무엇**(문제 하나 만들 때 자동 실행) · **맨 아랫줄 = 왜**(한 덩어리로 묶으려고)

📄 [main.py:33-37](main.py#L33-L37) `__init__` · [main.py:50-52](main.py#L50-L52) `check`

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| **함수 vs 클래스 한 문장** | **함수는 "동작"만 갖고, 클래스는 "동작 + 상태(데이터)"를 함께 갖는다** |
| 클래스 / 객체 | 붕어빵 **틀**(1개) / 붕어빵(**여러 개**) |
| `__init__` | 객체를 만들 때 **자동으로 실행**되는 메서드. 속성의 초기값을 채움 |
| `self` | "이 객체 자신". `self.answer = answer` = "**내** 정답 칸에 넣어라" |
| `.` (점) | "~의". `q.check(2)` = "q**의** check 기능을 부른다" |
| 기본값 인자 | `hint=""` — 안 주면 빈 문자열이 들어감 |

> 🔍 **꼬리질문 대비** — *"함수만 써도 되는 경우는?"*
> → **데이터를 들고 있을 필요가 없을 때**입니다. 실제로 `default_quizzes()`는 클래스로 만들 이유가 없어서 **그냥 함수로 뒀습니다.** 클래스를 쓰는 것 자체가 목적이 아니라고 생각합니다.

---

<a id="q3-2"></a>

## 📋 3-2. JSON 파일로 데이터를 저장하는 이유와 JSON 형식의 특징을 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — 끄면 값이 사라지니 **파일에 남겨야** 했고, 여러 방법 중 **JSON**을 골랐습니다.
>
> **② 용어** — JSON은 **`"이름": 값` 짝으로 적는 글자 형식**입니다.
>
> **③ 무엇 (JSON의 특징 4가지)** — ① `{ }`와 `[ ]`를 **겹쳐 넣을 수 있어** 중첩 구조를 그대로 담고, ② **파이썬 딕셔너리와 모양이 똑같아** 한 줄로 저장·복원되며, ③ **메모장으로 열면 사람이 읽을 수 있고**, ④ **언어·운영체제를 가리지 않습니다.**
>
> **④ ⭐ 왜** — 그냥 텍스트로 저장했으면 **어느 줄이 정답인지 직접 잘라 읽는 코드**를 제가 짜야 하고, 문제에 **줄바꿈이 하나 들어가는 순간 깨집니다.**
> JSON은 그 규칙이 **이미 정해져 있는 형식**이라 제가 짤 필요가 없습니다.

### 🖥️ 증빙

```bash
head -8 state.json
```
```json
{
  "quizzes": [
    {
      "question": "파이썬에서 리스트를 만들 때 사용하는 괄호는?",
      "choices": [ "( )", "[ ]", "{ }", "< >" ],
      "answer": 2,
```

| 짚을 곳 | 증명하는 것 |
| --- | --- |
| 한글이 그대로 보임 | UTF-8 + `ensure_ascii=False` |
| 2칸씩 계단 모양 | `indent=2` |
| `{` 안에 `[`, 그 안에 또 `{` | JSON의 **겹쳐 넣기(중첩)** |

```bash
python3.11 -c "import json; print(type(json.load(open('state.json'))))"
```
```
<class 'dict'>     ← 읽자마자 파이썬 딕셔너리
```

📄 [json.dump 161-162행](main.py#L161-L162) · [json.load 140행](main.py#L140)

👉 **짚을 곳** — [main.py:162](main.py#L162) `ensure_ascii=False, indent=2` ↔ [main.py:140](main.py#L140) `json.load`
→ 저장은 **사람이 읽게**, 불러오기는 **한 줄로** 끝납니다.

### 📖 세부 내용

**① 왜 JSON인가**

프로그램을 끄면 **변수는 사라지니까** 파일에 남겨야 합니다.

파일 형식 중 JSON을 고른 이유 —

| 이유 | 뜻 |
| --- | --- |
| **사람이 읽을 수 있음** | 메모장으로 열면 한글이 그대로 보임 → 문제 생기면 바로 확인 |
| **파이썬과 모양이 같음** | `json.load()` 한 줄이면 변환 끝 |
| **어디서나 통함** | 나중에 웹이나 다른 언어와 주고받기 편함 |

**② JSON의 생김새**

```json
{ "answer": 2 }
```

**`"이름": 값`** 짝으로 적는 형식입니다.

| 기호 | 뜻 |
| --- | --- |
| `{ }` | 한 덩어리의 칸들 |
| `[ ]` | 여러 개 목록 |
| `:` | 이름과 값 연결 |

**중첩** — 목록 안에 덩어리, 그 안에 또 목록을 넣을 수 있습니다.

**③ 저장할 때 붙인 옵션 3개**

```python
with open(STATE_FILE, "w", encoding="utf-8") as file:
    json.dump(data, file, ensure_ascii=False, indent=2)
```

| 옵션 | 붙인 곳 | 없으면 |
| --- | --- | --- |
| `encoding="utf-8"` | `open()` | 컴퓨터마다 한글이 깨짐 |
| `ensure_ascii=False` | `json.dump()` | 한글이 암호처럼 저장됨 |
| `indent=2` | `json.dump()` | 전부 한 줄로 붙어서 저장됨 |

세 옵션의 목적은 하나 — **사람이 파일을 열어 확인할 수 있게.**

📄 [main.py:161-162](main.py#L161-L162)

> 💡 **변수란?** 값을 담아두는 이름표입니다. `self.best_score = 100` 은 `best_score` 라는 이름표에 100을 담은 것. 프로그램을 끄면 **같이 사라집니다.**

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| JSON | **JavaScript Object Notation**. 원래 웹에서 나왔지만 지금은 언어 무관 표준 |
| 직렬화(serialize) | 메모리의 값을 **파일에 쓸 수 있는 글자로** 바꾸는 것 = `json.dump` |
| 역직렬화(deserialize) | 그 반대 = `json.load` |
| `dump` vs `dumps` | `dump`는 **파일에**, `dumps`는 **문자열로**(s = string) |
| UTF-8 | 전 세계 글자를 담는 표준 인코딩. 한글은 여기서만 안전 |
| 인코딩 | 글자를 **숫자로 바꾸는 규칙**. 규칙이 다르면 글자가 깨집니다 |

> 🔍 **꼬리질문 대비** — *"JSON에 없는 파이썬 타입은?"*
> → 튜플은 리스트로, `None`은 `null`로 바뀝니다. `datetime`은 아예 못 담아서, 저는 [`record_result()`](main.py#L295)에서 **문자열로 바꿔** 저장했습니다.

---

<a id="q3-3"></a>

## 📋 3-3. 파일 입출력에서 try/except가 필요한 이유(발생 가능한 실패 케이스 포함)를 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — 파일은 **제가 만든 게 아니라서** 없거나 깨져 있을 수 있고, 그래도 **프로그램이 안 죽게** 하려고 씁니다.
>
> **② 용어** — **예외**는 프로그램이 더 이상 못 가는 상황, **`try/except`** 는 그 상황을 **미리 받아내는 장치**입니다.
>
> **③ 무엇 (실패 4가지)** — ① 파일 없음 ② 내용 깨짐(`JSONDecodeError`) ③ 필요한 칸 없음(`KeyError`) ④ 권한·디스크 문제(`OSError`).
> 이 4개를 잡아서 **안내를 띄우고 기본 퀴즈로 복구**합니다.
>
> **④ ⭐ 왜** — 안 잡았으면 저장 파일이 **한 글자만 깨져도** 게임을 켤 때마다 죽어서, 사용자는 **파일을 지우는 것 말고 방법이 없습니다.**
> 반대로 **제가 통제할 수 있는 곳(제 코드 논리)에는 `try`를 안 붙였습니다.** 거기서 나는 오류는 **제가 고쳐야 할 버그**지 숨길 게 아니니까요.

### 📌 안 잡으면 무슨 일이 벌어지나

`state.json`이 깨졌다고 해봅시다. 쉼표 하나가 빠지거나, 저장 중에 컴퓨터가 꺼진 경우입니다.

```json
{ "quizzes": [ {"question": "리스트 괄호는?"   ← 여기서 끊김
```

**① `try/except`가 없다면**

```
게임 켬  →  파일 읽음  →  💥 오류  →  프로그램 죽음
게임 켬  →  파일 읽음  →  💥 오류  →  프로그램 죽음
```

파일을 **먼저 읽고 시작하는 구조**라서 그 지점을 절대 못 지나갑니다. 화면에는 이런 게 뜹니다.

```
Traceback (most recent call last):
json.decoder.JSONDecodeError: Expecting ',' delimiter
```

**② 유일한 탈출구가 "파일 삭제"**

```bash
rm state.json        # 이래야 겨우 실행됨
```

파일이 없으면 "처음 실행"으로 보고 기본 퀴즈로 시작하니까요.
**그런데 지우는 순간 그동안 만든 퀴즈와 점수가 전부 사라집니다.**

> 즉 **"게임을 아예 못 하거나, 데이터를 다 버리거나"** 둘 중 하나가 됩니다.

**③ `try/except`가 있으면**

```
⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. (Expecting ',' delimiter)

========================================
        🎯 나만의 퀴즈 게임 🎯
```

| | `try/except` 없음 | 있음 |
| --- | --- | --- |
| 화면 | 빨간 오류 글자 | ⚠️ 안내 한 줄 |
| 프로그램 | **켤 때마다 죽음** | 계속 실행됨 |
| 사용자가 할 수 있는 것 | **파일 삭제뿐** (데이터 다 잃음) | 그냥 게임 |

👉 [main.py:144](main.py#L144) `except (OSError, json.JSONDecodeError, KeyError, TypeError)`

> **"파일 한 글자가 깨졌다고 프로그램 전체를 못 쓰게 만들지 않는다"** — 이게 예외 처리를 하는 이유입니다.

### 🖥️ 증빙

⚠️ **반드시 백업부터.** 마지막 줄(되돌리기)까지 꼭 실행하세요.

```bash
cp state.json state.json.bak        # ① 백업 (필수!)
echo '{ 깨진 내용' > state.json      # ② 일부러 손상
python3.11 main.py                  # ③ 실행 → 3으로 확인 → 6으로 종료
mv state.json.bak state.json        # ④ 원상복구 ← 잊지 마세요
```
```
⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. (Expecting property name ...)
        🎯 나만의 퀴즈 게임 🎯        ← 파일을 망가뜨렸는데도 메뉴가 정상 출력
```

```bash
grep -n "except" main.py
```
```
131: except (OSError, json.JSONDecodeError, KeyError, TypeError)   ← 파일 읽기 4종
150: except OSError                                                ← 파일 쓰기
164: except ValueError                                             ← 숫자 변환
335: except (KeyboardInterrupt, EOFError)                          ← 중단
```

📄 [읽기 예외 138-151행](main.py#L138-L151) · [쓰기 예외 160-164행](main.py#L160-L164)

👉 **짚을 곳** — [main.py:156](main.py#L156) `except (OSError, JSONDecodeError, KeyError, TypeError)`
→ 이 **한 줄에 실패 4종류**가 다 적혀 있습니다.

### 📖 세부 내용

**① 왜 `try/except`가 필요한가**

파일은 **내 코드가 아닙니다.** 사용자가 지울 수도, 메모장으로 열어 망칠 수도 있습니다.
내가 아무리 잘 짜도 **파일 쪽 사정으로 실패할 수 있습니다.**

목표는 *"에러가 안 나게"* 가 아니라 **"에러가 나도 게임은 돌아가게"** 입니다.

**② 어떤 실패가 있을 수 있나 (4가지)**

| 실패 | 언제 생기나 | 파이썬이 내는 이름 |
| --- | --- | --- |
| 파일이 없음 | 처음 실행할 때 | (미리 `if`로 확인) |
| 내용이 깨짐 | 사용자가 잘못 고침 | `JSONDecodeError` |
| 필요한 칸이 없음 | 옛날 버전 파일 | `KeyError` `TypeError` |
| 권한·디스크 문제 | 읽기 권한 없음, 디스크 꽉 참 | `OSError` |

**③ 어떻게 처리했나**

```python
try:
    data = json.load(file)                  # 읽어보다가
except (OSError, json.JSONDecodeError, KeyError, TypeError) as error:
    print(f"⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. ({error})")
    self.quizzes = default_quizzes()        # 어떤 사고든 기본 퀴즈로
```

**🧒 쉽게 말하면** — 자전거 탈 때 **헬멧 쓰는 것**과 같습니다.
안 넘어지는 게 최선이지만, **넘어져도 안 다치게** 미리 준비하는 것입니다.

📄 [138-151](main.py#L138-L151)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| `try / except / else` | `try` 해보고, 터지면 `except`, **안 터졌을 때만** `else` |
| 예외를 **이름으로** 잡기 | `except:` 만 쓰면 진짜 버그까지 삼킴 |
| `as error` | 예외 객체를 변수에 담아 **무엇이 잘못됐는지 출력** |
| `OSError` | 파일·디스크·권한 관련 오류의 **부모 격** 이름 |
| 방어적 프로그래밍 | 바깥에서 오는 것(파일, 입력, 네트워크)은 **항상 의심**하고 짜는 태도 |

> 🔍 **꼬리질문 대비** — *"복구했는데 사용자 데이터는요?"*
> → **사라집니다.** 그게 지금의 명확한 한계이고, [4-2](#q4-2)에서 개선 방향을 말씀드릴 수 있습니다.

---

<a id="q3-4"></a>

## 📋 3-4. 브랜치를 분리해 작업한 이유와 병합(merge)의 의미를 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — `main`은 **항상 돌아가는 원본**으로 두고, 가장 복잡한 '퀴즈 풀기'만 **옆 브랜치**에서 만들었습니다.
>
> **② 용어** — **브랜치**는 원본을 복사해 **따로 작업하는 갈래**, **병합(merge)** 은 그 갈래의 결과를 **원본에 합치는 것**입니다.
>
> **③ 무엇** — `feature/play-quiz` 브랜치에서 개발하고, 완성한 뒤 `main`에 병합했습니다.
> 그래서 **부모가 2개인 커밋**이 생기고, 그래프에서 **갈라졌다 다시 합쳐지는 모양**으로 보입니다.
>
> **④ ⭐ 왜** — 브랜치 없이 `main`에서 바로 고쳤으면, **만들다 만 상태**에서 평가자가 내려받았을 때 **실행이 안 됩니다.**
> **실패해도 원본은 무사하도록** 갈라놓은 것입니다.

### 📌 핵심 — 문제가 묻는 2가지

**① 브랜치를 분리해 작업한 이유**

> **여러 사람이 같은 프로젝트를 동시에 고쳐도 서로 부딪히지 않게 하려고** 나눕니다.
> 혼자 할 때도 같은 원리로, **실패해도 `main`은 항상 돌아가게** 하려고 나눴습니다.

**② 병합(merge)의 의미**

> **갈라져 있던 갈래를 다시 하나로 합치는 것**입니다.

### 📖 세부 설명

**① 브랜치가 원래 왜 생겼나**

A는 로그인, B는 결제를 동시에 만든다고 해봅시다. 한 곳에서 같이 고치면 **서로의 코드를 덮어쓰고 뒤엉킵니다.**

```
main
 ├── A의 갈래 : 로그인 만드는 중
 └── B의 갈래 : 결제 만드는 중      ← 서로 영향 없음
```

각자 **완성한 뒤 `main`에 합칩니다.** 그동안 `main`은 계속 정상 상태입니다.

**② 이 프로젝트에서는**

혼자라 부딪힐 사람은 없지만, **가장 복잡한 '퀴즈 풀기'** 를 `feature/play-quiz` 에서 만들었습니다.
랜덤·힌트·채점·점수가 얽혀 **실패 위험이 가장 컸기 때문**입니다.

```
main:  ●───●───●─────────●   ← 항상 돌아가는 원본
                \       /
feature/play-quiz ●───●─/    ← 여기서 실패해도 main은 무사
```

**③ 병합하면 생기는 것**

두 줄기를 이어붙이므로 **부모가 2개인 커밋**이 생깁니다. 깃이 자동으로 새기는 값이라 **조작이 안 되는 증거**입니다.

```
Merge: ff3840e ec2775c      ← 부모 2개
```

### 🖥️ 증빙

```bash
git branch -a | cat                                          # ① main, feature/play-quiz
git log --oneline --graph --decorate 8adca1c..2d45928 | cat  # ② 갈라짐 → 합쳐짐
git show --stat 2d45928 | head -3                            # ③ Merge: 부모 2개
git log --oneline main..ec2775c | cat                        # ④ 브랜치에서만 한 작업
```
```
ec2775c Feat: 랜덤 출제, 문제 수 선택, 힌트 기능 추가 (보너스)
b432e80 Feat: 퀴즈 풀기 기능 구현
        ↑ 이 둘을 만드는 동안 main은 계속 정상 동작하는 상태였습니다
```

📸 [git_graph.png](docs/screenshots/git_graph.png)

🔗 **바로가기** — [병합 커밋 `2d45928`](https://github.com/ellysuh22/2-homework/commit/2d45928)
→ `Merge: ff3840e ec2775c` — **진짜 병합해야만** 깃이 새기는 줄입니다.

### 📖 세부 내용

```
main:  ●───●───●─────────●   ← 항상 돌아가는 원본
                \       /
feature/play-quiz ●───●─/    ← 여기서 실패해도 main은 무사
```

**왜 퀴즈 풀기만 뺐나** — 랜덤·힌트·채점·점수가 얽혀 **가장 복잡하고 실패 위험이 커서**입니다.

**병합** = 옆 책상 결과를 원본에 합치는 것. 그래서 **부모가 2개**가 됩니다.

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 브랜치 | 커밋을 가리키는 **이름표**일 뿐. 폴더를 복사하는 게 아니라서 가볍습니다 |
| `main` | 기본 브랜치. **항상 동작하는 상태**로 유지하는 게 관례 |
| `feature/이름` | 기능 브랜치 명명 관례. 슬래시로 분류를 표현 |
| `git switch -c` | 브랜치를 **만들고 동시에 이동**. `-c` = create |
| merge vs rebase | merge는 **합친 기록을 남김**(부모 2개), rebase는 **일자로 펴서** 기록을 다시 씀 |
| 충돌(conflict) | 양쪽이 **같은 줄**을 고쳤을 때. 깃이 못 정하니 사람이 골라야 함 |

> 🔍 **꼬리질문 대비** — *"충돌이 났었나요?"*
> → 이번엔 브랜치에서 **새 메서드만 추가**해서 충돌이 없었습니다. 충돌은 **양쪽이 같은 줄을 고쳤을 때** 나고, 그때는 깃이 `<<<<<<<` 표시로 양쪽을 보여주면 제가 골라서 커밋합니다.

---

<a id="q3-5"></a>

## 📋 3-5. state.json 데이터 구조(필드/중첩 구조)를 현재 형태로 설계한 이유를 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① "필드는 3개로 나누고, 중첩은 2단으로 설계했습니다."**
>
> **②** 필드는 **이름표 붙은 칸**이고, 중첩은 **칸 안에 또 칸**이 들어간 것입니다.
>
> **③** 큰 칸 3개는 **퀴즈 목록 / 최고 점수 / 게임 기록**입니다.
> 그리고 퀴즈 목록 안에 **문제 하나하나가 또 4칸짜리 상자**로 들어 있습니다 — 문제·선택지·정답·힌트. **이 부분이 중첩입니다.**
>
> **④ ⭐** 칸을 안 나눴으면 **최고 점수 하나 보려고 문제 전부를 뒤져야** 합니다.
> 중첩을 안 했으면 **문제를 지울 때 정답 짝이 어긋납니다.**
> 정답은 글자가 아니라 **번호로** 뒀는데, 글자면 **띄어쓰기 하나에 오답** 처리되기 때문입니다.

### 📌 먼저 — 필드와 중첩이 무슨 뜻인가

**필드 = 이름 하나 + 값 하나**

```
"best_score": 100
 └─이름─┘   └값┘      ← 이 한 쌍이 "필드 1개"
```

이름표를 붙여서 값을 적어둔 것 — 그게 필드입니다.

**필드가 3개라는 건**

```
"quizzes": ...        ← 필드 1개
"best_score": 100     ← 필드 1개
"history": ...        ← 필드 1개
                         합쳐서 3개
```

**중첩 = 값 자리에 또 다른 묶음이 들어가는 것**

값 자리에 뭐가 오느냐가 다릅니다.

```json
"best_score": 100                                     ← 중첩 아님. 값이 그냥 숫자
"quizzes": [ { "question": "괄호는?", "answer": 2 } ]   ← 중첩. 값 안에 또 필드 2개
```

**그림으로**

```
best_score : 100                       ← 1단 (그냥 값)

quizzes    : ┌─────────────────────┐
             │ question : 괄호는?    │   ← 2단 (값 안에 또 이름·값)
             │ answer   : 2         │
             └─────────────────────┘
```

> **필드 = 이름 하나에 값 하나.
> 중첩 = 그 값 자리에 또 이름·값 묶음이 들어간 것.**

### 📌 왜 이 형태로 설계했나 — 결정 4가지

**① 큰 필드를 3개로 나눈 이유**

> `quizzes` / `best_score` / `history` — **바뀌는 시점이 달라서** 따로 뒀습니다.

| 필드 | 언제 바뀌나 |
| --- | --- |
| `quizzes` | 퀴즈를 추가·삭제할 때 |
| `best_score` | 신기록이 나올 때 |
| `history` | 게임을 할 때마다 |

**안 나눴다면** — 최고 점수 하나 보려고 **퀴즈 전부를 뒤져야** 합니다.

**② 퀴즈 1개를 상자로 묶은 이유 (중첩)**

> 문제·선택지·정답·힌트 4개가 **한 문제에 딸린 정보**라서 묶었습니다.

**안 묶었다면** — 문제 목록과 정답 목록이 따로 놀아서 **문제를 지울 때 정답 짝이 어긋납니다.**
특히 이 프로그램은 **퀴즈를 랜덤으로 섞어서** 출제하기 때문에 치명적입니다.

**③ 필드 이름을 `Quiz` 속성과 똑같이 맞춘 이유**

```
파일     : "question"     "choices"     "answer"     "hint"
main.py  : self.question  self.choices  self.answer  self.hint
```

> 이름이 같으면 **보고 그대로 옮기면 끝**입니다.

**안 맞췄다면** — "파일의 `q`는 클래스의 `question`" 같은 **대조표를 계속 외워야** 하고, 칸을 추가할 때마다 **고칠 곳이 3군데**로 늘어납니다.

**④ `answer`를 글자가 아니라 번호(숫자)로 둔 이유**

```
글자로 저장했다면 : "for"  vs  "For"  vs  "for "   → 전부 오답 처리 💥
번호로 저장하면   : 2                              → 시비 없음
```

### 🖥️ 증빙

**① 실제 파일 모양** — 필드와 중첩이 눈에 보입니다

```bash
head -14 state.json
```
```json
{
  "quizzes": [                          ← 필드 ①
    {                                   ┐
      "question": "...괄호는?",          │
      "choices": [ "( )", "[ ]", ... ], │  ★ 중첩 — 칸 안에 또 칸
      "answer": 2,                      │
      "hint": "순서가 있고..."           │
    },                                  ┘ ← 문제 1개 = 상자 1개
```

**② 칸 이름 확인**

```bash
python3.11 -c "
import json; d = json.load(open('state.json'))
print('큰 칸 3개 :', list(d.keys()))
print('퀴즈 1개  :', list(d['quizzes'][0].keys()))
"
```
```
큰 칸 3개 : ['quizzes', 'best_score', 'history']
퀴즈 1개  : ['question', 'choices', 'answer', 'hint']
```
→ 뒤의 4개가 [`Quiz.__init__()` 33-38행](main.py#L33-L38)의 `self.question` `self.choices` `self.answer` `self.hint` 와 **글자까지 똑같습니다.**

**③ 옛날 형식 파일도 읽힙니다** (`hint` 칸이 없는 경우)

```bash
python3.11 -c "
from main import Quiz
q = Quiz.from_dict({'question':'옛날 문제','choices':['가','나','다','라'],'answer':1})
print('에러 없이 생성:', q.question, '| hint =', repr(q.hint))
"
```

📄 [to_dict() 54-61행](main.py#L54-L61) · [from_dict() 63-71행](main.py#L63-L71)

👉 **짚을 곳** — [main.py:54-61](main.py#L54-L61) `to_dict()`(파일로 내보내기) ↔ [main.py:63-71](main.py#L63-L71) `from_dict()`(파일에서 읽어오기)
→ **파일의 칸 이름 = 클래스의 속성 이름** 이라 대조표 없이 그대로 옮깁니다.

### 📖 세부 내용

```json
{ "quizzes": [ {"question":"...", "choices":[...], "answer":2, "hint":"..."} ],
  "best_score": 100,
  "history":   [ {"date":"...", "score":100} ] }
```

칸 이름 `question` `choices` `answer` `hint` 가 [`Quiz.__init__()`](main.py#L33-L37)의 속성 이름과 **글자까지 같습니다.**
그래서 [`to_dict()`](main.py#L54-L61) / [`from_dict()`](main.py#L63-L71) 두 개로 변환이 끝납니다.

**`answer`를 번호(int)로 둔 이유** — 글자로 두면 **띄어쓰기 하나에도** 오답 처리됩니다.

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 리스트 vs 딕셔너리 | 리스트는 **번호**로, 딕셔너리는 **이름(키)** 으로 값을 꺼냄 |
| 스키마(schema) | 데이터의 **칸 이름과 모양**을 정한 약속 |
| `data.get("hint", "")` | 키가 **없어도 죽지 않고** 기본값을 줌. `data["hint"]`는 없으면 `KeyError` |
| `@classmethod` | 객체가 아니라 **클래스 자체가** 부르는 메서드. `Quiz.from_dict(...)` 처럼 "만들어 주는" 용도 |
| `cls` | `self`의 클래스 버전 — "이 클래스 자신" |
| 하위 호환 | 옛날 파일도 **새 코드가 읽을 수 있게** 만드는 것 |

> 🔍 **꼬리질문 대비** — *"퀴즈에 고유 ID를 넣지 않은 이유는?"*
> → 지금은 목록의 **순서 자체가 번호** 역할을 합니다. 다만 삭제하면 뒤 번호가 밀리므로, 퀴즈를 외부에서 참조해야 한다면 **고유 ID를 넣는 게 맞습니다.** (미구현)

---

# [항목 4] 심층 인터뷰

<a id="q4-1"></a>

## 📋 4-1. 퀴즈 데이터가 1000개 이상으로 늘어난다면 현재 JSON 저장 방식에 어떤 한계가 생길 수 있는지 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — 퀴즈 **1개만 추가해도 파일 전체를 다시 씁니다.** 이게 뿌리 문제입니다.
>
> **② 용어** — **'전체 다시 쓰기'** 는 한 글자만 바뀌어도 **처음부터 끝까지 새로 적는** 방식입니다.
>
> **③ 무엇** — 지금은 2.4KB라 즉시 끝나지만, **1000개면 약 340KB를 매번** 다시 씁니다.
> 읽을 때도 **1000개를 통째로 메모리에 올립니다.**
>
> **④ ⭐ 왜 문제인가** — 저장이 느려지고, **쓰는 도중 전원이 끊기면 파일이 통째로 깨집니다.**
> 그리고 **"3번 문제만 고치기"가 안 됩니다.**
> 이 정도 규모면 **필요한 줄만 고칠 수 있는 SQLite** 같은 방식이 맞습니다. (미구현)

### 🖥️ 증빙

```bash
grep -n "json.dump" main.py     # → 149행. 여기서 전체를 다시 씀
python3.11 -c "
import os; size = os.path.getsize('state.json')
print(f'현재 {size:,}바이트 (퀴즈 7개) → 1000개면 약 {size*140:,}바이트')
"
```
```
현재 2,484바이트 (퀴즈 7개) → 1000개면 약 347,760바이트
```

| 한계 | 왜 생기나 |
| --- | --- |
| 저장이 느려짐 | 1개만 바꿔도 전체 재작성 |
| 메모리 부담 | 시작할 때 전부 올림 |
| 검색 어려움 | 조건 검색하려면 전부 훑어야 함 |
| 손상 위험 | 쓰는 도중 끊기면 통째로 깨짐 |
| 동시 실행 충돌 | 나중 쓰기가 앞 쓰기를 덮음 |

**개선 방향 (미구현)** — `sqlite3`(파이썬 표준 라이브러리)로 옮기면 필요한 줄만 읽고 쓸 수 있습니다.
고칠 곳은 [`load_state()` / `save_state()`](main.py#L132-L164) **2개뿐**입니다.

👉 **짚을 곳** — [main.py:156](main.py#L156) `[quiz.to_dict() for quiz in self.quizzes]`
→ 퀴즈 **1개만 추가해도 전부** 다시 씁니다 — 여기가 한계의 출발점입니다.

### 📖 세부 내용

```python
with open(STATE_FILE, "w", ...) as file:   # "w" = 기존 내용 삭제
    json.dump(data, file, ...)              # 1000개 전부 다시 씀
```

| 한계 | 왜 |
| --- | --- |
| 저장이 느려짐 | 1개 추가해도 전체 재작성 |
| 메모리 부담 | 시작할 때 전부 올림 |
| 검색 어려움 | 전부 훑어야 함 |
| 손상 위험 | 쓰는 도중 끊기면 통째로 깨짐 |
| 동시 실행 충돌 | 나중 쓰기가 앞 쓰기를 덮음 |

**개선** — `sqlite3`(표준 라이브러리)로 옮기면 됩니다. [`load/save_state()`](main.py#L132-L164) **2개만** 고치면 끝. (미구현)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 전체 재작성 vs 부분 수정 | 텍스트 파일은 **중간만 고치기가 사실상 불가능** |
| 시간 복잡도 | 데이터가 N배 늘면 저장 시간도 **N배**. 이걸 O(N)이라고 함 |
| 색인(index) | 책의 **찾아보기**. 없으면 처음부터 끝까지 훑어야 함 |
| 원자성(atomicity) | 저장이 **다 되거나 아예 안 되거나** 둘 중 하나여야 안전 |
| SQLite | 서버 없이 **파일 하나로 동작하는 DB**. 파이썬에 기본 포함 |

> 🔍 **꼬리질문 대비** — *"지금 방식이 틀린 건가요?"*
> → 아닙니다. **수십 개 규모에서는 가장 단순하고 안전한 선택**입니다. 처음부터 DB를 쓰면 코드가 훨씬 복잡해지는데, 그건 **지금 없는 문제를 위해 지금 비용을 내는 것**이라고 생각합니다.

---

<a id="q4-2"></a>

## 📋 4-2. 만약 state.json이 손상되어 JSON 파싱에 실패한다면, 사용자가 데이터를 잃지 않도록 어떤 대응(복구/백업/초기화)이 가능한지 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — ⚠️ 지금은 **"프로그램이 안 죽는 것"까지만** 됩니다. 사용자가 추가한 퀴즈는 사라집니다.
>
> **② 용어** — **"안 죽는 것"** 은 실행이 계속되는 것, **"안 잃는 것"** 은 데이터가 남는 것 — **다른 문제**입니다.
>
> **③ 무엇 (지금 되는 것)** — 깨진 파일을 만나면 안내를 띄우고 **기본 퀴즈 5개로 복구**해 계속 실행됩니다.
>
> **④ ⭐ 왜 부족한가 / 어떻게 해야 하나** — 복구하는 순간 **원본을 덮어써서** 사용자가 추가한 퀴즈를 되살릴 방법이 없습니다.
> 저장 전에 **`state.json.bak` 백업본을 남기고**, 깨졌을 때 **백업본부터 시도**한 뒤 그것도 실패하면 초기화하는 게 맞습니다. (미구현)

### 🖥️ 증빙

```bash
cp state.json state.json.bak        # ① 백업 (필수!)
echo '{ 깨진 내용' > state.json      # ② 손상
python3.11 main.py                  # ③ 실행 → 3으로 목록 확인 → 6 종료
mv state.json.bak state.json        # ④ 원상복구
```

| ③에서 보이는 것 | 무엇을 뜻하나 |
| --- | --- |
| `⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다.` | ✅ **죽지 않음** (1단계 완료) |
| `3` 입력 → 퀴즈가 **기본 5개뿐** | ❌ **잃지 않기는 미구현** (2단계) |

```bash
ls state.json*      # 백업 파일이 없음 = 개선 여지 그 자체
```

**개선 방향 (미구현)**

| 순위 | 방법 | 효과 |
| --- | --- | --- |
| 🥇 | 임시 파일에 다 쓰고 성공하면 바꿔치기 (`os.replace`) | 손상 자체를 막음 |
| 🥈 | 저장 전 `state.json.bak` 백업 | 직전 상태로 복구 |
| 🥉 | 깨진 파일을 지우지 않고 보존 | 나중에 수동 복구 |

📄 [손상 감지·복구 144-148행](main.py#L144-L148)

👉 **짚을 곳** — [main.py:146-148](main.py#L146-L148) `= default_quizzes()`
→ 복구가 아니라 **초기화** 입니다. 이 부분이 아직 못 푼 문제입니다.

### 📖 세부 내용

```python
# 지금 — 깨지면 초기화 (프로그램은 살지만 데이터는 잃음)
except json.JSONDecodeError:
    self.quizzes = default_quizzes()
```
```python
# 개선안 (미구현) — 임시 파일에 쓰고 성공하면 바꿔치기
os.replace(tmp, STATE_FILE)
```

| 순위 | 방법 | 효과 |
| --- | --- | --- |
| 🥇 | 임시 파일 교체 | **손상 자체를 막음** |
| 🥈 | 저장 전 `.bak` 백업 | 직전 상태로 복구 |
| 🥉 | 깨진 파일 보존 | 나중에 수동 복구 |
| ✅ | 초기화 (현재) | 게임은 실행됨 |

📄 [144-148](main.py#L144-L148)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| "죽지 않는 것" vs "잃지 않는 것" | 프로그램이 살아도 **데이터가 사라졌으면 해결이 아님**. 이 문항의 핵심 |
| 원자적 쓰기 | 임시 파일에 다 쓰고 **한 번에 교체**하는 기법. 실무의 표준 방식 |
| `os.replace()` | 같은 디스크 안에서 **원자적으로** 파일 이름 교체 |
| 백업 세대 관리 | `.bak` 하나만 두면 **두 번 연속 손상 시** 소용없음 |
| 실패 격리 | 깨진 파일을 **지우지 말고 남겨** 원인 분석과 수동 복구를 가능하게 |

> 🔍 **꼬리질문 대비** — *"왜 아직 구현 안 했나요?"*
> → 미션 요구사항이 "최소 1개 이상 예외 처리"였고, 우선순위를 **필수 기능 완성과 커밋·브랜치 실습**에 뒀습니다. 다만 **어떤 순서로 넣을지는 정해뒀습니다.**

---

<a id="q4-3"></a>

## 📋 4-3. "정답 채점 방식(점수 계산)"이나 "퀴즈 구조(선택지 개수 등)" 요구사항이 바뀐다면, 어떤 파일/클래스/메서드를 먼저 수정해야 하는지 설명할 수 있는가?

### ✅ 답

**말하는 순서 — ① 결론 → ② 용어 → ③ 무엇 → ④ 왜**

> **① 결론** — 점수 규칙은 `QuizGame`, 선택지 개수는 `Quiz`, 저장 방식은 `load_state()`/`save_state()` 부터 고칩니다.
>
> **② 용어** — **"먼저 고칠 곳"** 은 그 규칙이 **실제로 적혀 있는 단 한 곳**입니다.
>
> **③ 무엇** — 점수 계산은 `play_quiz()` 안의 **한 줄**, 힌트 벌점은 맨 위 **`HINT_PENALTY` 한 줄**만 바꾸면 끝납니다.
> 선택지 개수는 `Quiz.__init__` · `add_quiz()` · `ask_one()` 의 범위를 **함께** 봐야 합니다.
>
> **④ ⭐ 왜** — 이렇게 모아둔 이유는 요구사항이 바뀔 때 **고칠 지점을 한 곳으로 좁히려는 것**입니다.
> ⚠️ 다만 **선택지 개수 `4`가 여러 곳에 흩어져 있어서**, 이건 `HINT_PENALTY`처럼 **`CHOICE_COUNT` 상수로 뽑았어야** 맞습니다. 제 코드의 아쉬운 점입니다.

### 🖥️ 증빙

**👍 잘된 예 — 이름을 붙여 한 곳에 모아둔 값**

```bash
grep -n "HINT_PENALTY" main.py
```
```
12: HINT_PENALTY = 5      ← 정의는 여기 한 줄
237, 270, 274             ← 쓰는 곳
```
> 감점을 10점으로 바꾸려면 **12행 한 줄**만 고치면 됩니다.

**👎 아쉬운 예 — 숫자가 흩어진 곳**

```bash
grep -n "1-4\|range(1, 5)" main.py
```
```
189: for number in range(1, 5):                        ← 보기 4개 입력
191: answer = self.input_int("정답 번호 (1-4): ", 1, 4)
230: f"정답 입력 (1-4, 힌트 보기는 {HINT_CHOICE}): "
```
> 선택지를 5개로 늘리려면 **여기 전부를 동시에** 고쳐야 하고, 하나만 빠뜨리면 **안내 문구는 '1-4'인데 실제로는 5까지 받는** 어긋남이 생깁니다.

| 무엇이 바뀌면 | 먼저 고칠 곳 |
| --- | --- |
| 점수 규칙 | [`play_quiz()` 283행](main.py#L283) · [`record_result()`](main.py#L292-L305) |
| 힌트 감점 | [`HINT_PENALTY` 23행](main.py#L23) **한 줄** |
| 선택지 개수 | [`Quiz`](main.py#L30-L71) · [`add_quiz()`](main.py#L196-L208) · [`ask_one()`](main.py#L236-L259) ⚠️ 흩어짐 |
| 저장 방식 | [`load_state()`](main.py#L132-L151) · [`save_state()`](main.py#L153-L164) |

👉 **짚을 곳** — [main.py:23](main.py#L23) `HINT_PENALTY` · [main.py:283](main.py#L283) 점수식 · [main.py:202](main.py#L202) `range(1, 5)`
→ 점수는 상수로 뽑아 뒀지만, **선택지 개수 `4`는 흩어져 있습니다.**

### 📖 세부 내용

| 무엇이 바뀌면 | 고칠 곳 |
| --- | --- |
| 점수 규칙 | [`play_quiz()` 283](main.py#L283) · [`record_result()`](main.py#L292-L305) |
| 힌트 감점 | [`HINT_PENALTY` 23행](main.py#L23) **한 줄** |
| 선택지 개수 | [`Quiz`](main.py#L30-L71) · [`add_quiz()`](main.py#L196-L208) · [`ask_one()`](main.py#L236-L259) ⚠️ 흩어져 있음 |
| 저장 방식 | [`load_state()`](main.py#L132-L151) · [`save_state()`](main.py#L153-L164) |

**⚠️ 아쉬운 점** — 선택지 개수 `4`가 여러 곳에 박혀 있습니다. `HINT_PENALTY`처럼 **상수로 뽑았어야** 합니다. (미구현)

### 📚 학습 내용

| 개념 | 알아야 할 것 |
| --- | --- |
| 매직 넘버 | 코드에 **그냥 박혀 있는 숫자**. `4`가 무슨 뜻인지 코드만 봐선 모름 |
| 상수로 뽑기 | 이름을 붙여 **맨 위에 모아두면** 뜻도 드러나고 한 줄로 고칠 수 있음 |
| 변경 영향 범위 | 한 곳을 고칠 때 **함께 고쳐야 하는 곳**의 넓이. 좁을수록 좋은 설계 |
| 단일 책임 | 하나의 클래스·함수가 **한 가지 이유로만** 바뀌게 하는 것 |
| 대문자 이름 규칙 | 파이썬에서 `HINT_PENALTY`처럼 **전부 대문자**면 "바꾸지 않는 값(상수)"이라는 관례 |

> 🔍 **꼬리질문 대비** — *"그럼 모든 숫자를 상수로 빼야 하나요?"*
> → 아닙니다. **바뀔 가능성이 있는 값**만입니다. `range(1, 5)`의 `1`처럼 절대 안 바뀌는 건 그대로 두는 게 낫습니다. **판단 기준은 "이 값이 바뀔 일이 있는가"** 입니다.

---

<a id="part2"></a>

<a id="부록a"></a>

# 부록 A. 30초 요약 — 20문항 한 줄 답

> **결과가 아니라 이유를 말하세요.** 아래 표의 굵은 글씨가 이유입니다.

### [항목 1] 기능 동작 검증

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [1-1](#q1-1) | 메뉴로 돌아온다 — **파이썬은 한 번만 읽고 지나가서 반복문이 필요** | `python3.11 main.py <<< $'3\n5\n6'` |
| [1-2](#q1-2) | 5곳이 `input_int()` 하나를 거친다 — **복사했으면 5군데를 다 고쳐야** | `abc` `0` `9` 빈Enter 연속 |
| [1-3](#q1-3) | 바뀔 때마다 파일에 쓴다 — **끝에 한 번만 쓰면 강제 종료 때 다 잃어서** | 퀴즈 개수 **7 → 8** |
| [1-4](#q1-4) | 기본 퀴즈를 **함수로** 만들었다 — **변수면 삭제할 때 원본까지 줄어서** | 파일 치우고 실행 → 5개 |
| [1-5](#q1-5) | 기능 하나 = 커밋 1개 — **되돌릴 수 있는 최소 단위라서** | `git rev-list --count HEAD` |
| [1-6](#q1-6) | 위험한 기능만 브랜치로 — **실패해도 `main`을 지키려고** | `git show --stat 2d45928 \| head -3` |
| [1-7](#q1-7) | clone→pull **터미널 캡처 첨부 완료** ✅ | `clone_pull.png` · `git log --oneline -- README.md` |

### [항목 2] 코드 구조 및 설계

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [2-1](#q2-1) | `Quiz`=카드, `QuizGame`=사회자 — **바뀌는 이유가 다르니까** | `grep -n "^class" main.py` |
| [2-2](#q2-2) | 5곳에 같은 검증이 필요해서 — **저장 방식이 바뀌어도 게임 코드는 무사** | 정의 1 + 호출 5 |
| [2-3](#q2-3) | 읽기 1번, 쓰기는 바뀔 때마다 — **매번 읽으면 느리고 끝에 쓰면 다 잃어서** | 읽기 1곳 / 쓰기 4곳 |
| [2-4](#q2-4) | 가장 바깥에서 잡는다 — **예외는 위로 올라오니 한 곳이면 전부 커버** | `main.py < /dev/null` |
| [2-5](#q2-5) | 기능 추가와 구조 정리를 안 섞는다 — **문제 생겼을 때 원인을 가리려고** | `git log --oneline` |

### [항목 3] 핵심 기술 원리

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [3-1](#q3-1) | 함수는 **동작만**, 클래스는 **동작+데이터** — 데이터를 끌고 다닐 필요가 없어서 | `q.check(2)` 직접 실행 |
| [3-2](#q3-2) | 끄면 변수가 사라져 파일에 남김 — dict와 구조가 같고 **사람이 읽을 수 있어서** JSON | `head -20 state.json` |
| [3-3](#q3-3) | 파일은 **내 코드 밖 세상** — 없거나 깨져도 게임은 돌아야 하니까 | 파일 깨뜨려도 실행됨 |
| [3-4](#q3-4) | **위험이 큰 곳에만** 브랜치 — 실패해도 원본이 무사하려고 | 부모 2개 |
| [3-5](#q3-5) | 파일 칸 이름 = `Quiz` 속성 — **이름이 다르면 조용히 데이터가 빠져서** | 칸 이름 나란히 출력 |

### [항목 4] 심층 인터뷰 — 정답 없음, **한계를 아는지**가 답

| # | 한 줄 답 (무엇 + **왜**) | 증빙 |
| --- | --- | --- |
| [4-1](#q4-1) | 뿌리는 **"부분 수정이 안 되고 전체를 갈아끼운다"** → SQLite (미구현) | `json.dump` 1곳 |
| [4-2](#q4-2) | **"안 죽는 것"까지만.** "안 잃는 것"은 미구현 — 둘은 다른 문제 | 복구되지만 추가분 사라짐 |
| [4-3](#q4-3) | 상수 1곳 👍 vs 매직넘버 3곳 👎 — **바뀔 값이라 생각했는지의 차이** | `HINT_PENALTY` vs `range(1, 5)` |

### 🎤 막혔을 때 쓰는 문장

| 상황 | 이렇게 |
| --- | --- |
| 기능 질문 | "메뉴 루프가 돌면서 번호마다 메서드를 부르고, 6번에서만 빠져나옵니다." |
| 오류 처리 | "숫자를 받는 5곳이 전부 `input_int()` 하나를 거쳐 똑같이 걸러집니다." |
| 저장 질문 | "시작할 때 1번 읽고, 데이터가 바뀔 때마다 즉시 씁니다." |
| 설계 질문 | "고쳐야 할 원인이 다르면 코드도 따로 뒀습니다." |
| **모르는 질문** | "현재 구현은 ~까지입니다. 개선한다면 ~하겠습니다. (미구현)" |

---

<a id="부록b"></a>

# 부록 B. 평가 전 체크리스트

### B-1. 5분 만에 전부 확인하기

```bash
cd ~/Documents/2-homework
python3.11 --version                         # Python 3.11.15 ← 3.10 이상
git rev-list --count HEAD                    # 10보다 큰 숫자
git status                                   # nothing to commit (전부 push 됨)
git log --merges --oneline | cat             # 병합 커밋 1줄
python3.11 main.py <<< $'3\n5\n6'            # 메뉴·목록·점수·종료 한 번에
python3.11 main.py < /dev/null               # 안전 종료 (Traceback 없음)
```

⚠️ **`python3` 말고 반드시 `python3.11`** — 이 컴퓨터의 기본 `python3`는 3.9.6이라 미션 요구(3.10 이상) 미달입니다.

### B-2. 스크린샷 — 2장 남음

| 파일명 | 무엇 | 문항 | 상태 |
| --- | --- | --- | --- |
| `dev_env.png` | VSCode + Python 버전 + Git 설정 | 제출물 | ✅ |
| `menu.png` | 메뉴 화면 | 1-1 | ✅ |
| `add_quiz.png` | 퀴즈 추가 | 1-1 | ✅ |
| `list.png` | 퀴즈 목록 | 1-1, 1-4 | ✅ |
| `play.png` | 퀴즈 풀기 (정답·오답) | 1-2 | ✅ |
| `score.png` | 점수 확인 | 1-1 | ✅ |
| `git_graph.png` | 브랜치 그래프 | 1-6, 3-4 | ✅ |
| `commit_list.png` | 커밋 목록 | 1-5, 2-5 | ✅ |
| **`clone_pull.png`** | clone → pull 터미널 | **1-7** | ✅ |
| **`invalid_input.png`** | 잘못된 입력 4종 | **1-2** | 🟡 권장 |

**촬영 방법**

```bash
./shot.sh                    # 진행 상황 확인
./shot.sh clone_pull         # 방금 찍은 캡처를 이 이름으로 저장
```

| 단축키 | 하는 일 |
| --- | --- |
| `Cmd` + `Shift` + `4` | 드래그한 **영역**만 캡처 |
| `Cmd` + `Shift` + `4` → `Space` → 클릭 | **창 하나**를 통째로 |

- **`invalid_input.png`** — 실행 후 `선택:` 에서 연달아 `abc` → `0` → `9` → 그냥 Enter, ⚠️ 경고 4줄이 한 화면에 보이게

```bash
git add docs/screenshots/ && git commit -m "Docs: clone/pull 실습 스크린샷 추가" && git push
```

### B-3. 실습 후 뒷정리 ⚠️

파일을 건드리는 시연([1-4](#q1-4) · [3-3](#q3-3) · [4-2](#q4-2) · [4-3](#q4-3))을 했다면 되돌렸는지 확인하세요.

```bash
ls state.json.bak main.py.bak 2>/dev/null   # 나오면 되돌리기를 안 한 것
git status                                   # 파일이 바뀌지 않았는지
```

→ `.bak` 파일이 남아 있으면 `mv state.json.bak state.json` 으로 복구하세요.

---

# 부록 C. 공부용 — 헷갈리는 것 비교

> 평가 대비가 아니라 **내가 이해하려고** 만든 부분입니다.

<a id="c1"></a>

## C-1. ⭐ 함수 vs 클래스

### 함수 = 믹서기

```
사과 넣기  →  [믹서기]  →  사과주스
```

믹서기는 **아무것도 기억하지 않습니다.** 갈고 나면 끝이라, 다음에 쓰려면 또 넣어야 합니다.

> **함수 = 넣으면 결과가 나오는 기계. 자기 것을 안 갖고 있음.**

### 클래스 = 학생

```
학생 = 민수
  가진 것 : 이름(민수), 점수(90점)
  하는 것 : 자기소개하기
```

민수는 **자기 이름과 점수를 항상 갖고 다닙니다.** 학생이 30명이면 이런 게 30개 있고, 각자 내용이 다릅니다.

> **클래스 = 정보를 갖고 다니면서 뭔가도 할 수 있는 것.**

### 언제 뭘 쓰나

| 이런 일이면 | 쓰는 것 |
| --- | --- |
| **계산만** 하고 끝 (기억할 게 없음) | **함수** |
| **여러 개** 있고 각각 **정보가 다름** | **클래스** |

### 퀴즈에 적용하면

퀴즈가 7문제이고 **문제마다 정답이 다릅니다.**

```
퀴즈 1개 = 학생 1명 같은 것
  가진 것 : 문제, 선택지, 정답, 힌트
  하는 것 : 화면에 보여주기, 채점하기
```

→ **여러 개 + 각각 정보가 다름** → **클래스**

### 함수로 만들면 왜 안 되나

함수는 기억을 못 해서 채점할 때마다 **정답을 옆에서 찾아다 줘야** 합니다.
문제를 하나 지우면 **정답 목록도 같이 고쳐야 하는데**, 깜빡하면 **전부 오답 처리**됩니다.

```python
채점(정답목록[2], 3)      # 함수  — 정답을 찾아서 넘겨줘야 함
quiz.check(3)            # 클래스 — 퀴즈가 자기 정답을 앎
```

📄 [Quiz 클래스 30행](main.py#L30) · [check() 50행](main.py#L50) · [default_quizzes() 74행](main.py#L74)

### 🎤 평가자에게

> "**함수는 기억을 못 하고, 클래스는 자기 정보를 갖고 다닙니다.**
> 퀴즈는 7개인데 문제마다 정답이 달라서, **각 퀴즈가 자기 정답을 갖고 다니게** 클래스로 만들었습니다."

> 🔍 **꼬리질문** — *"그럼 다 클래스로 만들면 되나요?"*
> → 아닙니다. [`default_quizzes()`](main.py#L74)는 **함수로 뒀습니다.** 퀴즈 5개를 만들어 돌려주고 끝나는 일이라 들고 있을 게 없거든요. **클래스를 쓰는 것 자체가 목적은 아니라고 생각합니다.**

---

## C-2. 이름이 비슷한 것들

| 짝 | 차이 | 내 코드 |
| --- | --- | --- |
| **함수** vs **메서드** | **위치**뿐. 클래스 안에 있으면 메서드 | [`default_quizzes()` 74행](main.py#L74) ↔ [`check()` 50행](main.py#L50) |
| **변수** vs **속성** | **위치**뿐. 클래스 안에 있으면 속성 | [`LINE` 19행](main.py#L19) ↔ [`self.answer` 36행](main.py#L36) |
| **클래스** vs **객체** | **틀 vs 실물**. 틀 1개로 실물 여러 개 | [`class Quiz` 30행](main.py#L30) ↔ 퀴즈 7문제 |
| **객체** vs **인스턴스** | **차이 없음.** 완전히 같은 말 | — |

---

## C-3. 기호가 헷갈리는 것들

| 짝 | 차이 | 내 코드 |
| --- | --- | --- |
| `=` vs `==` | `=` 는 **넣기**, `==` 는 **같은가? 묻기** | [`self.answer = answer` 36행](main.py#L36) ↔ [`selected == self.answer` 52행](main.py#L52) |
| `[ ]` vs `{ }` | `[ ]` 리스트는 **번호**로 꺼냄 · `{ }` 딕셔너리는 **이름**으로 꺼냄 | [`choices` 리스트 35행](main.py#L35) ↔ [`to_dict()` 딕셔너리 56-60행](main.py#L56-L60) |
| `"3"` vs `3` | 따옴표가 있으면 **글자**, 없으면 **숫자**. 글자로는 계산이 안 됨 | [`int(value)` 176행](main.py#L176) — 글자를 숫자로 바꾸는 곳 |

---

## C-4. 흐름을 다루는 것들

| 짝 | 차이 | 내 코드 |
| --- | --- | --- |
| `for` vs `while` | `for` 는 **정해진 횟수**, `while` 은 **조건이 맞는 동안 계속** | [`for` 202행](main.py#L202) ↔ [`while True` 168행](main.py#L168) |
| `break` vs `continue` | `break` 는 **반복 밖으로 나감**, `continue` 는 **이번 회만 건너뛰고 다시** | [`break` 246행](main.py#L246) ↔ [`continue` 179행](main.py#L179) |
| `return` vs `print` | `return` 은 **값을 돌려줌**(부른 쪽이 씀), `print` 는 **화면에 보여줌**(그걸로 끝) | [`return number` 185행](main.py#L185) |
| `append` vs `pop` | `append` 는 **뒤에 하나 붙이기**, `pop` 은 **하나 꺼내 빼기** | [`append` 206행](main.py#L206) ↔ [`pop` 232행](main.py#L232) |

---

## C-5. 딕셔너리에서 값 꺼내기 — 이게 진짜 헷갈립니다

| 방법 | 칸이 없으면 | 언제 쓰나 |
| --- | --- | --- |
| `data["hint"]` | 💥 **에러 나고 프로그램이 멈춤** | **꼭 있어야 하는 칸** |
| `data.get("hint", "")` | 대신 `""` 를 줌 (안 죽음) | **없어도 되는 칸** |

📄 [`data["question"]` 67행](main.py#L67) ↔ [`data.get("hint", "")` 70행](main.py#L70)

> **왜 다르게 썼나** — 문제·선택지·정답은 **없으면 퀴즈가 성립 안 되니** 일부러 에러를 냅니다.
> 힌트는 **없어도 되는 정보**라 빈칸으로 넘어갑니다. (힌트가 없던 옛날 저장 파일도 읽히게)

---

## C-6. "안"과 "밖"이 무슨 뜻인가

파이썬은 **칸 띄우기(들여쓰기)로 소속을 정합니다.**

```
30  class Quiz:                    ← 맨 왼쪽에 붙어 있음

33      def __init__(...):         ← 4칸 들어가 있음  ┐
40      def display(...):          ← 4칸 들어가 있음  ├ Quiz "안"
50      def check(...):            ← 4칸 들어가 있음  ┘

74  def default_quizzes():         ← 맨 왼쪽에 붙어 있음 = Quiz "밖"
```

**오른쪽으로 밀려 있으면 = 안**, **맨 왼쪽에 붙어 있으면 = 밖**입니다.

### 목차처럼 보면

```
1장. 퀴즈 (class Quiz)
     1-1. 만들기        ← 1장에 속함
     1-2. 보여주기      ← 1장에 속함
     1-3. 채점하기      ← 1장에 속함

2장. 기본 퀴즈 만들기   ← 1장 밖. 따로 있는 항목
```

### ⭐ 왜 이게 중요한가

**클래스에 딸려 있어야 그 클래스의 데이터를 쓸 수 있습니다.**

- [`check()` 50행](main.py#L50) 은 `Quiz` **안**에 있어서 → **그 문제의 정답을 압니다** (`self.answer`)
- [`default_quizzes()` 74행](main.py#L74) 은 **밖**에 있어서 → **딸린 데이터가 없습니다**

> **소속이 있으면 메서드, 혼자면 함수.**

직접 확인 — [50행](main.py#L50)은 왼쪽에 빈칸이 있고, [74행](main.py#L74)은 딱 붙어 있습니다.

---

## C-7. 문법 한 줄 정리

### 뼈대를 만드는 것

| | | 내 코드 |
| --- | --- | --- |
| `def` | **일을 하는 것**을 만듭니다 | [74행](main.py#L74) · [50행](main.py#L50) |
| `class` | **틀**을 만듭니다 | [30행](main.py#L30) |
| `self.` | **이 물건 자신의** 라는 뜻입니다 | [36행](main.py#L36) |
| `#` | **컴퓨터가 안 읽는 메모**입니다 (사람만 봄) | [19행](main.py#L19) |
| `import` | **남이 만든 도구를 가져오는 것**입니다 | [14-17행](main.py#L14-L17) |

### 값을 다루는 것

| | | 내 코드 |
| --- | --- | --- |
| `=` | **넣는 것**입니다 | [36행](main.py#L36) |
| `==` | **같은지 물어보는 것**입니다 | [52행](main.py#L52) |
| `[ ]` | **순서대로 담는 것**입니다 (번호로 꺼냄) | [35행](main.py#L35) |
| `{ }` | **이름표를 붙여 담는 것**입니다 (이름으로 꺼냄) | [56-60행](main.py#L56-L60) |
| `f"..."` | 문장 속에 **값을 끼워 넣는 것**입니다 | [43행](main.py#L43) |

### 흐름을 정하는 것

| | | 내 코드 |
| --- | --- | --- |
| `if` | **갈림길**입니다 | [327행](main.py#L327) |
| `for` | **정해진 횟수만큼** 반복하는 것입니다 | [202행](main.py#L202) |
| `while` | **조건이 맞는 동안** 계속 반복하는 것입니다 | [168행](main.py#L168) |
| `break` | 반복에서 **밖으로 나가는 것**입니다 | [339행](main.py#L339) |
| `continue` | **다시 처음으로 돌아가는 것**입니다 | [179행](main.py#L179) |
| `return` | **값을 돌려주는 것**입니다 | [185행](main.py#L185) |
| `print` | **화면에 보여주는 것**입니다 | [121행](main.py#L121) |
| `try` / `except` | **실패해도 안 죽게 막는 것**입니다 | [138행](main.py#L138) · [144행](main.py#L144) |

### 자주 쓰는 도구

| | | 내 코드 |
| --- | --- | --- |
| `len()` | **개수를 세는 것**입니다 | [216행](main.py#L216) |
| `int()` | **글자를 숫자로 바꾸는 것**입니다 | [176행](main.py#L176) |
| `.strip()` | **앞뒤 공백을 지우는 것**입니다 | [169행](main.py#L169) |
| `.append()` | **뒤에 하나 붙이는 것**입니다 | [206행](main.py#L206) |
| `.pop()` | **하나 빼내는 것**입니다 | [232행](main.py#L232) |

### ⭐ 제일 중요한 구분 하나

> **`def` = 일을 하는 것 · `=` = 값을 담는 것**

이 둘만 구분되면 나머지가 따라옵니다.

```
일을 하는 것(def)  →  밖이면 함수, 안이면 메서드
값을 담는 것(=)    →  밖이면 변수, 안이면 속성
```

---

## C-8. 함수 vs 메서드 — 자세히

> **클래스 안에 있으면 메서드, 밖에 있으면 함수.**

```python
def default_quizzes():        # 클래스 밖  →  함수
    ...

class Quiz:
    def check(self, ...):     # 클래스 안  →  메서드
        ...
```

### 실제로 달라지는 것 2가지

**① 부르는 방법이 다릅니다**

```python
default_quizzes()        # 그냥 이름만
quiz.check(2)            # 누구의 것인지 앞에 붙임 ← quiz 의 check
```

**② 메서드에는 `self` 가 붙습니다**

```python
def check(self, selected):
         ↑ "나 자신" — 자동으로 들어옵니다
```

`self` 덕분에 **자기 데이터(`self.answer`)를 쓸 수 있습니다.** 함수에는 이게 없습니다.

### 정리

| | 함수 | 메서드 |
| --- | --- | --- |
| 위치 | 클래스 **밖** | 클래스 **안** |
| 부르기 | `이름()` | `누구.이름()` |
| `self` | 없음 | **있음** |
| 자기 데이터 | 없음 | **있음** |

📄 [`default_quizzes()` 74행](main.py#L74) 함수 · [`check()` 50행](main.py#L50) 메서드

> **"클래스 안에 있으면 메서드"** 가 핵심이고, 나머지는 그래서 따라오는 결과입니다.

---

## C-9. 리스트 vs 딕셔너리

| | 리스트 `[ ]` | 딕셔너리 `{ }` |
| --- | --- | --- |
| 담는 법 | **순서대로** | **이름표 붙여서** |
| 꺼내는 법 | `목록[0]` — **번호**로 | `목록["이름"]` — **이름**으로 |
| 비유 | 번호표 붙은 사물함 | 이름표 붙은 사물함 |

### 리스트 — 번호로 꺼냄

```python
선택지 = ["( )", "[ ]", "{ }", "< >"]
선택지[0]      # "( )"   ← 0번째 (파이썬은 0부터 셈)
```

### 딕셔너리 — 이름으로 꺼냄

```python
퀴즈 = {"question": "리스트 괄호는?", "answer": 2}
퀴즈["answer"]      # 2
```

### 언제 뭘 쓰나

**선택지 4개** → 1번·2번·3번·4번, **순서가 중요** → **리스트**

**퀴즈 한 문제의 정보** → 순서보다 **뭐가 뭔지가 중요** → **딕셔너리**

```python
["리스트 괄호는?", ["( )", "[ ]"], 2, "힌트..."]
                                   ↑ 이 2가 뭐지? 정답? 개수?  ← 리스트면 모름

{"question": "리스트 괄호는?", "answer": 2}
                              ↑ 아, 정답이구나              ← 딕셔너리면 보임
```

### 우리 코드에서

📄 [`choices` 리스트 35행](main.py#L35) · [`to_dict()` 딕셔너리 56-60행](main.py#L56-L60)

```python
return {                               # 딕셔너리 — 이름표를 붙임
    "question": self.question,
    "choices": self.choices,           # 그 안에 리스트가 들어감
    "answer": self.answer,
    "hint": self.hint,
}
```

이렇게 만든 게 그대로 `state.json`에 적힙니다.

> **순서가 중요하면 리스트, 뭐가 뭔지가 중요하면 딕셔너리.**

---

## C-10. JSON 헷갈리는 것 정리

### ① JSON은 파일인가?

**아닙니다. JSON은 "적는 방법"입니다.**

```
JSON            ← 적는 방법 (형식)
state.json      ← 그 방법으로 적은 파일
```

한글이 "글자"이고 일기장이 "파일"인 것과 같습니다. **JSON은 글자, `state.json`은 일기장.**

### ② 왜 글자로 저장하나?

**파일에는 글자밖에 못 넣기 때문**입니다.

프로그램 안의 값은 **컴퓨터 메모리**에 있어서 파일에 그대로 못 옮깁니다. 그래서 **글자로 바꿔서** 적습니다.

```
메모리 안의 값  →  글자로 바꿈  →  파일에 적음
   (퀴즈 객체)      json.dump       state.json
```

읽을 때는 반대로 **글자를 다시 값으로** 바꿉니다. (`json.load`)

### ③ "파이썬과 구조가 같다"가 무슨 뜻?

**생긴 모양이 거의 똑같다**는 뜻입니다.

```python
파이썬 :  {"answer": 2, "hint": "..."}
```
```json
JSON   :  {"answer": 2, "hint": "..."}
```

`{ }` 도 같고 `[ ]` 도 같고 `:` 도 같습니다. 그래서 변환이 **한 줄로 끝납니다.**

```python
json.load(file)      # JSON 글자 → 파이썬 딕셔너리, 끝
```

모양이 달랐다면 **하나하나 뜯어 옮기는 코드를 제가 짜야** 했습니다.

### ④ 왜 많이 쓰나?

| 이유 | |
| --- | --- |
| **사람이 읽을 수 있음** | 메모장으로 열면 그냥 보임 |
| **규칙이 이미 정해져 있음** | 내가 만들 필요 없음 |
| **언어를 안 가림** | 파이썬·자바·웹 어디서나 읽힘 |
| **겹쳐 넣을 수 있음** | 복잡한 데이터도 담김 |

### ⑤ 필드와 중첩

```
"best_score": 100
 └─이름─┘   └값┘      ← 이 한 쌍이 필드 1개
```

**중첩** = 그 **값 자리에 또 이름·값 묶음**이 들어간 것

```json
"best_score": 100                                   ← 중첩 아님 (값이 그냥 숫자)
"quizzes": [ { "question": "괄호는?", "answer": 2 } ] ← 중첩 (값 안에 또 필드)
```

### ⑥ ⚠️ 클래스·함수와 헷갈리지 말 것

| | 어디 얘기인가 |
| --- | --- |
| **필드 · 중첩** | **파일(`state.json`)** 에 데이터를 적어놓은 모양 |
| **함수 · 클래스** | **코드(`main.py`)** 를 짜는 방식 |

**중첩은 클래스와 상관없습니다.** 다만 연결점은 있습니다 — `state.json`의 중첩 구조는 **`Quiz` 객체를 그대로 옮겨 적은 것**입니다.

📄 [`to_dict()` 54-61행](main.py#L54-L61) 객체 → 글자 · [`from_dict()` 63-71행](main.py#L63-L71) 글자 → 객체

> **JSON은 "데이터를 글자로 적는 세계 공통 방법"입니다.
> 파이썬과 모양이 거의 같아서 변환이 한 줄로 끝나고, 사람도 읽을 수 있어서 많이 씁니다.**
