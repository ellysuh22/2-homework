# 🎯 미션2 평가 답변 노트

> **각 문항은 [`미션2_평가기준.txt`](미션2_평가기준.txt)의 원문을 한 글자도 바꾸지 않고 그대로 옮긴 것입니다.**
> 문항 아래에 바로 답변이 옵니다. 순서도 평가기준과 동일합니다.
> 코드 근거는 실제 [main.py](main.py)와 `git log`로 확인한 사실만 담았습니다.

**답변 3단 뼈대** — 모든 답은 이렇게 말하면 됩니다.
> "저는 ~하게 만들었습니다(무엇) → 왜냐하면 ~하기 때문입니다(이유) → 안 그러면 ~한 문제가 생깁니다(근거)"

모르는 질문이 나오면: *"현재 구현은 ~까지입니다. 그 부분은 ~한 방향으로 개선할 수 있다고 생각합니다."*

---

## 📘 이 문서 읽는 법

각 문항은 **두 부분**으로 되어 있습니다. 목적이 다르니 쓰는 때도 다릅니다.

| 부분 | 언제 쓰나 | 내용 |
| --- | --- | --- |
| **✅ 답변 / 🗣️ 이렇게 말하세요** | **평가 당일** — 그대로 읽으면 되는 대본 | 무엇을 만들었고 왜 그렇게 했는가 |
| **🎓 학습 노트** | **평가 전 공부할 때** — 각 문항 맨 아래 | ① 질문의 요지 ② 왜 이걸 묻는가 ③ 공부할 개념 ④ 코드를 어떻게 짰나 |

> 💡 **학습 노트를 먼저 읽으세요.** 대본을 외우면 꼬리질문 한 번에 무너지지만,
> "왜 이걸 묻는지"를 알면 어떻게 물어도 대답할 수 있습니다.

### 🧭 평가자가 진짜 보는 것 — 구현력이 아니라 **설계력**

20문항을 관통하는 기준은 하나입니다.

> **"돌아가는 코드를 짰는가"가 아니라 "왜 그렇게 짰는지 말할 수 있는가"**

돌아가는 코드는 검색해서 베껴도 만들어집니다. 하지만 **왜 그 구조인지**는 직접 고민한 사람만 답합니다.
그래서 [항목 1]만 "동작 확인"이고, **[항목 2]·[3]·[4] 15문항은 전부 "설명할 수 있는가"** 로 끝납니다.

| 평가자가 듣고 싶은 것 | 나쁜 답 | 좋은 답 |
| --- | --- | --- |
| **나눈 기준** | "그냥 나눴습니다" | "**바뀌는 이유가 다르면** 따로 뒀습니다" |
| **선택의 근거** | "JSON이 편해서요" | "규모가 수십 개고 **사람이 읽을 수 있어야** 해서요" |
| **한계 인식** | "문제 없습니다" | "1000개가 되면 **전체 재작성이 병목**입니다" |
| **개선 방향** | (모름) | "**임시 파일에 쓰고 교체**하는 방식이 우선입니다 (미구현)" |

**이 문서 전체를 관통하는 답변 뼈대**

> **① 무엇을** 이렇게 만들었고 → **② 왜냐하면** ~하기 때문이고 → **③ 안 그러면** ~한 문제가 생깁니다

③번이 가장 중요합니다. **"안 그러면 어떻게 되는지"** 를 말할 수 있다는 건
그 구조를 **직접 겪어봤다**는 뜻이라, 평가자가 가장 신뢰하는 신호입니다.

> ⚠️ **정직함이 곧 점수입니다.** 특히 [항목 4]는 미구현을 구현했다고 말하면 꼬리질문에서 바로 드러납니다.
> **"현재 구현은 ~까지입니다 / 개선한다면 ~하겠습니다"** 로 나눠 말하는 것이 만점 답변입니다.

각 문항은 아래 5개 블록이 **항상 같은 순서**로 들어 있습니다.

| 블록 | 내용 |
| --- | --- |
| **✅ 답변** | 결론 한두 줄 |
| **🗣️ 이렇게 말하세요 / 🖥️ 시연** | 평가 당일 대본 |
| **🎓 학습 노트** | 질문의 요지 · 왜 묻는가 · 공부할 개념 · 코드를 어떻게 짰나 |
| **🔍 검증 방법** | **명령 → 나와야 하는 결과 → 코드 위치** 3단 표 |
| **🔗 이동** | 목차 · 이전/다음 문항 · 관련 문항 링크 |

---

<a id="toc"></a>

## 🗺️ 목차 — 문항별 바로가기

**문항 이름**을 누르면 답변으로, **코드 링크**를 누르면 `main.py`의 해당 줄로 바로 이동합니다.

### [항목 1] 기능 동작 검증 — *실제로 돌아가는가*

| # | 문항 | 검증 한 줄 | 핵심 코드 |
| --- | --- | --- | --- |
| 1-1 | [메뉴 표시와 기능 동작](#q1-1) | 실행 후 `1`~`6` 차례로 입력 | [`run()`](main.py#L308-L326) |
| 1-2 | [정답 판정·입력 오류 처리](#q1-2) | `abc` `0` `9` 빈Enter 입력 | [`input_int()`](main.py#L153-L172) |
| 1-3 | [재실행해도 데이터 유지](#q1-3) | 추가 → 종료 → 재실행 | [`load_state()`](main.py#L119-L138) |
| 1-4 | [기본 퀴즈 5개](#q1-4) | `state.json` 치우고 실행 | [`default_quizzes()`](main.py#L61-L94) |
| 1-5 | [커밋 10개 이상](#q1-5) | `git rev-list --count HEAD` | — (git) |
| 1-6 | [브랜치 생성·병합 기록](#q1-6) | `git log --graph` | — (git) |
| 1-7 | [clone·pull 실습 흔적](#q1-7) | ⚠️ **스크린샷 필요** | — (git) |

### [항목 2] 코드 구조 및 설계 — *왜 그렇게 나눴는가*

| # | 문항 | 검증 한 줄 | 핵심 코드 |
| --- | --- | --- | --- |
| 2-1 | [클래스 책임 분리](#q2-1) | `grep -n "^class" main.py` | [`Quiz`](main.py#L18-L58) / [`QuizGame`](main.py#L97-L326) |
| 2-2 | [입력·진행·저장 분리 기준](#q2-2) | `grep -n "input_int(" main.py` | [`input_int()`](main.py#L153-L172) |
| 2-3 | [state.json 읽기/쓰기 흐름](#q2-3) | `grep -n "save_state()" main.py` | [`save_state()`](main.py#L140-L151) |
| 2-4 | [Ctrl+C·EOF 안전 종료](#q2-4) | 실행 후 `Ctrl+C` | [`main()`](main.py#L329-L338) |
| 2-5 | [커밋 단위·메시지 규칙](#q2-5) | `git log --oneline` | — (git) |

### [항목 3] 핵심 기술 원리 — *원리를 이해했는가*

| # | 문항 | 검증 한 줄 | 핵심 코드 |
| --- | --- | --- | --- |
| 3-1 | [클래스 vs 함수](#q3-1) | `grep -n "quiz.check" main.py` | [`Quiz.check()`](main.py#L37-L39) |
| 3-2 | [JSON을 고른 이유](#q3-2) | `head -20 state.json` | [`json.dump()`](main.py#L149) |
| 3-3 | [파일 입출력의 try/except](#q3-3) | 파일 깨뜨리고 실행 | [`load_state()`](main.py#L125-L138) |
| 3-4 | [브랜치·merge의 의미](#q3-4) | `git show --stat 2d45928` | — (git) |
| 3-5 | [데이터 구조 설계 이유](#q3-5) | `head -20 state.json` | [`to_dict()`](main.py#L41-L48) |

### [항목 4] 심층 인터뷰 — *한계를 아는가* (정답 없음)

| # | 문항 | 검증 한 줄 | 핵심 코드 |
| --- | --- | --- | --- |
| 4-1 | [퀴즈 1000개로 늘면](#q4-1) | `grep -n "json.dump" main.py` | [`save_state()`](main.py#L140-L151) |
| 4-2 | [state.json 손상 대응](#q4-2) | 파일 깨뜨리고 실행 | [손상 감지](main.py#L131-L135) |
| 4-3 | [요구사항이 바뀌면](#q4-3) | `grep -n "1-4" main.py` | [`HINT_PENALTY`](main.py#L12) |

### 부록

[부록 A. 그림으로 보는 구조](#appendix-a) · [부록 B. 용어 사전](#appendix-b) · [부록 C. 1분 요약](#appendix-c) · [부록 D. 평가 전 준비](#appendix-d) · [부록 E. 검증 명령 모음](#appendix-e) · [부록 F. 20문항 핵심 요약](#appendix-f)

---

# [항목 1] 기능 동작 검증

<a id="q1-1"></a>

## 1-1. 프로그램 실행 시 메뉴가 정상적으로 표시되고, 퀴즈 풀기/추가/목록/점수 확인 기능이 모두 동작하는가?

> **🎯 이 문항의 핵심** — **"기능이 다 있는가**가 아니라 **기능이 끝나면 메뉴로 돌아오는가?"**
> *한 번 쓰고 죽는 프로그램인지 아닌지.*

**✅ 답변** — 네, 메뉴 6개가 모두 동작합니다. 필수 4개 기능은 1·2·3·5번이고, 4번(삭제)은 보너스입니다.

**🖥️ 시연 순서** — 터미널(VSCode: `Terminal → New Terminal`)에서 프로젝트 폴더로 이동한 뒤 실행합니다.

```bash
cd /Users/ellysuh/Documents/2-homework
python3.11 main.py
```

> ⚠️ 이 컴퓨터의 기본 `python3`는 3.9.6이라 과제 요구(3.10 이상)에 미달합니다.
> **시연·스크린샷은 반드시 `python3.11`로** 실행하세요. (평가자가 다른 컴퓨터에서 clone한 경우에는 `python3 main.py`)

실행하면 메뉴가 뜹니다. → `1`(풀기) → `2`(추가) → `3`(목록) → `4`(삭제) → `5`(점수) → `6`(종료) 순서로 눌러 전부 보여줍니다.

**"정상적으로 표시된다"가 확인하는 6가지**

| # | 확인 항목 | 왜 보는가 |
| --- | --- | --- |
| 1 | 실행하자마자 **저절로** 메뉴가 뜬다 | 별도 명령 없이 바로 쓸 수 있는가 |
| 2 | 번호 1~6이 **빠짐없이** 보인다 | 만들다 만 메뉴가 아닌가 |
| 3 | 한글·이모지가 **안 깨진다** | 인코딩을 제대로 처리했는가 |
| 4 | `선택:` 프롬프트가 뜨고 **입력을 기다린다** | 화면만 뿌리고 끝나는 게 아닌가 |
| 5 | 기능이 끝나면 **다시 메뉴로 돌아온다** | 한 번 쓰고 죽는 프로그램이 아닌가 ← **핵심** |
| 6 | `6`을 누르면 **정상 종료된다** | 나가는 길이 있는가 |

**🗣️ 이렇게 말하세요**

> "`run()`의 `while True` 반복문이 돌면서 `show_menu()`로 메뉴를 그리고 번호를 기다립니다.
> 번호를 고르면 해당 메서드를 실행하고, 끝나면 반복문이 다시 돌아 메뉴가 또 나옵니다.
> `6`을 고를 때만 `break`로 빠져나가 종료됩니다. 그래서 몇 번을 쓰든 항상 메뉴로 돌아옵니다."

> 💡 **위 문장에 나온 용어 풀이** — 프로그램을 **식당**에 비유하면 이해가 쉽습니다.
>
> | 용어 | 뜻 | 식당 비유 |
> | --- | --- | --- |
> | **메서드** | 클래스 안에 들어 있는 함수(= 기능 하나). 이름 뒤에 `()`가 붙습니다 | 직원이 할 줄 아는 일 하나 |
> | **`run()`** | 제가 만든 메서드 이름. run(달리다) = **프로그램을 계속 굴리는 담당** | 가게 문을 열고 영업을 도는 매니저 |
> | **`show_menu()`** | 제가 만든 메서드 이름. show(보여주다) + menu(메뉴) = **메뉴 화면 출력 담당** | 손님에게 메뉴판을 내미는 일 |
> | **반복문** | 같은 일을 여러 번 되풀이하라는 명령 | "손님 올 때마다 똑같이 응대해" |
> | **`while True`** | while(~인 동안) + True(참) → 조건이 **항상 참**이라 **끝없이** 반복 | "영업 끝낼 때까지 계속" |
> | **`break`** | 반복문을 그 자리에서 **깨고 빠져나가는** 명령 | "이제 문 닫자" (퇴근) |
>
> **한 문장으로** — `run()`이 `while True`로 끝없이 돌면서 매번 `show_menu()`로 메뉴판을 보여주고,
> 손님이 고른 번호에 맞는 메서드를 실행합니다. `6`(종료)일 때만 `break`로 반복을 끊고 나갑니다.
>
> ```python
> while True:              # ① 계속 반복
>     self.show_menu()     # ② 메뉴판 보여주기
>     choice = ...         # ③ 번호 받기
>     if choice == 1: ...  # ④ 맞는 기능 실행 → 끝나면 ①로 되돌아감
>     else: break          # ⑤ 6번일 때만 반복 탈출 = 종료
> ```
>
> 📂 실제 코드: [`run()`](main.py#L308-L326)
>
> ⚠️ **`break`가 없으면?** 6번을 눌러도 안 꺼지고 메뉴가 무한히 반복됩니다.
> 반대로 **`while True`가 없으면?** 기능 한 번 쓰고 프로그램이 그냥 끝나 버립니다.
>
> **왜 그런가** — 파이썬은 코드를 **위에서 아래로 딱 한 번** 읽고 지나갑니다. 다 읽으면 프로그램은 끝납니다.
> `while`은 **"여기서 다시 위로 올라가라"** 는 유일한 되돌아가기 명령입니다. 그게 없으면 되돌아갈 방법이 없습니다.
>
> | `while True` 없을 때 | `while True` 있을 때 |
> | --- | --- |
> | 메뉴 출력 → 번호 받기 → 기능 실행 → **끝** | 메뉴 출력 → 번호 받기 → 기능 실행 → **다시 메뉴로** ↺ |
> | 점수를 보려면 프로그램을 **다시 실행**해야 함 | 계속 이어서 씀 |
>
> ```python
> # while이 없으면 — 아래로 흐르고 끝
> self.show_menu()          # 1️⃣
> choice = ...              # 2️⃣
> self.play_quiz()          # 3️⃣  ← 여기까지 읽으면 파이썬은 할 일이 없어 종료
>
> # while이 있으면 — 3️⃣ 다음에 1️⃣로 되돌아감
> while True:
>     self.show_menu()      # 1️⃣ ←──────┐
>     choice = ...          # 2️⃣        │ 되돌아감
>     self.play_quiz()      # 3️⃣ ───────┘
> ```
>
> 엘리베이터에 비유하면 — `while`이 없는 건 **1층에서 3층까지 올라간 뒤 전원이 꺼지는** 것이고,
> `while`이 있는 건 **3층에 도착하면 다시 1층으로 내려가 대기**하는 것입니다.

| 메뉴 | 기능 | 담당 메서드 |
| --- | --- | --- |
| 1 | 퀴즈 풀기 | [`play_quiz()`](main.py#L248-L277) |
| 2 | 퀴즈 추가 | [`add_quiz()`](main.py#L183-L195) |
| 3 | 퀴즈 목록 | [`list_quizzes()`](main.py#L197-L207) |
| 4 | 퀴즈 삭제 (보너스) | [`delete_quiz()`](main.py#L209-L221) |
| 5 | 점수 확인 | [`show_score()`](main.py#L294-L306) |
| 6 | 종료 | [`run()`](main.py#L308-L326) |

**📂 코드 위치** — 메뉴 출력 [`show_menu()`](main.py#L105-L117) / 메뉴 반복 [`run()`](main.py#L308-L326)

### 🎓 학습 노트 (1-1)

**📖 질문의 요지** — "기능을 만들었는가"가 아니라 **"기능들이 하나의 프로그램으로 연결되어 있는가"** 를 봅니다.

**🎯 왜 이걸 묻는가**

초보가 만든 프로그램의 가장 흔한 실패는 **"기능은 다 있는데 한 번 쓰면 끝나는 것"** 입니다.
퀴즈를 풀고 나면 프로그램이 그냥 종료돼서, 점수를 보려면 다시 실행해야 하는 식이죠.

| 평가자의 속마음 | 확인하는 것 |
| --- | --- |
| "이걸 실제로 써봤나?" | 메뉴 → 기능 → 메뉴 순환이 되는가 |
| "나가는 길은 있나?" | 종료 번호가 실제로 동작하는가 |
| "만들다 만 건 아닌가?" | 메뉴에 적힌 번호가 전부 진짜 동작하는가 |

> 이 구조를 실무에서는 **메인 루프**(main loop)라고 부릅니다. 게임·앱·서버 모두 이 뼈대 위에 얹혀 있습니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 | 이 프로그램에서 |
| --- | --- | --- |
| **반복문 `while`** | 조건이 참인 동안 계속 되풀이 | 메뉴를 계속 다시 보여줌 |
| **`while True`** | 조건이 항상 참 = 끝없이 반복 | 사용자가 나갈 때까지 유지 |
| **`break`** | 반복문을 깨고 밖으로 나감 | 6번(종료)을 골랐을 때만 |
| **`if` / `elif` / `else`** | 조건에 따라 갈림길 선택 | 번호별로 다른 기능 실행 |
| **함수/메서드 호출 `()`** | 이름 뒤에 `()`를 붙여 실행 | `self.play_quiz()` 등 |

**💻 코드를 이렇게 짰습니다**

```python
def run(self):
    while True:                      # ① 끝없이 반복 = 메뉴가 계속 돌아옴
        self.show_menu()             # ② 메뉴판 출력
        choice = self.input_int("선택: ", 1, 6)   # ③ 검증된 번호만 받음

        if choice == 1:
            self.play_quiz()         # ④ 번호에 맞는 기능 실행
        elif choice == 2:
            self.add_quiz()
        ...
        else:                        # ⑤ 6번일 때
            print("👋 게임을 종료합니다...")
            break                    #    반복 탈출 = 프로그램 끝
```

**설계 포인트 3가지**
1. `run()`은 **직접 일하지 않고 시키기만** 합니다. 실제 작업은 각 메서드가 합니다 → 메뉴가 늘어도 `run()`은 한 줄만 추가.
2. 번호 검증을 `input_int("선택: ", 1, 6)`에 맡겨서, `run()` 안에는 **"1~6이 맞나?" 검사가 아예 없습니다.**
3. `else`를 6번 전용으로 쓸 수 있는 이유는 **1~6만 들어온다고 보장되기** 때문입니다. 검증을 앞단에서 끝냈기에 가능한 단순화입니다.

> 💡 **`if` / `elif` / `else`란?** — **갈림길**을 만드는 명령입니다.
>
> | 명령 | 어원 | 뜻 |
> | --- | --- | --- |
> | `if` | if (만약) | "만약 ~라면 이걸 해라" — **첫 번째 조건** |
> | `elif` | **el**se + **if** | "**아니고** 만약 ~라면" — 두 번째부터의 조건 |
> | `else` | else (그 밖에) | "**위 어느 것도 아니면**" — 남은 전부 |
>
> ```python
> if choice == 1:      # 1번이면 → 퀴즈 풀기
>     self.play_quiz()
> elif choice == 2:    # 1번이 아니고 2번이면 → 퀴즈 추가
>     self.add_quiz()
> else:                # 위 전부 아니면(= 6번) → 종료
>     break
> ```
>
> **핵심 규칙 2가지**
> 1. **위에서부터 순서대로** 검사하고, **맞는 것 하나를 만나면 나머지는 건너뜁니다.** (`elif`가 5개여도 실행되는 건 1개)
> 2. `if`는 혼자 쓸 수 있지만 `elif`·`else`는 **`if` 없이 못 씁니다.** 갈림길의 시작이 `if`이기 때문입니다.
>
> 자판기에 비유하면 — "1번 버튼이면 콜라, 2번이면 사이다, **그 외에는** 반환"입니다.

**🔍 검증 방법** — 평가자 앞에서 이 순서대로 보여줍니다.

| 단계 | 입력 | 나와야 하는 결과 | 확인 포인트 |
| --- | --- | --- | --- |
| 1 | `python3.11 main.py` | 메뉴 6줄이 **바로** 출력 | 별도 명령 없이 즉시 실행 |
| 2 | `3` | 퀴즈 목록 → **메뉴 재출력** | ← **가장 중요** (루프가 도는 증거) |
| 3 | `1` → 문제 수 → 답 | 채점 결과 → **메뉴 재출력** | 기능 후 복귀 |
| 4 | `2` → 문제·보기·정답 | `✅ 퀴즈가 추가되었습니다!` | |
| 5 | `5` | 최고 점수 + 최근 기록 | |
| 6 | `6` | `👋 게임을 종료합니다...` 후 종료 | 나가는 길이 있음 |

```bash
grep -n "while True" main.py     # → 155, 176, 310  (310행이 메뉴 루프)
```

**🔗 이동** — [↑ 목차](#toc) · [다음 1-2 ▶](#q1-2) · 관련: [2-2 함수 분리 기준](#q2-2)


---

<a id="q1-2"></a>

## 1-2. 퀴즈 풀기 시 정답/오답 판정이 정확하고, 최소 입력 오류 케이스(공백/문자/범위 밖/빈 입력)에 대한 처리가 되어 있는가?

> **🎯 이 문항의 핵심** — **"사용자가 실수해도 프로그램이 안 죽는가?"**
> *틀린 입력에 에러로 종료하는지, 다시 물어보는지.*

**✅ 답변** — 네, 4가지 오류 케이스가 모두 처리되어 있습니다. 숫자를 받는 **모든 곳**이
[`input_int()`](main.py#L153-L172) 하나를 거치기 때문에 메뉴·정답·문제 수·삭제 번호에서 **동일하게** 동작합니다.

> 💡 **`input_int()`란?** — 파이썬 명령어가 아니라 **제가 직접 만들어 이름 붙인 함수**입니다.
> `input`(입력받다) + `int`(integer, 정수) = **"정수를 입력받는"** 이라는 뜻입니다.
> (같은 규칙으로 `input_text()`는 글자를 입력받는 함수입니다.)
>
> **하는 일** — 올바른 숫자가 나올 때까지 계속 다시 묻는 "문지기"입니다.
>
> | 단계 | 하는 일 |
> | --- | --- |
> | 1 | 앞뒤 공백 제거 (`.strip()`) |
> | 2 | 빈 입력이면 → 다시 묻기 |
> | 3 | 숫자로 변환 실패(`abc`)면 → 다시 묻기 |
> | 4 | 범위 밖(`0`, `9`)이면 → 다시 묻기 |
> | 5 | 전부 통과하면 → 그 숫자를 돌려줌 |
>
> 부를 때 허용 범위를 넘겨줍니다: `input_int("선택: ", 1, 6)` → 1~6만 받음.
> 돌려준 값은 이미 검증이 끝났으므로 **부른 쪽에서 다시 확인할 필요가 없습니다.**

**📞 이 함수를 부르는 곳 5군데** — 검증 코드는 한 곳뿐이고 나머지는 불러다 씁니다.

| 어디서 | 코드 | 받는 범위 |
| --- | --- | --- |
| 메뉴 선택 | [main.py:312](main.py#L312) | `1~6` |
| 퀴즈 추가 — 정답 번호 | [main.py:191](main.py#L191) | `1~4` |
| 퀴즈 삭제 — 번호 | [main.py:216](main.py#L216) | `1~퀴즈개수` |
| 퀴즈 풀기 — 문제 수 | [main.py:256](main.py#L256) | `1~퀴즈개수` |
| 퀴즈 풀기 — 정답 입력 | [main.py:229](main.py#L229) | `1~5` (5는 힌트) |

**🖥️ 시연** — 메뉴에서 차례로 `abc` → `0` → `9` → 그냥 Enter → ` 3 `(앞뒤 공백)을 입력합니다.

| 오류 케이스 | 입력 예 | 실제 출력 |
| --- | --- | --- |
| 문자 (숫자 변환 실패) | `abc` | `⚠️ 숫자가 아닙니다. 1-6 사이의 숫자를 입력하세요.` |
| 범위 밖 | `0`, `9` | `⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.` |
| 빈 입력 | 그냥 Enter | `⚠️ 입력이 비어 있습니다. 1-6 사이의 숫자를 입력하세요.` |
| 앞뒤 공백 | ` 3 ` | `.strip()`으로 제거 후 **정상 동작** |

**정답/오답 판정** — [`Quiz.check()`](main.py#L37-L39)가 입력 번호와 `answer`를 비교합니다.
오답이면 정답 번호와 그 내용까지 함께 알려줍니다. ([main.py:244-245](main.py#L244-L245))

**📂 코드 위치** — [`input_int()`](main.py#L153-L172) · [`input_text()`](main.py#L174-L181) · [`Quiz.check()`](main.py#L37-L39)

### 🎬 시연 대본 (1-2) — 키보드 한 번씩 그대로

> 아래는 **실제로 돌려서 받아 적은 화면**입니다. 그대로 따라 하면 됩니다.
> 이 문항은 **두 부분**을 보여줘야 합니다 — ① 오류 입력 4종 ② 정답/오답 판정.

**0단계 — 터미널 준비**

```bash
cd /Users/ellysuh/Documents/2-homework
clear
python3.11 main.py
```

---

## 【1부】 오류 입력 4종 — `선택:` 프롬프트에서 연달아 입력

**이 4개를 쉬지 않고 연달아 칩니다.** 프로그램이 죽지 않는 게 핵심이라 끊지 않는 편이 극적입니다.

| 순서 | 내가 누르는 것 | 화면에 뜨는 것 | 어떤 오류 |
| --- | --- | --- | --- |
| 1 | `abc` + Enter | `⚠️ 숫자가 아닙니다. 1-6 사이의 숫자를 입력하세요.` | **문자** |
| 2 | `0` + Enter | `⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.` | **범위 밖(아래)** |
| 3 | `9` + Enter | `⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.` | **범위 밖(위)** |
| 4 | 그냥 Enter | `⚠️ 입력이 비어 있습니다. 1-6 사이의 숫자를 입력하세요.` | **빈 입력** |
| 5 | `  3  ` (앞뒤 공백) + Enter | **정상 동작** — 퀴즈 목록 출력 | **공백 제거** |

실제 화면은 이렇게 이어집니다.

```
선택: abc
⚠️ 숫자가 아닙니다. 1-6 사이의 숫자를 입력하세요.
선택: 0
⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.
선택: 9
⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.
선택:
⚠️ 입력이 비어 있습니다. 1-6 사이의 숫자를 입력하세요.
선택:   3
📋 등록된 퀴즈 목록 (총 5개)
```

> 🗣️ **"네 번을 연속으로 잘못 넣었는데 프로그램이 한 번도 죽지 않았습니다.**
> 매번 무엇이 잘못됐는지 알려주고 **다시 물어봅니다.**
> 마지막에는 앞뒤에 공백을 넣은 `  3  `을 줬는데, `.strip()`으로 공백을 지운 뒤 정상 처리됐습니다.
> 이 검사는 전부 [`input_int()`](main.py#L153-L172) **한 함수 안에서** 이뤄집니다."

> 💡 **평가자가 캐물으면** — *"메뉴 말고 다른 데서도 되나요?"*
> → `2`(퀴즈 추가)로 들어가 `정답 번호 (1-4):` 에서도 `abc`를 넣어 보이세요.
> **똑같은 경고가 나옵니다.** 숫자를 받는 5곳이 전부 같은 함수를 쓰기 때문입니다.

---

## 【2부】 정답/오답 판정 — 메뉴에서 `1` 입력

| 순서 | 내가 누르는 것 | 화면에 뜨는 것 |
| --- | --- | --- |
| 1 | `1` + Enter | `📝 등록된 퀴즈는 총 5문제입니다.` / `몇 문제를 푸시겠습니까? (1-5):` |
| 2 | `2` + Enter | 문제 1과 보기 4개 출력 |
| 3 | `5` + Enter | `💡 힌트: ... (힌트 사용 시 5점 차감)` ← **보너스 기능** |
| 4 | **틀린 번호** + Enter | `❌ 오답입니다! 정답은 2번 (for)` ← **번호와 내용까지** |
| 5 | (문제 2에서) **맞는 번호** + Enter | `✅ 정답입니다!` |
| 6 | | `🏆 결과: 2문제 중 1문제 정답! (45점)` + 힌트 차감 안내 |

실제 화면입니다.

```
[문제 1]
정해진 횟수만큼 반복할 때 주로 사용하는 반복문은?

1. while
2. for
3. if
4. try

정답 입력 (1-4, 힌트 보기는 5): 5
💡 힌트: range()나 리스트와 함께 자주 쓰는 반복문입니다. (힌트 사용 시 5점 차감)
정답 입력 (1-4, 힌트 보기는 5): 1
❌ 오답입니다! 정답은 2번 (for)
```

> 🗣️ **"오답일 때 그냥 '틀렸습니다'로 끝내지 않고 정답 번호와 그 내용까지 알려줍니다.**
> 채점은 [`Quiz.check()`](main.py#L37-L39)가 합니다. **정답을 아는 건 문제 카드 자신뿐**이고,
> 게임 쪽은 `quiz.check(입력)` 한 줄로 물어보기만 합니다."

---

**⚠️ 시연 팁 3가지**

| # | 팁 | 왜 |
| --- | --- | --- |
| 1 | **일부러 틀린 답도 보여주세요** | 정답만 맞히면 오답 판정이 되는지 확인이 안 됩니다 |
| 2 | 1부에서 **끊지 말고 연달아** 입력 | "안 죽는다"가 눈에 확 들어옵니다 |
| 3 | 공백 입력(` 3 `)은 **마지막에** | 4번 실패 후 성공해야 대비가 됩니다 |

> 💡 **이 문항의 핵심 한 문장** — *"네 번 잘못 넣어도 안 죽고, 매번 다시 물어봅니다."*
> 긴장되면 1부의 4줄만 보여줘도 통과합니다.

### 🎓 학습 노트 (1-2)

**📖 질문의 요지** — **"사용자가 실수해도 프로그램이 안 죽는가?"** 딱 이것 하나입니다.

**🎯 왜 이걸 묻는가**

내가 만든 프로그램은 내가 **바르게만** 씁니다. 남이 쓰면 반드시 이상한 걸 넣습니다.
평가자는 일부러 이상하게 눌러봅니다.

| 평가자가 해볼 짓 | 처리 안 했으면 | 처리했으면 |
| --- | --- | --- |
| 그냥 Enter (빈 입력) | 빨간 에러 뜨고 **강제 종료** | 안내 후 **다시 물음** |
| `ㅁ`, `abc` 입력 | `ValueError` 터지며 **강제 종료** | 안내 후 다시 물음 |
| `99` 입력 | 반응 없거나 엉뚱하게 동작 | "1-6 사이" 안내 |
| ` 3 ` (앞뒤 공백) | 숫자로 못 읽고 에러 | 공백 지우고 **정상 처리** |

**평가자의 속마음 3가지**
1. 이 사람이 **자기 코드를 자기가 써보긴 했나?** (안 써봤으면 이런 상황을 모릅니다)
2. **사용자 입장**에서 생각할 줄 아나? (에러 메시지 던지고 죽는 건 사용자 잘못이 아닙니다)
3. `try/except`를 **외운 게 아니라 필요해서** 썼나?

> 실무 용어로 **방어적 프로그래밍**(defensive programming)이라고 합니다.
> "사용자는 반드시 예상 밖의 짓을 한다"고 미리 가정하고 막아두는 태도입니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 | 코드 위치 |
| --- | --- | --- |
| **정수(int)** | 소수점이 없는 딱 떨어지는 수 (`1`, `2`, `-7`) ↔ 실수(`1.5`) | [main.py:163](main.py#L163) |
| **`try` / `except`** | "일단 해보고(try), 터지면(except) 대신 이렇게 해라" | [162-166행](main.py#L162-L166) |
| **`ValueError`** | 에러의 **이름표**. `int("ㅁ")`처럼 값이 이상할 때 붙음 | [main.py:164](main.py#L164) |
| **`.strip()`** | 문자열 **앞뒤 공백 잘라내기** | [main.py:156](main.py#L156) |
| **`continue`** | 이번 차례만 건너뛰고 **반복문 처음으로** | [main.py:160](main.py#L160) |
| **`return`** | 합격한 값을 **불러준 곳에 돌려주고** 함수 끝 | [main.py:172](main.py#L172) |

> 📖 **정수·상수·함수를 더 자세히** → [부록 B-0.5](#terms-basic)

> ⚠️ **가장 헷갈리는 곳 — `continue` vs `break` vs `return`** (검색대 비유)
> - `continue` = "불합격, **다시 줄 서세요**" → 검색대 맨 앞으로
> - `break` = "검색대 **폐쇄**" → 반복문 밖으로
> - `return` = "**통과**" → 값을 들고 함수 밖으로

**💻 코드를 이렇게 짰습니다**

```python
def input_int(self, prompt, low, high):   # low~high = 허용 범위
    while True:                            # 합격할 때까지 무한 반복
        value = input(prompt).strip()      # ① 앞뒤 공백 제거

        if value == "":                    # ② 빈 입력 검사
            print("⚠️ 입력이 비어 있습니다...")
            continue                       #    → 다시 물음

        try:
            number = int(value)            # ③ 글자를 정수로 변환 시도
        except ValueError:                 #    "abc" 같으면 여기로
            print("⚠️ 숫자가 아닙니다...")
            continue

        if number < low or number > high:  # ④ 범위 검사
            print("⚠️ 잘못된 입력입니다...")
            continue

        return number                      # ⑤ 4관문 통과한 값만 밖으로
```

**설계 포인트 3가지**
1. **검사 순서가 중요합니다.** 공백 제거 → 빈값 → 숫자변환 → 범위 순. 순서를 바꾸면 `int("")`에서 먼저 터집니다.
2. **검증을 함수 하나에 모았습니다.** 숫자를 받는 [5군데](main.py#L191)가 전부 이 함수를 부르므로, 규칙을 바꿀 때 **한 곳만** 고칩니다.
3. **`low`, `high`를 밖에서 받습니다.** 그래서 메뉴에선 `1~6`, 정답에선 `1~4`로 **같은 함수가 다르게** 동작합니다.

**🔍 검증 방법** — 메뉴 프롬프트(`선택:`)에서 차례로 입력합니다.

| 입력 | 나와야 하는 결과 | 무엇이 증명되나 |
| --- | --- | --- |
| `abc` | `⚠️ 숫자가 아닙니다. 1-6 사이의 숫자를 입력하세요.` | `try/except ValueError` 동작 |
| `0` 또는 `9` | `⚠️ 잘못된 입력입니다. 1-6 사이의 숫자를 입력하세요.` | 범위 검사 동작 |
| 그냥 Enter | `⚠️ 입력이 비어 있습니다. 1-6 사이의 숫자를 입력하세요.` | 빈 입력 검사 동작 |
| ` 3 ` (앞뒤 공백) | **정상 동작** (퀴즈 목록 출력) | `.strip()` 동작 |
| **위 4개 후에도 계속 물어봄** | 프로그램이 **안 죽음** | ← **핵심** |

정답/오답 판정은 퀴즈를 풀며 확인합니다.

| 입력 | 나와야 하는 결과 |
| --- | --- |
| 정답 번호 | `✅ 정답입니다!` |
| 틀린 번호 | `❌ 오답입니다! 정답은 2번 ([ ])` ← 정답 **번호와 내용**까지 |

```bash
grep -n "input_int(" main.py    # → 153(정의) + 191,216,229,256,312(호출 5곳)
```
> 정의는 **1개**, 호출은 **5개** — 검증 코드가 한 곳에만 있다는 증거입니다.

**🔗 이동** — [↑ 목차](#toc) · [◀ 1-1](#q1-1) · [다음 1-3 ▶](#q1-3) · 관련: [3-3 try/except](#q3-3)


---

<a id="q1-3"></a>

## 1-3. 프로그램 종료 후 재실행해도 추가한 퀴즈와 최고 점수가 유지되는가?

> **🎯 이 문항의 핵심** — **"프로그램을 껐다 켜도 기억하는가?"**
> *실행할 때마다 처음으로 돌아가면 저장 기능이 없는 것.*

**✅ 답변** — 네, `state.json` 파일에 저장하기 때문에 유지됩니다.

**🖥️ 시연** — 메뉴 `2`로 퀴즈 1개 추가 → 메뉴 `6` 종료 → 다시 `python3.11 main.py`

```
📂 저장된 데이터를 불러왔습니다. (퀴즈 6개, 최고점수 100점)
```
→ 메뉴 `3`에서 추가한 퀴즈가 남아 있고, 메뉴 `5`에서 최고 점수가 유지된 것을 확인합니다.

**📂 코드 위치** — 불러오기 [`load_state()`](main.py#L119-L138) / 저장 [`save_state()`](main.py#L140-L151)

### 🎬 시연 대본 (1-3) — 키보드 한 번씩 그대로

> 아래는 **실제로 돌려서 받아 적은 화면**입니다. 그대로 따라 하면 됩니다.
> 왼쪽은 **내가 누르는 것**, 가운데는 **화면에 뜨는 것**, 오른쪽은 **이때 말할 것**입니다.

**0단계 — 터미널 열기** (시연 전에 미리 해두세요)

| 순서 | 하는 일 |
| --- | --- |
| 1 | VSCode 상단 메뉴 → `Terminal` → `New Terminal` (단축키 `Ctrl` + `` ` ``) |
| 2 | 아래에 검은 창이 뜨고 커서가 깜빡임 |
| 3 | `cd /Users/ellysuh/Documents/2-homework` 입력 후 Enter |
| 4 | `clear` 입력 후 Enter — **화면을 깨끗이** (지저분하면 신뢰가 떨어집니다) |

---

**1단계 — 프로그램 실행**

| 내가 누르는 것 | 화면에 뜨는 것 |
| --- | --- |
| `python3.11 main.py` + Enter | 아래 화면 |

```
📂 저장된 데이터를 불러왔습니다. (퀴즈 5개, 최고점수 100점)

========================================
        🎯 나만의 퀴즈 게임 🎯
========================================
1. 퀴즈 풀기
2. 퀴즈 추가
...
6. 종료
========================================
선택:
```

> 🗣️ **"맨 윗줄을 봐주세요.** 프로그램이 시작하자마자 `state.json`을 읽어서
> **퀴즈 5개, 최고점수 100점**을 불러왔다고 알려줍니다. 이게 [`load_state()`](main.py#L119-L138)입니다.
> 지금 **퀴즈가 5개**라는 걸 기억해 주세요."

---

**2단계 — 퀴즈를 1개 추가** (`선택:` 뒤에 입력)

| 내가 누르는 것 | 화면에 뜨는 것 |
| --- | --- |
| `2` + Enter | `📌 새로운 퀴즈를 추가합니다.` |
| | `문제를 입력하세요:` |
| `파이썬에서 문자열을 만들 때 쓰는 기호는?` + Enter | `선택지 1:` |
| `대괄호` + Enter | `선택지 2:` |
| `따옴표` + Enter | `선택지 3:` |
| `중괄호` + Enter | `선택지 4:` |
| `소괄호` + Enter | `정답 번호 (1-4):` |
| `2` + Enter | `✅ 퀴즈가 추가되었습니다!` → **메뉴 다시 등장** |

> 🗣️ **"추가하자마자 저장됩니다.** 종료할 때가 아니라 **바뀌는 순간** 파일에 씁니다.
> [`add_quiz()`의 마지막 줄](main.py#L194)이 `save_state()`라서 저장을 깜빡할 수가 없는 구조입니다."

---

**3단계 — 파일에 진짜 적혔는지 눈으로 확인** ← **이 단계가 설득력을 만듭니다**

| 내가 누르는 것 | 화면에 뜨는 것 |
| --- | --- |
| `6` + Enter | `👋 게임을 종료합니다. 안녕히 가세요!` (프로그램 종료) |
| `tail -20 state.json` + Enter | 방금 넣은 문제의 **글자가 파일 안에** 보임 |

> 🗣️ **"프로그램이 아니라 파일을 직접 열어본 겁니다.** 방금 만든 문제가
> `state.json` 안에 글자로 남아 있습니다. **메모리가 아니라 디스크에 적혔다**는 증거입니다."

---

**4단계 — 다시 실행해서 남아 있는지 확인** ← **여기가 이 문항의 정답**

| 내가 누르는 것 | 화면에 뜨는 것 |
| --- | --- |
| `python3.11 main.py` + Enter | `📂 저장된 데이터를 불러왔습니다. (퀴즈 6개, 최고점수 100점)` |

> 🗣️ **"아까는 5개였는데 지금은 6개입니다.** 프로그램을 완전히 껐다 켰는데도
> 추가한 퀴즈가 살아 있습니다. 이게 **데이터 영속성**입니다."

---

**5단계 — 목록과 점수로 최종 확인**

| 내가 누르는 것 | 화면에 뜨는 것 |
| --- | --- |
| `3` + Enter | `📋 등록된 퀴즈 목록 (총 6개)` + `[6] 파이썬에서 문자열을...` ← **추가한 것이 맨 아래** |
| `5` + Enter | `🏆 최고 점수: 100점` + `📜 최근 기록` |
| `6` + Enter | `👋 게임을 종료합니다. 안녕히 가세요!` |

> 🗣️ **"[6]번이 방금 추가한 문제입니다.** 최고 점수 100점도 그대로 유지됐습니다.
> 퀴즈·최고점수·게임기록 **세 가지가 모두** `state.json`에 함께 저장되기 때문입니다."

---

**⚠️ 시연 전에 꼭 확인할 것 3가지**

| # | 확인 | 왜 |
| --- | --- | --- |
| 1 | `python3` 말고 **`python3.11`** 로 실행 | 기본 `python3`는 3.9.6이라 과제 요구(3.10 이상) 미달 |
| 2 | 실행 전 `clear`로 화면 정리 | 이전 출력이 섞이면 어디가 결과인지 안 보임 |
| 3 | 1단계의 **"퀴즈 5개"를 소리내어 짚기** | 4단계의 "6개"와 대비되어야 증명이 됩니다 |

> 💡 **핵심은 "숫자가 5 → 6으로 바뀐 것"** 하나입니다. 나머지 설명은 그 숫자를 뒷받침하는 것뿐이니,
> 긴장되면 **1단계의 5개와 4단계의 6개만** 짚어도 이 문항은 통과합니다.

### 🎓 학습 노트 (1-3)

**📖 질문의 요지** — **"프로그램을 껐다 켜도 기억하는가?"** = 데이터 영속성(persistence)

**🎯 왜 이걸 묻는가**

초보가 가장 늦게 깨닫는 사실이 있습니다 — **변수에 담은 값은 프로그램이 꺼지면 전부 사라집니다.**

| | 메모리(변수) | 파일(state.json) |
| --- | --- | --- |
| 비유 | **머릿속 암기** | **공책에 적어두기** |
| 속도 | 매우 빠름 | 느림 |
| 전원 끄면 | **사라짐** | 남아 있음 |

퀴즈를 100개 추가해도 저장하지 않으면 껐다 켤 때 0개입니다.
평가자는 이 차이를 이해했는지, 그리고 **저장/불러오기를 실제로 연결했는지** 봅니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 | 코드 위치 |
| --- | --- | --- |
| **데이터 영속성** | 프로그램을 꺼도 데이터가 남는 성질 | 전체 설계 |
| **`open()`** | 파일 열기. `"r"`=읽기, `"w"`=쓰기 | [126](main.py#L126), [148](main.py#L148) |
| **`with` 문** | 다 쓰면 **자동으로 파일을 닫아줌** (닫기 깜빡함 방지) | [main.py:126](main.py#L126) |
| **`json.dump()` / `json.load()`** | 파이썬 값 ↔ 파일 글자 변환 | [149](main.py#L149), [127](main.py#L127) |
| **`encoding="utf-8"`** | 한글이 안 깨지게 하는 글자 저장 방식 | [126](main.py#L126), [148](main.py#L148) |

**💻 코드를 이렇게 짰습니다**

```python
# 저장 — 메모리의 값을 파일 글자로 바꿔 씀
def save_state(self):
    data = {
        "quizzes": [quiz.to_dict() for quiz in self.quizzes],  # 객체 → 딕셔너리
        "best_score": self.best_score,
        "history": self.history,
    }
    with open(STATE_FILE, "w", encoding="utf-8") as file:   # 쓰기 모드로 열기
        json.dump(data, file, ensure_ascii=False, indent=2)  # 파일에 쓰기

# 불러오기 — 파일 글자를 다시 메모리 값으로
with open(STATE_FILE, "r", encoding="utf-8") as file:
    data = json.load(file)
self.quizzes = [Quiz.from_dict(item) for item in data["quizzes"]]  # 딕셔너리 → 객체
```

**설계 포인트 3가지**
1. **저장과 불러오기가 정확히 짝**입니다. `to_dict()`로 나가고 `from_dict()`로 들어옵니다. 짝이 안 맞으면 저장은 되는데 못 읽는 사태가 납니다.
2. **파일 위치를 고정**했습니다. [`STATE_FILE`](main.py#L15)이 `main.py`와 같은 폴더를 가리켜서, 어느 폴더에서 실행해도 같은 파일을 씁니다.
3. **저장 시점을 "바뀔 때마다"로** 잡았습니다 (자세히는 [2-3번](#q2-3)) — 종료할 때 한 번만 저장하면 갑자기 꺼졌을 때 다 잃습니다.

**🔍 검증 방법** — "껐다 켜도 남아 있다"를 3단계로 보여줍니다.

```bash
# ① 퀴즈를 추가하고 종료
python3.11 main.py     # 메뉴에서 2 → 문제·보기4개·정답 입력 → 6으로 종료

# ② 파일에 진짜 적혔는지 눈으로 확인 (← 이 단계가 설득력을 만듭니다)
tail -20 state.json

# ③ 다시 실행
python3.11 main.py
```

| 단계 | 나와야 하는 결과 |
| --- | --- |
| ② | 방금 추가한 문제의 글자가 `state.json` 안에 보임 |
| ③ | `📂 저장된 데이터를 불러왔습니다. (퀴즈 6개, 최고점수 100점)` |
| ③ 후 `3` 입력 | 추가한 퀴즈가 **목록에 남아 있음** |
| ③ 후 `5` 입력 | 최고 점수가 **유지됨** |

**🔗 이동** — [↑ 목차](#toc) · [◀ 1-2](#q1-2) · [다음 1-4 ▶](#q1-4) · 관련: [2-3 저장 흐름](#q2-3) · [3-2 왜 JSON](#q3-2)


---

<a id="q1-4"></a>

## 1-4. 기본 퀴즈가 5개 이상 포함되어 있는가?

> **🎯 이 문항의 핵심** — **"처음 켰을 때 빈 화면이 아닌가?"**
> *퀴즈 0개면 시연도 테스트도 불가능.*

**✅ 답변** — 네, **파이썬 기초 문법** 주제로 직접 작성한 기본 퀴즈 5개가 포함되어 있습니다.

**🖥️ 시연** — `state.json`을 지운 뒤 실행

```
📂 저장된 데이터가 없어 기본 퀴즈로 시작합니다. (퀴즈 5개)
```
→ 메뉴 `3`으로 5개 확인.

**주제 선정 이유** — 이번 미션에서 직접 배우고 쓴 개념(리스트, 불린, `__init__`, 딕셔너리, 반복문)을
그대로 문제로 만들었습니다. **프로그램을 완성하는 과정 자체가 복습**이 되도록 골랐습니다.

**📂 코드 위치** — [`default_quizzes()`](main.py#L61-L94)

### 🎓 학습 노트 (1-4)

**📖 질문의 요지** — **"처음 켰을 때 빈 화면이 아닌가?"** = 기본값(default) 설계

**🎯 왜 이걸 묻는가**

퀴즈가 0개인 퀴즈 게임은 **시연도 테스트도 불가능**합니다.
평가자가 clone 받아 처음 실행했을 때 바로 놀 수 있어야 합니다.

| 확인하는 것 | 왜 |
| --- | --- |
| 첫 실행에 바로 5개가 있는가 | 사용자가 뭘 준비할 필요 없이 즉시 쓸 수 있는가 |
| 문제 내용이 성의 있는가 | 복붙·아무 말이 아니라 직접 만들었는가 |
| `state.json`을 지워도 되살아나는가 | 기본값이 **코드 안**에 있는가 (파일에만 있으면 지우면 끝) |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 | 이 프로그램에서 |
| --- | --- | --- |
| **기본값(default)** | 아무것도 없을 때 쓰는 미리 정해둔 값 | 퀴즈 5개 |
| **객체 리스트** | 리스트 안에 객체(붕어빵)를 여러 개 담기 | `[Quiz(...), Quiz(...)]` |
| **`return`으로 목록 돌려주기** | 함수가 값을 만들어 넘겨줌 | `default_quizzes()` |
| **기본 매개변수** | `hint=""` — 안 주면 빈 문자열 | [main.py:21](main.py#L21) |

**💻 코드를 이렇게 짰습니다**

```python
def default_quizzes():
    """저장 파일이 없을 때 사용할 기본 퀴즈 5개를 만들어 돌려준다."""
    return [
        Quiz(
            "파이썬에서 리스트를 만들 때 사용하는 괄호는?",   # 문제
            ["( )", "[ ]", "{ }", "< >"],                    # 보기 4개
            2,                                               # 정답 = 2번
            "순서가 있고 나중에 값을 바꿀 수 있는 자료형에 쓰는 기호입니다.",  # 힌트
        ),
        ... 총 5개
    ]
```

**설계 포인트 3가지**
1. **변수가 아니라 함수로 만들었습니다.** 부를 때마다 **새 객체**를 만들어야 하기 때문입니다. 변수 하나를 공유하면 한 곳에서 퀴즈를 지웠을 때 다른 곳의 목록까지 같이 사라집니다. → 아래 상세 설명
2. **두 곳에서 씁니다** — 프로그램 시작 시([main.py:101](main.py#L101))와 파일이 깨졌을 때([main.py:133](main.py#L133)). 복구 경로가 자동으로 확보됩니다.
3. **문제 주제를 "이번에 배운 문법"으로** 골랐습니다 — 리스트·불린·`__init__`·딕셔너리·반복문. 프로그램을 만드는 과정 자체가 복습이 되게 한 의도입니다.

> 🔬 **"변수가 아니라 함수로 만든 이유" 상세 설명** — 이건 실수하기 정말 쉬운 함정입니다.
>
> **먼저 알아야 할 것 — 리스트는 "복사"가 아니라 "가리키기"입니다.**
>
> ```python
> 게임1 = DEFAULT      # DEFAULT를 복사한 게 아니라
> 게임2 = DEFAULT      # 셋 다 "같은 하나"를 가리킴
> ```
>
> **왜 그런가** — 퀴즈 1000개짜리 목록을 대입할 때마다 통째로 복사하면 메모리도 시간도 낭비입니다.
> 그래서 파이썬은 리스트·딕셔너리 같은 **큰 것**은 복사하지 않고 **주소만 알려줍니다.**
> 집 주소를 적어준 것과 같아서, 한 사람이 그 집 가구를 옮기면 **다른 사람이 가도 옮겨져 있습니다.**
>
> **① 변수로 만들었다면 (나쁜 방식)** — 실제로 돌려본 결과입니다.
>
> ```python
> DEFAULT = default_quizzes()   # 변수 하나에 담아두고 계속 씀
> 게임1 = DEFAULT
> 게임2 = DEFAULT
> 게임1.pop(0)                  # 게임1에서만 1개 삭제했는데...
> ```
> ```
> 시작:        게임1 5개 / 게임2 5개
> 게임1만 삭제: 게임1 4개 / 게임2 4개   ← 건드리지도 않은 게임2가 줄었다!
> 둘이 같은 것인가? True
> ```
>
> **② 함수로 만들면 (현재 방식)**
>
> ```python
> 게임3 = default_quizzes()     # 부를 때마다 return이 새 리스트를 만듦
> 게임4 = default_quizzes()
> 게임3.pop(0)
> ```
> ```
> 시작:        게임3 5개 / 게임4 5개
> 게임3만 삭제: 게임3 4개 / 게임4 5개   ← 게임4는 그대로
> 둘이 같은 것인가? False
> ```
>
> **③ 이 프로그램에서 실제로 터졌을 지점**
>
> [`default_quizzes()`](main.py#L61-L94)는 **두 곳**에서 불립니다.
>
> | 위치 | 언제 |
> | --- | --- |
> | [main.py:101](main.py#L101) | 프로그램 **시작**할 때 |
> | [main.py:133](main.py#L133) | 저장 파일이 **깨져서 복구**할 때 |
>
> 변수 방식이었다면 이런 일이 일어납니다.
>
> ```
> ① 프로그램 시작        → 퀴즈 5개
> ② 사용자가 4번으로 2개 삭제 → 퀴즈 3개
> ③ state.json이 깨져서 복구  → 변수 방식: 3개 (지운 게 안 돌아옴) ❌
>                              함수 방식: 5개 (온전히 복구)      ✅
> ```
>
> **"기본 퀴즈로 복구합니다"라고 해놓고 기본 퀴즈가 아닌 상태로 복구**되는 셈입니다.
> 게다가 이런 버그는 **에러가 안 나서** 원인을 찾기가 매우 어렵습니다.
>
> **한 문장으로** — `return [...]` 은 **부를 때마다 새로 만드는** 명령이라,
> 함수로 두면 누가 몇 번을 부르든 **항상 깨끗한 5개**가 나옵니다.
>
> > 💡 **직접 확인해 보기**
> > ```bash
> > python3.11 -c "
> > from main import default_quizzes
> > a = default_quizzes(); b = default_quizzes()
> > a.pop()
> > print('a:', len(a), '개 / b:', len(b), '개 / 같은 것?', a is b)"
> > ```
> > → `a: 4 개 / b: 5 개 / 같은 것? False`
>
> **❓ 예상 추가 질문** — *"그럼 상수는 다 함수로 만들어야 하나요?"*
> → 아닙니다. **바뀔 수 있는 것**(리스트·딕셔너리)만 그렇습니다.
> [`HINT_PENALTY = 5`](main.py#L12) 같은 숫자·글자는 **바뀌지 않는 값**이라 변수로 둬도 안전합니다.
> 기준은 **"누가 이걸 고칠 수 있나?"** 입니다.

**🔍 검증 방법** — ⚠️ **파일을 지우지 말고 옮기세요.** 실습 후 되돌릴 수 있습니다.

```bash
mv state.json state.json.bak    # ① 저장 파일을 잠시 치움 (삭제 아님)
python3.11 main.py              # ② 실행 → 메뉴에서 3 입력
# ...확인 후 6으로 종료...
mv state.json.bak state.json    # ③ 원래대로 되돌리기 ← 잊지 마세요
```

| 단계 | 나와야 하는 결과 |
| --- | --- |
| ② 실행 직후 | `📂 저장된 데이터가 없어 기본 퀴즈로 시작합니다. (퀴즈 5개)` |
| ② `3` 입력 | `📋 등록된 퀴즈 목록 (총 5개)` + 문제 5줄 |

```bash
grep -c "        Quiz(" main.py    # → 5  (기본 퀴즈 객체 5개)
```

**🔗 이동** — [↑ 목차](#toc) · [◀ 1-3](#q1-3) · [다음 1-5 ▶](#q1-5) · 관련: [3-1 클래스를 쓴 이유](#q3-1)


---

<a id="q1-5"></a>

## 1-5. GitHub 저장소에 코드가 업로드되어 있고, 10개 이상의 의미 있는 커밋이 존재하는가?

> **🎯 이 문항의 핵심** — **"결과물만 냈는가, 만들어 온 과정을 남겼는가?"**
> *커밋 1개면 어디서 망가졌는지 되돌릴 수 없음.*

**✅ 답변** — 네, [GitHub 저장소](https://github.com/ellysuh22/2-homework)에 업로드했고 커밋은 **총 18개**입니다.

**🖥️ 시연**

```bash
git log --oneline
git rev-list --count HEAD      # → 18
```

**"의미 있는" 커밋인 이유** — `기능 하나 완성 = 커밋 1개` 원칙으로 나누고,
`Feat:` `Fix:` `Docs:` `Refactor:` `Chore:` 접두어로 무슨 작업인지 드러나게 썼습니다.
전체 목록은 [README 커밋표](README.md#전체-커밋-이력)에서 GitHub 링크로 확인할 수 있습니다.

### 🎓 학습 노트 (1-5)

**📖 질문의 요지** — **"결과물만 냈는가, 만들어 온 과정을 남겼는가?"**

**🎯 왜 이걸 묻는가**

완성된 코드를 한 번에 올리면 커밋은 1개입니다. 돌아가긴 하지만 —

| 커밋 1개일 때 | 커밋 18개일 때 |
| --- | --- |
| 어디서 망가졌는지 **알 수 없음** | 어느 커밋부터 문제인지 **좁힐 수 있음** |
| 되돌릴 지점이 **없음** | 원하는 시점으로 **되돌아감** |
| 남이 봐도 **순서를 모름** | `git log`가 **개발 일기**가 됨 |

"10개 이상"이라는 숫자는 **작게 나눠 자주 저장하는 습관**이 있는지 보는 기준입니다.
숫자만 채우려고 의미 없이 쪼갠 커밋(`수정`, `수정2`)은 오히려 감점 요인입니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **커밋(commit)** | 게임의 **세이브 포인트**. 그 시점을 이력으로 저장 |
| **`git add`** | 저장할 파일을 **장바구니에 담기** |
| **`git commit -m "메시지"`** | 담은 것을 **이름 붙여 저장** |
| **`git push`** | 내 컴퓨터 저장분을 **GitHub에 올리기** |
| **원자적 커밋** | 커밋 1개 = **의미 있는 변경 1개** |

**💻 이렇게 했습니다** (코드가 아니라 명령입니다)

```bash
git add main.py                                # ① 바뀐 파일 담기
git commit -m "Feat: 퀴즈 풀기 기능 구현"        # ② 이름 붙여 저장
git push origin main                           # ③ GitHub에 올리기

git log --oneline          # 확인: 커밋 목록
git rev-list --count HEAD  # 확인: 커밋 개수 → 18
```

**핵심 원칙 3가지**
1. **기능 하나 완성 = 커밋 1개.** "메뉴 만들기"와 "저장 기능"을 한 커밋에 섞지 않았습니다.
2. **접두어로 종류 표시.** `Feat:`(새 기능) `Fix:`(버그) `Docs:`(문서) `Refactor:`(구조 정리) `Chore:`(잡무)
3. **돌아가는 상태에서 커밋.** 깨진 코드를 커밋하면 그 지점으로 되돌릴 수 없습니다.

**🔍 검증 방법**

```bash
git rev-list --count HEAD    # ① 커밋 개수 → 18  (요구 기준 10개 이상)
git log --oneline            # ② 목록 — 접두어가 전부 붙어 있는지 보여줌
git remote -v                # ③ GitHub에 연결되어 있는지
```

| 명령 | 나와야 하는 결과 |
| --- | --- |
| ① | `18` |
| ② | `Feat:` `Fix:` `Docs:` `Refactor:` `Chore:`로 시작하는 18줄 |
| ③ | `origin  https://github.com/ellysuh22/2-homework.git` |

**웹에서도 확인** — [GitHub 커밋 목록](https://github.com/ellysuh22/2-homework/commits/main) /
[README 커밋표 전체](README.md#전체-커밋-이력)

**🔗 이동** — [↑ 목차](#toc) · [◀ 1-4](#q1-4) · [다음 1-6 ▶](#q1-6) · 관련: [2-5 커밋 규칙](#q2-5)


---

<a id="q1-6"></a>

## 1-6. 브랜치 생성 및 병합 기록이 git log --oneline --graph 에서 확인되는가?

> **🎯 이 문항의 핵심** — **"원본을 지키면서 실험할 줄 아는가?"**
> *팀에서는 브랜치 없이 일할 수 없음.*

**✅ 답변** — 네, `feature/play-quiz` 브랜치를 만들어 작업한 뒤 `main`에 병합한 기록이 남아 있습니다.

**🖥️ 시연**

```bash
git log --oneline --graph --all --decorate
```

```
*   2d45928 Merge: 퀴즈 풀기 기능을 main에 병합
|\
| * ec2775c (feature/play-quiz) Feat: 랜덤 출제, 문제 수 선택, 힌트 기능 추가 (보너스)
| * b432e80 Feat: 퀴즈 풀기 기능 구현
|/
* ff3840e Feat: state.json 저장/불러오기 구현
```

선이 **갈라졌다가**(`|\`) 다시 **합쳐지는**(`|/`) 부분을 가리키면 됩니다.
GitHub에서는 [병합 커밋 `2d45928`](https://github.com/ellysuh22/2-homework/commit/2d45928)의
부모가 2개인 것으로도 확인됩니다.

### 🎓 학습 노트 (1-6)

**📖 질문의 요지** — **"원본을 안전하게 지키면서 실험할 줄 아는가?"**

**🎯 왜 이걸 묻는가**

브랜치는 **혼자 작업할 땐 없어도 되지만, 팀에서는 없으면 안 되는** 기능입니다.
평가자는 취업·협업을 전제로 "팀에 들어와도 되는 사람인가"를 봅니다.

| 브랜치 없이 | 브랜치 쓰면 |
| --- | --- |
| 새 기능 만들다 망가지면 **main도 같이 망가짐** | main은 **항상 멀쩡** |
| 여러 명이 동시에 작업 **불가** | 각자 옆 책상에서 따로 작업 |
| 실패하면 손으로 되돌려야 함 | 브랜치를 **버리면 끝** |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **브랜치(branch)** | 원본은 두고 실험하는 **옆 책상** |
| **`main`** | 항상 잘 돌아가야 하는 **원본 책상** |
| **병합(merge)** | 옆 책상 결과를 **원본에 합치기** |
| **머지 커밋** | 합친 기록. **부모가 2개**인 특별한 커밋 |
| **충돌(conflict)** | 양쪽이 같은 줄을 고쳐 Git이 못 고르는 상황 |

**💻 이렇게 했습니다**

```bash
git switch -c feature/play-quiz    # ① 옆 책상(브랜치) 만들고 이동
# ... 퀴즈 풀기 기능 작업, 커밋 2개 ...
git switch main                    # ② 원본으로 복귀
git merge feature/play-quiz        # ③ 결과를 원본에 합침

git log --oneline --graph --all --decorate   # 확인: 선이 갈라졌다 합쳐짐
```

```
*   2d45928 Merge: 퀴즈 풀기 기능을 main에 병합    ← 합쳐진 지점 (부모 2개)
|\
| * ec2775c (feature/play-quiz) Feat: 랜덤 출제...  ← 옆 책상에서 한 작업
| * b432e80 Feat: 퀴즈 풀기 기능 구현
|/                                                  ← 갈라진 지점
* ff3840e Feat: state.json 저장/불러오기 구현
```

**포인트 3가지**
1. **가장 복잡한 기능을 골라 분리**했습니다. 퀴즈 풀기는 출제·힌트·채점·점수가 얽혀 실패 위험이 가장 컸습니다.
2. **브랜치 이름에 규칙**을 줬습니다 — `feature/기능이름`. 이름만 봐도 목적이 보입니다.
3. **`--graph`로 눈에 보이게** 남겼습니다. 그림의 갈라짐(`|\`)과 합쳐짐(`|/`)이 그대로 증거입니다.

**🔍 검증 방법**

```bash
git log --oneline --graph --all --decorate   # ① 그림으로 보기
git log --merges --oneline                   # ② 병합 커밋만 뽑기
git show --stat 2d45928 | head -5            # ③ 부모가 2개인지 확인
```

| 명령 | 나와야 하는 결과 |
| --- | --- |
| ① | 선이 `|\`로 갈라졌다 `|/`로 합쳐지는 그림 |
| ② | `2d45928 Merge: 퀴즈 풀기 기능을 main에 병합` **1줄** |
| ③ | `Merge: ff3840e ec2775c` ← **부모 2개 = 병합의 증거** |

> 💡 평가자에게는 **②·③을 보여주는 게 가장 빠릅니다.** 그래프는 눈으로 찾아야 하지만,
> "부모가 2개"는 반박할 수 없는 사실이기 때문입니다.

**🔗 이동** — [↑ 목차](#toc) · [◀ 1-5](#q1-5) · [다음 1-7 ▶](#q1-7) · 관련: [3-4 브랜치를 쓴 이유](#q3-4)


---

<a id="q1-7"></a>

## 1-7. clone과 pull 실습 수행 흔적을 README 또는 스크린샷으로 확인할 수 있는가?

> **🎯 이 문항의 핵심** — **"내 컴퓨터와 GitHub 사이 양방향 흐름을 아는가?"**
> *혼자면 push만 쓰지만 팀에서는 pull이 더 자주 필요.*

**⚠️ 답변** — README에는 흔적이 있으나, **터미널 스크린샷은 아직 첨부하지 않았습니다.**

**현재 있는 증거**

| 증거 | 위치 |
| --- | --- |
| clone 실습 후 남긴 커밋 | [`3bf2517`](https://github.com/ellysuh22/2-homework/commit/3bf2517) "Docs: clone 실습으로 README에 동기화 안내 문구 추가" |
| README 동기화 문구 | [README 맨 아래](README.md#동기화-기록) |

**수행한 절차** — ① 저장소를 별도 폴더에 `git clone` → ② 그쪽에서 README 한 줄 추가 후 `commit`·`push`
→ ③ 원래 작업 폴더에서 `git pull` → ④ 반영 확인.

> ⚠️ **평가 전 반드시 할 일** — 커밋 이력만으로는 그 커밋이 clone된 폴더에서 만들어졌는지 구분되지 않습니다.
> `git clone` / `git push` / `git pull` 실행 터미널을 캡처해 **`docs/screenshots/clone_pull.png`** 로 추가하세요.

### 🎓 학습 노트 (1-7)

**📖 질문의 요지** — **"내 컴퓨터와 GitHub 사이에서 코드가 오가는 흐름을 아는가?"**

**🎯 왜 이걸 묻는가**

혼자 작업하면 `commit` → `push` 한 방향만 씁니다. 하지만 팀에서는 **남이 올린 걸 받아오는**
`pull`이 훨씬 자주 필요합니다. `pull`을 안 하고 작업하면 충돌이 쌓입니다.

| 명령 | 방향 | 언제 |
| --- | --- | --- |
| **clone** | GitHub → 내 컴퓨터 (**통째로 새로**) | 처음 받을 때 딱 1번 |
| **push** | 내 컴퓨터 → GitHub | 내 작업을 올릴 때 |
| **pull** | GitHub → 내 컴퓨터 (**바뀐 것만**) | 남이 올린 걸 받을 때 |

> 헷갈리기 쉬운 점: **clone은 폴더를 새로 만들고, pull은 있는 폴더를 갱신**합니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **로컬 / 원격** | 내 컴퓨터 / GitHub 서버 |
| **`origin`** | 내가 연결한 원격 저장소의 **기본 별명** |
| **clone** | 저장소를 통째로 복사해 새 폴더 생성 |
| **pull** | 원격의 새 커밋을 가져와 **내 것과 합침**(fetch + merge) |

**💻 이렇게 했습니다**

```bash
# ① 다른 폴더에 통째로 복사 (= 다른 사람이 내 코드를 받는 상황 재현)
cd ~/Desktop && git clone https://github.com/ellysuh22/2-homework.git

# ② 복사본에서 README 수정 후 올리기
cd 2-homework && git commit -am "Docs: clone 실습..." && git push

# ③ 원래 작업 폴더로 돌아와 변경분 받아오기
cd ~/Documents/2-homework && git pull
```

**⚠️ 이 문항의 현재 상태** — 위 절차는 실제로 수행했고 커밋([`3bf2517`](https://github.com/ellysuh22/2-homework/commit/3bf2517))도 남았지만,
**커밋만으로는 "clone된 폴더에서 만들었다"는 증거가 안 됩니다.** 그래서 터미널 스크린샷이 필요합니다.
이 문항은 **코드 실력이 아니라 증거 제출의 문제**입니다 — 캡처 하나로 해결됩니다.

**🔍 검증 방법** — ⚠️ **현재 이 문항만 증거가 부족합니다.** 아래 ①을 반드시 먼저 하세요.

**① 지금 해야 할 일 (스크린샷 촬영)**

```bash
cd ~/Desktop
git clone https://github.com/ellysuh22/2-homework.git clone-test   # ← 이 화면 캡처
cd clone-test && git log --oneline | head -3
cd ~/Documents/2-homework && git pull                              # ← 이 화면도 캡처
```
→ 두 화면을 합쳐 **`docs/screenshots/clone_pull.png`** 로 저장 후 커밋·푸시

**② 이미 있는 증거**

| 명령 | 나와야 하는 결과 |
| --- | --- |
| `git log --oneline -- README.md` | `3bf2517 Docs: clone 실습으로 README에 동기화 안내 문구 추가` |
| README 맨 아래 확인 | [동기화 기록](README.md#동기화-기록) 문구 |

**🔗 이동** — [↑ 목차](#toc) · [◀ 1-6](#q1-6) · [다음 2-1 ▶](#q2-1) · 관련: [부록 D 준비물](#appendix-d)


---

> **[항목 1] (PASS / FAIL)** — 1-1 ~ 1-6은 즉시 시연 가능, 1-7만 스크린샷 보완 필요

---

# [항목 2] 코드 구조 및 설계

<a id="q2-1"></a>

## 2-1. Quiz와 QuizGame 등 클래스들의 책임을 어떻게 나눴는지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"클래스를 왜 2개로 나눴는지 말할 수 있는가?"**
> *문법을 쓸 줄 아는 것과 왜 쓰는지 아는 것은 다름.*

**✅ 한 줄 답변** — `Quiz`는 "문제 카드 한 장", `QuizGame`은 "사회자"입니다.

**🗣️ 이렇게 말하세요**

> "`Quiz`는 자기 문제·보기 4개·정답·힌트만 알고, 자기를 보여주는 `display()`와 자기를 채점하는
> `check()`만 합니다. `QuizGame`은 메뉴를 띄우고, 카드를 꺼내 출제하고, 점수를 계산하고,
> 공책(`state.json`)에 기록하는 진행 전체를 맡습니다.
> 나눈 기준은 **'바뀌는 이유가 다르면 클래스도 다르다'** 입니다. 문제의 생김새가 바뀌는 일과
> 게임 규칙이 바뀌는 일은 서로 다른 사건이니까요."

| | `Quiz` | `QuizGame` |
| --- | --- | --- |
| 비유 | 문제 카드 한 장 | 게임을 진행하는 사회자 |
| 아는 것 | 자기 문제·보기·정답·힌트만 | 카드 전부 + 점수 + 저장 파일 |
| 하는 일 | 자기 출력, 자기 채점 | 메뉴 진행, 출제, 점수 계산, 저장/불러오기 |
| 모르는 것 | 점수 규칙, 메뉴, 파일 존재 자체 | 개별 문제의 내부 형식 |

**실제 효과** — `Quiz`가 자기 채점을 책임지므로 `QuizGame`은 정답이 몇 번인지 몰라도
`quiz.check(입력값)` 한 줄로 채점합니다. 힌트를 추가할 때도 `Quiz`에 속성 하나 더한 것으로 끝났습니다.

**📂 코드 위치** — [`Quiz`](main.py#L18-L58) · [`QuizGame`](main.py#L97-L326)

**❓ 예상 추가 질문** — *"그럼 `Quiz`에 점수 계산을 넣지 않은 이유는?"*
→ 점수 규칙은 문제 한 장의 성질이 아니라 **게임 전체의 규칙**입니다. 힌트 감점이나 만점 기준이
바뀌어도 문제 카드는 그대로여야 하므로 `QuizGame` 쪽에 두었습니다.

### 🎓 학습 노트 (2-1)

**📖 질문의 요지** — "클래스 문법을 쓸 줄 아는가"가 아니라 **"왜 2개로 나눴는지 설명할 수 있는가"** 입니다.

**🎯 왜 이걸 묻는가**

클래스는 **문법을 외우는 것**과 **왜 쓰는지 아는 것**이 완전히 다릅니다.
초보는 보통 클래스 하나에 전부 몰아넣습니다. 그러면 —

| 클래스 1개에 다 넣으면 | 역할대로 나누면 |
| --- | --- |
| 300줄짜리 덩어리 → 어디를 고칠지 모름 | 고칠 위치가 **바로 특정됨** |
| 문제 형식만 바꿔도 게임 코드가 딸려 나옴 | 서로 **영향 없음** |
| 남이 읽어도 구조가 안 보임 | 이름만 봐도 역할이 보임 |

평가자가 진짜 듣고 싶은 것은 **"나눈 기준"** 입니다. "그냥 나눴습니다"는 답이 아닙니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 | 이 프로그램에서 |
| --- | --- | --- |
| **클래스** | 붕어빵 **틀** (설계도) | `Quiz`, `QuizGame` |
| **객체/인스턴스** | 틀로 찍은 **붕어빵 하나** | 퀴즈 5개 = 객체 5개 |
| **속성(attribute)** | 객체가 **가진 정보** | `self.question`, `self.answer` |
| **메서드(method)** | 객체가 **할 수 있는 행동** | `display()`, `check()` |
| **단일 책임 원칙** | 클래스 하나는 **한 가지 이유로만** 바뀌어야 함 | 나눈 기준 그 자체 |

**💻 코드를 이렇게 짰습니다**

```python
class Quiz:                       # "문제 카드 한 장"
    def __init__(self, question, choices, answer, hint=""):
        self.question = question  # 자기 문제만 알고 있음
        self.answer = answer

    def display(self, number):    # 자기를 보여주기
        ...
    def check(self, selected):    # 자기를 채점하기
        return selected == self.answer    # ← 정답을 아는 건 자기 자신뿐

class QuizGame:                   # "사회자"
    def __init__(self):
        self.quizzes = default_quizzes()  # 카드 전부를 들고 있음
        self.best_score = 0               # 점수 규칙은 사회자의 몫
```

**설계 포인트 3가지**
1. **나눈 기준은 "바뀌는 이유"** 입니다. 문제 생김새가 바뀌는 일과 게임 규칙이 바뀌는 일은 서로 다른 사건이니 클래스도 다릅니다.
2. **`check()`를 `Quiz`에 둔 것이 핵심**입니다. 덕분에 `QuizGame`은 정답이 몇 번인지 **몰라도** `quiz.check(입력)` 한 줄로 채점합니다.
3. **효과를 실제로 봤습니다** — 힌트 기능을 넣을 때 `Quiz`에 `hint` 속성 하나 추가로 끝났고, `QuizGame`은 거의 손대지 않았습니다.

**🔍 검증 방법** — 설명형 문항이라 **코드를 열어 짚으면서** 말합니다.

```bash
grep -n "^class" main.py           # ① 클래스가 2개뿐임을 보여줌
grep -n "    def " main.py         # ② 각 클래스가 가진 메서드 목록
```

| 명령 | 나와야 하는 결과 | 말할 것 |
| --- | --- | --- |
| ① | `18:class Quiz:` / `97:class QuizGame:` | "18~58행이 문제 카드, 97행부터가 사회자입니다" |
| ② | `Quiz`는 4개, `QuizGame`은 12개 | "카드는 아는 게 적고, 사회자는 할 일이 많습니다" |

**짚어야 할 3곳**

| 위치 | 무엇을 보여주나 |
| --- | --- |
| [`Quiz.check()` 37-39행](main.py#L37-L39) | **정답을 아는 건 카드 자신뿐** |
| [`ask_one()` 240행](main.py#L240) | `QuizGame`은 `quiz.check(selected)` 한 줄로 채점 |
| [`play_quiz()` 270행](main.py#L270) | 점수 계산은 **사회자 쪽에만** 있음 |

**🔗 이동** — [↑ 목차](#toc) · [◀ 1-7](#q1-7) · [다음 2-2 ▶](#q2-2) · 관련: [3-1 클래스 vs 함수](#q3-1) · [4-3 수정 위치](#q4-3)


---

<a id="q2-2"></a>

## 2-2. "입력 처리(검증)", "게임 진행", "데이터 저장/불러오기" 로직을 어떤 기준으로 분리했는지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"함수를 나눈 데 기준이 있는가?"**
> *"길어서 잘랐다"는 결과지 기준이 아님.*

**✅ 한 줄 답변** — 기준은 **"바뀌는 이유가 다르면 함수도 다르다"** 였습니다.

**🗣️ 이렇게 말하세요**

> "① **입력 처리**는 메뉴·정답·문제 수·삭제 번호 등 **6곳 이상**에서 똑같은 검증이 필요해서
> `input_int()` 하나로 만들어 재사용했습니다. 복사해 붙였다면 규칙이 바뀔 때 6군데를 다 고쳐야 합니다.
> ② **게임 진행**은 출제·채점·점수 계산이고,
> ③ **저장**은 파일을 다루는 `load_state()`, `save_state()` 두 개뿐입니다.
> 그래서 저장 방식을 JSON에서 DB로 바꿔도 **게임 진행 코드는 한 줄도 건드릴 필요가 없습니다.**"

| 묶음 | 담당 메서드 | 왜 따로 뺐나 |
| --- | --- | --- |
| 입력 처리(검증) | [`input_int()`](main.py#L153-L172) · [`input_text()`](main.py#L174-L181) | 6곳 이상에서 같은 검증 필요 → 한곳에 모아 한 번만 고침 |
| 게임 진행 | [`play_quiz()`](main.py#L248-L277) · [`ask_one()`](main.py#L223-L246) · [`record_result()`](main.py#L279-L292) | 출제·점수 규칙이 바뀔 때만 수정 |
| 저장/불러오기 | [`load_state()`](main.py#L119-L138) · [`save_state()`](main.py#L140-L151) | 저장 방식이 바뀌어도 여기 2개만 |
| 메뉴 흐름 | [`run()`](main.py#L308-L326) | 메뉴가 늘거나 줄 때만 수정 |

**📂 코드 위치** — 위 표의 링크

**❓ 예상 추가 질문** — *"`play_quiz()`와 `ask_one()`은 왜 또 나눴나요?"*
→ `ask_one()`은 **한 문제**의 출제·힌트·채점, `play_quiz()`는 **한 판 전체**의 진행과 점수 집계입니다.
문제 하나의 흐름과 판 전체의 흐름을 따로 읽고 고칠 수 있게 나눴습니다.

### 🎓 학습 노트 (2-2)

**📖 질문의 요지** — **"함수를 나눈 데 기준이 있었는가, 아니면 그냥 길어져서 잘랐는가?"**

**🎯 왜 이걸 묻는가**

함수를 나누는 이유를 초보는 보통 "길어서"라고 답합니다. 그건 결과지 기준이 아닙니다.
진짜 기준은 **"같이 바뀌는 것끼리 모으고, 따로 바뀌는 것은 떼어놓는다"** 입니다.

| 기준 없이 나누면 | 기준대로 나누면 |
| --- | --- |
| 한 곳 고치면 엉뚱한 데가 깨짐 | 고칠 범위가 **그 함수 안에서 끝남** |
| 같은 코드가 여러 곳에 복사됨 | 한 곳만 고치면 전부 반영 |
| "이건 왜 여기 있지?" | 이름만 봐도 있을 자리가 맞음 |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 | 이 프로그램에서 |
| --- | --- | --- |
| **DRY 원칙** | Don't Repeat Yourself — **같은 코드를 두 번 쓰지 마라** | `input_int()` 하나로 5곳 처리 |
| **관심사 분리** | 하는 일의 종류가 다르면 코드도 분리 | 입력 / 진행 / 저장 |
| **재사용** | 한 번 만든 함수를 여러 곳에서 부름 | 검증 코드는 한 곳뿐 |
| **결합도** | 한 곳을 고칠 때 딸려 오는 정도. **낮을수록 좋음** | 저장 방식 바꿔도 게임 코드 무사 |

> 📖 **함수가 정확히 무엇인지** → [부록 B-0.5 ③ 함수](#terms-basic)

**💻 코드를 이렇게 짰습니다** — 세 묶음으로 나눴습니다.

```python
# ① 입력 처리 — "올바른 값만 통과시킨다"  (5곳에서 재사용)
def input_int(self, prompt, low, high): ...
def input_text(self, prompt): ...

# ② 게임 진행 — "출제하고 채점하고 점수 낸다"
def ask_one(self, quiz, number): ...    # 한 문제
def play_quiz(self): ...                # 한 판 전체
def record_result(self, total, correct, score): ...   # 기록·최고점 갱신

# ③ 저장/불러오기 — "파일을 다룬다"  (이 2개뿐)
def load_state(self): ...
def save_state(self): ...
```

**설계 포인트 3가지**
1. **입력 검증을 왜 뺐나** — 메뉴·정답·문제 수·삭제 번호 등 **5곳 이상**에서 같은 검증이 필요했습니다. 복사했다면 규칙 하나 바꿀 때 5군데를 다 고쳐야 합니다.
2. **저장을 왜 2개로 몰았나** — 나중에 JSON을 DB로 바꿔도 **`load_state()`/`save_state()` 안만** 고치면 됩니다. 게임 진행 코드는 한 줄도 안 바뀝니다.
3. **`play_quiz()`와 `ask_one()`을 또 나눈 이유** — `ask_one()`은 **한 문제**(출제·힌트·채점), `play_quiz()`는 **한 판**(문제 수 선택·반복·점수 집계). 층이 다릅니다.

**🔍 검증 방법** — "재사용하고 있다"를 명령 한 줄로 증명합니다.

```bash
grep -n "input_int(" main.py                 # ① 정의 1곳 + 호출 5곳
grep -n "save_state()\|load_state()" main.py  # ② 저장 관련은 이 2개뿐
```

| 명령 | 나와야 하는 결과 | 이게 증명하는 것 |
| --- | --- | --- |
| ① | `153`(정의) + `191, 216, 229, 256, 312`(호출) | **검증 코드는 한 곳뿐** — 5곳이 불러다 씀 |
| ② | `194, 220, 291, 332, 338` | 파일을 만지는 곳이 **딱 2개 메서드**로 모임 |

**말할 것** — "①에서 정의가 1개인 게 핵심입니다. 규칙이 바뀌면 **153행 하나만** 고치면
5곳이 전부 따라옵니다. 복사해 붙였다면 5군데를 찾아 고쳐야 하고, 하나 빠뜨리면 거기서만 터집니다."

**🔗 이동** — [↑ 목차](#toc) · [◀ 2-1](#q2-1) · [다음 2-3 ▶](#q2-3) · 관련: [1-2 입력 검증](#q1-2) · [4-3 수정 위치](#q4-3)


---

<a id="q2-3"></a>

## 2-3. state.json 읽기/쓰기 흐름이 프로그램의 어디에서, 어떤 순서로 발생하는지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"데이터가 언제 읽히고 언제 쓰이는지 머릿속에 그림이 있는가?"**
> *저장 시점은 정답이 없는 트레이드오프 — 왜 그때인지가 답.*

**✅ 한 줄 답변** — **읽기는 시작할 때 딱 1번, 쓰기는 데이터가 바뀔 때마다**입니다.

**🗣️ 이렇게 말하세요**

> "① 프로그램이 시작되면 `main()`이 `load_state()`를 호출해 파일을 읽고 퀴즈·최고점수·기록을
> 메모리에 올립니다. 파일이 없으면 기본 퀴즈 5개로, 깨졌으면 안내 후 복구합니다.
> ② 실행 중에는 파일을 건드리지 않고 메모리에서만 작업합니다.
> ③ 퀴즈를 추가·삭제한 직후, 한 판이 끝나 점수가 기록될 때, 그리고 Ctrl+C로 중단될 때 저장합니다.
> 매 순간 읽으면 느리고, 끝에서 한 번만 저장하면 갑자기 꺼졌을 때 다 잃기 때문에 그 중간을 택했습니다."

| 순서 | 시점 | 동작 | 코드 |
| --- | --- | --- | --- |
| ① | 프로그램 시작 | `load_state()` — **읽기 1회** | [main.py:332](main.py#L332) |
| ② | 실행 중 | 메모리(변수)에서만 작업 | — |
| ③ | 퀴즈 추가 직후 | `save_state()` | [main.py:194](main.py#L194) |
| ④ | 퀴즈 삭제 직후 | `save_state()` | [main.py:220](main.py#L220) |
| ⑤ | 한 판 종료·점수 기록 | `save_state()` | [main.py:291](main.py#L291) |
| ⑥ | `Ctrl+C` / `EOF` | 안내 후 `save_state()` | [main.py:338](main.py#L338) |

**📂 코드 위치** — 위 표의 링크

**❓ 예상 추가 질문** — *"왜 종료할 때 한 번만 저장하지 않았나요?"*
→ 종료 전에 전원이 꺼지거나 프로세스가 죽으면 그 판의 기록을 통째로 잃습니다.
바뀔 때마다 즉시 쓰면 잃는 범위가 **직전 한 동작**으로 줄어듭니다.

### 🎓 학습 노트 (2-3)

**📖 질문의 요지** — **"데이터가 언제 파일에서 올라오고 언제 파일로 내려가는지, 머릿속에 흐름이 있는가?"**

**🎯 왜 이걸 묻는가**

파일 저장은 **횟수와 시점이 곧 설계 결정**입니다. 정답이 하나가 아니라 **트레이드오프**(득실 맞바꾸기)입니다.

| 저장 시점 | 장점 | 단점 |
| --- | --- | --- |
| 매 순간 저장 | 절대 안 잃음 | **느림** (파일 쓰기는 비쌈) |
| 종료할 때 한 번 | 빠름 | 갑자기 꺼지면 **전부 잃음** |
| **바뀔 때마다** (내 선택) | 잃는 건 직전 한 동작뿐 | 중간 정도 비용 |

평가자는 "저장했다"가 아니라 **"왜 그 시점을 골랐는가"** 를 듣고 싶어 합니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **메모리 vs 파일** | 머릿속 암기(빠름·휘발) vs 공책(느림·보존) |
| **읽기는 1회, 쓰기는 여러 번** | 시작할 때만 올리고, 바뀔 때만 내림 |
| **트레이드오프** | 뭘 얻으면 뭘 잃는다 — 정답 대신 **선택과 근거** |
| **진입점(`main()`)** | 프로그램이 시작되는 자리 |

**💻 코드를 이렇게 짰습니다**

```python
def main():
    game = QuizGame()
    game.load_state()      # ① 시작할 때 딱 1번 읽기
    try:
        game.run()         # ② 실행 중에는 메모리에서만 작업
    except (KeyboardInterrupt, EOFError):
        game.save_state()  # ⑥ 강제 중단 때도 저장하고 나감
```

쓰기(`save_state()`)는 **데이터가 바뀌는 순간에만** 부릅니다.

| 시점 | 코드 |
| --- | --- |
| ③ 퀴즈 추가 직후 | [main.py:194](main.py#L194) |
| ④ 퀴즈 삭제 직후 | [main.py:220](main.py#L220) |
| ⑤ 한 판 끝나 점수 기록 | [main.py:291](main.py#L291) |

**설계 포인트 3가지**
1. **읽기가 1번인 이유** — 파일 내용은 실행 중 바뀌지 않습니다. 매번 읽으면 느리기만 합니다.
2. **저장을 각 기능 안에 넣었습니다.** `add_quiz()` 마지막 줄이 `save_state()`라서, 저장을 **깜빡할 수 없는 구조**입니다.
3. **점수 저장은 `record_result()` 한 곳으로 모았습니다.** 기록 추가 + 최고점 갱신 + 저장이 항상 세트로 일어납니다.

**🔍 검증 방법** — 읽기·쓰기 지점을 명령으로 세어 보여줍니다.

```bash
grep -n "load_state()" main.py    # ① 읽기 → 332행 딱 1곳
grep -n "save_state()" main.py    # ② 쓰기 → 194, 220, 291, 338행
```

| 시점 | 코드 | 확인 방법 |
| --- | --- | --- |
| ① 시작 시 읽기 1회 | [main.py:332](main.py#L332) | 실행하면 `📂 ...불러왔습니다` 1번만 출력 |
| ③ 퀴즈 추가 직후 | [main.py:194](main.py#L194) | `2`로 추가 후 다른 터미널에서 `tail state.json` |
| ④ 퀴즈 삭제 직후 | [main.py:220](main.py#L220) | `4`로 삭제 후 파일 확인 |
| ⑤ 한 판 종료 | [main.py:291](main.py#L291) | `1`로 풀고 나서 파일의 `history` 확인 |
| ⑥ Ctrl+C | [main.py:338](main.py#L338) | → [2-4번](#q2-4)에서 시연 |

**실시간으로 보여주는 법** (터미널 2개)

```bash
# 터미널 A
python3.11 main.py
# 터미널 B — A에서 퀴즈를 추가하는 순간 파일이 바뀌는 걸 보여줌
ls -l state.json && sleep 1 && ls -l state.json
```

**🔗 이동** — [↑ 목차](#toc) · [◀ 2-2](#q2-2) · [다음 2-4 ▶](#q2-4) · 관련: [1-3 데이터 유지](#q1-3) · [4-1 확장 한계](#q4-1)


---

<a id="q2-4"></a>

## 2-4. Ctrl+C 또는 EOF 상황에서 "안전 종료"를 위해 어떤 처리를 했는지(또는 어떤 처리가 필요했는지) 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"사용자가 정상적으로 안 끄고 나가도 데이터를 지키는가?"**
> *Ctrl+C에 빨간 에러가 뜨면 대비가 없는 것.*

**✅ 한 줄 답변** — 게임 루프 전체를 `try/except`로 감싸 **안내 → 저장 → 정상 종료** 순서로 처리했습니다.

**🗣️ 이렇게 말하세요**

> "`Ctrl+C`를 누르면 `KeyboardInterrupt`, 입력이 끊기면 `EOFError`가 발생합니다.
> 처리하지 않으면 빨간 오류 메시지와 함께 비정상 종료하고, 저장되지 않은 상태가 사라집니다.
> 그래서 두 예외를 함께 잡아 안내 메시지를 출력하고 `save_state()`로 저장한 뒤 정상 종료합니다.
> **문을 쾅 닫고 나가더라도 공책은 챙겨 나가도록** 만든 것입니다."

```python
try:
    game.run()
except (KeyboardInterrupt, EOFError):
    print("\n\n⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.")
    game.save_state()
```

**🖥️ 시연** — 실행 후 `Ctrl+C` → Traceback 없이 안내 메시지 출력 후 종료

**📂 코드 위치** — [`main()` 333-338행](main.py#L333-L338)

**❓ 예상 추가 질문** — *"저장하는 도중에 다시 Ctrl+C를 누르면요?"*
→ 지금은 파일이 반쯤 쓰인 채 깨질 수 있습니다. 다음 실행 때 `json.JSONDecodeError`로 감지되어
기본 퀴즈로 복구되므로 프로그램이 죽지는 않지만 데이터는 잃습니다.
**임시 파일에 먼저 쓰고 성공하면 교체하는 방식**으로 개선할 수 있습니다. (아직 미구현)

### 🎓 학습 노트 (2-4)

**📖 질문의 요지** — **"사용자가 정상적으로 안 끄고 나가도 데이터를 지키는가?"**

**🎯 왜 이걸 묻는가**

사람은 메뉴에서 `6`을 얌전히 누르지 않습니다. 답답하면 **`Ctrl+C`** 를 누릅니다.
그 순간 아무 대비가 없으면 이렇게 됩니다.

```
^CTraceback (most recent call last):
  File "main.py", line 334, in <module>
KeyboardInterrupt          ← 빨간 글씨 + 저장 안 된 데이터 소멸
```

| 확인하는 것 | 왜 |
| --- | --- |
| Traceback 없이 깔끔히 끝나는가 | 예외를 **예상하고 처리**했는가 |
| 나가면서 저장하는가 | 데이터를 지키는 설계인가 |
| 왜 그 두 예외인가 설명 가능한가 | 복붙이 아니라 이해했는가 |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **예외(exception)** | 프로그램 실행 중 생긴 **사고** |
| **`KeyboardInterrupt`** | 사용자가 **Ctrl+C**로 중단했을 때 나는 예외 |
| **`EOFError`** | 입력이 **더 이상 없을 때** (Ctrl+D, 파이프 입력 끝) |
| **예외 전파** | 사고는 **부른 쪽으로 위로 올라감** → 맨 바깥에서 한 번만 잡으면 됨 |
| **`except (A, B)`** | 예외 **여러 개를 한 번에** 잡기 |

**💻 코드를 이렇게 짰습니다**

```python
def main():
    game = QuizGame()
    game.load_state()
    try:
        game.run()                              # 게임 루프 전체를 감쌈
    except (KeyboardInterrupt, EOFError):       # 두 예외를 한 번에
        print("\n\n⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.")
        game.save_state()                       # 나가기 전에 공책 챙기기
```

**설계 포인트 3가지**
1. **왜 `main()`에서 잡았나** — 예외는 부른 쪽으로 **위로 전파**됩니다. `input()`이 있는 모든 곳에 `try`를 두는 대신, **가장 바깥 한 곳**에서 잡으면 전부 커버됩니다.
2. **순서가 안내 → 저장 → 종료** 입니다. 저장을 먼저 하고 안내하면, 저장이 실패했을 때 사용자가 잘못된 안심을 합니다.
3. **남은 한계도 압니다** — 저장 도중 다시 `Ctrl+C`를 누르면 파일이 반쯤 쓰인 채 깨질 수 있습니다. 개선안(임시 파일 → 교체)은 [4-2번](#q4-2)에 정리했습니다.

**🔍 검증 방법** — 두 상황을 **직접 일으켜서** 보여줍니다.

```bash
# ① Ctrl+C 상황 — 실행한 뒤 Control 키와 C를 함께 누름
python3.11 main.py

# ② EOF(입력 끊김) 상황 — 입력을 아예 주지 않고 실행
python3.11 main.py < /dev/null
```

| 상황 | 나와야 하는 결과 | 나오면 안 되는 것 |
| --- | --- | --- |
| ① | `⚠️ 입력이 중단되었습니다. 저장 후 안전하게 종료합니다.` | `Traceback`, `KeyboardInterrupt` 빨간 글씨 |
| ② | 같은 안내 후 정상 종료 | `EOFError` 빨간 글씨 |

**말할 것** — "`Ctrl+C`는 `KeyboardInterrupt`, 입력이 끊기면 `EOFError`가 납니다.
[`main()` 335행](main.py#L335)에서 두 개를 함께 잡아 **안내 → 저장 → 정상 종료** 순으로 처리합니다.
예외는 부른 쪽으로 위로 올라오기 때문에 **가장 바깥 한 곳**에서만 잡으면 전부 커버됩니다."

```bash
grep -n "KeyboardInterrupt\|EOFError" main.py    # → 335행 한 줄에 둘 다
```

**🔗 이동** — [↑ 목차](#toc) · [◀ 2-3](#q2-3) · [다음 2-5 ▶](#q2-5) · 관련: [3-3 try/except](#q3-3) · [4-2 손상 대응](#q4-2)


---

<a id="q2-5"></a>

## 2-5. 커밋을 어떤 단위로 나누었고, 커밋 메시지 규칙을 어떻게 정했는지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"커밋을 나누고 이름 붙이는 나만의 규칙이 있는가?"**
> *1-5가 개수를 봤다면 여긴 기준을 봄.*

**✅ 한 줄 답변** — **"기능 하나 완성 = 커밋 1개"**, 메시지는 **`종류: 요약`** 형식으로 통일했습니다.

**🗣️ 이렇게 말하세요**

> "메뉴 → 입력 검증 → `Quiz` 클래스 → 기본 퀴즈 → 파일 저장 → 퀴즈 풀기 → 추가 → 목록 → 삭제 →
> 점수 → 안전 종료 → 리팩터링 → README 순으로 **18개**를 남겼습니다.
> 접두어는 `Feat:`(새 기능) `Fix:`(버그 수정) `Docs:`(문서) `Refactor:`(구조 정리) `Chore:`(설정)입니다.
> 이렇게 하면 `git log`만 봐도 **개발 일기처럼 순서가 보이고**, 문제가 생겼을 때
> 그 기능이 들어온 커밋으로 되돌아가 원인을 좁힐 수 있습니다."

| 접두어 | 뜻 | 예시 커밋 |
| --- | --- | --- |
| `Feat:` | 새 기능 | [`243592b`](https://github.com/ellysuh22/2-homework/commit/243592b) Quiz 클래스 구현 |
| `Fix:` | 버그·문제 수정 | [`c961ad8`](https://github.com/ellysuh22/2-homework/commit/c961ad8) Ctrl+C 안전 종료 |
| `Docs:` | 문서 | [`6934ef3`](https://github.com/ellysuh22/2-homework/commit/6934ef3) README 작성 |
| `Refactor:` | 기능은 그대로, 구조만 정리 | [`035cbde`](https://github.com/ellysuh22/2-homework/commit/035cbde) 입력 처리 정리 |
| `Chore:` | 설정·잡무 | [`7234ac8`](https://github.com/ellysuh22/2-homework/commit/7234ac8) 저장소 초기 설정 |

**📂 확인 방법** — `git log --oneline` / [README 커밋표 전체](README.md#전체-커밋-이력)

**❓ 예상 추가 질문** — *"`Refactor: QuizGame 입력 처리 정리`에서는 정확히 뭘 바꿨나요?"*
→ 입력 검증이 메뉴마다 조금씩 흩어져 있던 것을 `input_int()` 한 곳으로 모으고 중복 분기를 제거했습니다.
**기능은 그대로, 구조만 정리한 커밋**이라 `Feat`이 아니라 `Refactor`로 표시했습니다.

### 🎓 학습 노트 (2-5)

**📖 질문의 요지** — **"커밋을 나누고 이름 붙이는 데 나만의 규칙이 있는가?"**

**🎯 왜 이걸 묻는가**

1-5번이 "**개수**"를 봤다면, 2-5번은 "**기준**"을 봅니다. 규칙 없이 쌓인 이력은 이렇게 보입니다.

| 규칙 없는 로그 | 규칙 있는 로그 |
| --- | --- |
| `수정`, `수정2`, `ㅇㅇ`, `최종` | `Feat: 퀴즈 풀기 기능 구현` |
| 뭘 바꿨는지 **열어봐야 앎** | 제목만 봐도 **알 수 있음** |
| 되돌릴 지점을 **못 고름** | 원하는 지점을 **바로 지목** |

실무에서는 커밋 메시지가 **동료에게 보내는 편지**입니다. 6개월 뒤의 나 자신에게도요.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **원자적 커밋** | 커밋 1개 = **되돌려도 말이 되는 최소 단위 1개** |
| **커밋 접두어 규칙** | `Feat:` `Fix:` `Docs:` `Refactor:` `Chore:` — 종류를 앞에 붙이는 관례 |
| **`Feat` vs `Refactor`** | 기능이 **늘었으면** Feat, 기능은 같고 **구조만 정리**했으면 Refactor |
| **리팩터링** | 겉보기 동작은 그대로 두고 **속 구조만 개선**하는 일 |

**💻 이렇게 나눴습니다** (18개의 흐름)

```
메뉴 → 입력 검증 → Quiz 클래스 → 기본 퀴즈 → 파일 저장
  → 퀴즈 풀기(브랜치) → 추가 → 목록 → 삭제 → 점수
  → 안전 종료 → 리팩터링 → README
```

| 접두어 | 뜻 | 실제 커밋 |
| --- | --- | --- |
| `Feat:` | 새 기능 | [`243592b`](https://github.com/ellysuh22/2-homework/commit/243592b) Quiz 클래스 구현 |
| `Fix:` | 버그·문제 수정 | [`c961ad8`](https://github.com/ellysuh22/2-homework/commit/c961ad8) Ctrl+C 안전 종료 |
| `Docs:` | 문서 | [`6934ef3`](https://github.com/ellysuh22/2-homework/commit/6934ef3) README 작성 |
| `Refactor:` | 구조만 정리 | [`035cbde`](https://github.com/ellysuh22/2-homework/commit/035cbde) 입력 처리 정리 |
| `Chore:` | 설정·잡무 | [`7234ac8`](https://github.com/ellysuh22/2-homework/commit/7234ac8) 저장소 초기 설정 |

**설계 포인트 3가지**
1. **커밋 순서가 곧 개발 순서**입니다. 뼈대(메뉴) → 안전장치(검증) → 데이터(클래스) → 기능 → 문서 순으로, `git log`가 개발 일기가 됩니다.
2. **`Refactor: QuizGame 입력 처리 정리`가 따로 있는 게 포인트**입니다. 기능 추가와 구조 정리를 **한 커밋에 섞지 않았다**는 증거입니다.
3. **메시지를 한국어로 통일**했습니다. 접두어는 영어 관례를 따르되 내용은 읽는 사람 기준으로 썼습니다.

**🔍 검증 방법**

```bash
git log --oneline                        # ① 전체 18줄
git log --oneline --grep="^Refactor"     # ② 구조 정리 커밋만
git show --stat 035cbde | head -8        # ③ 그 커밋이 정말 구조만 바꿨는지
```

| 명령 | 나와야 하는 결과 | 말할 것 |
| --- | --- | --- |
| ① | 18줄 전부 `종류:` 접두어로 시작 | "규칙을 처음부터 끝까지 지켰습니다" |
| ② | `035cbde Refactor: QuizGame 입력 처리 정리...` | "기능 추가와 구조 정리를 **섞지 않았습니다**" |
| ③ | `main.py` 1개 파일만 변경 | "동작은 그대로, 구조만 바꾼 커밋입니다" |

**커밋 순서로 개발 흐름 보여주기**

```bash
git log --oneline --reverse    # 오래된 것부터 = 실제 개발 순서
```
→ 메뉴 → 입력 검증 → `Quiz` 클래스 → 기본 퀴즈 → 저장 → 퀴즈 풀기(브랜치) → … → README

**🔗 이동** — [↑ 목차](#toc) · [◀ 2-4](#q2-4) · [다음 3-1 ▶](#q3-1) · 관련: [1-5 커밋 개수](#q1-5)


---

> **[항목 2] (PASS / FAIL)** — 5문항 모두 코드 위치와 함께 설명 가능

---

# [항목 3] 핵심 기술 원리 적용

<a id="q3-1"></a>

## 3-1. 클래스를 사용한 이유는 무엇이며, 함수만으로 구현할 때와 어떤 차이가 있는지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"클래스를 안 쓰면 뭐가 불편한지 비교해서 말할 수 있는가?"**
> *장점만 외운 사람은 "재사용성이 좋습니다"에서 끝남.*

**✅ 한 줄 답변** — 클래스는 **관련된 데이터와 기능을 한 묶음으로 붙여두는 것**이라 썼습니다.

**🗣️ 이렇게 말하세요**

> "함수만 쓰면 문제·보기·정답은 리스트나 딕셔너리에 따로 있고 출력·채점 함수도 따로 있어서,
> **매번 데이터를 함수에 끌고 다녀야** 합니다. 퀴즈가 늘수록 어느 데이터가 어느 함수와 짝인지
> 헷갈리고, 딕셔너리 키를 오타내도 실행 전에는 모릅니다.
> 클래스로 만들면 `quiz.display()`, `quiz.check(답)`처럼 **데이터가 자기 기능을 스스로 갖고** 있어
> 읽기 쉽고, 붕어빵 틀처럼 같은 구조의 퀴즈를 몇 개든 일정하게 찍어낼 수 있습니다."

| | 함수만 사용 | 클래스 사용 (현재) |
| --- | --- | --- |
| 데이터 전달 | `check_answer(quiz_dict, 입력)` — 매번 끌고 다님 | `quiz.check(입력)` — 자기가 가지고 있음 |
| 새 속성 추가 | 관련 함수를 전부 찾아 수정 | `Quiz`에 속성 하나 추가로 끝 |
| 실수 위험 | 딕셔너리 키 오타를 실행 전엔 모름 | 속성 이름이 한곳에 정의됨 |

**실제 사례** — 힌트 기능을 넣을 때 `Quiz`에 `hint` 속성 하나를 더하는 것으로 끝났습니다.

**📂 코드 위치** — [`Quiz.__init__()` 21-25행](main.py#L21-L25) · [`check()`](main.py#L37-L39)

**❓ 예상 추가 질문** — *"`self`가 무슨 역할인가요?"*
→ "지금 이 객체 자신"을 가리키는 이름표입니다. `self.question`은 *이 문제의* 질문이라는 뜻이고,
덕분에 퀴즈를 100개 만들어도 각자 자기 값을 따로 갖습니다.

### 🎓 학습 노트 (3-1)

**📖 질문의 요지** — **"클래스를 왜 쓰는지, 안 쓰면 뭐가 불편한지 비교해서 말할 수 있는가?"**

**🎯 왜 이걸 묻는가**

이 질문의 핵심은 **"비교"** 입니다. 클래스의 장점만 외운 사람은 "재사용성이 좋습니다"라고 답하고 끝납니다.
평가자는 **함수만 쓸 때 실제로 뭐가 불편한지** 겪어봤는지를 봅니다.

| 함수 + 딕셔너리로만 만들면 | 클래스로 만들면 |
| --- | --- |
| `check_answer(quiz_dict, 입력)` — **데이터를 매번 끌고 다님** | `quiz.check(입력)` — 데이터가 **자기 기능을 가짐** |
| `quiz["anwser"]` 오타 → **실행해야 발견** | 속성 이름이 `__init__` **한 곳에 정의됨** |
| 힌트 추가 시 관련 함수 **전부 수정** | `Quiz`에 속성 **하나 추가로 끝** |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 | 이 프로그램에서 |
| --- | --- | --- |
| **클래스 = 틀** | 붕어빵 **틀**, 같은 구조를 몇 개든 찍어냄 | `Quiz` 하나로 퀴즈 5개 |
| **`__init__`** | 객체가 태어날 때 **자동 실행**되는 준비 메서드 | 속성 4개를 채움 |
| **`self`** | "**지금 이** 객체 자신"을 가리키는 이름표 | `self.question` = 이 문제의 질문 |
| **캡슐화** | 데이터와 그 데이터를 다루는 기능을 **한 묶음**으로 | `answer`와 `check()`가 같은 클래스 |
| **기본 매개변수** | `hint=""` — 안 넘기면 빈 문자열 | 힌트 없는 퀴즈도 만들 수 있음 |

**💻 코드를 이렇게 짰습니다**

```python
class Quiz:
    def __init__(self, question, choices, answer, hint=""):
        self.question = question    # ← self = 이 붕어빵
        self.choices = choices
        self.answer = answer
        self.hint = hint            # hint는 안 줘도 됨 (기본값 "")

    def check(self, selected):
        return selected == self.answer   # 정답 비교를 자기가 함
```

```python
# 쓰는 쪽 — 정답이 몇 번인지 몰라도 채점됨
if quiz.check(selected):
    print("✅ 정답입니다!")
```

**설계 포인트 3가지**
1. **정답을 아는 건 `Quiz` 자신뿐**입니다. `QuizGame`은 `check()`에 물어보기만 합니다. 채점 규칙이 바뀌어도 `Quiz` 안에서 끝납니다.
2. **`hint=""` 기본값**을 준 덕에, 힌트 없이 만든 예전 퀴즈도 **에러 없이** 만들어집니다.
3. **실제 효과를 겪었습니다** — 힌트 기능을 나중에 넣을 때 `Quiz`에 속성 한 줄 + 저장/불러오기 한 줄로 끝났습니다. 딕셔너리였다면 힌트를 쓰는 함수를 전부 찾아 고쳐야 했습니다.

**🔍 검증 방법** — 코드 2곳을 나란히 보여주면 차이가 바로 드러납니다.

```bash
grep -n "def check\|quiz.check" main.py     # 정의한 곳 / 쓰는 곳
```

| 위치 | 코드 | 말할 것 |
| --- | --- | --- |
| [37-39행](main.py#L37-L39) | `return selected == self.answer` | "정답을 아는 건 **카드 자신**입니다" |
| [240행](main.py#L240) | `if quiz.check(selected):` | "부르는 쪽은 정답이 몇 번인지 **모릅니다**" |

**직접 확인해 보기** (파이썬 대화창)

```bash
python3.11 -c "
from main import Quiz
q = Quiz('테스트', ['가','나','다','라'], 2)
print(q.question, '| 정답:', q.answer)   # 데이터를 자기가 갖고 있음
print('2번 →', q.check(2), '| 3번 →', q.check(3))   # 기능도 자기가 가짐
"
```
→ `테스트 | 정답: 2` / `2번 → True | 3번 → False`

**🔗 이동** — [↑ 목차](#toc) · [◀ 2-5](#q2-5) · [다음 3-2 ▶](#q3-2) · 관련: [2-1 책임 분리](#q2-1) · [부록 B 용어](#appendix-b)


---

<a id="q3-2"></a>

## 3-2. JSON 파일로 데이터를 저장하는 이유와 JSON 형식의 특징을 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"왜 하필 JSON인가?"**
> *저장 방법은 여러 개 — 골랐다면 이유가 있어야 함.*

**✅ 한 줄 답변** — 변수는 프로그램이 꺼지면 사라지므로 **파일로 남겨야 다음 실행에 이어집니다.**

**🗣️ 이렇게 말하세요**

> "그중 JSON을 고른 이유는 세 가지입니다.
> ① **사람이 읽을 수 있는 텍스트**라 메모장으로 열어 확인·수정할 수 있어 문제를 찾기 쉽고,
> ② 파이썬의 **딕셔너리·리스트와 모양이 거의 같아서** `json.dump()` / `json.load()` 두 줄로 변환되고,
> ③ **세계 표준**이라 나중에 웹이나 다른 언어와도 그대로 주고받을 수 있습니다.
> 형식적 특징은 `{"키": 값}` 짝으로 이루어지고, 리스트와 딕셔너리를 **중첩**할 수 있다는 점입니다."

| JSON 특징 | 설명 |
| --- | --- |
| `{"키": 값}` 짝 구조 | 파이썬 딕셔너리와 1:1 대응 |
| 중첩 가능 | 리스트 안에 딕셔너리, 그 안에 또 리스트 |
| 텍스트 형식 | 메모장으로 열림 → 확인·수정이 쉬움 |
| 언어 무관 표준 | 웹·다른 언어와 그대로 교환 가능 |

한글이 `\uXXXX`로 깨지지 않도록 **UTF-8 인코딩 + `ensure_ascii=False`**,
사람이 읽기 좋게 **`indent=2`** 로 저장했습니다.

**📂 코드 위치** — 쓰기 [main.py:148-149](main.py#L148-L149) / 읽기 [main.py:127](main.py#L127)

**❓ 예상 추가 질문** — *"`ensure_ascii=False`를 빼면 어떻게 되나요?"*
→ 한글이 이스케이프 문자로 저장됩니다. 프로그램은 읽지만 사람이 파일을 열었을 때 알아볼 수 없어서,
확인·수정이 쉽다는 JSON의 장점이 사라집니다.

### 🎓 학습 노트 (3-2)

**📖 질문의 요지** — **"왜 하필 JSON인가?"** 저장 방법은 여러 개인데 그중 왜 이걸 골랐는지를 묻습니다.

**🎯 왜 이걸 묻는가**

저장 방법은 txt, CSV, JSON, DB 등 여러 가지입니다. **골랐다는 건 이유가 있어야** 합니다.

| 방법 | 이럴 때 좋음 | 이 프로젝트엔 |
| --- | --- | --- |
| **txt** | 단순한 한 줄 메모 | 중첩 구조를 담기 어려움 ❌ |
| **CSV** | 엑셀 같은 표 | 보기 4개(리스트)를 한 칸에 못 담음 ❌ |
| **JSON** | 중첩된 구조를 그대로 | 파이썬 dict와 모양이 같음 ✅ |
| **DB(SQLite)** | 수천 개 이상, 검색 필요 | 지금 규모엔 과함 (→ [4-1번](#q4-1)) |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **직렬화(serialization)** | 프로그램 속 값을 **파일에 쓸 수 있는 글자로** 바꾸는 일 |
| **`json.dump()` / `json.load()`** | 파이썬 → 파일 / 파일 → 파이썬 |
| **UTF-8** | 한글·이모지를 담을 수 있는 **글자 저장 방식** |
| **`ensure_ascii=False`** | 한글을 `\uXXXX`로 안 바꾸고 **한글 그대로** 저장 |
| **`indent=2`** | 두 칸씩 들여써서 **사람이 읽기 좋게** |

**💻 코드를 이렇게 짰습니다**

```python
with open(STATE_FILE, "w", encoding="utf-8") as file:   # UTF-8로 열기
    json.dump(data, file, ensure_ascii=False, indent=2)  # 한글 그대로, 들여쓰기 2칸
```

옵션을 뺐다면 이렇게 저장됩니다 — 프로그램은 읽지만 **사람은 못 읽습니다.**

```json
{"quizzes": [{"question": "파이썬에서...", "answer": 2}]}
```

**설계 포인트 3가지**
1. **JSON ↔ 파이썬이 거의 1:1** 입니다. `{}`는 딕셔너리, `[]`는 리스트로 그대로 대응해서 변환 코드가 두 줄입니다.
2. **사람이 읽을 수 있게 저장한 게 실제로 도움이 됐습니다.** 저장이 제대로 됐는지 `state.json`을 열어보는 것만으로 확인됩니다.
3. **`ensure_ascii=False`와 `encoding="utf-8"`은 둘 다 필요합니다.** 앞은 "한글을 그대로 쓰겠다", 뒤는 "그 한글을 담을 방식"입니다. 하나만 있으면 깨집니다.

**🔍 검증 방법** — **파일을 열어 보여주는 것**이 가장 강력합니다.

```bash
head -20 state.json     # ① 한글이 그대로, 2칸씩 들여쓰기
```

| 확인할 것 | 나와야 하는 모습 | 무엇이 증명되나 |
| --- | --- | --- |
| 한글 | `"question": "파이썬에서 리스트를..."` | `ensure_ascii=False` + UTF-8 |
| 들여쓰기 | 2칸씩 계단 모양 | `indent=2` |
| 구조 | `{` 안에 `[`, 그 안에 또 `{` | JSON의 **중첩** 특징 |

**옵션을 뺐다면 어떻게 되는지 직접 보여주기**

```bash
python3.11 -c "
import json
d = {'question': '파이썬에서 리스트를 만들 때?'}
print('옵션 O:', json.dumps(d, ensure_ascii=False))
print('옵션 X:', json.dumps(d))
"
```
→ `옵션 X`는 `\uD30C\uC774...`처럼 나와 **사람이 못 읽습니다.**

**파이썬과 모양이 같다는 것도 확인**

```bash
python3.11 -c "import json; print(type(json.load(open('state.json'))))"   # <class 'dict'>
```

**🔗 이동** — [↑ 목차](#toc) · [◀ 3-1](#q3-1) · [다음 3-3 ▶](#q3-3) · 관련: [3-5 구조 설계](#q3-5) · [4-1 확장 한계](#q4-1)


---

<a id="q3-3"></a>

## 3-3. 파일 입출력에서 try/except가 필요한 이유(발생 가능한 실패 케이스 포함)를 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"내 코드가 완벽해도 실패할 수 있는 곳을 아는가?"**
> *목표는 "에러가 안 나게"가 아니라 "에러가 나도 실행되게".*

**✅ 한 줄 답변** — 파일은 **내 코드 바깥 세상**이라, 내 코드가 완벽해도 실패할 수 있기 때문입니다.

**🗣️ 이렇게 말하세요**

> "제가 대비한 실패 케이스는 네 가지입니다. `try/except`가 없으면 이 중 하나만 생겨도
> 프로그램이 시작조차 못 하고 죽습니다. 목표는 **'파일이 어떤 상태여도 게임은 반드시 실행된다'** 입니다."

| 실패 케이스 | 언제 생기나 | 발생 예외 | 내 대응 |
| --- | --- | --- | --- |
| 파일이 없음 | 첫 실행 | (`os.path.exists`로 사전 확인) | 기본 퀴즈 5개로 시작 |
| 내용이 깨짐 | 사용자가 잘못 수정, 저장 중 강제 종료 | `json.JSONDecodeError` | 안내 후 기본 퀴즈로 복구 |
| 필요한 칸이 없음 | JSON은 멀쩡하나 구조가 다름 | `KeyError`, `TypeError` | 안내 후 기본 퀴즈로 복구 |
| 권한·디스크 문제 | 읽기/쓰기 권한 없음, 디스크 오류 | `OSError` | 읽기는 복구 / 쓰기는 안내 후 계속 |

**🖥️ 시연**

```bash
echo '{ 깨진 내용' > state.json && python3.11 main.py
# → ⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. (...) 후 정상 실행
```

**📂 코드 위치** — 읽기 [125-138행](main.py#L125-L138) / 쓰기 [147-151행](main.py#L147-L151)

**❓ 예상 추가 질문** — *"`except Exception`으로 전부 잡지 않은 이유는?"*
→ 전부 잡으면 **예상하지 못한 진짜 버그까지 조용히 숨겨져** 원인을 못 찾습니다.
예상한 4종만 이름으로 지정해 잡고, 나머지는 드러나게 두는 편이 안전하다고 판단했습니다.

### 🎓 학습 노트 (3-3)

**📖 질문의 요지** — **"내 코드가 완벽해도 실패할 수 있는 곳이 어디인지 아는가?"**

**🎯 왜 이걸 묻는가**

1-2번이 **사람의 실수**를 막는 얘기였다면, 3-3번은 **바깥 세상**이 말썽인 경우입니다.
파일은 내 코드 밖에 있어서 내가 통제할 수 없습니다 — 사용자가 지울 수도, 메모장으로 고칠 수도 있습니다.

| 파일에서 생길 수 있는 사고 | 언제 |
| --- | --- |
| 파일이 없음 | 첫 실행, 사용자가 지움 |
| 내용이 깨짐 | 메모장으로 잘못 수정, 저장 중 강제 종료 |
| 칸이 비었음 | JSON은 멀쩡한데 구조가 다름 (옛 버전 파일) |
| 권한·디스크 문제 | 읽기 전용 폴더, 디스크 꽉 참 |

**목표는 "에러가 안 나게"가 아니라 "에러가 나도 게임은 실행되게"** 입니다. 이 차이가 핵심입니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **`try` / `except` / `else`** | 해보고(try) / 터지면(except) / **안 터졌을 때만**(else) |
| **예외 이름 지정** | `except ValueError:` — 잡을 사고를 **콕 집기** |
| **`json.JSONDecodeError`** | JSON 글자를 해석 못 할 때 나는 예외 |
| **`KeyError`** | 딕셔너리에 **그 칸이 없을 때** |
| **`OSError`** | 파일·디스크·권한 관련 사고 |
| **`os.path.exists()`** | 파일이 **있는지 미리 확인** |

**💻 코드를 이렇게 짰습니다**

```python
def load_state(self):
    if not os.path.exists(STATE_FILE):        # ① 없는 건 사고가 아님 → 미리 확인
        print("📂 저장된 데이터가 없어 기본 퀴즈로 시작합니다.")
        return

    try:
        with open(STATE_FILE, "r", encoding="utf-8") as file:
            data = json.load(file)             # ② 깨졌으면 여기서 터짐
        self.quizzes = [Quiz.from_dict(item) for item in data["quizzes"]]
        self.best_score = data["best_score"]   # ③ 칸이 없으면 여기서 터짐
        self.history = data["history"]
    except (OSError, json.JSONDecodeError, KeyError, TypeError) as error:
        print(f"⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. ({error})")
        self.quizzes = default_quizzes()       # ④ 어떤 사고든 → 기본 퀴즈로 복구
        self.best_score = 0
        self.history = []
    else:                                       # ⑤ 사고가 없었을 때만 실행
        print(f"📂 저장된 데이터를 불러왔습니다. (퀴즈 {len(self.quizzes)}개...)")
```

**설계 포인트 3가지**
1. **"없음"은 `try`로 안 잡았습니다.** 첫 실행에 파일이 없는 건 **정상**이지 사고가 아닙니다. `os.path.exists()`로 미리 갈라내 의도를 코드로 드러냈습니다.
2. **`else`를 쓴 이유** — 성공 메시지를 `try` 안에 넣으면, 뒤에서 터졌을 때 "불러왔습니다"가 먼저 출력되고 곧바로 "손상되었습니다"가 나옵니다. `else`는 **사고가 없었을 때만** 실행됩니다.
3. **`except Exception`으로 다 잡지 않았습니다.** 전부 잡으면 예상 못 한 **진짜 버그까지 조용히 숨겨져** 원인을 못 찾습니다. 예상한 4종만 이름으로 지정했습니다.

**🔍 검증 방법** — ⚠️ **반드시 백업부터.** 파일을 일부러 깨뜨리는 시연입니다.

```bash
cp state.json state.json.bak        # ① 백업 (필수!)
echo '{ 깨진 내용' > state.json      # ② 일부러 손상
python3.11 main.py                  # ③ 실행 → 6으로 바로 종료
mv state.json.bak state.json        # ④ 원상복구 ← 잊지 마세요
```

| 단계 | 나와야 하는 결과 | 나오면 안 되는 것 |
| --- | --- | --- |
| ③ | `⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. (Expecting property name...)` | `Traceback` 빨간 글씨 |
| ③ 후 `3` | 기본 퀴즈 5개가 정상 출력 | 프로그램 종료 |

> ⚠️ ③에서 퀴즈를 추가하거나 풀면 **복구된 상태가 저장되어 백업이 무의미해집니다.**
> 확인만 하고 `6`으로 바로 나오세요.

**파일이 아예 없는 경우도 보여주기**

```bash
mv state.json state.json.bak && python3.11 main.py    # 📂 저장된 데이터가 없어 기본 퀴즈로...
mv state.json.bak state.json
```

```bash
grep -n "except" main.py    # → 131(파일 4종), 150(쓰기), 164(숫자변환), 335(중단)
```

**🔗 이동** — [↑ 목차](#toc) · [◀ 3-2](#q3-2) · [다음 3-4 ▶](#q3-4) · 관련: [1-2 입력 검증](#q1-2) · [4-2 손상 대응](#q4-2)


---

<a id="q3-4"></a>

## 3-4. 브랜치를 분리해 작업한 이유와 병합(merge)의 의미를 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"브랜치를 왜 쓰는지 아는가?"**
> *1-6이 "했는가"라면 여긴 "왜 하는지".*

**✅ 한 줄 답변** — **`main`은 "항상 잘 돌아가는 원본", 브랜치는 "실험용 옆 책상"** 입니다.

**🗣️ 이렇게 말하세요**

> "퀴즈 풀기 기능은 이 프로젝트에서 가장 복잡한 부분이라 `feature/play-quiz` 브랜치를 만들어
> 작업했습니다. 만들다 실패하거나 코드가 엉망이 돼도 **`main`은 멀쩡하게 보존**되기 때문입니다.
> **merge(병합)는 옆 책상에서 완성한 결과를 원본에 합치는 것**이고,
> `git log --graph`에서 선이 갈라졌다 합쳐지는 게 그 기록입니다.
> 혼자 작업할 땐 필수가 아니지만, **팀에서는 각자 브랜치에서 작업하고 merge로 합치는 게 기본**이라
> 미리 연습했습니다."

**📂 확인 위치** — [병합 커밋 `2d45928`](https://github.com/ellysuh22/2-homework/commit/2d45928) (부모가 `ff3840e`, `ec2775c` 두 개)

**❓ 예상 추가 질문** — *"merge 할 때 충돌(conflict)이 나면요?"*
→ 같은 파일의 같은 줄을 양쪽에서 고쳤을 때 Git이 자동으로 못 합치고 표시해 줍니다.
사람이 어느 쪽을 남길지 직접 골라 수정한 뒤 다시 `add`·`commit` 하면 됩니다.
이번에는 브랜치 작업 중 `main`이 바뀌지 않아 충돌 없이 병합되었습니다.

### 🎓 학습 노트 (3-4)

**📖 질문의 요지** — 1-6번이 "**했는가**"라면, 3-4번은 "**왜 하는지 아는가**"입니다.

**🎯 왜 이걸 묻는가**

혼자 하는 과제에서 브랜치는 솔직히 없어도 됩니다. 그런데도 굳이 시킨 이유는 —
**실무는 100% 브랜치로 돌아가기 때문**입니다. 팀에 들어가면 첫날부터 씁니다.

| 브랜치 없이 여럿이 작업하면 | 브랜치를 쓰면 |
| --- | --- |
| 남이 코드를 올리는 순간 내 것과 충돌 | 각자 자기 책상에서 작업 |
| main이 깨지면 **전원이 멈춤** | main은 항상 **배포 가능한 상태** |
| 실패한 실험을 손으로 되돌려야 함 | 브랜치를 **지우면 끝** |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **브랜치** | 원본을 두고 실험하는 **옆 책상** |
| **merge(병합)** | 옆 책상 결과를 원본에 **합치기** |
| **머지 커밋** | 합친 기록. **부모가 2개** ← 이게 병합의 증거 |
| **충돌(conflict)** | 양쪽이 **같은 줄**을 고쳐 Git이 못 고르는 상황 |
| **`feature/이름`** | "새 기능 작업용 브랜치"라는 흔한 이름 규칙 |

**💻 이렇게 판단했습니다**

```
main:  ●───●───●─────────────●  ← 항상 돌아가는 상태 유지
                \           /
feature/play-quiz ●───●────/    ← 여기서 실패해도 main은 무사
                  (퀴즈 풀기 기능)
```

**포인트 3가지**
1. **왜 하필 "퀴즈 풀기"를 분리했나** — 출제·힌트·채점·점수 집계가 얽힌 **가장 복잡하고 실패 위험이 큰 기능**이었습니다. 브랜치는 위험이 큰 곳에 씁니다.
2. **merge의 의미** — 단순히 "합친다"가 아니라 **"두 갈래의 역사를 하나로 잇는다"** 입니다. 그래서 머지 커밋만 부모가 2개입니다.
3. **충돌이 안 난 이유도 설명 가능합니다** — 브랜치에서 작업하는 동안 `main`이 바뀌지 않아 겹치는 줄이 없었습니다. "충돌이 안 났다"가 아니라 **"안 날 상황이었다"** 고 말하는 게 정확합니다.

**🔍 검증 방법**

```bash
git log --oneline --graph --all --decorate   # ① 갈라짐/합쳐짐 그림
git show --stat 2d45928 | head -5            # ② 부모 2개 = 병합의 증거
git branch -a                                # ③ 브랜치 목록
git log --oneline main..ec2775c              # ④ 브랜치에서만 한 작업
```

| 명령 | 나와야 하는 결과 | 말할 것 |
| --- | --- | --- |
| ① | `|\` → `|/` 그림 | "여기서 갈라져 여기서 합쳐졌습니다" |
| ② | `Merge: ff3840e ec2775c` | "**부모가 2개** — 이게 병합의 정의입니다" |
| ③ | `main`, `feature/play-quiz` | "기능 이름으로 브랜치를 팠습니다" |

**말할 것** — "가장 복잡하고 실패 위험이 큰 **퀴즈 풀기**를 골라 분리했습니다.
브랜치는 위험이 큰 곳에 씁니다. 혼자면 선택이지만 **팀에서는 기본**이라 미리 연습했습니다."

**🔗 이동** — [↑ 목차](#toc) · [◀ 3-3](#q3-3) · [다음 3-5 ▶](#q3-5) · 관련: [1-6 병합 기록](#q1-6) · [2-5 커밋 규칙](#q2-5)


---

<a id="q3-5"></a>

## 3-5. state.json 데이터 구조(필드/중첩 구조)를 현재 형태로 설계한 이유를 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"저장 파일의 칸 이름과 모양을 왜 그렇게 정했는가?"**
> *한 번 정하면 가장 바꾸기 어려운 결정.*

**✅ 한 줄 답변** — **"프로그램 속 모습과 파일 속 모습을 똑같이"** 만들어 변환 실수를 없애는 게 목표였습니다.

**🗣️ 이렇게 말하세요**

> "최상위는 세 칸입니다 — `quizzes`(퀴즈 목록), `best_score`(최고 점수), `history`(게임 기록).
> `quizzes`는 개수가 변하고 순서가 있으니 **리스트**, 그 안의 퀴즈 하나는 **딕셔너리**로
> `question / choices / answer / hint` — **`Quiz` 클래스의 속성과 1:1로 동일**합니다.
> 그래서 `to_dict()` / `from_dict()`만으로 객체 ↔ 파일 변환이 끝납니다."

```json
{
  "quizzes": [
    { "question": "...", "choices": ["( )", "[ ]", "{ }", "< >"], "answer": 2, "hint": "..." }
  ],
  "best_score": 100,
  "history": [ { "date": "2026-08-04 20:21", "total": 3, "correct": 3, "score": 100 } ]
}
```

**설계 이유 4가지**

| # | 결정 | 이유 |
| --- | --- | --- |
| 1 | 필드 이름을 `Quiz` 속성과 동일하게 | `to_dict()`/`from_dict()`만으로 변환 완료, 이름 어긋남 실수 없음 |
| 2 | `answer`를 **글자가 아닌 번호(int)** 로 | 글자로 저장하면 띄어쓰기 하나에도 오답 처리됨 |
| 3 | `hint`는 `data.get("hint", "")`로 읽기 | 힌트 칸이 없는 옛 형식 파일도 에러 없이 읽힘 |
| 4 | 최상위를 3칸으로 분리 | "문제 데이터"와 "플레이 결과"를 섞지 않음 |

**📂 코드 위치** — [`to_dict()`](main.py#L41-L48) · [`from_dict()`](main.py#L50-L58) · [`save_state()`](main.py#L140-L151)

**❓ 예상 추가 질문** — *"예전에 저장한 파일에 `hint` 칸이 없으면 어떻게 되나요?"*
→ `from_dict()`에서 `data.get("hint", "")`로 읽기 때문에 **없으면 빈 문자열로 처리되고 에러가 나지 않습니다.**
힌트 기능을 나중에 추가하면서 옛 형식과도 호환되도록 일부러 그렇게 했습니다.

### 🎓 학습 노트 (3-5)

**📖 질문의 요지** — **"저장 파일의 칸 이름과 모양을 아무렇게나 정했는가, 이유가 있었는가?"**

**🎯 왜 이걸 묻는가**

데이터 구조는 **한 번 정하면 바꾸기가 가장 어려운 결정**입니다.
이미 저장된 파일들이 옛 구조로 남아 있기 때문입니다. 그래서 처음 설계가 중요합니다.

| 확인하는 것 | 왜 |
| --- | --- |
| 리스트와 딕셔너리를 **구분해서** 썼는가 | 순서가 있는 것 vs 이름으로 찾는 것 |
| 정답을 **번호**로 저장했는가 | 글자로 하면 띄어쓰기 하나에 오답 |
| 나중에 칸이 늘어도 **옛 파일이 읽히는가** | 하위 호환을 생각했는가 |

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **스키마(schema)** | 공책의 **양식** — 어떤 칸에 어떤 값이 들어가는지의 약속 |
| **리스트 vs 딕셔너리** | 순서대로 줄 세우기(`[]`) vs 이름표로 찾기(`{}`) |
| **중첩(nesting)** | 리스트 안에 딕셔너리, 그 안에 또 리스트 |
| **`.get("키", 기본값)`** | 그 칸이 **없어도 에러 안 나고** 기본값을 줌 |
| **하위 호환** | 새 버전 코드가 **옛날 파일도 읽을 수 있음** |

**💻 코드를 이렇게 짰습니다**

```json
{
  "quizzes": [                                    ← 개수가 변하고 순서 있음 → 리스트
    { "question": "...", "choices": ["( )", "[ ]"], "answer": 2, "hint": "..." }
  ],                                              ← 칸 이름으로 찾음 → 딕셔너리
  "best_score": 100,
  "history": [ { "date": "...", "score": 100 } ]
}
```

```python
def to_dict(self):                       # 객체 → 딕셔너리 (저장용)
    return {"question": self.question, "choices": self.choices,
            "answer": self.answer, "hint": self.hint}

@classmethod
def from_dict(cls, data):                # 딕셔너리 → 객체 (불러오기용)
    return cls(data["question"], data["choices"], data["answer"],
               data.get("hint", ""))     # ← 힌트 칸이 없어도 에러 안 남
```

**설계 포인트 3가지**
1. **JSON 칸 이름 = `Quiz` 속성 이름**으로 맞췄습니다. 그래서 변환이 `to_dict()`/`from_dict()` 두 개로 끝나고, 이름이 어긋나 생기는 버그가 원천 차단됩니다.
2. **`answer`를 번호(정수)로 저장**했습니다. 글자로 하면 `"[ ]"`와 `"[]"`가 다른 값이라 정답인데 오답 처리됩니다.
3. **`data.get("hint", "")`가 하위 호환의 핵심**입니다. `data["hint"]`로 썼다면 힌트 칸 없는 옛 파일에서 `KeyError`로 죽습니다. **한 글자 차이가 호환성을 만듭니다.**

**🔍 검증 방법** — 파일 구조와 코드가 **1:1로 같다**는 것을 보여줍니다.

```bash
head -20 state.json                       # ① 파일의 칸 이름
grep -n "to_dict\|from_dict" main.py      # ② 변환 메서드 위치
python3.11 -c "
import json; d = json.load(open('state.json'))
print('최상위 3칸:', list(d.keys()))
print('퀴즈 1개의 칸:', list(d['quizzes'][0].keys()))
"
```

| 명령 | 나와야 하는 결과 |
| --- | --- |
| ③ 첫 줄 | `최상위 3칸: ['quizzes', 'best_score', 'history']` |
| ③ 둘째 줄 | `퀴즈 1개의 칸: ['question', 'choices', 'answer', 'hint']` |

→ 이 4개가 [`Quiz.__init__()` 21-25행](main.py#L21-L25)의 속성 이름과 **글자까지 똑같습니다.**

**하위 호환을 직접 증명하기** — 힌트 칸이 없는 옛날 형식을 만들어 넣어봅니다.

```bash
python3.11 -c "
from main import Quiz
old = {'question':'옛날 문제','choices':['가','나','다','라'],'answer':1}  # hint 없음
q = Quiz.from_dict(old)
print('에러 없이 생성:', q.question, '| hint =', repr(q.hint))
"
```
→ `에러 없이 생성: 옛날 문제 | hint = ''` ← **`.get()` 덕분에 안 죽음**

**🔗 이동** — [↑ 목차](#toc) · [◀ 3-4](#q3-4) · [다음 4-1 ▶](#q4-1) · 관련: [3-2 JSON 특징](#q3-2) · [4-3 수정 위치](#q4-3)


---

> **[항목 3] (PASS / FAIL)** — 5문항 모두 이유와 코드 근거를 함께 설명 가능

---

# [항목 4] 심층 인터뷰

> **이 항목의 답변 규칙** — 반드시 **"현재 구현은 ~ / 개선하려면 ~"** 두 부분으로 나눠 말합니다.
> 아직 만들지 않은 것을 만들었다고 말하면 안 됩니다.

<a id="q4-1"></a>

## 4-1. 퀴즈 데이터가 1000개 이상으로 늘어난다면 현재 JSON 저장 방식에 어떤 한계가 생길 수 있는지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"지금 선택이 언제까지 유효한지 아는가?"**
> *틀렸다는 지적이 아니라 한계를 아는지 묻는 것.*

**✅ 한 줄 답변** — 제 방식은 **파일 전체를 읽고 전체를 다시 쓰는** 구조라, 규모가 커지면 다섯 가지가 문제됩니다.

**📌 현재 구현** — `load_state()`가 시작할 때 전체를 메모리에 올리고,
`save_state()`가 변경 때마다 전체를 통째로 다시 씁니다. 수십 개 규모에선 가장 단순하고 안전합니다.

**⚠️ 1000개일 때의 한계**

| 한계 | 왜 생기나 |
| --- | --- |
| 저장이 점점 느려짐 | 퀴즈 1개만 추가해도 1000개를 전부 다시 씀 |
| 메모리 부담 | 시작할 때 전부 메모리에 올려야 함 |
| 검색·필터가 어려움 | "정답이 3번인 문제만" 같은 조건은 전부 훑어야 찾음 |
| 손상 위험 확대 | 전체 덮어쓰기 도중 중단되면 파일 하나가 통째로 깨짐 |
| 동시 실행 충돌 | 두 프로그램이 동시에 저장하면 나중 쓰기가 앞선 쓰기를 덮어씀 |

**🔧 개선 방법 (아직 미구현)** — 파이썬 표준 라이브러리에 들어 있는 **SQLite(`sqlite3`)** 로 옮기면
필요한 부분만 골라 읽고 쓸 수 있어 위 문제 대부분이 해소됩니다. 외부 라이브러리 없이 가능한 경로입니다.

**📂 코드 위치** — [`save_state()` 전체 덮어쓰기](main.py#L140-L151)

**❓ 예상 추가 질문** — *"그럼 처음부터 DB로 하지 그랬나요?"*
→ 이번 미션의 목표가 **파일 입출력과 JSON을 직접 다뤄보는 것**이었고, 데이터 규모도 수십 개
수준이라 JSON이 요구에 맞는 선택이었습니다. 규모가 요구를 넘어서는 시점에 옮기는 게 맞다고 봅니다.

### 🎓 학습 노트 (4-1)

**📖 질문의 요지** — **"지금 선택이 언제까지 유효한지 아는가?"** 틀렸다고 지적하는 게 아니라, **한계를 아는지** 묻는 것입니다.

**🎯 왜 이걸 묻는가**

[항목 4]는 **답이 정해져 있지 않은 질문**입니다. 평가자가 보는 건 정답이 아니라 **사고 과정**입니다.

| 나쁜 답 | 좋은 답 |
| --- | --- |
| "문제 없습니다" | "**이런 한계가 생깁니다**" |
| "DB 쓰면 됩니다" | "왜 지금은 JSON이 맞았고, **언제** 옮겨야 하는지" |
| 만들지 않은 걸 만들었다고 함 | "**현재 구현은 ~ / 개선하려면 ~**" 구분 |

> ⚠️ 이 항목에서 **가장 큰 감점은 "허세"** 입니다. 미구현을 구현했다고 말하면 그 자리에서 들통납니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **전체 재작성 비용** | 1개만 바뀌어도 **1000개를 다시 쓰는** 낭비 |
| **확장성(scalability)** | 데이터가 늘어도 견디는 성질 |
| **인덱스(index)** | 책 뒤의 **찾아보기** — 전부 안 훑고 바로 찾게 해줌 |
| **SQLite** | 파이썬에 **기본 내장**된 파일 하나짜리 DB (설치 불필요) |
| **동시성 문제** | 두 프로그램이 동시에 쓰면 **하나가 덮여 사라짐** |

**💻 한계가 생기는 지점 (코드로 보기)**

```python
def save_state(self):
    data = {
        "quizzes": [quiz.to_dict() for quiz in self.quizzes],  # ← 1000개 전부 변환
        ...
    }
    with open(STATE_FILE, "w", encoding="utf-8") as file:
        json.dump(data, file, ...)                              # ← 1000개 전부 다시 씀
```

**퀴즈 1개를 추가해도 1000개를 전부 변환하고 전부 다시 씁니다.** 이게 모든 한계의 뿌리입니다.

| 한계 | 뿌리 |
| --- | --- |
| 저장이 느려짐 | 전체 재작성 |
| 메모리 부담 | 시작할 때 전부 메모리에 올림 |
| 검색이 느림 | 조건에 맞는 걸 찾으려면 전부 훑어야 함 |
| 손상 위험 확대 | 덮어쓰는 도중 중단 = 파일 통째로 깨짐 |
| 동시 실행 충돌 | 나중 쓰기가 앞선 쓰기를 통째로 덮음 |

**말할 포인트 3가지**
1. **"틀린 선택"이 아니라 "조건이 바뀌면 안 맞는 선택"** 입니다. 수십 개 규모에서 JSON은 가장 단순하고 안전합니다.
2. **개선 경로를 구체적으로** 댑니다 — SQLite는 파이썬 **표준 라이브러리**라 설치 없이 `import sqlite3`로 씁니다.
3. **미구현임을 분명히** 밝힙니다. "옮기면 해결됩니다(아직 미구현)"라고 말해야 신뢰를 얻습니다.

**🔍 검증 방법** — 심층 문항이라 **한계가 생기는 코드 지점을 짚는 것**이 검증입니다.

```bash
grep -n "json.dump" main.py    # ① 전체를 덮어쓰는 지점 → 149행 딱 1곳
wc -c state.json               # ② 현재 파일 크기 (퀴즈 5개 기준)
```

| 명령 | 나와야 하는 결과 | 말할 것 |
| --- | --- | --- |
| ① | `149: json.dump(data, file, ...)` | "퀴즈 1개만 추가해도 **여기서 전체를 다시 씁니다**" |
| ② | 약 1,800바이트 | "5개에 1.8KB니 1000개면 **약 360KB를 매번** 다시 씁니다" |

**규모를 실제로 보여주기** (선택 — 임시 파일로만 계산)

```bash
python3.11 -c "
import os; size = os.path.getsize('state.json')
print(f'현재 {size:,}바이트 → 1000개 환산 약 {size*200:,}바이트')
print('퀴즈 1개 추가할 때마다 이 전체를 다시 씁니다.')
"
```

**개선 경로가 실현 가능함을 보여주기**

```bash
python3.11 -c "import sqlite3; print('sqlite3 사용 가능 — 설치 불필요, 파이썬 표준 라이브러리')"
```

**🔗 이동** — [↑ 목차](#toc) · [◀ 3-5](#q3-5) · [다음 4-2 ▶](#q4-2) · 관련: [2-3 저장 흐름](#q2-3) · [3-2 왜 JSON](#q3-2)


---

<a id="q4-2"></a>

## 4-2. 만약 state.json이 손상되어 JSON 파싱에 실패한다면, 사용자가 데이터를 잃지 않도록 어떤 대응(복구/백업/초기화)이 가능한지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **""죽지 않는 것"과 "잃지 않는 것"의 차이를 아는가?"**
> *프로그램은 살았는데 데이터가 사라졌다면 해결된 게 맞나.*

**✅ 한 줄 답변** — 현재는 **"죽지 않게"** 까지 되어 있고, **"잃지 않게"** 는 개선 과제로 남아 있습니다.

**📌 현재 구현 (초기화)** — `json.JSONDecodeError`를 잡아 안내 메시지를 출력하고 기본 퀴즈로 초기화합니다.

```
⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. (Expecting property name ...)
```

프로그램이 멈추지는 않지만 **사용자가 추가한 퀴즈는 잃습니다.** 이게 지금의 명확한 한계입니다.

**🔧 개선 방법 (아직 미구현)** — 평가기준이 말한 복구·백업·초기화 세 가지를 조합하면 됩니다.

| 구분 | 방법 | 효과 |
| --- | --- | --- |
| **백업** | 저장 전에 기존 파일을 `state.json.bak`으로 복사 | 손상되면 직전 상태로 되돌림 |
| **복구** | 임시 파일에 다 쓰고 성공하면 `os.replace()`로 교체 | 저장 도중 꺼져도 원본은 무사 |
| **보존** | 깨진 파일을 지우지 말고 `state_broken.json`으로 이름 변경 | 나중에 수동 복구·원인 분석 가능 |
| **초기화** | (현재 구현) 기본 퀴즈로 시작 | 최소한 게임은 실행됨 |

핵심 원칙은 **"덮어쓰기 전에 되살릴 길을 만들어 둔다"** 입니다.

**📂 코드 위치** — [손상 감지·복구 131-135행](main.py#L131-L135)

**❓ 예상 추가 질문** — *"셋 중 하나만 한다면 무엇을 먼저 하겠습니까?"*
→ **임시 파일에 쓰고 교체하는 방식**입니다. 손상의 가장 흔한 원인이 "저장하는 도중 종료"인데,
이것만으로 그 원인 자체를 없앨 수 있어 비용 대비 효과가 가장 큽니다.

### 🎓 학습 노트 (4-2)

**📖 질문의 요지** — **"'죽지 않는 것'과 '잃지 않는 것'의 차이를 아는가?"**

**🎯 왜 이걸 묻는가**

3-3번에서 "파일이 깨져도 프로그램은 실행된다"를 만들었습니다. 여기서 한 발 더 들어갑니다 —
**프로그램은 살았는데 사용자의 퀴즈 100개가 사라졌다면, 사용자 입장에서 해결된 게 맞나요?**

| 단계 | 상태 | 현재 내 코드 |
| --- | --- | --- |
| 1단계 | **죽지 않는다** (에러로 종료 안 함) | ✅ 되어 있음 |
| 2단계 | **잃지 않는다** (데이터 복구) | ❌ 미구현 ← 이 질문의 핵심 |

**이 문항은 솔직함을 시험합니다.** "복구됩니다"라고 하면 "어떻게요?"에서 무너집니다.
**"초기화까지 되어 있고 복구는 개선 과제입니다"** 가 정답에 가깝습니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **원자적 쓰기** | 임시 파일에 **다 쓴 뒤** 한 번에 교체 → 중간 상태가 없음 |
| **`os.replace()`** | 파일 교체를 **한 순간에** 끝내는 함수 |
| **백업(backup)** | 덮어쓰기 **전에** 이전 파일을 따로 복사해둠 |
| **손상본 보존** | 깨진 파일을 지우지 말고 **이름만 바꿔** 남김 |
| **초기화(reset)** | 최후의 수단 — 기본값으로 되돌림 (현재 구현) |

**💻 현재 코드와 개선 코드**

```python
# 현재 — 깨지면 초기화 (프로그램은 살지만 데이터는 잃음)
except (OSError, json.JSONDecodeError, KeyError, TypeError) as error:
    print(f"⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다. ({error})")
    self.quizzes = default_quizzes()
```

```python
# 개선안 (미구현) — 임시 파일에 다 쓰고 성공하면 교체
tmp = STATE_FILE + ".tmp"
with open(tmp, "w", encoding="utf-8") as file:
    json.dump(data, file, ensure_ascii=False, indent=2)   # ① 임시 파일에 먼저
os.replace(tmp, STATE_FILE)    # ② 성공했을 때만 한 순간에 교체
```

**말할 포인트 3가지**
1. **핵심 원칙 한 문장** — *"덮어쓰기 전에 되살릴 길을 만들어 둔다."*
2. **하나만 고른다면 임시 파일 교체**입니다. 손상의 가장 흔한 원인이 "저장 도중 종료"인데, 이 방식은 원본을 **건드리기 전에** 새 파일을 완성하므로 **원인 자체가 사라집니다.**
3. **현재 한계를 먼저 말합니다** — "지금은 사용자가 추가한 퀴즈를 잃습니다. 이게 명확한 한계입니다." 한계를 먼저 인정하면 개선안이 설득력을 얻습니다.

**🔍 검증 방법** — **"죽지 않는다"까지는 시연, "잃지 않는다"는 미구현**임을 구분해 보여줍니다.

```bash
cp state.json state.json.bak        # ① 백업 (필수!)
echo '{ 깨진 내용' > state.json      # ② 손상
python3.11 main.py                  # ③ 실행 → 3으로 목록 확인 → 6 종료
mv state.json.bak state.json        # ④ 원상복구
```

| 단계 | 결과 | 이게 보여주는 것 |
| --- | --- | --- |
| ③ 첫 줄 | `⚠️ 저장 파일이 손상되어 기본 퀴즈로 복구합니다.` | ✅ **죽지 않음** (1단계 완료) |
| ③ `3` 입력 | 퀴즈가 **기본 5개뿐** — 추가했던 건 사라짐 | ❌ **잃지 않기는 미구현** (2단계) |

> 💡 **이 시연의 핵심은 ②번째 줄입니다.** 스스로 한계를 드러내 보이면
> "알고 있다"가 증명됩니다. 숨기면 꼬리질문에서 드러납니다.

```bash
grep -n "JSONDecodeError" main.py    # → 131행 (손상 감지 지점)
ls state.json*                       # → 백업 파일이 없음 = 개선 여지 그 자체
```

**말할 것** — "지금은 **초기화까지**입니다. 개선한다면 임시 파일에 다 쓰고
[`os.replace()`](main.py#L148)로 교체하는 방식을 **가장 먼저** 넣겠습니다. 손상의 가장 흔한 원인인
'저장 도중 종료'를 원인 자체에서 없애기 때문입니다. (아직 미구현)"

**🔗 이동** — [↑ 목차](#toc) · [◀ 4-1](#q4-1) · [다음 4-3 ▶](#q4-3) · 관련: [3-3 try/except](#q3-3) · [2-4 안전 종료](#q2-4)


---

<a id="q4-3"></a>

## 4-3. "정답 채점 방식(점수 계산)"이나 "퀴즈 구조(선택지 개수 등)" 요구사항이 바뀐다면, 어떤 파일/클래스/메서드를 먼저 수정해야 하는지 설명할 수 있는가?

> **🎯 이 문항의 핵심** — **"요구사항이 바뀌면 어디를 고칠지 바로 짚을 수 있는가?"**
> *앞의 설계 답변이 진짜였는지 시험하는 최종 문제.*

**✅ 한 줄 답변** — 역할을 나눠 두었기 때문에 **요구사항 종류마다 수정 위치가 좁게 특정됩니다.**

**📌 현재 구조 기준 수정 위치**

| 바뀌는 요구사항 | 먼저 고칠 곳 | 안 건드려도 되는 곳 |
| --- | --- | --- |
| **점수 계산 방식** 변경 (난이도별 배점 등) | `QuizGame` → [`play_quiz()` 270행 계산식](main.py#L270), [`record_result()`](main.py#L279-L292) | `Quiz` 클래스 전체, 저장 로직 |
| **힌트 감점** 변경 | 상수 [`HINT_PENALTY`](main.py#L12) **한 줄** | 나머지 전부 |
| **선택지 개수** 4개 → 5개 | `Quiz` → [`display()`](main.py#L27-L35)·[`check()`](main.py#L37-L39), `QuizGame` → [`add_quiz()` 입력 루프](main.py#L183-L195)·[`ask_one()` 입력 범위](main.py#L223-L246), 상수 [`HINT_CHOICE`](main.py#L11), `state.json` 스키마 문서 | [`load_state()`](main.py#L119-L138) / [`save_state()`](main.py#L140-L151) |
| **저장 위치·형식** 변경 | [`load_state()`](main.py#L119-L138) / [`save_state()`](main.py#L140-L151) **2개 메서드** | 게임 진행 코드 전부 |

**🗣️ 이렇게 말하세요**

> "점수 계산은 `QuizGame`의 일이라 `play_quiz()`와 `record_result()`만 고치면 되고,
> `Quiz` 클래스는 건드릴 필요가 없습니다. 반대로 선택지 개수는 문제 카드의 성질이라
> `Quiz` 쪽과 입력받는 부분을 고칩니다.
> **요구사항 종류에 따라 수정 위치가 다르고 그 위치를 바로 짚을 수 있는 것**이
> 클래스로 역할을 나눈 가장 큰 이점이라고 생각합니다."

**⚠️ 솔직하게 덧붙일 한계** — 선택지 개수 변경이 가장 넓게 번집니다.
지금은 `4`와 힌트 번호 `5`가 여러 곳에 흩어져 있기 때문입니다.
`HINT_PENALTY`처럼 **선택지 개수도 상수 하나로 뽑아 한 곳에서 관리**하도록 개선하는 것이 맞습니다. (미구현)

**❓ 예상 추가 질문** — *"선택지 개수를 상수로 뽑으면 정확히 어디가 좋아지나요?"*
→ 지금은 `range(1, 5)`, `1-4` 안내 문구, 힌트 번호 `5`가 서로 맞물려 있어 한 곳만 고치면
안내 문구와 실제 범위가 어긋납니다. 상수 하나로 모으면 **그 한 줄만 바꿔도 전부 함께 따라옵니다.**

### 🎓 학습 노트 (4-3)

**📖 질문의 요지** — **"요구사항이 바뀌었을 때 어디를 고칠지 바로 짚을 수 있는가?"**
= 2-1, 2-2에서 나눈 설계가 **진짜 효과가 있는지 시험하는 최종 문제**입니다.

**🎯 왜 이걸 묻는가**

실무에서 요구사항은 **반드시** 바뀝니다. 그래서 "잘 돌아가는 코드"보다 **"고치기 쉬운 코드"** 가 중요합니다.

| 설계가 나쁘면 | 설계가 좋으면 |
| --- | --- |
| "전부 다시 봐야 합니다" | "**이 메서드 2개만** 고치면 됩니다" |
| 한 곳 고치면 딴 데가 깨짐 | 영향 범위가 **예측됨** |
| 같은 값이 여기저기 흩어져 있음 | **상수 한 줄**만 바꾸면 전부 반영 |

이 질문에 막힘없이 답하면 **앞의 설계 답변들이 진짜였다는 증명**이 됩니다.

**📚 공부해야 할 개념**

| 개념 | 쉬운 뜻 |
| --- | --- |
| **매직 넘버** | 코드에 그냥 박힌 숫자(`4`, `5`). **뜻을 알 수 없고 흩어짐** |
| **상수(constant)** | 뜻 있는 이름을 붙여 **맨 위 한 곳에** 모은 값 |
| **변경 영향 범위** | 한 곳을 고칠 때 **같이 고쳐야 하는 곳들** |
| **결합도** | 서로 얽힌 정도. 낮을수록 고치기 쉬움 |

> 📖 **상수와 매직 넘버를 더 자세히** → [부록 B-0.5 ② 상수](#terms-basic)

**💻 코드로 보는 좋은 예 / 나쁜 예**

```python
# 👍 좋은 예 — 상수로 뽑아둠. 힌트 감점을 10점으로 바꾸려면 이 한 줄만.
HINT_PENALTY = 5
score = max(0, round(correct / total * 100) - hint_count * HINT_PENALTY)
```

```python
# 👎 아쉬운 예 — 선택지 개수 4가 여러 곳에 흩어져 있음
for number in range(1, 5):                        # add_quiz — 보기 4개 입력
answer = self.input_int("정답 번호 (1-4): ", 1, 4)  # 숫자 4 + 안내 문구 "1-4"
f"정답 입력 (1-4, 힌트 보기는 {HINT_CHOICE}): "     # 여기도 "1-4"
```
→ 5개로 바꾸려면 **네 곳 이상**을 동시에 고쳐야 하고, 하나만 빠뜨리면 **안내 문구와 실제 범위가 어긋납니다.**

| 바뀌는 요구사항 | 고칠 곳 | 안 건드려도 되는 곳 |
| --- | --- | --- |
| 점수 계산 방식 | `play_quiz()`, `record_result()` | `Quiz` 전체, 저장 로직 |
| 힌트 감점 | [`HINT_PENALTY`](main.py#L12) **한 줄** | 나머지 전부 |
| 선택지 4→5개 | `Quiz.display()`, `check()`, `add_quiz()`, `ask_one()` | `load/save_state()` |
| 저장 방식 | `load_state()`, `save_state()` **2개** | 게임 진행 전부 |

**말할 포인트 3가지**
1. **요구사항 종류마다 고칠 위치가 다르다**는 걸 짚습니다 — 점수는 `QuizGame`, 선택지는 `Quiz`, 저장은 `load/save_state`.
2. **약점을 먼저 말합니다.** "선택지 개수 변경이 가장 넓게 번집니다. `4`와 `5`가 흩어져 있기 때문입니다." 스스로 아는 게 실력입니다.
3. **개선 방향까지 제시합니다** — `HINT_PENALTY`처럼 `CHOICE_COUNT = 4` 상수로 뽑으면 그 한 줄로 전부 따라옵니다. (미구현)

**🔍 검증 방법** — **좋은 예와 아쉬운 예를 명령 두 줄로 대비**시키는 게 가장 설득력 있습니다.

```bash
grep -n "HINT_PENALTY" main.py       # ① 좋은 예 — 상수로 모음
grep -n "1-4\|range(1, 5)" main.py   # ② 아쉬운 예 — 숫자가 흩어짐
```

| 명령 | 나와야 하는 결과 | 말할 것 |
| --- | --- | --- |
| ① | `12`(정의) + `237, 270, 274`(사용) | "감점을 10점으로 바꾸려면 **12행 한 줄**만" |
| ② | `189: range(1, 5)` / `191: "정답 번호 (1-4): ", 1, 4` / `230: "정답 입력 (1-4...` | "선택지를 5개로 늘리려면 **여기 전부**를 동시에" |

**직접 실험해 보기** — ①이 진짜 한 줄인지 확인합니다.

```bash
cp main.py main.py.bak
sed -i '' 's/^HINT_PENALTY = 5/HINT_PENALTY = 10/' main.py
grep -n "HINT_PENALTY = " main.py     # → 10으로 바뀜
python3.11 main.py                     # 퀴즈 풀며 힌트 사용 → 10점 차감 확인
mv main.py.bak main.py                 # 원상복구 ← 잊지 마세요
```

**수정 위치 요약**

| 바뀌는 요구사항 | 먼저 볼 곳 |
| --- | --- |
| 점수 계산 | [`play_quiz()` 270행](main.py#L270) · [`record_result()`](main.py#L279-L292) |
| 힌트 감점 | [`HINT_PENALTY` 12행](main.py#L12) **한 줄** |
| 선택지 개수 | [`Quiz`](main.py#L18-L58) · [`add_quiz()`](main.py#L183-L195) · [`ask_one()`](main.py#L223-L246) |
| 저장 방식 | [`load_state()`](main.py#L119-L138) · [`save_state()`](main.py#L140-L151) |

**🔗 이동** — [↑ 목차](#toc) · [◀ 4-2](#q4-2) · 관련: [2-1 책임 분리](#q2-1) · [2-2 함수 분리](#q2-2) · [부록 E 명령 모음](#appendix-e)


---

> **[항목 4] (PASS / FAIL)** — 3문항 모두 "현재 구현 / 개선 방법"을 구분해 설명 가능

---

<a id="appendix-a"></a>

# 부록 A. 그림 한 장으로 보는 내 프로그램

```
        사용자 (키보드 입력)
              │
              ▼
   ┌─────────────────────┐      "사회자" 역할
   │      QuizGame       │  메뉴 보여주기, 게임 진행,
   │  (게임 전체 관리자)  │  점수 계산, 저장/불러오기
   └──────┬───────┬──────┘
          │       │
    보여주고 채점  읽고 쓰기
          │       │
          ▼       ▼
   ┌──────────┐  ┌─────────────┐
   │   Quiz   │  │ state.json  │   "공책" 역할
   │(문제 카드)│  │  (저장 파일) │  퀴즈·최고점수·기록을
   │문제/보기/ │  │             │  적어두는 곳
   │정답/힌트  │  └─────────────┘
   └──────────┘
```

```
프로그램 시작
  → state.json 읽기 (없으면 기본 퀴즈 5개 / 깨졌으면 안내 후 복구)
  → 메뉴 반복: [1]풀기 [2]추가 [3]목록 [4]삭제 [5]점수 [6]종료
  → 데이터가 바뀌는 순간마다 state.json에 저장
  → 종료 (Ctrl+C로 꺼도 저장하고 종료)
```

---

<a id="appendix-b"></a>

# 부록 B. 용어 미니 사전

### B-00. 코드에 나오는 기호 읽는 법 (가장 먼저)

코드는 **글자보다 기호가 더 헷갈립니다.** 이 문서의 코드 블록에 나오는 기호를 전부 풀어씁니다.

| 기호 | 읽는 법 | 뜻 | 예 |
| --- | --- | --- | --- |
| `#` | 샵 / 우물 정 | **주석** — 파이썬이 **무시하는** 설명글 | `x = 1  # 여긴 안 읽힘` |
| `:` | 콜론 | "**이 아래는 내 것**"이라는 선언 | `if x == 1:` |
| **들여쓰기** | 띄어쓰기 4칸 | `:` 아래 **소속된 코드**임을 표시 | 아래 예 참고 |
| `=` | 이퀄 (**넣기**) | 오른쪽 값을 왼쪽에 **저장** | `score = 100` |
| `==` | 이퀄이퀄 (**비교**) | 두 값이 **같은지 묻기** (참/거짓) | `if score == 100:` |
| `.` | 점 / 닷 | "**~의**" — 소속 표시 | `quiz.answer` = 퀴즈**의** 정답 |
| `()` | 괄호 | **실행 버튼** + 재료 넣는 자리 | `run()` |
| `""` | 따옴표 | 안의 것은 **글자**라는 표시 | `"안녕"` |
| `f"..."` | 에프 스트링 | 글자 안에 **값을 끼워 넣기** | `f"{총점}점"` → `100점` |
| `_` | 언더바 | 단어 사이 **띄어쓰기 대용** | `input_int` = input int |
| `__` | 언더바 2개 | 파이썬이 **특별 취급**하는 이름 | `__init__` |
| `...` | 말줄임 | (문서에서만) **생략했다**는 표시 | 실제 코드엔 없음 |
| `←` `→` | 화살표 | (문서에서만) 설명을 가리킴 | 실제 코드엔 없음 |
| `1️⃣` | 번호 | (문서에서만) 실행 순서 표시 | 실제 코드엔 없음 |

**괄호 4형제 — 모양마다 이름과 역할이 다릅니다**

| 기호 | 이름 | 파이썬에서 하는 일 | 예 |
| --- | --- | --- | --- |
| `( )` | **소괄호** | **실행**하기 / 함수·메서드 | `run()`, `print("안녕")` |
| `[ ]` | **대괄호** | **리스트** — 순서대로 줄 세우기 | `["( )", "[ ]", "{ }"]` |
| `{ }` | **중괄호** | **딕셔너리** — 이름표로 찾기 | `{"answer": 2}` |
| `< >` | 꺾쇠괄호 | 파이썬에선 **크다/작다** 비교 | `if number < low:` |

> 💡 **"소·중·대"는 크기 순서**입니다. 작을 소 → 가운데 중 → 클 대.
> 모양이 헷갈리면 **`{`는 중간이 뾰족**하다고 기억하세요.

**리스트(`[]`)와 딕셔너리(`{}`)를 왜 나눠놨나**

```python
점수 = [90, 80, 70]              # 대괄호 → 번호로 꺼냄
점수[0]                          # → 90 (0번째)

점수 = {"국어": 90, "수학": 80}   # 중괄호 → 이름으로 꺼냄
점수["국어"]                      # → 90
```

성적표를 생각해 보세요. `[90, 80, 70]`만 있으면 **몇 번째가 무슨 과목인지 외워야** 합니다.
`{"국어": 90}`은 이름표가 붙어 있어 헷갈릴 일이 없죠. 대신 **순서가 중요할 땐** 리스트가 편합니다.
그래서 파이썬은 **둘 다** 만들어 두고 상황에 맞게 고르게 했습니다.

이 프로젝트에서는 [`state.json`](state.json)이 두 개를 **겹쳐서** 씁니다.

```json
"quizzes": [ { "question": "...", "answer": 2 } ]
            ↑                                  ↑
         대괄호 = 퀴즈 여러 개              중괄호 = 퀴즈 한 개의 칸들
```

**따옴표 3형제 — "이건 글자다"라는 표시**

| 기호 | 이름 | 쓰임 |
| --- | --- | --- |
| `" "` | **큰따옴표** | 글자 묶기 (이 프로젝트의 기본) |
| `' '` | **작은따옴표** | 글자 묶기 — 파이썬에선 큰따옴표와 **똑같음** |
| `""" """` | 삼중따옴표 | **여러 줄** 글자 / 설명문(독스트링) |
| `` ` `` | **역따옴표** | ⚠️ 파이썬에선 **안 씀**. 터미널·마크다운 전용 |

**왜 따옴표가 필요한가** — 컴퓨터는 `bool`을 보고 *"자료형 이름인가, 그냥 글자인가"* 를 구분할 수 없습니다.

```python
bool      # 따옴표 없음 → 파이썬의 자료형 (명령어)
"bool"    # 따옴표 있음 → 그냥 네 글자짜리 글자
3         # 숫자 3 → 계산 가능
"3"       # 글자 3 → 계산 불가 (그래서 int("3")으로 바꿔야 함)
```

따옴표는 **"여기서부터 여기까지는 글자입니다"** 라고 울타리를 치는 기호입니다.

> ⚠️ **역따옴표(`` ` ``)를 조심하세요.** 터미널에서 명령 뒤에 실수로 붙으면
> `bquote>` 가 뜨면서 멈춥니다. 그때는 **`control` + `C`** 로 빠져나오면 됩니다.
> (이 문서의 회색 글씨 표시에 쓰는 기호라, 복사할 때 딸려 들어가기 쉽습니다.)

**키보드에서 치는 법**

| 기호 | 누르는 키 |
| --- | --- |
| `[ ]` | `P` 오른쪽 두 개 키 |
| `{ }` | **`shift`** + 위의 그 키들 |
| `( )` | **`shift`** + `9` , `0` |
| `" "` | **`shift`** + `'` (Enter 왼쪽) |
| `` ` `` / `~` | 왼쪽 맨 위 (`1` 왼쪽) / 거기에 `shift` |

> 💡 [기본 퀴즈 1번](main.py#L65-L69)이 바로 이 문제입니다 — *"리스트를 만들 때 쓰는 괄호는?"* → **`[ ]` 대괄호**

> ⚠️ **`=` 와 `==` 는 완전히 다릅니다.** 가장 흔한 초보 실수입니다.
> - `answer = 2` → "answer에 2를 **넣어라**" (명령)
> - `answer == 2` → "answer가 2와 **같니?**" (질문 → `True`/`False`)
>
> **왜 굳이 다르게 만들었나** — 컴퓨터는 "넣기"와 "묻기"를 구분해야 합니다.
> 사람 말로는 둘 다 "같다"지만, 컴퓨터에겐 **명령**과 **질문**은 전혀 다른 일이기 때문입니다.

**`self.`가 붙는 이유** — 가장 많이 나오는데 가장 안 와닿는 기호입니다.

```python
class Quiz:
    def __init__(self, question, answer):
        self.question = question   # ← self. 를 붙여야 "이 붕어빵의" 값이 됨
        self.answer = answer
```

`Quiz`라는 **틀**로 퀴즈를 5개 찍어내면 붕어빵 5개가 생깁니다.
그런데 `question = question`이라고만 쓰면 **누구의 질문인지** 알 수 없습니다.
`self.question`은 **"지금 이 붕어빵의 질문"** 이라는 뜻이라, 5개가 각자 자기 값을 갖게 됩니다.

| 쓰는 위치 | 모양 | 뜻 |
| --- | --- | --- |
| 클래스 **안**에서 | `self.answer` | **이** 객체의 answer |
| 클래스 **밖**에서 | `quiz.answer` | **저** quiz의 answer |

→ 같은 것을 안에서 부르면 `self.`, 밖에서 부르면 `객체이름.` 입니다.

**들여쓰기가 문법인 이유** — 다른 언어는 `{ }`로 묶지만 파이썬은 **띄어쓰기로 묶습니다.**

```python
while True:
    self.show_menu()      # 4칸 들여씀 → while 안에 소속 (반복됨)
    choice = ...          # 4칸 → 반복됨

print("끝")               # 안 들여씀 → while 밖 (반복 안 됨, 딱 1번)
```

**왜 그런가** — 파이썬은 **"보기 좋은 코드가 곧 올바른 코드"** 를 목표로 만든 언어입니다.
괄호로 묶으면 들여쓰기를 대충 해도 돌아가서 사람이 읽기 어려워집니다.
그래서 아예 **들여쓰기 자체를 문법으로 삼아** 눈에 보이는 모양과 실제 동작을 일치시켰습니다.

### B-0. 기초 중의 기초

| 용어 | 쉬운 비유 | 정확한 뜻 | 예 |
| --- | --- | --- | --- |
| **정수(int)** | 손가락으로 셀 수 있는 수 | 소수점이 **없는** 수 | `1`, `2`, `-7`, `100` |
| **실수(float)** | 소수점이 붙은 수 | 소수점이 **있는** 수 | `1.5`, `3.14` |
| **문자열(str)** | 따옴표로 묶은 **글자** | 글자들의 나열 | `"안녕"`, `"3"` ← 이건 글자 3 |
| **불린(bool)** | **예 / 아니오** 스위치 | 참(`True`)·거짓(`False`) 두 값만 | `True`, `False` |
| **변수** | 값을 담는 **상자** | 값에 이름을 붙여 저장한 것 | `score = 100` |
| **함수** | **자판기** (넣으면 나옴) | 이름 붙여둔 기능 한 덩어리 | `input_int(...)` |
| **`()`** | **실행 버튼** | 함수·메서드를 실제로 실행 | `run()` |
| **`[ ]`** | 순서대로 줄 세우기 | 리스트 | `["( )", "[ ]"]` |
| **`{ }`** | 이름표로 찾기 | 딕셔너리 | `{"answer": 2}` |

> 💡 **`"3"`과 `3`은 다릅니다.** 앞은 **글자** 3, 뒤는 **숫자** 3입니다.
> `input()`으로 받은 값은 항상 **글자**라서, 계산·비교를 하려면 `int("3")`으로 **정수로 바꿔야** 합니다.
> 이게 [`input_int()`](main.py#L153-L172)가 하는 일의 절반입니다.

<a id="terms-basic"></a>

### B-0.5. 정수·상수·함수 — 자주 나오는 3가지 자세히

이 세 단어는 문서 곳곳에 나오는데 뜻이 헷갈리기 쉽습니다. **역할이 서로 다릅니다.**

| 무엇 | 한 줄 뜻 | 비유 |
| --- | --- | --- |
| **정수** | 값의 **종류** (소수점 없는 수) | 담긴 **내용물** |
| **상수** | 안 바뀌는 값에 붙인 **이름** | 내용물에 붙인 **고정 이름표** |
| **함수** | 이름 붙여둔 **기능 한 덩어리** | 내용물을 다루는 **기계** |

---

#### ① 정수(int) — "값의 종류"

소수점이 **없는** 딱 떨어지는 수입니다. `1`, `2`, `-7`, `100`.

| 종류 | 영어 | 예 | 이 프로젝트에서 |
| --- | --- | --- | --- |
| **정수** | int**eger** | `2`, `100` | 정답 번호, 메뉴 번호, 점수 |
| 실수 | float | `3.14` | (안 씀) |
| 문자열 | str**ing** | `"안녕"` | 문제, 보기, 힌트 |
| 불린 | bool**ean** | `True` | 정답 여부 |

**왜 중요한가 — `3`과 `"3"`은 다릅니다.**

```python
3 == 3        # True   숫자끼리 비교
"3" == 3      # False  글자와 숫자는 다름!
```

키보드로 입력받은 값은 **무조건 글자**입니다. 그래서 비교하려면 숫자로 바꿔야 합니다.

```python
value = input("선택: ")     # "3"  ← 글자
number = int(value)         # 3    ← 정수로 변환
```

**이게 [`input_int()`](main.py#L153-L172)라는 이름의 뜻입니다** — `input`(입력받다) + `int`(정수) =
**"글자로 들어온 걸 정수로 바꿔서 돌려주는 함수"**. 변환에 실패하면 `ValueError`가 납니다.

```python
int("3")     # 3      성공
int("abc")   # ValueError!  ← 그래서 try/except로 감쌌습니다
```

📂 [main.py:163](main.py#L163) · 관련 문항 [1-2번](#q1-2)

---

#### ② 상수(constant) — "안 바뀌는 값에 붙인 이름"

한 번 정하면 프로그램이 도는 동안 **바뀌지 않는 값**입니다.
파이썬은 문법으로 강제하지 않아서, **전부 대문자로 쓰는 약속**으로 표시합니다.

```python
HINT_PENALTY = 5   # ← 대문자 = "이건 상수다, 건드리지 마라"
hint_count = 0     # ← 소문자 = 계속 바뀌는 보통 변수
```

**왜 쓰는가 — 숫자만 덜렁 있으면 두 가지가 문제입니다.**

| 문제 | 상수 없이 | 상수를 쓰면 |
| --- | --- | --- |
| **뜻을 모름** | `score - hint * 5` — 5가 뭐지? | `score - hint * HINT_PENALTY` — 힌트 감점이구나 |
| **흩어짐** | 5가 코드 여기저기 → 하나 빠뜨리면 어긋남 | **맨 위 한 줄**만 고치면 전부 따라옴 |

> 코드에 뜻 없이 박힌 숫자를 **매직 넘버**(magic number)라고 부릅니다. "왜 5인지 아무도 모르는 마법의 숫자"라는 뜻이라, 좋은 말이 아닙니다.

이 프로젝트의 상수는 3개입니다.

| 상수 | 값 | 뜻 |
| --- | --- | --- |
| [`HINT_CHOICE`](main.py#L11) | `5` | 힌트를 요청하는 입력 번호 |
| [`HINT_PENALTY`](main.py#L12) | `5` | 힌트 1회당 차감 점수 |
| [`STATE_FILE`](main.py#L15) | 경로 | 저장 파일 위치 |

> 💡 **`HINT_CHOICE`와 `HINT_PENALTY`가 둘 다 5인 게 포인트**입니다.
> 숫자만 봤다면 "같은 5니까 하나로 합치자"는 실수를 할 수 있습니다.
> **뜻이 다르면 이름도 달라야** 합니다 — 나중에 감점만 10점으로 바꿔도 힌트 번호는 5번 그대로여야 하니까요.

📂 [main.py:11-15](main.py#L11-L15) · 관련 문항 [4-3번](#q4-3)

---

#### ③ 함수(function) — "이름 붙여둔 기능 한 덩어리"

자주 쓰는 일을 **한 번 만들어 이름 붙여두고** 필요할 때마다 부르는 것입니다.

```python
def input_int(self, prompt, low, high):   # def = define(정의하다)
    ...                                    # 하는 일
    return number                          # 결과를 돌려줌
```

| 부분 | 뜻 | 비유 (자판기) |
| --- | --- | --- |
| `def` | "지금부터 함수를 **정의**한다" | 자판기를 **만듦** |
| `input_int` | **이름** | 자판기 이름표 |
| `(prompt, low, high)` | **매개변수** — 받는 재료 | 넣는 동전 |
| `return number` | **돌려주는 값** | 나오는 음료 |
| `input_int("선택: ", 1, 6)` | **호출** — 실제 실행 | 버튼 누르기 |

**왜 쓰는가 — 같은 코드를 여러 번 쓰지 않기 위해서입니다.**

이 프로젝트에서 숫자를 받는 곳은 **5군데**인데, 검증 코드는 **1곳뿐**입니다.

```
정의 1곳: main.py:153
호출 5곳: 191, 216, 229, 256, 312
```

만약 복사해서 5번 붙였다면 —

| 상황 | 복사했다면 | 함수로 만들면 |
| --- | --- | --- |
| 경고 문구 수정 | **5곳** 전부 수정 | **1곳** |
| 하나 빠뜨림 | 거기서만 다르게 동작 (찾기 어려움) | 그럴 일 없음 |
| 새 규칙 추가 | 5곳에 똑같이 추가 | 1곳 |

**함수 / 메서드 / 클래스 차이**

| 이름 | 뜻 | 예 |
| --- | --- | --- |
| **함수** | 클래스 **밖**에 있는 기능 | [`default_quizzes()`](main.py#L61-L94), `print()` |
| **메서드** | 클래스 **안**에 있는 함수 | [`show_menu()`](main.py#L105-L117), `quiz.check()` |
| **클래스** | 데이터 + 메서드를 묶은 **설계도** | [`Quiz`](main.py#L18-L58), [`QuizGame`](main.py#L97-L326) |

→ **메서드는 "클래스에 소속된 함수"** 일 뿐, 본질은 같습니다.
소속이 있으니 `self.`나 `quiz.`처럼 **점(`.`)을 찍어 부릅니다.**

📂 [main.py:153](main.py#L153) · 관련 문항 [2-2번](#q2-2) · [3-1번](#q3-1)

---

#### 한눈에 비교

```python
HINT_PENALTY = 5                     # 상수 — 안 바뀌는 값에 붙인 이름 (대문자)

def input_int(self, prompt, low, high):   # 함수 — 기능 한 덩어리
    number = int(value)                   # 정수 — 값의 종류
    return number
```

| | 정수 | 상수 | 함수 |
| --- | --- | --- | --- |
| **정체** | 값의 종류 | 이름 붙인 값 | 기능 덩어리 |
| **표시** | `3` (따옴표 없음) | `대문자` | `이름()` |
| **바뀌나** | — | 안 바뀜 | — |
| **이 문서 관련 문항** | [1-2](#q1-2) | [4-3](#q4-3) | [2-2](#q2-2) |

### B-1. 이 프로젝트 용어

| 용어 | 쉬운 비유 | 정확한 뜻 |
| --- | --- | --- |
| **클래스(class)** | 붕어빵 **틀** | 데이터와 기능을 묶어 정의한 설계도 |
| **객체/인스턴스** | 틀로 찍은 **붕어빵 하나** | 클래스로 실제 만들어진 값 |
| **`__init__`** | 붕어빵에 **팥 채우는 순간** | 객체가 만들어질 때 자동 실행되어 속성을 채움 |
| **`self`** | "**이** 붕어빵" 이라는 이름표 | 지금 이 객체 자신을 가리키는 이름 |
| **속성(attribute)** | 객체가 **가진 정보** | 객체에 저장된 값 |
| **메서드(method)** | 객체가 **할 수 있는 행동** | 클래스 안에 정의된 함수 |
| **역할과 책임 분리** | 사회자와 문제 카드가 **하는 일이 다름** | 바뀌는 이유가 다른 코드를 따로 나누는 설계 원칙 |
| **입력값 검증** | 놀이기구 **키 재는 곳** | 넣은 값이 다룰 수 있는 값인지 미리 확인 |
| **`try/except`** | 사고 나도 안 다치는 **안전망** | 실패해도 프로그램을 죽이지 않고 정한 대응을 함 |
| **JSON** | 세계 공통 **메모지 형식** | `{"키": 값}` 짝으로 데이터를 적는 텍스트 표준 |
| **데이터 영속성** | 공책에 **적어두기** | 프로그램을 꺼도 데이터가 남아 있는 성질 |
| **JSON 파싱** | 메모를 **읽어서 이해하기** | 텍스트를 프로그램이 다룰 값으로 해석하는 일 |
| **스키마** | 공책의 **양식(칸 이름)** | 어떤 칸에 어떤 종류의 값이 들어가는지 정한 약속 |
| **`KeyboardInterrupt`** | **Ctrl+C로 강제 중단** | 사용자가 중단했을 때 발생하는 예외 |
| **`EOFError`** | 입력이 **더 이상 없음** | 입력이 끝났을 때 발생하는 예외 |
| **커밋(commit)** | 게임의 **세이브 포인트** | 그 시점의 변경 내용을 이력으로 기록 |
| **브랜치(branch)** | 원본 두고 실험하는 **옆 책상** | main과 분리된 독립 작업선 |
| **병합(merge)** | 옆 책상 결과를 **원본에 합치기** | 두 브랜치의 변경을 하나로 통합 |
| **clone** | 저장소 **통째로 복사** | 원격 저장소를 로컬로 새로 내려받기 |
| **pull** | 바뀐 것 **받아오기** | 원격의 새 커밋을 내 로컬로 가져와 합치기 |
| **반복문 `while`** | "**될 때까지 다시**" | 조건이 참인 동안 같은 코드를 되풀이 |
| **`while True`** | "**끝낼 때까지 계속**" | 조건이 항상 참이라 끝없이 반복 |
| **`break`** | "**이제 그만**" | 반복문을 깨고 밖으로 나감 |
| **`continue`** | "**다시 줄 서세요**" | 이번 차례만 건너뛰고 반복문 처음으로 |
| **`return`** | "**결과 가져가세요**" | 값을 돌려주고 함수를 끝냄 |
| **매직 넘버** | 정체불명의 **맨숫자** | 뜻을 알 수 없이 코드에 박힌 숫자 (`4`, `5`) |
| **상수** | 맨 위에 붙인 **이름표** | 뜻 있는 이름으로 한 곳에 모아둔 고정값 |
| **방어적 프로그래밍** | **안전벨트 먼저** | 사용자가 실수할 것을 전제하고 미리 막는 태도 |
| **리팩터링** | 겉은 그대로, **속만 정리** | 동작을 바꾸지 않고 코드 구조만 개선 |

---

<a id="appendix-c"></a>

# 부록 C. 마지막 1분 요약

| 질문 유형 | 한 줄 답 |
| --- | --- |
| 왜 클래스 2개? | `Quiz`는 문제 카드, `QuizGame`은 사회자 — 바뀌는 이유가 다르니까 |
| 왜 함수를 나눴나? | 같은 검증이 6곳에 필요해서. 한곳에 모아야 한 번만 고침 |
| 파일은 언제 읽고 쓰나? | 시작할 때 1번 읽고, 바뀔 때마다 즉시 씀 |
| 왜 JSON? | 사람이 읽을 수 있고, 파이썬 dict와 모양이 같아 변환이 두 줄 |
| 왜 try/except? | 파일은 내 코드 밖 세상 — 없거나 깨져도 게임은 돌아가야 하니까 |
| 왜 브랜치? | `main`은 항상 멀쩡한 원본, 실험은 옆 책상에서 |
| 왜 기능 단위 커밋? | `git log`가 개발 일기가 되고, 문제 생기면 그 지점으로 돌아갈 수 있어서 |
| 왜 정답을 번호로? | 글자로 저장하면 띄어쓰기 하나에도 오답 처리되니까 |
| 1000개로 커지면? | 전체 읽고 전체 쓰는 한계 → SQLite로 (미구현) |
| 파일이 깨지면? | 지금은 초기화까지. 백업·임시파일 교체·손상본 보존이 개선 방향 (미구현) |
| 요구사항 바뀌면? | 점수는 `QuizGame`, 선택지는 `Quiz`, 저장은 `load/save_state` |

---

<a id="appendix-d"></a>

# 부록 D. 평가 전 반드시 준비할 것

### D-1. 실행해 볼 명령어

```bash
git log --oneline --graph --all --decorate   # 1-5, 1-6 증거
git rev-list --count HEAD                    # → 18
python3.11 --version                         # → Python 3.11.15 (3.10 이상 확인)
rm state.json && python3.11 main.py          # 1-4 기본 퀴즈 5개 시연
```

### D-2. 스크린샷 촬영 — 9장, 30분이면 끝납니다

> 과제에서 **명시적으로 요구한 것**과 평가문항 보강용을 합쳐 9장입니다.

| 파일명 | 무엇을 찍나 | 근거 | 필수도 |
| --- | --- | --- | --- |
| `dev_env.png` | VSCode + Python 버전 + Git 설정 | 과제 제출물 | 🔴 필수 |
| `menu.png` | 메뉴 화면 | README 체크리스트 | 🔴 필수 |
| `add_quiz.png` | 퀴즈 추가 | 과제 제출물 | 🔴 필수 |
| `list.png` | 퀴즈 목록 | 과제 제출물 | 🔴 필수 |
| `play.png` | 퀴즈 풀기 (정답·오답) | 과제 제출물 | 🔴 필수 |
| `score.png` | 점수 확인 | 과제 제출물 | 🔴 필수 |
| `git_graph.png` | `git log --oneline --graph` | 과제 제출물 | 🔴 필수 |
| `clone_pull.png` | clone → push → pull 터미널 | 평가 1-7 | 🔴 필수 |
| `invalid_input.png` | 잘못된 입력 4종 | 평가 1-2 보강 | 🟡 권장 |

---

#### 📸 맥에서 화면 캡처하는 법

| 단축키 | 하는 일 |
| --- | --- |
| `Cmd` + `Shift` + `4` | 십자 커서 → **드래그한 영역**만 캡처 |
| `Cmd` + `Shift` + `4` → `Space` → 클릭 | **창 하나**를 통째로 캡처 |
| `Cmd` + `Shift` + `5` | 캡처 도구 열기 (저장 위치 변경 가능) |

찍으면 **바탕화면**에 `스크린샷 2026-08-08 오후 4.30.00.png` 같은 이름으로 저장됩니다.

#### 🛠️ 이름 바꾸고 옮기기 — `shot.sh` 사용

바탕화면에서 파일을 찾아 드래그할 필요 없습니다. 터미널에서 한 줄이면 됩니다.

```bash
./shot.sh menu       # 방금 찍은 캡처 → docs/screenshots/menu.png
./shot.sh            # 이름 없이 실행 → 진행 상황 보여줌
```

```
📸 스크린샷 진행 상황
────────────────────────────────
  ✅ menu.png
  ⬜ add_quiz.png   ← 아직
  ...
  1 / 9 완료
```

---

#### 순서대로 따라 하기

**① `dev_env.png` — 개발 환경**

터미널에 아래를 치고, **VSCode 창 전체**가 보이게 캡처합니다.

```bash
clear
python3.11 --version
git --version
git config user.name
git config user.email
```
> 📸 `Cmd+Shift+4` → 왼쪽 파일탐색기 + 터미널이 **함께** 보이도록 드래그
> 그다음 `./shot.sh dev_env`

---

**② `menu.png` — 메뉴 화면**

```bash
clear
python3.11 main.py
```
> 📸 메뉴 6줄과 `선택:` 이 보이는 상태에서 캡처 → `./shot.sh menu`
> ⚠️ 캡처 후 `Ctrl+C`로 나오지 말고 **그대로 두세요.** ③④⑤⑥에서 계속 씁니다.

---

**③ `add_quiz.png` — 퀴즈 추가**

`선택:` 에서 `2` 를 누르고 순서대로 입력합니다.

| 화면 | 치는 것 |
| --- | --- |
| `문제를 입력하세요:` | `파이썬에서 문자열을 만들 때 쓰는 기호는?` |
| `선택지 1:` ~ `4:` | `대괄호` / `따옴표` / `중괄호` / `소괄호` |
| `정답 번호 (1-4):` | `2` |

> 📸 `✅ 퀴즈가 추가되었습니다!` 까지 보이게 캡처 → `./shot.sh add_quiz`

---

**④ `list.png` — 퀴즈 목록**

`선택:` 에서 `3`

> 📸 `📋 등록된 퀴즈 목록 (총 6개)` 와 문제 6줄이 보이게 → `./shot.sh list`

---

**⑤ `play.png` — 퀴즈 풀기 (정답과 오답 **둘 다**)**

`선택:` 에서 `1` → 문제 수 `2` → 첫 문제는 **일부러 틀리게**, 둘째 문제는 **맞게**

> 📸 `❌ 오답입니다!` 와 `✅ 정답입니다!` 가 **한 화면에** 보이게 캡처 → `./shot.sh play`
> ⚠️ 정답만 맞히면 안 됩니다. **오답 판정도 되는지**가 평가 항목([1-2](#q1-2))입니다.

---

**⑥ `score.png` — 점수 확인**

`선택:` 에서 `5`

> 📸 `🏆 최고 점수` 와 `📜 최근 기록` 이 보이게 → `./shot.sh score`

---

**⑦ `invalid_input.png` — 잘못된 입력 4종** 🟡

`선택:` 에서 **연달아** `abc` → `0` → `9` → 그냥 Enter

> 📸 ⚠️ 경고 4줄이 **한 화면에** 보이게 → `./shot.sh invalid_input`
> 그다음 `6` 으로 프로그램 종료

---

**⑧ `git_graph.png` — 브랜치 그래프**

```bash
clear
git log --oneline --graph --all --decorate
```

> 📸 `|\` 로 갈라지고 `|/` 로 합쳐지는 부분이 보이게 → `./shot.sh git_graph`

---

**⑨ `clone_pull.png` — clone → push → pull** 🔴 **가장 중요 (1-7)**

⚠️ **먼저 로컬 변경사항을 커밋·푸시해 두세요.** 안 그러면 마지막 `pull`에서 충돌납니다.

```bash
clear
# (1) 다른 폴더에 통째로 복사 — clone
cd ~/Desktop
git clone https://github.com/ellysuh22/2-homework.git clone-test
cd clone-test

# (2) 복사본에서 한 줄 고치고 올리기 — add / commit / push
echo "" >> README.md
echo "<!-- clone 실습: $(date +%Y-%m-%d) -->" >> README.md
git add README.md
git commit -m "Docs: clone 실습 기록 추가"
git push

# (3) 원래 폴더로 돌아와 받아오기 — pull
cd ~/Documents/2-homework
git pull
```

> 📸 **(1)~(3)이 한 화면에 다 보이도록** 캡처 → `./shot.sh clone_pull`
> 화면이 넘치면 **두 번 나눠 찍고** `clone_pull.png` / `clone_pull2.png` 로 저장해도 됩니다.
>
> 실습이 끝나면 연습용 폴더는 지웁니다: `rm -rf ~/Desktop/clone-test`

---

#### ✅ 마무리 — 전부 찍었는지 확인하고 올리기

```bash
cd ~/Documents/2-homework
./shot.sh                    # 9/9 완료인지 확인
git add docs/screenshots/
git commit -m "Docs: 실행 화면 및 Git 이력 스크린샷 추가"
git push
```

> 💡 **Git 기초 7종 명령**(`init` `add` `commit` `push` `pull` `checkout` `clone`)도 과제 요구사항인데,
> ⑨번 실습으로 `clone` · `push` · `pull` 이 채워지고 나머지는 이미 이력에 있습니다.
> (`checkout`은 `git reflog`의 `checkout: moving from feature/play-quiz to main` 에서 확인됩니다.)

### D-3. 개발 환경 주의 사항 ⚠️

현재 이 컴퓨터의 기본 `python3`는 **3.9.6**이고, 미션 요구는 **3.10 이상**입니다.
(3.11.15가 `python3.11`로 따로 설치되어 있으며, 코드는 두 버전 모두에서 정상 동작합니다.)

**시연·스크린샷 촬영 시에는 반드시 3.10 이상으로 실행하세요.**

```bash
python3.11 --version    # Python 3.11.15
python3.11 main.py
```

---

<a id="appendix-e"></a>

# 부록 E. 검증 명령 한눈에 보기

> 문항 번호를 누르면 그 문항의 **🔍 검증 방법**으로 이동합니다.
> ⚠️ 표시가 있는 것은 **파일을 되돌리는 마지막 줄까지 꼭 실행**하세요.

### E-1. 준비 (평가 시작 전 1회)

```bash
cd /Users/ellysuh/Documents/2-homework
python3.11 --version          # Python 3.11.15  ← 3.10 이상 확인
cp state.json state.json.SAFE # 전체 백업 (실습 중 사고 대비)
```

### E-2. 프로그램 실행으로 확인하는 것

| 문항 | 명령 / 입력 | 나와야 하는 결과 |
| --- | --- | --- |
| [1-1](#q1-1) | `python3.11 main.py` → `3` → `1` → `6` | 기능 후 **매번 메뉴 복귀**, `6`에서 종료 |
| [1-2](#q1-2) | 메뉴에서 `abc` `0` `9` Enter ` 3 ` | 4종 경고 출력 + **안 죽음** |
| [1-3](#q1-3) | `2`로 추가 → `6` → 재실행 | `📂 저장된 데이터를 불러왔습니다.` |
| [2-4](#q2-4) | 실행 후 `Ctrl+C` | `⚠️ 입력이 중단되었습니다...` (Traceback 없음) |
| [2-4](#q2-4) | `python3.11 main.py < /dev/null` | 같은 안내 후 정상 종료 |

### E-3. 파일을 건드리는 확인 ⚠️ 되돌리기 필수

```bash
# [1-4] 기본 퀴즈 5개
mv state.json state.json.bak && python3.11 main.py   # 📂 ...기본 퀴즈로 시작합니다. (퀴즈 5개)
mv state.json.bak state.json                          # ← 되돌리기

# [3-3][4-2] 파일 손상 복구
cp state.json state.json.bak && echo '{ 깨진 내용' > state.json
python3.11 main.py                                    # ⚠️ 저장 파일이 손상되어... (확인만 하고 6으로 종료)
mv state.json.bak state.json                          # ← 되돌리기

# [4-3] 상수 한 줄로 동작이 바뀌는지
cp main.py main.py.bak && sed -i '' 's/^HINT_PENALTY = 5/HINT_PENALTY = 10/' main.py
python3.11 main.py                                    # 힌트 사용 시 10점 차감
mv main.py.bak main.py                                # ← 되돌리기
```

### E-4. git으로 확인하는 것

| 문항 | 명령 | 나와야 하는 결과 |
| --- | --- | --- |
| [1-5](#q1-5) | `git rev-list --count HEAD` | `18` |
| [1-5](#q1-5) | `git log --oneline` | 18줄 전부 `종류:` 접두어 |
| [1-6](#q1-6) | `git log --merges --oneline` | `2d45928 Merge: ...` 1줄 |
| [1-6](#q1-6) | `git show --stat 2d45928 \| head -5` | `Merge: ff3840e ec2775c` ← **부모 2개** |
| [3-4](#q3-4) | `git log --oneline --graph --all --decorate` | `\|\` 갈라짐 → `\|/` 합쳐짐 |
| [2-5](#q2-5) | `git log --oneline --reverse` | 개발 순서대로 보임 |
| [1-7](#q1-7) | `git log --oneline -- README.md` | `3bf2517 Docs: clone 실습...` |

### E-5. grep으로 코드 구조를 증명하는 것

설명형 문항([항목 2]·[항목 3]·[항목 4])은 **말로만 하지 말고 이 명령을 같이 보여주세요.**

| 문항 | 명령 | 나와야 하는 결과 | 증명하는 것 |
| --- | --- | --- | --- |
| [2-1](#q2-1) | `grep -n "^class" main.py` | `18:class Quiz:` `97:class QuizGame:` | 클래스 2개로 역할 분리 |
| [2-2](#q2-2) | `grep -n "input_int(" main.py` | 정의 1 + 호출 5 | 검증 코드가 **한 곳뿐** |
| [2-3](#q2-3) | `grep -n "save_state()" main.py` | `194 220 291 338` | 바뀔 때마다 저장 |
| [2-3](#q2-3) | `grep -n "load_state()" main.py` | `332` **1곳** | 읽기는 시작 시 1회 |
| [2-4](#q2-4) | `grep -n "KeyboardInterrupt\|EOFError" main.py` | `335` 한 줄에 둘 다 | 가장 바깥에서 한 번에 처리 |
| [3-1](#q3-1) | `grep -n "def check\|quiz.check" main.py` | `37`(정의) `240`(사용) | 정답은 카드 자신만 앎 |
| [3-3](#q3-3) | `grep -n "except" main.py` | `131 150 164 335` | 예외를 **이름으로 지정**해 잡음 |
| [4-1](#q4-1) | `grep -n "json.dump" main.py` | `149` **1곳** | 전체 덮어쓰기 구조 |
| [4-3](#q4-3) | `grep -n "HINT_PENALTY" main.py` | `12`(정의) + 3곳 사용 | 👍 상수로 모음 |
| [4-3](#q4-3) | `grep -n "1-4\|range(1, 5)" main.py` | `189 191 230` | 👎 매직 넘버가 흩어짐 |

### E-6. 파이썬으로 직접 실행해 보이는 것

```bash
# [3-1] 객체가 데이터와 기능을 함께 가짐
python3.11 -c "
from main import Quiz
q = Quiz('테스트', ['가','나','다','라'], 2)
print(q.question, '| 정답:', q.answer, '| 2번?', q.check(2), '| 3번?', q.check(3))"

# [3-2] JSON이 파이썬 dict와 같은 모양
python3.11 -c "import json; print(type(json.load(open('state.json'))))"

# [3-5] 파일의 칸 이름 = Quiz 속성 이름
python3.11 -c "
import json; d = json.load(open('state.json'))
print('최상위:', list(d.keys())); print('퀴즈 1개:', list(d['quizzes'][0].keys()))"

# [3-5] 힌트 칸이 없는 옛 파일도 읽힘 (하위 호환)
python3.11 -c "
from main import Quiz
q = Quiz.from_dict({'question':'옛날 문제','choices':['가','나','다','라'],'answer':1})
print('에러 없이 생성:', q.question, '| hint =', repr(q.hint))"
```

### E-7. 마무리 (실습 후 정리)

```bash
ls state.json.bak main.py.bak 2>/dev/null   # 되돌리기를 빠뜨렸는지 확인
git status                                   # 실습 중 파일이 바뀌지 않았는지
```
→ `.bak` 파일이 남아 있으면 **되돌리기를 안 한 것**입니다. `mv`로 복구하세요.

**🔗 이동** — [↑ 목차](#toc) · [부록 D 준비물](#appendix-d)

---

<a id="appendix-f"></a>

# 부록 F. 20문항 핵심 한 줄 + 시연 가능 여부

> **이 표 하나만 외워도 절반은 통과합니다.** 문항 번호를 누르면 그 문항으로 이동합니다.
> "시연" 칸이 🖥️ 인 것은 **터미널을 열어 직접 보여줄 수 있는** 문항입니다.

### [항목 1] 기능 동작 검증 — 전부 시연으로 증명

| # | 핵심 한 줄 | 시연 | 무엇을 보여주나 |
| --- | --- | --- | --- |
| [1-1](#q1-1) | 기능이 끝나면 **메뉴로 돌아오는가** | 🖥️ | `3` 눌러 목록 → 메뉴 재출력 |
| [1-2](#q1-2) | 사용자가 실수해도 **안 죽는가** | 🖥️ | `abc` `0` `9` 빈Enter 연속 입력 |
| [1-3](#q1-3) | 껐다 켜도 **기억하는가** | 🖥️ | 퀴즈 **5개 → 6개** |
| [1-4](#q1-4) | 처음 켰을 때 **빈 화면이 아닌가** | 🖥️ | 파일 치우고 실행 → 기본 5개 |
| [1-5](#q1-5) | 결과물만이 아니라 **과정을 남겼는가** | 💻 | `git rev-list --count HEAD` → 18 |
| [1-6](#q1-6) | 원본을 지키며 **실험할 줄 아는가** | 💻 | 머지 커밋 **부모 2개** |
| [1-7](#q1-7) | GitHub와 **양방향**으로 주고받는가 | ⚠️ | **스크린샷 필요 (미완)** |

### [항목 2] 코드 구조 및 설계 — 말 + `grep`으로 증명

| # | 핵심 한 줄 | 시연 | 무엇을 보여주나 |
| --- | --- | --- | --- |
| [2-1](#q2-1) | 클래스를 **왜 2개로 나눴는지** 말할 수 있는가 | 💻 | `grep -n "^class" main.py` |
| [2-2](#q2-2) | 함수를 나눈 **기준이 있는가** | 💻 | `input_int` 정의 1 + 호출 5 |
| [2-3](#q2-3) | **언제 읽고 언제 쓰는지** 아는가 | 💻 | 읽기 1곳 / 쓰기 4곳 |
| [2-4](#q2-4) | 안 끄고 나가도 **데이터를 지키는가** | 🖥️ | `Ctrl+C` → Traceback 없음 |
| [2-5](#q2-5) | 커밋에 **나만의 규칙이 있는가** | 💻 | `git log --oneline` 접두어 |

### [항목 3] 핵심 기술 원리 — 원리를 이해했는가

| # | 핵심 한 줄 | 시연 | 무엇을 보여주나 |
| --- | --- | --- | --- |
| [3-1](#q3-1) | 클래스를 **안 쓰면 뭐가 불편한지** 비교 가능한가 | 💻 | `quiz.check()` 한 줄 |
| [3-2](#q3-2) | **왜 하필 JSON인가** | 🖥️ | `head -20 state.json` — 한글 그대로 |
| [3-3](#q3-3) | 내 코드가 완벽해도 **실패할 곳**을 아는가 | 🖥️ | 파일 깨뜨려도 실행됨 |
| [3-4](#q3-4) | 브랜치를 **왜 쓰는지** 아는가 | 💻 | `git show --stat 2d45928` |
| [3-5](#q3-5) | 저장 구조를 **왜 그렇게 정했는가** | 💻 | 파일 칸 이름 = `Quiz` 속성 |

### [항목 4] 심층 인터뷰 — 정답 없음, **한계를 아는지**가 답

| # | 핵심 한 줄 | 시연 | 무엇을 보여주나 |
| --- | --- | --- | --- |
| [4-1](#q4-1) | 지금 선택이 **언제까지 유효한지** 아는가 | 💻 | `json.dump` 1곳 = 전체 덮어쓰기 |
| [4-2](#q4-2) | **"안 죽는 것"과 "안 잃는 것"** 의 차이를 아는가 | 🖥️ | 복구되지만 **추가분은 사라짐** |
| [4-3](#q4-3) | 바뀌면 **어디를 고칠지 짚을 수 있는가** | 💻 | 상수 1곳 👍 vs 매직넘버 3곳 👎 |

---

### 🎤 30초 안에 다 말해야 한다면

| 질문 유형 | 이렇게 한 문장으로 |
| --- | --- |
| 기능 관련 | "메뉴 루프가 돌면서 번호마다 메서드를 부르고, 6번에서만 빠져나옵니다." |
| 오류 처리 | "숫자를 받는 5곳이 전부 `input_int()` 하나를 거쳐 똑같이 걸러집니다." |
| 저장 관련 | "시작할 때 1번 읽고, 데이터가 바뀔 때마다 즉시 씁니다." |
| 설계 관련 | "바뀌는 이유가 다르면 클래스도 함수도 다르게 나눴습니다." |
| 심층 질문 | "현재 구현은 ~까지입니다. 개선한다면 ~하겠습니다. (미구현)" |

**🔗 이동** — [↑ 목차](#toc) · [부록 E 검증 명령](#appendix-e) · [부록 D 준비물](#appendix-d)
